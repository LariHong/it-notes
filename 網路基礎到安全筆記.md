# 網路基礎到 HTTPS、OSI、DDoS 與 C# 對應筆記

## 0. 簡介：一次 HTTPS Request/Response 到底經過什麼？

當使用者在瀏覽器或 C# 程式中呼叫：

```csharp
await httpClient.GetAsync("https://example.com/api/products");
```

表面上只是送出一個 HTTP request，但背後其實是一整條網路流程：

1. 應用程式產生 HTTP request，例如 `GET /api/products`。
2. DNS 將 `example.com` 解析成 IP 位址。
3. 若是 HTTPS，Client 與 Server 先做 TLS handshake。
4. TLS 使用憑證、公開金鑰加密、工作階段金鑰來建立安全通道。
5. HTTP request 被 TLS 加密後，交給 TCP 或 QUIC 傳輸。
6. 傳輸層資料被切成封包，交給 IP 層處理來源/目的 IP。
7. 封包經過交換器、路由器、LAN、WAN、ISP、BGP、IXP，到達目標網路。
8. 若網站使用 CDN/Anycast，使用者通常會被導到最近或最合適的邊緣節點。
9. CDN、WAF、Bot Management、DDoS 防護可能先檢查流量。
10. 請求抵達來源伺服器或邊緣快取，伺服器產生 HTTP response。
11. Response 沿相反方向回來，經 TLS 解密後，應用程式取得狀態碼、標頭、內容。

這份筆記的核心觀念是：**HTTP/HTTPS 是應用層看到的結果；OSI 是拆解整條路徑的地圖；DNS、IP、Routing、BGP、CDN、TLS、WAF、DDoS 都是這條路上不同位置的機制。**

---

## 1. OSI 七層總覽：把網路問題拆開看

OSI 模型把網路通訊分成七層，方便理解與排錯。實際網際網路更常用 TCP/IP 模型，但 OSI 很適合用來對照 request/response 與安全攻擊。

| OSI 層 | 名稱 | 主要功能 | 常見例子 | C# 對應 |
|---|---|---|---|---|
| L7 | 應用層 | 應用程式通訊協定 | HTTP、SMTP、IMAP、DNS 查詢概念上常放應用層 | `HttpClient`、`SmtpClient`、IMAP 套件 |
| L6 | 表示層 | 編碼、壓縮、加密格式 | TLS 加密、JSON、UTF-8、gzip | JSON 序列化、TLS 設定、壓縮 |
| L5 | 工作階段層 | 建立、維持、結束通訊工作階段 | TLS session、連線重用、Cookie session | `HttpClientFactory`、Cookie、Session Token |
| L4 | 傳輸層 | 端到端傳輸、Port、可靠性 | TCP、UDP、QUIC | `TcpClient`、`UdpClient`、Kestrel port |
| L3 | 網路層 | IP 位址、路由 | IPv4、IPv6、ICMP、Routing | `IPAddress`、`Dns.GetHostAddressesAsync` |
| L2 | 資料連結層 | 同一區域網路內傳遞 frame | Ethernet、Wi-Fi、Switch、MAC | 通常不直接寫 C# 控制 |
| L1 | 實體層 | 電纜、無線電、光纖訊號 | 網路線、Wi-Fi 訊號 | 通常不直接寫 C# 控制 |

### Request/Response 對應 OSI

送出 request 時，資料從 L7 往 L1 包裝：

```text
HTTP Request
-> TLS 加密
-> TCP segment / QUIC packet
-> IP packet
-> Ethernet/Wi-Fi frame
-> 實體訊號
```

收到 response 時，資料從 L1 往 L7 解包：

```text
實體訊號
-> Ethernet/Wi-Fi frame
-> IP packet
-> TCP segment / QUIC packet
-> TLS 解密
-> HTTP Response
```

---

## 2. 從網域到 IP：DNS、IP、封包

### 2.1 DNS 是什麼？

DNS 是網際網路的名稱解析系統。人類記 `www.example.com`，電腦傳輸時需要 IP 位址。DNS 的任務就是把網域名稱轉成 IP。

流程可以想成：

```text
使用者輸入網址
-> 查 DNS 快取
-> 查遞迴解析器
-> 查根 DNS
-> 查 TLD DNS，例如 .com
-> 查權威 DNS
-> 回傳 IP
```

C# 對應：

```csharp
using System.Net;

IPAddress[] addresses = await Dns.GetHostAddressesAsync("www.cloudflare.com");

foreach (var address in addresses)
{
    Console.WriteLine(address);
}
```

DNS 常見安全問題：

- DNS flood：大量 DNS 查詢壓垮 DNS 伺服器。
- DNS spoofing/cache poisoning：讓使用者解析到錯誤 IP。
- DNS 可用性很重要，因為 DNS 掛掉時，使用者可能連網站在哪裡都找不到。

### 2.2 IP 是什麼？

IP 是網路層協定，負責讓封包知道「從哪裡來、要到哪裡去」。IP 不保證可靠送達，可靠性通常由 TCP 或應用層補上。

重點：

- IPv4：例如 `192.0.2.1`
- IPv6：例如 `2001:db8::1`
- IP header 會包含來源 IP、目的 IP 等資訊。
- 路由器根據目的 IP 決定封包下一站。

C# 對應：

```csharp
using System.Net;

var ip = IPAddress.Parse("1.1.1.1");
Console.WriteLine(ip.AddressFamily);
```

### 2.3 封包是什麼？

大型資料不會一次完整丟到網路上，而是切成小單位傳送。這些小單位可稱為 packet。封包通常包含：

- Header：控制資訊，例如來源、目的地、順序、協定。
- Payload：真正要傳的資料。

以 HTTPS request 來看，HTTP 內容會被 TLS 加密，然後被放進傳輸層資料，再包進 IP packet。

---

## 3. 區域網路到網際網路：LAN、Switch、Router、WAN、Routing、BGP

### 3.1 LAN 與 Switch

LAN 是區域網路，例如家裡、公司、學校同一個局部範圍的網路。Switch 主要在同一個 LAN 內轉送資料，通常根據 MAC 位址決定資料要送到哪個裝置。

簡化理解：

```text
同一間辦公室內：
電腦 A -> Switch -> 電腦 B
```

C# 通常不直接控制 Switch，但如果你寫內網服務，就會受到 LAN 拓樸、內部 DNS、防火牆規則影響。

### 3.2 Router 與 Routing

Router 連接不同網路，負責把 IP packet 從一個網路轉送到另一個網路。

```text
你的電腦
-> 家用 Router
-> ISP
-> 多個中間路由器
-> 目標網站所在網路
```

Routing 是選路的過程。Router 會看封包目的 IP，查路由表，決定下一站。

### 3.3 WAN

WAN 是廣域網路，範圍比 LAN 大很多。網際網路本身可以視為由許多大型 WAN 與自治系統互相連接而成。

### 3.4 Autonomous System 與 BGP

Autonomous System，簡稱 AS，是由單一組織管理的一組 IP 網路，例如 ISP、大型雲端服務商、CDN 業者。BGP 是不同 AS 之間交換路由資訊的協定。

簡單說：

- Routing：封包怎麼走。
- BGP：不同大型網路之間怎麼告訴彼此「哪些 IP 前綴可以從我這邊到」。
- Anycast/CDN 常和 BGP 搭配，讓同一個 IP 從多個地點被宣告，使用者通常會被路由到較近或較合適的節點。

C# 開發者通常不會直接操作 BGP，但會感受到它的結果：

- 某地區使用者連線很慢。
- 某 ISP 到服務異常。
- CDN 節點被導向不同地區。
- `traceroute` 路徑改變。

---

## 4. HTTP：Request 與 Response 的核心格式

HTTP 是應用層通訊協定。典型流程是 Client 發出 request，Server 回傳 response。

### 4.1 HTTP Request 包含什麼？

一個 request 通常包含：

- HTTP 方法：`GET`、`POST`、`PUT`、`DELETE`
- URL / Path
- HTTP 版本
- Headers
- Body，通常 POST/PUT 才會有

範例：

```http
GET /api/products HTTP/1.1
Host: example.com
Accept: application/json
Authorization: Bearer <token>
```

C# 對應：

```csharp
using System.Net.Http.Headers;

using var httpClient = new HttpClient();
using var request = new HttpRequestMessage(HttpMethod.Get, "https://example.com/api/products");

request.Headers.Accept.Add(new MediaTypeWithQualityHeaderValue("application/json"));
request.Headers.Authorization = new AuthenticationHeaderValue("Bearer", token);

using HttpResponseMessage response = await httpClient.SendAsync(request);
```

### 4.2 HTTP Response 包含什麼？

一個 response 通常包含：

- Status Code：`200`、`301`、`400`、`401`、`404`、`500`
- Headers
- Body

狀態碼大分類：

| 區間 | 意義 | 常見例子 |
|---|---|---|
| 1xx | 資訊 | `100 Continue` |
| 2xx | 成功 | `200 OK`、`201 Created` |
| 3xx | 重新導向 | `301 Moved Permanently`、`302 Found` |
| 4xx | Client 錯誤 | `400 Bad Request`、`401 Unauthorized`、`404 Not Found` |
| 5xx | Server 錯誤 | `500 Internal Server Error`、`503 Service Unavailable` |

C# 對應：

```csharp
using HttpResponseMessage response = await httpClient.GetAsync("https://example.com/api/products");

Console.WriteLine((int)response.StatusCode);
Console.WriteLine(response.Content.Headers.ContentType);

string body = await response.Content.ReadAsStringAsync();
```

---

## 5. HTTPS、SSL/TLS、公開金鑰、憑證、TLS Handshake

### 5.1 HTTPS 是什麼？

HTTPS 可以理解為：**HTTP + TLS**。

HTTP 本身是明文傳輸；HTTPS 透過 TLS 加密通道，提供：

- 機密性：中間人看不到明文內容。
- 完整性：資料被竄改時可偵測。
- 身分驗證：透過憑證確認伺服器身分。

### 5.2 SSL 與 TLS

現在實務上主要使用 TLS。SSL 是較舊名稱，很多人仍口語稱 SSL 憑證，但真正運作通常是 TLS。

### 5.3 公開金鑰加密如何連到 HTTPS？

公開金鑰加密使用一組金鑰：

- Public Key：可公開。
- Private Key：伺服器保管。

在 TLS 中，公開金鑰與憑證用於身分驗證與協商安全參數；真正大量資料傳輸通常使用對稱式的 session key，因為效能較好。

### 5.4 SSL/TLS 憑證是什麼？

憑證用來證明「這個公開金鑰屬於這個網域」。瀏覽器會檢查：

- 憑證是否由可信任 CA 簽發。
- 網域名稱是否符合。
- 憑證是否過期。
- 憑證鏈是否可信。

C# 對應：一般情況不需要手動驗憑證，`HttpClient` 會使用系統信任存放區檢查。

```csharp
using var httpClient = new HttpClient();
string html = await httpClient.GetStringAsync("https://www.cloudflare.com/");
```

開發環境若使用自簽憑證，不建議直接關閉驗證；應把測試 CA 加入信任，或只在本機測試中明確限制範圍。

### 5.5 TLS Handshake 簡化流程

```text
ClientHello
-> ServerHello + Certificate
-> Client 驗證憑證
-> 雙方協商金鑰
-> 產生 session key
-> 後續 HTTP 內容用 session key 加密
```

與 OSI 對應：

- HTTP：L7
- TLS：常對應 L6/L5 的加密與工作階段概念
- TCP：L4
- IP：L3

---

## 6. TCP、UDP、QUIC 與攻擊關係

### 6.1 TCP

TCP 是可靠傳輸，會建立連線、確認資料、重傳遺失資料。傳統 HTTPS 多跑在 TCP 上。

TCP 三向交握：

```text
Client -> SYN -> Server
Client <- SYN-ACK <- Server
Client -> ACK -> Server
```

### 6.2 SYN Flood

SYN flood 是利用 TCP 三向交握。攻擊者送出大量 SYN，但不完成 ACK，使伺服器保留半開連線資源，最後壓垮服務。

對應 OSI：

- 主要是 L4 傳輸層攻擊。

開發者觀察：

- App 可能沒有收到大量 HTTP request，但服務仍連不上。
- 這不是單靠 Controller 限流就能解決，通常需要網路層、負載平衡器、DDoS 防護處理。

### 6.3 UDP 與 QUIC

UDP 不像 TCP 需要建立可靠連線。QUIC 建立在 UDP 上，常用於 HTTP/3。QUIC 能降低連線建立延遲，也整合加密特性。

QUIC flood 則是針對 QUIC/UDP 流量造成資源耗盡。

---

## 7. ICMP、Ping 與相關攻擊

ICMP 是網路層控制訊息協定，常用於錯誤回報與診斷。`ping` 就常使用 ICMP Echo Request/Reply。

C# 對應：

```csharp
using System.Net.NetworkInformation;

using var ping = new Ping();
PingReply reply = await ping.SendPingAsync("1.1.1.1");

Console.WriteLine(reply.Status);
Console.WriteLine(reply.RoundtripTime);
```

相關攻擊：

- ICMP flood / Ping flood：大量 ping 類流量壓垮目標。
- Ping of Death：利用異常大小或分片封包攻擊舊系統。

注意：有些環境會封鎖 ICMP，所以 ping 不通不一定代表 HTTP/HTTPS 不通。

---

## 8. CDN、IXP、Anycast：為什麼網站可以更快、更穩？

### 8.1 CDN

CDN 是地理分散的伺服器網路，把內容快取在靠近使用者的位置。好處：

- 降低延遲。
- 減少來源伺服器流量。
- 提高可用性。
- 協助吸收 DDoS 流量。

Request 流程加入 CDN 後：

```text
Client
-> DNS 解析到 CDN
-> 最近/合適的 CDN Edge
-> 若快取命中，直接回應
-> 若快取未命中，回源站 Origin
```

C# 角度：

- 你呼叫的是同一個 URL，但實際回應可能來自 CDN edge。
- Response header 可能看到 `cf-cache-status`、`cache-control`、`etag` 等快取資訊。

### 8.2 IXP

IXP 是不同網路提供者交換流量的地方。CDN 把節點放在靠近 IXP 或高度互連的位置，可以降低傳輸時間與成本。

### 8.3 Anycast

Anycast 讓多個地點宣告同一個 IP。使用者連同一個 IP，但 BGP 通常會把流量導到較近或路由較佳的節點。

簡化：

```text
同一個 Anycast IP
台灣使用者 -> 亞洲節點
歐洲使用者 -> 歐洲節點
美國使用者 -> 美國節點
```

---

## 9. DDoS、DoS 與常見攻擊分類

### 9.1 DoS 與 DDoS

DoS 是阻斷服務攻擊；DDoS 是分散式阻斷服務攻擊。DDoS 使用大量被控制的裝置或來源同時攻擊目標，使正常使用者無法使用服務。

### 9.2 依 OSI 分類

| 類型 | OSI 層 | 例子 | 現象 |
|---|---|---|---|
| 應用層攻擊 | L7 | HTTP flood、Credential stuffing | API/登入頁壓力暴增 |
| 協定攻擊 | L3/L4 | SYN flood、Ping flood | 連線耗盡、網路設備壓力 |
| 容量攻擊 | L3/L4 | DNS amplification、NTP amplification | 頻寬被塞滿 |

### 9.3 DNS Flood

DNS flood 是大量查詢打向 DNS 服務，使合法使用者無法解析網域。DNS 是 request 的前置步驟，所以 DNS 被打掛時，HTTP 伺服器即使正常，使用者也可能連不上。

### 9.4 NTP Amplification

NTP amplification 利用可被濫用的 NTP 伺服器放大流量。攻擊者偽造受害者 IP，讓大量回應打到受害者。

### 9.5 Blackhole Routing

黑洞路由會把攻擊流量導到「黑洞」丟棄。優點是快速止血；缺點是可能連合法流量也一起丟掉，因此比較像緊急措施。

---

## 10. WAF、Bot Management、CAPTCHA、robots.txt、MFA

### 10.1 WAF

WAF 是 Web Application Firewall，主要保護 L7 Web 流量。它檢查 HTTP request，阻擋常見攻擊，例如：

- SQL Injection
- XSS
- 惡意路徑掃描
- 異常 User-Agent 或 Header
- 部分 Layer 7 DDoS

C# 開發者應知道：WAF 不是取代安全程式碼。後端仍要做輸入驗證、授權、參數化查詢、CSRF 防護等。

### 10.2 Bot Management

Bot Management 用來辨識自動化流量。不是所有 Bot 都壞，例如搜尋引擎爬蟲是正常 Bot；惡意 Bot 可能做撞庫、爬價、洗流量、註冊垃圾帳號。

### 10.3 Credential Stuffing

Credential stuffing 是使用外洩的帳號密碼組合，自動嘗試登入其他網站。它不是單純暴力破解，而是利用「使用者重複使用密碼」。

防護：

- MFA
- 登入限流
- 密碼外洩檢查
- Bot 偵測
- 異常登入風險判斷

C# 登入 API 概念：

```csharp
// 概念：不要只檢查帳密，還要納入速率限制、MFA、風險訊號。
if (await loginRateLimiter.IsBlockedAsync(userName, clientIp))
{
    return Results.StatusCode(429);
}

bool passwordOk = await authService.CheckPasswordAsync(userName, password);

if (passwordOk && await mfaService.IsRequiredAsync(userName))
{
    return Results.Json(new { requiresMfa = true });
}
```

### 10.4 CAPTCHA

CAPTCHA 目標是區分人類與自動化程式。現代 Bot 管理通常不只靠 CAPTCHA，還會看行為、裝置特徵、風險分數。

### 10.5 robots.txt

`robots.txt` 是給善意爬蟲看的規則檔。它不是安全機制，惡意爬蟲可以忽略它。

### 10.6 MFA

MFA 是多重要素驗證，要求使用者提供超過一種因素：

- 知道的東西：密碼。
- 擁有的東西：手機、硬體金鑰。
- 本人的特徵：生物辨識。

MFA 對 credential stuffing 很有效，因為攻擊者就算拿到密碼，也未必能通過第二因素。

---

## 11. SMTP 與 IMAP：電子郵件也是應用層協定

SMTP 與 IMAP 都是應用層協定，但用途不同：

| 協定 | 用途 | 類比 |
|---|---|---|
| SMTP | 傳送郵件 | 把信寄出去 |
| IMAP | 讀取/同步郵件 | 到信箱查看信 |

SMTP 流程會建立 TCP 連線，使用 `HELO/EHLO`、`MAIL FROM`、`RCPT TO`、`DATA` 等命令傳送郵件。

C# 對應：

```csharp
// .NET 內建 SmtpClient 已不建議用於新開發的大型情境；
// 實務常用 MailKit 等套件。這裡只表示 SMTP 是應用層郵件傳輸協定。
```

電子郵件安全常見議題：

- 網路釣魚。
- 垃圾郵件。
- 寄件者偽造。
- 惡意附件。
- 帳號被盜後濫發信件。

---

## 12. 一次完整 HTTPS API 呼叫：融合 OSI、Request、Response、安全

假設 C# 後端呼叫：

```csharp
using System.Net.Http.Headers;
using System.Text.Json;

using var httpClient = new HttpClient();

using var request = new HttpRequestMessage(
    HttpMethod.Get,
    "https://api.example.com/v1/orders");

request.Headers.Accept.Add(new MediaTypeWithQualityHeaderValue("application/json"));
request.Headers.UserAgent.ParseAdd("MyService/1.0");

using HttpResponseMessage response = await httpClient.SendAsync(request);

response.EnsureSuccessStatusCode();

string json = await response.Content.ReadAsStringAsync();
var orders = JsonSerializer.Deserialize<List<OrderDto>>(json);

public sealed record OrderDto(string Id, decimal Total);
```

背後流程：

| 步驟 | 發生什麼 | OSI 對應 | 相關技術 |
|---|---|---|---|
| 1 | C# 建立 HTTP request | L7 | HTTP method、URL、headers |
| 2 | 解析 `api.example.com` | L7/L3 概念交界 | DNS |
| 3 | 找到目的 IP | L3 | IP |
| 4 | 如果使用 CDN，DNS/BGP 導向 Edge | L3/L7 | CDN、Anycast、BGP |
| 5 | 建立 TCP 或 QUIC 連線 | L4 | TCP、UDP、QUIC |
| 6 | TLS handshake | L6/L5 | TLS、憑證、公開金鑰 |
| 7 | 傳送加密後 HTTP request | L7 -> L1 | HTTPS |
| 8 | WAF/Bot/DDoS 檢查 | L3/L4/L7 | WAF、Bot Management |
| 9 | Edge 快取命中或回源 | L7 | CDN cache、Origin |
| 10 | Server 回傳 HTTP response | L7 | Status code、headers、body |
| 11 | Client 解密並讀取 response | L6/L7 | TLS、JSON |

---

## 13. 排錯時怎麼用這份地圖？

### 情境 A：DNS 解析失敗

症狀：

- C# 出現 host not known。
- 瀏覽器顯示 DNS 相關錯誤。

可能層級：

- DNS 設定錯誤。
- 權威 DNS 異常。
- DNS flood。
- 本機或公司 DNS cache 問題。

C# 可先測：

```csharp
await Dns.GetHostAddressesAsync("api.example.com");
```

### 情境 B：TLS 憑證錯誤

症狀：

- C# `HttpRequestException`。
- 瀏覽器顯示憑證不受信任、過期、網域不符。

可能層級：

- L6/TLS。
- 憑證鏈不完整。
- 憑證過期。
- 使用自簽憑證但 Client 不信任。

### 情境 C：HTTP 404/500

症狀：

- DNS、TCP、TLS 都成功，但回應狀態碼是錯誤。

可能層級：

- L7 應用層。
- 路由設定錯誤。
- API endpoint 不存在。
- 後端程式例外。

### 情境 D：大量登入失敗

症狀：

- 登入 API 流量異常。
- 很多不同 IP 嘗試不同帳密。

可能攻擊：

- Credential stuffing。
- Bot 自動化。

防護：

- MFA。
- Rate limit。
- Bot Management。
- 帳密外洩檢查。

### 情境 E：網站整個變慢或不可用

可能層級：

- L7 HTTP flood。
- L4 SYN flood。
- L3/L4 容量型 DDoS。
- DNS flood。
- CDN/Origin 故障。
- BGP/路由異常。

---

## 14. 最後範例：把網路觀念放進 ASP.NET Core Middleware

這個範例不是實作完整 WAF，而是示範在應用層可以看到哪些 request/response 訊號。

```csharp
var builder = WebApplication.CreateBuilder(args);
var app = builder.Build();

app.Use(async (context, next) =>
{
    string method = context.Request.Method;
    string path = context.Request.Path;
    string? userAgent = context.Request.Headers.UserAgent.ToString();
    string? forwardedFor = context.Request.Headers["X-Forwarded-For"].FirstOrDefault();
    string remoteIp = context.Connection.RemoteIpAddress?.ToString() ?? "unknown";

    Console.WriteLine($"Request {method} {path}");
    Console.WriteLine($"Remote IP: {remoteIp}");
    Console.WriteLine($"X-Forwarded-For: {forwardedFor}");
    Console.WriteLine($"User-Agent: {userAgent}");

    await next();

    int statusCode = context.Response.StatusCode;
    Console.WriteLine($"Response Status: {statusCode}");
});

app.MapGet("/api/health", () => Results.Ok(new
{
    status = "ok",
    time = DateTimeOffset.UtcNow
}));

app.Run();
```

這段程式對應：

- `context.Request.Method`：HTTP request method，L7。
- `context.Request.Headers`：HTTP headers，L7。
- `context.Connection.RemoteIpAddress`：來源 IP，L3 資訊在應用程式中的呈現。
- `X-Forwarded-For`：經過 Proxy/CDN/Load Balancer 時常見的原始 Client IP header。
- `context.Response.StatusCode`：HTTP response status code，L7。

注意：如果服務在 CDN、反向代理或 Load Balancer 後方，`RemoteIpAddress` 可能是代理伺服器 IP，不一定是真實使用者 IP。這時要正確設定 Forwarded Headers，且只信任你控制的 Proxy。

---

## 15. 快速總結

- OSI 是理解網路的地圖，不是每個實作都完美照七層切。
- HTTP 是應用層協定，request/response 是後端工程師最常接觸的網路格式。
- HTTPS = HTTP + TLS；TLS 連到憑證、公開金鑰、session key。
- DNS 先把網域變 IP；IP 封包靠 routing 前進；跨大型網路靠 BGP 交換路由。
- Switch 管同一網路內的轉送；Router 管不同網路之間的轉送。
- CDN/Anycast 讓使用者連到更近、更能承受流量的節點。
- DDoS 可能打 L7，也可能打 L3/L4；不同層需要不同防護。
- WAF/Bot Management/MFA 是應用安全的重要補強，但不能取代後端安全設計。
- C# 開發者不一定實作底層網路，但要知道錯誤發生在哪一層，才不會用錯方法排錯。

---

## 參考來源

- Cloudflare：OSI 模型：<https://www.cloudflare.com/zh-tw/learning/ddos/glossary/open-systems-interconnection-model-osi/>
- Cloudflare：HTTP：<https://www.cloudflare.com/zh-tw/learning/ddos/glossary/hypertext-transfer-protocol-http/>
- Cloudflare：DNS：<https://www.cloudflare.com/zh-tw/learning/dns/what-is-dns/>
- Cloudflare：IP：<https://www.cloudflare.com/zh-tw/learning/network-layer/internet-protocol/>
- Cloudflare：封包：<https://www.cloudflare.com/zh-tw/learning/network-layer/what-is-a-packet/>
- Cloudflare：路由器：<https://www.cloudflare.com/zh-tw/learning/network-layer/what-is-a-router/>
- Cloudflare：Routing：<https://www.cloudflare.com/zh-tw/learning/network-layer/what-is-routing/>
- Cloudflare：BGP：<https://www.cloudflare.com/zh-tw/learning/security/glossary/what-is-bgp/>
- Cloudflare：HTTPS：<https://www.cloudflare.com/zh-tw/learning/ssl/what-is-https/>
- Cloudflare：TLS：<https://www.cloudflare.com/zh-tw/learning/ssl/transport-layer-security-tls/>
- Cloudflare：TLS Handshake：<https://www.cloudflare.com/zh-tw/learning/ssl/what-happens-in-a-tls-handshake/>
- Cloudflare：公開金鑰加密：<https://www.cloudflare.com/zh-tw/learning/ssl/how-does-public-key-encryption-work/>
- Cloudflare：SSL 憑證：<https://www.cloudflare.com/zh-tw/learning/ssl/what-is-an-ssl-certificate/>
- Cloudflare：DDoS：<https://www.cloudflare.com/zh-tw/learning/ddos/what-is-a-ddos-attack/>
- Cloudflare：SYN Flood：<https://www.cloudflare.com/zh-tw/learning/ddos/syn-flood-ddos-attack/>
- Cloudflare：DNS Flood：<https://www.cloudflare.com/zh-tw/learning/ddos/dns-flood-ddos-attack/>
- Cloudflare：ICMP：<https://www.cloudflare.com/zh-tw/learning/ddos/glossary/internet-control-message-protocol-icmp/>
- Cloudflare：CDN：<https://www.cloudflare.com/zh-tw/learning/cdn/what-is-a-cdn/>
- Cloudflare：Anycast：<https://www.cloudflare.com/zh-tw/learning/cdn/glossary/anycast-network/>
- Cloudflare：WAF：<https://www.cloudflare.com/zh-tw/learning/ddos/glossary/web-application-firewall-waf/>
- Cloudflare：SMTP：<https://www.cloudflare.com/zh-tw/learning/email-security/what-is-smtp/>
- Cloudflare：IMAP：<https://www.cloudflare.com/zh-tw/learning/email-security/what-is-imap/>
- Cloudflare：Credential Stuffing：<https://www.cloudflare.com/zh-tw/learning/bots/what-is-credential-stuffing/>
- Cloudflare：MFA：<https://www.cloudflare.com/zh-tw/learning/access-management/what-is-multi-factor-authentication/>
