# Vue.js 手把手教學：Vue 3 / Vite / Pinia 2026 逐日實戰整理

來源系列：
- <https://ithelp.ithome.com.tw/m/users/20129072/ironman/3052>

整理日期：2026-05-04

這份筆記依照 iT 邦幫忙鐵人賽「30天手把手的 Vue.js 教學」逐日整理，並把 2020 年 Vue 2 常見做法更新成 2026 年的 Vue 3、Composition API、`<script setup>`、Vite、Pinia、Vue Router 4 與現代部署流程。每一天都保留獨立學習目標、實作流程、範例、反例與練習。

## 這份筆記怎麼讀

Vue 的直覺是：不要手動找 DOM 改畫面，而是讓資料狀態驅動畫面。

- `ref` / `reactive`：保存狀態。
- `template`：描述畫面如何依狀態呈現。
- `computed`：從狀態推導結果。
- `watch`：狀態改變時做副作用，例如呼叫 API。
- component：拆出可重用的畫面與行為。
- Pinia：保存跨頁或跨元件共用狀態。
- Router：讓 URL 對應頁面。

## Vue 2 到 Vue 3 對照

| 2020 常見做法 | 2026 建議 |
| --- | --- |
| Vue CLI | Vite |
| Options API | Composition API + `<script setup>` |
| Vuex | Pinia |
| EventBus | props/emit、provide/inject、Pinia |
| `new Vue()` | `createApp()` |
| filters | computed 或 method |

## 來源清單

| Day | 原文主題 |
| --- | --- |
| 1 | 什麼是 Vue.js |
| 2 | data & methods |
| 3 | Vue directives 上 |
| 4 | Vue directives 中：v-if & v-for |
| 5 | Vue directives 下：v-model & custom directives |
| 6 | 簡單計算機專案 |
| 7 | Vue Devtools |
| 8 | computed |
| 9 | watch |
| 10 | Vue lifecycle |
| 11 | Vue components |
| 12 | props |
| 13 | emit |
| 14 | slot |
| 15 | Single File Components |
| 16 | vue-cli，建立本地專案 |
| 17 | EventBus |
| 18 | store 集中管理資料 |
| 19 | Vuex 上 |
| 20 | Vuex 下 |
| 21 | Router 上 |
| 22 | Router 下 |
| 23 | Netlify 部署 |
| 24 | 天氣 app 上 |
| 25 | 天氣 app 中 |
| 26 | 天氣 app 下 |
| 27 | Vue best practices |
| 28 | COVID-19 追蹤 app 上 |
| 29 | COVID-19 追蹤 app 中 |
| 30 | COVID-19 追蹤 app 下 |

## 主線專案：Weather Dashboard 實戰累積地圖

這份 Vue 筆記要更接近 CSharp MVC 那份的品質，關鍵不是多背 Vue API，而是讓每一天都累積到同一個可部署專案。後續請把每天的範例都接到 `weather-dashboard`：一個可以查詢城市天氣、保存查詢歷史、切換頁面、用 Pinia 管狀態、最後部署的 Vue 3 專案。

### 專案最終會長成什麼

使用者可以在首頁看到 dashboard，進入天氣查詢頁輸入城市，系統顯示 loading、error、empty、success 狀態；查詢結果會存到 Pinia，使用者可以看歷史紀錄；專案有 router、component 拆分、API service、store、部署設定與 README。

### 需要的檔案地圖

| 類型 | 位置 | 負責的事情 |
| --- | --- | --- |
| App 入口 | `src/main.ts` | 建立 Vue app，註冊 router 與 Pinia |
| Root Component | `src/App.vue` | 放導覽列與 `RouterView` |
| Router | `src/router/index.ts` | 管理首頁、天氣頁、歷史頁、關於頁 |
| Store | `src/stores/weather.ts` | 保存查詢結果、loading、error |
| API Service | `src/services/weatherApi.ts` | 封裝天氣 API request 與 response mapping |
| Composable | `src/composables/useDebouncedSearch.ts` | 重用搜尋防抖邏輯 |
| View | `src/views/WeatherView.vue` | 查詢流程與頁面狀態 |
| View | `src/views/HistoryView.vue` | 顯示查詢歷史 |
| Component | `src/components/WeatherSearchForm.vue` | 搜尋表單 |
| Component | `src/components/WeatherCard.vue` | 顯示單筆天氣 |
| Component | `src/components/BaseCard.vue` | 共用卡片外框與 slot |
| Component | `src/components/AppNavbar.vue` | 導覽列 |
| Deploy | `public/_redirects` 或平台設定 | SPA fallback |

### 30 天交付物地圖

| Day | 當天交付物 | 做完後應該看得到 |
| --- | --- | --- |
| 1 | 建立 Vite Vue 3 專案 | `npm run dev` 可開首頁 |
| 2 | 建立第一個 reactive counter | 點按鈕畫面會更新 |
| 3 | 使用 directive 綁定連結與事件 | `:href`、`@click` 正常 |
| 4 | 建立 todo list 練 `v-if` / `v-for` | 清單與空狀態正常 |
| 5 | 建立搜尋 input 與 `v-model` | 輸入和畫面同步 |
| 6 | 建立小計算機 | 可處理加減乘除與除以零 |
| 7 | 用 Devtools 檢查 state | 看得到 component 狀態 |
| 8 | 用 computed 整理待辦統計 | 未完成數量自動更新 |
| 9 | 用 watch 做搜尋副作用 | 輸入改變可觸發查詢流程 |
| 10 | 加入 lifecycle 清理 timer | 離開元件不殘留 interval |
| 11 | 抽出 `WeatherCard` component | 天氣結果由子元件顯示 |
| 12 | 用 props 傳天氣資料 | 子元件只讀 props |
| 13 | 用 emit 送出搜尋事件 | 父層掌控查詢流程 |
| 14 | 建立 `BaseCard` slot | 卡片外框可重用 |
| 15 | 整理 SFC 結構與 scoped style | component 邏輯和樣式清楚 |
| 16 | 建立正式 Vite 專案結構 | `src/views`、`src/components` 完成 |
| 17 | 移除 EventBus 思維 | 改用 props/emit 或 Pinia |
| 18 | 建立 weather store | 查詢結果集中保存 |
| 19 | 註冊 Pinia | Devtools 看得到 store |
| 20 | Store 加入 async action | loading/error 由 store 管 |
| 21 | 建立 Vue Router | 首頁與天氣頁可切換 |
| 22 | 加入動態路由或 guard | history/detail 頁可讀 route |
| 23 | 完成部署設定 | build 後子頁刷新不 404 |
| 24 | 建立 `weatherApi.ts` | API 細節不在 component |
| 25 | WeatherView 補 loading/error | API 失敗有訊息 |
| 26 | 抽出 WeatherCard/SearchForm | 頁面流程與顯示分離 |
| 27 | 依 best practices 重構 | service/store/component 邊界清楚 |
| 28 | 建立 dashboard 資料模型 | API response 被 mapping |
| 29 | 加入篩選與排序 | computed 產生顯示資料 |
| 30 | 補 README、build、部署檢查 | 專案可被別人啟動 |

### 主線端到端流程

1. 使用者進入 `/weather`。
2. `WeatherView` 顯示 `WeatherSearchForm`。
3. 使用者輸入城市並送出，子元件 emit `search`。
4. 父層呼叫 Pinia store 的 `load(city)` action。
5. Store 設定 `loading = true`，呼叫 `weatherApi.getWeather(city)`。
6. API service 把外部 response mapping 成 `WeatherResult`。
7. Store 保存結果與查詢歷史，並清除錯誤。
8. `WeatherView` 依狀態顯示 loading、error、empty 或 `WeatherCard`。
9. 使用者切到 `/history`，頁面從同一個 store 讀取歷史。
10. 部署後重新整理 `/weather` 或 `/history`，平台 fallback 回 `index.html`，Router 正常接手。

### 主線做完後檢查

- `npm run dev` 可正常啟動。
- `npm run build` 可通過。
- 所有 `v-for` 都使用穩定 key。
- 搜尋流程有 loading、error、empty、success 狀態。
- Weather API key 沒有放在前端可見程式碼。
- `WeatherCard` 不直接呼叫 API，只吃 props。
- Pinia 只保存跨頁共用狀態，不吸收所有 local state。
- 部署後子頁重新整理不會 404。

---

## Day 1：什麼是 Vue.js

### 這篇文章主要在講什麼

介紹 Vue 是什麼，以及它如何幫前端用資料驅動畫面。

### 為什麼需要這個概念

如果只用 jQuery 手動改 DOM，專案一大就會不知道現在畫面是誰改的。Vue 讓你把重點放在狀態，而不是到處操作 DOM。

### 學完這篇你應該會做到什麼

你應該能建立 Vue 3 專案並理解 `createApp()`、component、template 的關係。

### 核心重點

- Vue 是漸進式前端框架。
- Vue 3 新專案通常用 Vite。
- 畫面由 state 推導，不手動改 DOM。
- Vue 可做小互動，也可做 SPA。


### 主線專案銜接

這一天要接回 weather-dashboard 主線，而不是只停在單一語法。完成「專案啟動」後，讀者應該能在同一個專案裡看到明確進度。

| 主線部分 | 這一天要補上的東西 | 完成後的可見結果 |
| --- | --- | --- |
| 專案啟動 | 建立 Vite Vue 3 專案 | 首頁可開啟，dev server 正常 |

當天接到主線專案的流程：

1. 建立 weather-dashboard 專案。
2. 啟動 Vite dev server。
3. 修改 App.vue，確認熱更新。

### 當天做完後檢查

- npm run dev 成功。
- 瀏覽器可開本機 URL。
- 知道入口是 src/main.ts。
### 範例範圍地圖

| 部分 | 位置 / 指令 | 負責的事情 |
| --- | --- | --- |
| 建立專案 | `npm create vite@latest vue-notes -- --template vue-ts` | 產生 Vue 3 專案 |
| 入口 | `src/main.ts` | 建立 app |
| 根元件 | `src/App.vue` | 顯示第一個畫面 |

### 完整實作流程、範例與注意事項

1. 確認 Node.js LTS 已安裝。
2. 建立 Vite 專案。
3. 執行 `npm install`。
4. 執行 `npm run dev`。
5. 修改 `App.vue`，確認畫面熱更新。

```powershell
# 範例用途：建立 Vue 3 + TypeScript 練習專案。
# 預期結果：終端顯示本機 dev server URL。
npm create vite@latest vue-notes -- --template vue-ts
cd vue-notes
npm install
npm run dev
```

### 如果結果和預期不同

- `npm` 找不到：確認 Node.js 已安裝。
- port 被占用：Vite 會自動換 port，注意終端顯示。

### 負面例子 / 錯誤用法

錯誤做法：在 Vue component 裡用 `document.querySelector()` 改文字。
問題：Vue state 和真實 DOM 可能不同步。
修正方向：修改 reactive state。

### 小練習

把首頁文字改成自己的名字，確認瀏覽器自動更新。

### Junior 常見誤解

Vue 不是 HTML 補強工具而已，它的核心是資料與畫面的同步模型。

### 一句話總結

Vue 的第一個觀念是：資料改，畫面跟著改。

---

## Day 2：data & methods

### 這篇文章主要在講什麼

原文介紹 Vue 2 的 data 與 methods；2026 建議用 Vue 3 的 `ref` 和普通 function。

### 為什麼需要這個概念

互動畫面需要保存狀態，也需要定義使用者操作時要做什麼。

### 學完這篇你應該會做到什麼

你應該能用 `ref` 保存資料，並用 function 處理按鈕點擊。

### 核心重點

- `ref()` 保存 primitive 或物件。
- script 中讀寫要用 `.value`。
- template 中 Vue 會自動解包。
- method 就是一般 function。


### 主線專案銜接

這一天要接回 weather-dashboard 主線，而不是只停在單一語法。完成「互動狀態」後，讀者應該能在同一個專案裡看到明確進度。

| 主線部分 | 這一天要補上的東西 | 完成後的可見結果 |
| --- | --- | --- |
| 互動狀態 | 建立 reactive counter | 點擊按鈕畫面更新 |

當天接到主線專案的流程：

1. 用 ref 建立 count。
2. 用 function 修改 count。
3. 在 template 顯示 count。

### 當天做完後檢查

- script 內使用 .value。
- template 可直接顯示 ref。
- 沒有手動操作 DOM。
### 範例範圍地圖

| 部分 | 位置 | 負責的事情 |
| --- | --- | --- |
| State | `ref` | 保存 count |
| Method | `increment()` | 更新 count |
| Template | button | 觸發事件 |

### 完整實作流程、範例與注意事項

1. 在 `App.vue` 引入 `ref`。
2. 建立 `count`。
3. 建立 `increment()`。
4. template 用 `@click` 呼叫。

```vue
<script setup lang="ts">
import { ref } from 'vue'

// 範例用途：保存目前點擊次數。
// 主要輸入：使用者點擊按鈕。
// 回傳結果 / 副作用：count 增加，畫面自動更新。
const count = ref(0)

function increment() {
  count.value++
}
</script>

<template>
  <button type="button" @click="increment">
    點了 {{ count }} 次
  </button>
</template>
```

### 如果結果和預期不同

- script 裡 `count++` 無效：要用 `count.value++`。
- 點擊沒反應：確認事件是 `@click`。

### 負面例子 / 錯誤用法

錯誤做法：把 count 放普通變數 `let count = 0`。
問題：變數改了，Vue 不知道要更新畫面。
修正方向：用 `ref()`。

### 小練習

新增一個 `decrement()`，讓 count 可以減一。

### Junior 常見誤解

Template 可以直接寫 `count`，但 script 裡要寫 `count.value`。

### 一句話總結

Vue 互動的基本單位是 reactive state 加事件方法。

---

## Day 3：認識 Vue directives 上

### 這篇文章主要在講什麼

認識 Vue directive，例如 `v-bind`、`v-on`、`v-text`、`v-html`。

### 為什麼需要這個概念

Directive 是 Vue 在 template 裡把資料與 DOM 行為連起來的語法。

### 學完這篇你應該會做到什麼

你應該能綁定 attribute、事件與文字內容。

### 核心重點

- `:href` 是 `v-bind:href` 縮寫。
- `@click` 是 `v-on:click` 縮寫。
- `{{ }}` 或 `v-text` 顯示文字。
- `v-html` 有 XSS 風險。


### 主線專案銜接

這一天要接回 weather-dashboard 主線，而不是只停在單一語法。完成「Directive 基礎」後，讀者應該能在同一個專案裡看到明確進度。

| 主線部分 | 這一天要補上的東西 | 完成後的可見結果 |
| --- | --- | --- |
| Directive 基礎 | 綁定連結、事件與文字 | :href、@click 正常運作 |

當天接到主線專案的流程：

1. 建立會用在 dashboard 的連結狀態。
2. 用 :href 綁定 URL。
3. 用 @click 更新提示訊息。

### 當天做完後檢查

- 沒有把變數當純字串。
- 不用 v-html 顯示不可信內容。
- 事件 function 在 script 中存在。
### 範例範圍地圖

| Directive | 用途 |
| --- | --- |
| `:href` | 綁定連結 |
| `@click` | 綁定事件 |
| `v-text` | 顯示純文字 |

### 完整實作流程、範例與注意事項

1. 建立 `profileUrl`。
2. 用 `:href` 綁到 a tag。
3. 用 `@click` 切換文字。

```vue
<script setup lang="ts">
import { ref } from 'vue'

const profileUrl = ref('https://github.com/')
const message = ref('尚未點擊')

function markClicked() {
  message.value = '已點擊連結'
}
</script>

<template>
  <a :href="profileUrl" target="_blank" @click="markClicked">
    GitHub
  </a>
  <p v-text="message"></p>
</template>
```

### 如果結果和預期不同

- href 變成字串 `"profileUrl"`：少了冒號。
- click 沒執行：確認 function 有在 `<script setup>` 定義。

### 負面例子 / 錯誤用法

錯誤做法：用 `v-html` 顯示使用者留言。
問題：可能插入惡意 script。
修正方向：用 `{{ comment }}` 顯示純文字。

### 小練習

做一個按鈕，點擊後切換圖片 URL。

### Junior 常見誤解

`:` 不是裝飾，它代表後面是 JavaScript 表達式。

### 一句話總結

Directive 是 Vue template 和狀態互動的橋。

---

## Day 4：v-if & v-for

### 這篇文章主要在講什麼

學習條件渲染與清單渲染。

### 為什麼需要這個概念

真實畫面常要依登入狀態、資料是否存在、清單內容顯示不同 UI。

### 學完這篇你應該會做到什麼

你應該能用 `v-if` 顯示狀態，用 `v-for` 渲染資料陣列。

### 核心重點

- `v-if` 會真正新增/移除 DOM。
- `v-show` 只是切 display。
- `v-for` 要給穩定 `:key`。
- 不建議同一元素同時用 `v-if` 與 `v-for`。


### 主線專案銜接

這一天要接回 weather-dashboard 主線，而不是只停在單一語法。完成「清單與狀態」後，讀者應該能在同一個專案裡看到明確進度。

| 主線部分 | 這一天要補上的東西 | 完成後的可見結果 |
| --- | --- | --- |
| 清單與狀態 | 建立 todo list | 清單與空狀態正常 |

當天接到主線專案的流程：

1. 建立臨時 todos 練清單。
2. 用 v-for 顯示。
3. 用 v-if/v-else 顯示空狀態。

### 當天做完後檢查

- 每筆有穩定 key。
- 刪除資料後畫面正確。
- 沒有複雜混用 v-if 和 v-for。
### 範例範圍地圖

| 部分 | 負責的事情 |
| --- | --- |
| `todos` | 清單資料 |
| `v-for` | 逐筆顯示 |
| `v-if` | 顯示空狀態 |

### 完整實作流程、範例與注意事項

1. 建立 todo array。
2. 用 `v-if` 判斷是否有資料。
3. 用 `v-for` 顯示每筆。
4. `:key` 使用 todo id。

```vue
<script setup lang="ts">
import { ref } from 'vue'

const todos = ref([
  { id: 1, title: '學 v-if' },
  { id: 2, title: '學 v-for' },
])
</script>

<template>
  <p v-if="todos.length === 0">目前沒有待辦。</p>
  <ul v-else>
    <li v-for="todo in todos" :key="todo.id">
      {{ todo.title }}
    </li>
  </ul>
</template>
```

### 如果結果和預期不同

- 清單更新錯亂：確認 key 不是 index。
- 空資料沒顯示：判斷條件是否正確。

### 負面例子 / 錯誤用法

錯誤做法：`:key="index"`。
問題：插入或刪除時 component 狀態可能錯位。
修正方向：使用資料本身的穩定 id。

### 小練習

新增刪除按鈕，刪除指定 todo。

### Junior 常見誤解

`key` 不是為了消警告，而是幫 Vue 正確追蹤每筆資料。

### 一句話總結

清單渲染最重要的是資料範圍與穩定 key。

---

## Day 5：v-model & custom directives

### 這篇文章主要在講什麼

學習雙向綁定與自訂 directive。

### 為什麼需要這個概念

表單輸入是前端最常見的互動，`v-model` 能讓 input 與 state 保持同步。

### 學完這篇你應該會做到什麼

你應該能用 `v-model` 建立表單，並知道自訂 directive 適合少數 DOM 行為。

### 核心重點

- `v-model` 常用於 input、textarea、select。
- checkbox 可綁 boolean 或 array。
- custom directive 適合 focus、tooltip 等低階 DOM 行為。
- 不要把商業邏輯塞進 directive。


### 主線專案銜接

這一天要接回 weather-dashboard 主線，而不是只停在單一語法。完成「搜尋輸入」後，讀者應該能在同一個專案裡看到明確進度。

| 主線部分 | 這一天要補上的東西 | 完成後的可見結果 |
| --- | --- | --- |
| 搜尋輸入 | 建立 v-model 搜尋欄 | 輸入和畫面同步 |

當天接到主線專案的流程：

1. 建立 city state。
2. input 用 v-model。
3. 將 city 作為後續天氣查詢輸入。

### 當天做完後檢查

- 空白輸入能被處理。
- 表單送出前會 trim。
- custom directive 不放商業邏輯。
### 範例範圍地圖

| 部分 | 負責的事情 |
| --- | --- |
| `name` | 表單輸入 |
| `v-model` | 同步 input 與 state |
| `v-focus` | 自動 focus |

### 完整實作流程、範例與注意事項

1. 建立 `name`。
2. input 用 `v-model`。
3. 建立簡單 `v-focus` directive。

```vue
<script setup lang="ts">
import { ref } from 'vue'

const name = ref('')

const vFocus = {
  mounted(el: HTMLInputElement) {
    // 範例用途：DOM 掛載後自動 focus。
    el.focus()
  },
}
</script>

<template>
  <input v-focus v-model="name" placeholder="輸入姓名" />
  <p>你好，{{ name || '訪客' }}</p>
</template>
```

### 如果結果和預期不同

- 自訂 directive 無效：在 `<script setup>` 中命名要以 `v` 開頭，例如 `vFocus` 對應 `v-focus`。

### 負面例子 / 錯誤用法

錯誤做法：用 custom directive 呼叫 API 或改 store。
問題：directive 變成難追的商業邏輯入口。
修正方向：directive 只處理低階 DOM。

### 小練習

建立一個 `v-select-all`，input focus 時選取全部文字。

### Junior 常見誤解

`v-model` 方便，但表單送出前仍要驗證。

### 一句話總結

`v-model` 管表單同步，自訂 directive 管少量 DOM 行為。

---

## Day 6：簡單計算機專案

### 這篇文章主要在講什麼

用前幾天學到的 state、event、directive 做一個小計算機。

### 為什麼需要這個概念

小專案能幫你把單點語法串成完整流程：輸入、計算、顯示、錯誤處理。

### 學完這篇你應該會做到什麼

你應該能建立一個加減乘除計算機。

### 核心重點

- 表單輸入要轉數字。
- 除以零要處理。
- 結果可以用 computed 推導。
- 錯誤訊息也是 state。


### 主線專案銜接

這一天要接回 weather-dashboard 主線，而不是只停在單一語法。完成「小功能整合」後，讀者應該能在同一個專案裡看到明確進度。

| 主線部分 | 這一天要補上的東西 | 完成後的可見結果 |
| --- | --- | --- |
| 小功能整合 | 建立計算機練流程 | 可處理輸入、計算、錯誤狀態 |

當天接到主線專案的流程：

1. 用 number input 收資料。
2. 用 computed 算結果。
3. 處理除以零錯誤。

### 當天做完後檢查

- 使用 v-model.number。
- 錯誤狀態有顯示。
- 沒有手動改 DOM 結果。
### 範例範圍地圖

| 部分 | 負責的事情 |
| --- | --- |
| `left` / `right` | 數字輸入 |
| `operator` | 運算子 |
| `result` | 計算結果 |

### 完整實作流程、範例與注意事項

1. 建立兩個 number input。
2. 建立 operator select。
3. 用 computed 計算結果。
4. 處理除以零。

```vue
<script setup lang="ts">
import { computed, ref } from 'vue'

const left = ref(0)
const right = ref(0)
const operator = ref('+')

const result = computed(() => {
  if (operator.value === '+') return left.value + right.value
  if (operator.value === '-') return left.value - right.value
  if (operator.value === '*') return left.value * right.value
  if (right.value === 0) return '不能除以 0'
  return left.value / right.value
})
</script>

<template>
  <input v-model.number="left" type="number" />
  <select v-model="operator">
    <option>+</option><option>-</option><option>*</option><option>/</option>
  </select>
  <input v-model.number="right" type="number" />
  <p>結果：{{ result }}</p>
</template>
```

### 如果結果和預期不同

- 變成字串相加：使用 `v-model.number`。
- 除以零顯示 Infinity：補條件判斷。

### 負面例子 / 錯誤用法

錯誤做法：每次 input 事件都手動寫入 DOM 結果。
問題：資料和畫面分離，難維護。
修正方向：用 computed。

### 小練習

加上清除按鈕，把數字歸零。

### Junior 常見誤解

小專案也要處理錯誤情境，否則會養成只寫 happy path 的習慣。

### 一句話總結

計算機練的是資料輸入、推導結果與錯誤狀態。

---

## Day 7：認識 Vue Devtools

### 這篇文章主要在講什麼

用 Vue Devtools 檢查 component、props、state 與事件。

### 為什麼需要這個概念

前端 debug 不能只靠 console.log；Devtools 能看出資料流與 component 階層。

### 學完這篇你應該會做到什麼

你應該能找到 component、檢查 state、觀察事件。

### 核心重點

- Vue Devtools 可檢查 component tree。
- Pinia store 也能在 Devtools 看。
- State 改變可以追蹤。
- Production 不應依賴 Devtools 排錯。


### 主線專案銜接

這一天要接回 weather-dashboard 主線，而不是只停在單一語法。完成「Debug 工具」後，讀者應該能在同一個專案裡看到明確進度。

| 主線部分 | 這一天要補上的東西 | 完成後的可見結果 |
| --- | --- | --- |
| Debug 工具 | 用 Devtools 看 state | 能檢查 component 與資料 |

當天接到主線專案的流程：

1. 開啟 Vue Devtools。
2. 操作前面做的互動元件。
3. 觀察 state 是否如預期更新。

### 當天做完後檢查

- Devtools 看得到 Vue app。
- 能定位 App 或子元件。
- 不只靠 console.log。
### 範例範圍地圖

| 工具 | 用途 |
| --- | --- |
| Components tab | 看元件階層與 props |
| Timeline | 看事件與更新 |
| Pinia tab | 看 store state |

### 完整實作流程、範例與注意事項

1. 安裝 Vue Devtools browser extension。
2. 開啟本機 Vue 專案。
3. 選擇 component。
4. 點按鈕並觀察 state 變化。

```text
驗證方式：
1. 在頁面點計算機按鈕。
2. Devtools Components 裡選 App。
3. 確認 left、right、operator 或 count 有更新。
```

### 如果結果和預期不同

- 看不到 Vue tab：確認頁面是 dev mode，且 extension 已啟用。
- State 不更新：先檢查資料是不是 reactive。

### 負面例子 / 錯誤用法

錯誤做法：只靠 console.log 猜 component 狀態。
問題：跨元件資料流會很難追。
修正方向：搭配 Devtools 看 component tree。

### 小練習

用 Devtools 找出 Day 6 計算機的 operator state。

### Junior 常見誤解

Devtools 不是只有出錯才用，平常也能用來理解資料流。

### 一句話總結

Vue Devtools 是看懂 component 與 state 的顯微鏡。

---

## Day 8：computed 屬性

### 這篇文章主要在講什麼

學習 computed，用既有 state 推導新資料。

### 為什麼需要這個概念

畫面常需要「由資料算出來」的結果，例如未完成數量、折扣後價格、過濾清單。

### 學完這篇你應該會做到什麼

你應該能用 computed 做清單過濾與統計。

### 核心重點

- computed 有快取，依賴改變才重新算。
- computed 不適合做副作用。
- 不要在 template 寫太複雜的運算。
- computed 可以讓 template 更乾淨。


### 主線專案銜接

這一天要接回 weather-dashboard 主線，而不是只停在單一語法。完成「衍生資料」後，讀者應該能在同一個專案裡看到明確進度。

| 主線部分 | 這一天要補上的東西 | 完成後的可見結果 |
| --- | --- | --- |
| 衍生資料 | 建立 computed 統計 | 未完成數量自動更新 |

當天接到主線專案的流程：

1. 保留原始 todos。
2. 用 computed 產生未完成清單。
3. 將同樣模式準備用在 dashboard 篩選。

### 當天做完後檢查

- computed 沒有副作用。
- template 不塞長邏輯。
- 依賴改變時會更新。
### 範例範圍地圖

| 部分 | 負責的事情 |
| --- | --- |
| `todos` | 原始資料 |
| `remainingTodos` | 推導未完成清單 |
| `remainingCount` | 推導數量 |

### 完整實作流程、範例與注意事項

```vue
<script setup lang="ts">
import { computed, ref } from 'vue'

const todos = ref([
  { id: 1, title: '學 computed', done: false },
  { id: 2, title: '喝水', done: true },
])

const remainingTodos = computed(() => {
  return todos.value.filter((todo) => !todo.done)
})

const remainingCount = computed(() => remainingTodos.value.length)
</script>

<template>
  <p>剩下 {{ remainingCount }} 件</p>
  <li v-for="todo in remainingTodos" :key="todo.id">
    {{ todo.title }}
  </li>
</template>
```

### 如果結果和預期不同

- computed 沒更新：確認依賴是 reactive。
- template 太複雜：把邏輯移到 computed。

### 負面例子 / 錯誤用法

錯誤做法：computed 裡呼叫 API。
問題：computed 可能重算，副作用不可預測。
修正方向：API call 用 watch 或事件。

### 小練習

新增 `doneCount` computed。

### Junior 常見誤解

computed 是「算出結果」，不是「發生事情」。

### 一句話總結

computed 讓畫面需要的衍生資料保持乾淨。

---

## Day 9：watch 屬性

### 這篇文章主要在講什麼

學習 watch，在 state 改變時執行副作用。

### 為什麼需要這個概念

搜尋、同步 localStorage、依條件呼叫 API 都不是單純顯示資料，而是狀態改變後要做事。

### 學完這篇你應該會做到什麼

你應該能監聽搜尋字串並呼叫 API。

### 核心重點

- watch 適合副作用。
- 搜尋輸入要 debounce。
- watch callback 可取得新值與舊值。
- 複雜清理要用 cleanup。


### 主線專案銜接

這一天要接回 weather-dashboard 主線，而不是只停在單一語法。完成「副作用」後，讀者應該能在同一個專案裡看到明確進度。

| 主線部分 | 這一天要補上的東西 | 完成後的可見結果 |
| --- | --- | --- |
| 副作用 | 用 watch 觸發搜尋流程 | 輸入改變可觸發查詢準備 |

當天接到主線專案的流程：

1. 監聽 city 或 keyword。
2. 長度不足時不查。
3. 留出 debounce/API 接點。

### 當天做完後檢查

- API 不會每個無效字元都打。
- 舊錯誤狀態會清除。
- 知道 watch 和 computed 差異。
### 範例範圍地圖

| 部分 | 負責的事情 |
| --- | --- |
| `keyword` | 搜尋字 |
| `watch` | 監聽改變 |
| `results` | API 結果 |

### 完整實作流程、範例與注意事項

```vue
<script setup lang="ts">
import { ref, watch } from 'vue'

const keyword = ref('')
const results = ref<string[]>([])

watch(keyword, async (nextKeyword) => {
  // 範例用途：keyword 改變時查詢資料。
  // 主要輸入：使用者輸入的 keyword。
  // 回傳結果 / 副作用：更新 results。
  if (nextKeyword.trim().length < 2) {
    results.value = []
    return
  }

  const response = await fetch(`/api/search?q=${encodeURIComponent(nextKeyword)}`)
  results.value = await response.json()
})
</script>
```

### 如果結果和預期不同

- API 打太多：加入 debounce。
- 舊 request 蓋掉新結果：使用 AbortController。

### 負面例子 / 錯誤用法

錯誤做法：watch 每打一個字就立即打 API。
問題：浪費 API quota，使用者體驗差。
修正方向：debounce 或送出才搜尋。

### 小練習

把搜尋改成按 Enter 才查詢。

### Junior 常見誤解

watch 很強，但不是所有資料推導都要用 watch；能用 computed 就先用 computed。

### 一句話總結

watch 負責「狀態變了，所以我要做一件事」。

---

## Day 10：Vue Lifecycle

### 這篇文章主要在講什麼

認識元件生命週期，特別是 Vue 3 的 `onMounted`、`onUnmounted`。

### 為什麼需要這個概念

有些工作要在元件出現後做，例如抓資料；有些資源要在元件消失時清掉，例如 timer。

### 學完這篇你應該會做到什麼

你應該能在 mounted 抓資料，unmounted 清 interval。

### 核心重點

- `onMounted`：DOM 掛載後。
- `onUnmounted`：元件移除前後清理。
- API call 常放 mounted 或路由切換後。
- Timer、event listener 要清理。


### 主線專案銜接

這一天要接回 weather-dashboard 主線，而不是只停在單一語法。完成「生命週期」後，讀者應該能在同一個專案裡看到明確進度。

| 主線部分 | 這一天要補上的東西 | 完成後的可見結果 |
| --- | --- | --- |
| 生命週期 | 加 timer 並清理 | 離開元件不殘留 interval |

當天接到主線專案的流程：

1. onMounted 啟動資源。
2. onUnmounted 清理資源。
3. 切頁時確認不重複累積。

### 當天做完後檢查

- timer 有 clear。
- DOM 操作放 mounted 後。
- 切頁後不繼續跑。
### 範例範圍地圖

| Hook | 用途 |
| --- | --- |
| `onMounted` | 啟動時計時 |
| `onUnmounted` | 清除 timer |

### 完整實作流程、範例與注意事項

```vue
<script setup lang="ts">
import { onMounted, onUnmounted, ref } from 'vue'

const seconds = ref(0)
let timerId: number | undefined

onMounted(() => {
  // 範例用途：元件出現後啟動 timer。
  timerId = window.setInterval(() => {
    seconds.value++
  }, 1000)
})

onUnmounted(() => {
  // 回傳結果 / 副作用：清理 timer，避免 memory leak。
  if (timerId) window.clearInterval(timerId)
})
</script>
```

### 如果結果和預期不同

- 切頁後 timer 還在跑：忘記 clear。
- DOM 還沒存在就操作：放到 onMounted。

### 負面例子 / 錯誤用法

錯誤做法：在 module 最外層啟動 interval。
問題：元件移除後仍持續執行。
修正方向：mounted 啟動，unmounted 清理。

### 小練習

做一個現在時間元件，每秒更新並在離開頁面時清理。

### Junior 常見誤解

元件消失時，外部資源不一定自動消失，要自己清。

### 一句話總結

Lifecycle 管的是元件出現、更新、消失時該做的事。

---

## Day 11：Vue Components

### 這篇文章主要在講什麼

學習把畫面拆成 component。

### 為什麼需要這個概念

當畫面越來越大，全部寫在 `App.vue` 會很難維護。

### 學完這篇你應該會做到什麼

你應該能建立 `TodoItem.vue`，由父元件呼叫。

### 核心重點

- Component 是可重用 UI 單位。
- 父層保存資料，子層顯示單筆。
- 檔名通常用 PascalCase。
- Component 不要拆太碎也不要太大。


### 主線專案銜接

這一天要接回 weather-dashboard 主線，而不是只停在單一語法。完成「元件拆分」後，讀者應該能在同一個專案裡看到明確進度。

| 主線部分 | 這一天要補上的東西 | 完成後的可見結果 |
| --- | --- | --- |
| 元件拆分 | 抽出 WeatherCard | 天氣結果由子元件顯示 |

當天接到主線專案的流程：

1. 建立 WeatherCard.vue。
2. 父層 import 使用。
3. 子層只負責顯示資料。

### 當天做完後檢查

- component 路徑正確。
- 沒有把所有東西塞 App.vue。
- 命名清楚。
### 範例範圍地圖

| Component | 負責的事情 |
| --- | --- |
| `App.vue` | 保存 todos |
| `TodoItem.vue` | 顯示單筆 |

### 完整實作流程、範例與注意事項

```vue
<!-- src/components/TodoItem.vue -->
<script setup lang="ts">
defineProps<{
  title: string
}>()
</script>

<template>
  <li>{{ title }}</li>
</template>
```

```vue
<!-- src/App.vue -->
<script setup lang="ts">
import TodoItem from './components/TodoItem.vue'

const todos = ['拆 component', '傳 props']
</script>

<template>
  <TodoItem v-for="todo in todos" :key="todo" :title="todo" />
</template>
```

### 如果結果和預期不同

- Component 沒顯示：確認 import 路徑。
- prop 沒值：確認傳入時有冒號或字串。

### 負面例子 / 錯誤用法

錯誤做法：一頁所有功能都塞在 `App.vue`。
問題：難讀、難測、難重用。
修正方向：依職責拆 component。

### 小練習

把按鈕抽成 `BaseButton.vue`。

### Junior 常見誤解

Component 不是只為了少寫 HTML，而是為了切分責任。

### 一句話總結

Component 是 Vue 專案的基本組織單位。

---

## Day 12：Props：由外向內的資料傳遞

### 這篇文章主要在講什麼

學習父元件用 props 傳資料給子元件。

### 為什麼需要這個概念

子元件需要資料才能顯示，但資料來源應由父層掌控，避免每個元件各自查資料。

### 學完這篇你應該會做到什麼

你應該能定義 typed props 並在 template 使用。

### 核心重點

- props 是 parent to child。
- props 應視為唯讀。
- TypeScript 可定義 prop 型別。
- 子元件不要直接改 props。


### 主線專案銜接

這一天要接回 weather-dashboard 主線，而不是只停在單一語法。完成「Props」後，讀者應該能在同一個專案裡看到明確進度。

| 主線部分 | 這一天要補上的東西 | 完成後的可見結果 |
| --- | --- | --- |
| Props | 用 props 傳天氣資料 | 子元件只讀父層資料 |

當天接到主線專案的流程：

1. 定義 WeatherResult 型別。
2. WeatherCard 用 defineProps。
3. 父層傳 city/temperature。

### 當天做完後檢查

- 子元件不直接改 props。
- prop 型別和資料一致。
- 資料來源仍在父層或 store。
### 範例範圍地圖

| 部分 | 負責的事情 |
| --- | --- |
| Parent | 傳入 todo |
| Child | 顯示 todo |

### 完整實作流程、範例與注意事項

```vue
<script setup lang="ts">
type Todo = {
  id: number
  title: string
  done: boolean
}

defineProps<{
  todo: Todo
}>()
</script>

<template>
  <span>{{ todo.done ? '完成' : '未完成' }} - {{ todo.title }}</span>
</template>
```

### 如果結果和預期不同

- TypeScript 報錯：父層傳入資料型別不符。
- 子層顯示空白：確認 prop 名稱一致。

### 負面例子 / 錯誤用法

錯誤做法：子元件直接改 `props.todo.done = true`。
問題：資料流不清楚。
修正方向：用 emit 通知父層更新。

### 小練習

替 `TodoItem` 加 `priority` prop。

### Junior 常見誤解

Props 是輸入，不是子元件自己的資料庫。

### 一句話總結

Props 讓父層把資料交給子層顯示。

---

## Day 13：Emit：由內向外的資料傳遞

### 這篇文章主要在講什麼

學習子元件透過 emit 通知父元件。

### 為什麼需要這個概念

子元件可以觸發事件，但真正改資料的人應該是擁有資料的父元件。

### 學完這篇你應該會做到什麼

你應該能用 `defineEmits` 建立 typed event。

### 核心重點

- emit 是 child to parent。
- 子元件發事件，父元件處理。
- event 名稱要清楚。
- emit payload 要有型別。


### 主線專案銜接

這一天要接回 weather-dashboard 主線，而不是只停在單一語法。完成「Emit」後，讀者應該能在同一個專案裡看到明確進度。

| 主線部分 | 這一天要補上的東西 | 完成後的可見結果 |
| --- | --- | --- |
| Emit | SearchForm emit 搜尋事件 | 父層掌控查詢流程 |

當天接到主線專案的流程：

1. 建立 WeatherSearchForm。
2. 子層 submit 時 emit city。
3. 父層接事件後呼叫查詢。

### 當天做完後檢查

- 事件名稱一致。
- payload 型別清楚。
- 子層不直接呼叫全域資料。
### 範例範圍地圖

| 部分 | 負責的事情 |
| --- | --- |
| Child | emit `toggle` |
| Parent | 更新 todo state |

### 完整實作流程、範例與注意事項

```vue
<script setup lang="ts">
defineProps<{ id: number; title: string }>()

const emit = defineEmits<{
  toggle: [id: number]
}>()
</script>

<template>
  <button type="button" @click="emit('toggle', id)">
    切換 {{ title }}
  </button>
</template>
```

```vue
<TodoItem :id="todo.id" :title="todo.title" @toggle="toggleTodo" />
```

### 如果結果和預期不同

- 父層沒收到：事件名稱是否一致。
- payload 型別錯：檢查 `defineEmits`。

### 負面例子 / 錯誤用法

錯誤做法：子元件偷偷 import 父層資料並修改。
問題：耦合嚴重，元件不可重用。
修正方向：用 emit。

### 小練習

新增 `remove` event，讓父層刪除 todo。

### Junior 常見誤解

Emit 不是廣播給全世界，它是子元件對父元件說「發生了這件事」。

### 一句話總結

Emit 讓子元件發出事件，父元件決定怎麼改資料。

---

## Day 14：Slot 基礎用法

### 這篇文章主要在講什麼

學習 slot，讓父元件把內容塞進子元件預留位置。

### 為什麼需要這個概念

有些 component 外框固定，但內容要由使用端決定，例如 card、modal、layout。

### 學完這篇你應該會做到什麼

你應該能建立 `BaseCard` 並用 slot 放內容。

### 核心重點

- slot 是內容插槽。
- default slot 放主要內容。
- named slot 放指定區塊。
- slot 適合 layout 類 component。


### 主線專案銜接

這一天要接回 weather-dashboard 主線，而不是只停在單一語法。完成「Slot」後，讀者應該能在同一個專案裡看到明確進度。

| 主線部分 | 這一天要補上的東西 | 完成後的可見結果 |
| --- | --- | --- |
| Slot | 建立 BaseCard | 卡片外框可重用 |

當天接到主線專案的流程：

1. BaseCard 提供外框。
2. title/content 用 slot。
3. WeatherCard 或 dashboard card 使用它。

### 當天做完後檢查

- slot name 對應正確。
- 避免重複卡片 HTML。
- slot 不取代 props 的資料傳遞角色。
### 範例範圍地圖

| Component | 負責的事情 |
| --- | --- |
| `BaseCard` | 提供外框 |
| 使用端 | 提供內容 |

### 完整實作流程、範例與注意事項

```vue
<!-- BaseCard.vue -->
<template>
  <section class="card">
    <header><slot name="title" /></header>
    <div><slot /></div>
  </section>
</template>
```

```vue
<BaseCard>
  <template #title>履歷摘要</template>
  <p>這裡是履歷內容。</p>
</BaseCard>
```

### 如果結果和預期不同

- named slot 沒顯示：確認使用 `#title` 對應 `name="title"`。

### 負面例子 / 錯誤用法

錯誤做法：為了每種卡片建立一個幾乎相同的 component。
問題：重複太多。
修正方向：抽 BaseCard，內容用 slot。

### 小練習

替 BaseCard 加 footer slot。

### Junior 常見誤解

Props 傳資料，slot 傳畫面內容；兩者用途不同。

### 一句話總結

Slot 讓 component 外框重用，內容保持彈性。

---

## Day 15：Single File Components

### 這篇文章主要在講什麼

認識 `.vue` 單檔元件，包含 template、script、style。

### 為什麼需要這個概念

SFC 讓一個 component 的畫面、邏輯、樣式集中管理，是 Vue 專案的主要寫法。

### 學完這篇你應該會做到什麼

你應該能用 `<script setup lang="ts">` 寫現一個完整 component。

### 核心重點

- `<template>` 放畫面。
- `<script setup>` 放邏輯。
- `<style scoped>` 限制樣式作用範圍。
- TypeScript 可提升維護性。


### 主線專案銜接

這一天要接回 weather-dashboard 主線，而不是只停在單一語法。完成「SFC 結構」後，讀者應該能在同一個專案裡看到明確進度。

| 主線部分 | 這一天要補上的東西 | 完成後的可見結果 |
| --- | --- | --- |
| SFC 結構 | 整理 component 的 script/template/style | 元件責任清楚 |

當天接到主線專案的流程：

1. 使用 script setup lang ts。
2. 樣式用 scoped。
3. 把共用 component 放 components。

### 當天做完後檢查

- 樣式不污染其他元件。
- 型別錯誤能被提示。
- 單一 component 不混太多責任。
### 範例範圍地圖

| 區塊 | 負責的事情 |
| --- | --- |
| template | HTML 結構 |
| script setup | state 與 function |
| style scoped | 本元件樣式 |

### 完整實作流程、範例與注意事項

```vue
<script setup lang="ts">
defineProps<{
  label: string
}>()
</script>

<template>
  <button class="base-button" type="button">
    {{ label }}
  </button>
</template>

<style scoped>
.base-button {
  padding: 8px 12px;
}
</style>
```

### 如果結果和預期不同

- 樣式影響其他元件：確認有 `scoped`。
- prop 型別錯：檢查父層傳值。

### 負面例子 / 錯誤用法

錯誤做法：所有 component 都共用巨大 global CSS。
問題：樣式互相污染。
修正方向：元件樣式用 scoped，設計系統樣式才放 global。

### 小練習

建立 `WeatherCard.vue`，包含 props、template、scoped style。

### Junior 常見誤解

SFC 不是把所有東西塞一起，而是把同一個元件的責任放一起。

### 一句話總結

SFC 是 Vue component 的標準包裝方式。

---

## Day 16：建立第一個本地 Vue 專案

### 這篇文章主要在講什麼

原文介紹 vue-cli；2026 新專案建議用 Vite。

### 為什麼需要這個概念

本地專案工具會影響開發速度、build 方式與部署流程。

### 學完這篇你應該會做到什麼

你應該能用 Vite 建立、啟動、build Vue 專案。

### 核心重點

- Vite dev server 啟動快。
- `npm run dev` 開發。
- `npm run build` 產生正式檔。
- `npm run preview` 預覽 build。


### 主線專案銜接

這一天要接回 weather-dashboard 主線，而不是只停在單一語法。完成「專案結構」後，讀者應該能在同一個專案裡看到明確進度。

| 主線部分 | 這一天要補上的東西 | 完成後的可見結果 |
| --- | --- | --- |
| 專案結構 | 建立 views/components/services/stores | 正式主線目錄成形 |

當天接到主線專案的流程：

1. 建立 src/views。
2. 建立 src/services 與 src/stores。
3. 把 demo code 移進對應資料夾。

### 當天做完後檢查

- npm run build 仍通過。
- import path 正確。
- 資料夾不是空殼，有明確責任。
### 範例範圍地圖

| 指令 | 負責的事情 |
| --- | --- |
| `npm run dev` | 開發 |
| `npm run build` | 打包 |
| `npm run preview` | 預覽正式包 |

### 完整實作流程、範例與注意事項

```powershell
npm create vite@latest weather-app -- --template vue-ts
cd weather-app
npm install
npm run dev
npm run build
npm run preview
```

### 如果結果和預期不同

- build 失敗：先看 TypeScript 或 import 錯誤。
- preview 空白：確認 base path 設定。

### 負面例子 / 錯誤用法

錯誤做法：只確認 dev server 能跑，不跑 build。
問題：部署時才發現錯誤。
修正方向：每次發布前跑 build。

### 小練習

建立專案後，把 app title 改成 Weather App 並跑 build。

### Junior 常見誤解

dev 能跑不代表 production build 一定能過。

### 一句話總結

Vite 是 2026 Vue 新專案的主流起點。

---

## Day 17：EventBus 與現代替代方案

### 這篇文章主要在講什麼

原文用 EventBus 做子層元件溝通；2026 建議改用 props/emit、Pinia 或 provide/inject。

### 為什麼需要這個概念

EventBus 容易讓事件來源不明，專案變大後很難追。

### 學完這篇你應該會做到什麼

你應該能判斷跨元件溝通該用哪種方式。

### 核心重點

- 父子溝通：props/emit。
- 跨頁共用：Pinia。
- 深層局部依賴：provide/inject。
- 避免全域 EventBus。


### 主線專案銜接

這一天要接回 weather-dashboard 主線，而不是只停在單一語法。完成「跨元件溝通」後，讀者應該能在同一個專案裡看到明確進度。

| 主線部分 | 這一天要補上的東西 | 完成後的可見結果 |
| --- | --- | --- |
| 跨元件溝通 | 移除 EventBus 思維 | 依資料範圍選 props/emit 或 Pinia |

當天接到主線專案的流程：

1. 父子仍用 props/emit。
2. 跨頁查詢歷史才放 store。
3. 局部深層設定才考慮 provide/inject。

### 當天做完後檢查

- 沒有全域事件亂飛。
- 能說明資料擁有者。
- component 不互相偷 import 狀態。
### 範例範圍地圖

| 情境 | 建議 |
| --- | --- |
| 子元件通知父元件 | emit |
| 多頁共用登入者 | Pinia |
| 表單群組深層傳遞設定 | provide/inject |

### 完整實作流程、範例與注意事項

```ts
// 不用 EventBus，改用 Pinia store 集中管理跨元件訊息。
import { defineStore } from 'pinia'
import { ref } from 'vue'

export const useToastStore = defineStore('toast', () => {
  const message = ref('')

  function show(nextMessage: string) {
    // 範例用途：跨元件顯示 toast。
    message.value = nextMessage
  }

  return { message, show }
})
```

### 如果結果和預期不同

- 不知道誰改了資料：資料流太分散，改用 store。
- 小元件過度依賴 store：父子情境先用 props/emit。

### 負面例子 / 錯誤用法

錯誤做法：所有 component 都透過 EventBus 發事件。
問題：事件像全域煙霧，debug 很痛苦。
修正方向：依資料範圍選 props/emit 或 Pinia。

### 小練習

把一個 EventBus toast 改成 Pinia toast store。

### Junior 常見誤解

跨元件不是一律用全域狀態；先看資料範圍。

### 一句話總結

現代 Vue 盡量用清楚資料流取代 EventBus。

---

## Day 18：建立簡單 Store 集中管理資料

### 這篇文章主要在講什麼

學習集中管理跨元件資料。

### 為什麼需要這個概念

當多個頁面都需要同一份資料，靠 props 一層層傳會很累。

### 學完這篇你應該會做到什麼

你應該能建立 Pinia store 保存 todos。

### 核心重點

- Store 保存跨元件 state。
- action 修改 state。
- getter/computed 推導資料。
- Store 不應變成所有東西的大雜燴。


### 主線專案銜接

這一天要接回 weather-dashboard 主線，而不是只停在單一語法。完成「Store 起點」後，讀者應該能在同一個專案裡看到明確進度。

| 主線部分 | 這一天要補上的東西 | 完成後的可見結果 |
| --- | --- | --- |
| Store 起點 | 建立 weather store | 查詢結果集中保存 |

當天接到主線專案的流程：

1. 定義 useWeatherStore。
2. 保存 items/loading/error。
3. component 從 store 讀資料。

### 當天做完後檢查

- Pinia 已註冊。
- store 不保存純 local UI 狀態。
- Devtools 看得到 store。
### 範例範圍地圖

| 部分 | 位置 | 負責的事情 |
| --- | --- | --- |
| Store | `src/stores/todos.ts` | 集中資料 |
| Component | 任意頁面 | 使用 store |

### 完整實作流程、範例與注意事項

```ts
import { defineStore } from 'pinia'
import { computed, ref } from 'vue'

export const useTodoStore = defineStore('todos', () => {
  const items = ref([{ id: 1, title: '學 Pinia', done: false }])

  const remainingCount = computed(() => {
    return items.value.filter((item) => !item.done).length
  })

  function add(title: string) {
    items.value.push({ id: Date.now(), title, done: false })
  }

  return { items, remainingCount, add }
})
```

### 如果結果和預期不同

- Store 無法使用：確認 `app.use(createPinia())`。
- 狀態重新整理就消失：需要 localStorage 或 API 保存。

### 負面例子 / 錯誤用法

錯誤做法：所有 component local state 都放 Pinia。
問題：全域狀態過度膨脹。
修正方向：只有跨元件共享才放 store。

### 小練習

把 Day 4 todo list 改成 Pinia 管理。

### Junior 常見誤解

Store 不是替代所有 component state，它只適合共享狀態。

### 一句話總結

Store 解決的是跨元件共享資料，不是所有資料。

---

## Day 19：Vuex 上，更新為 Pinia 上

### 這篇文章主要在講什麼

原文介紹 Vuex 基礎；2026 建議使用 Pinia 的 state 與 action。

### 為什麼需要這個概念

大型前端需要可預期的資料修改入口，避免每個元件任意改資料。

### 學完這篇你應該會做到什麼

你應該能安裝 Pinia 並註冊到 app。

### 核心重點

- `createPinia()` 註冊 store。
- `defineStore()` 定義 store。
- setup store 寫法接近 Composition API。
- action 負責修改或載入資料。


### 主線專案銜接

這一天要接回 weather-dashboard 主線，而不是只停在單一語法。完成「Pinia 註冊」後，讀者應該能在同一個專案裡看到明確進度。

| 主線部分 | 這一天要補上的東西 | 完成後的可見結果 |
| --- | --- | --- |
| Pinia 註冊 | 在 main.ts 註冊 Pinia | store 可在任意頁使用 |

當天接到主線專案的流程：

1. 安裝或確認 Pinia。
2. app.use createPinia。
3. 建立 user/weather store 測試。

### 當天做完後檢查

- 沒有 getActivePinia 錯誤。
- store action 可被呼叫。
- 共享狀態可追蹤。
### 範例範圍地圖

| 檔案 | 負責的事情 |
| --- | --- |
| `main.ts` | 註冊 Pinia |
| `stores/user.ts` | 使用者狀態 |

### 完整實作流程、範例與注意事項

```ts
// src/main.ts
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import App from './App.vue'

createApp(App)
  .use(createPinia())
  .mount('#app')
```

```ts
// src/stores/user.ts
import { defineStore } from 'pinia'
import { ref } from 'vue'

export const useUserStore = defineStore('user', () => {
  const name = ref('Guest')

  function login(nextName: string) {
    name.value = nextName
  }

  return { name, login }
})
```

### 如果結果和預期不同

- `getActivePinia` error：忘記 `app.use(createPinia())`。

### 負面例子 / 錯誤用法

錯誤做法：元件之間互相 import 對方資料。
問題：耦合嚴重。
修正方向：共享資料放 store。

### 小練習

建立 user store，顯示目前登入者名稱。

### Junior 常見誤解

Pinia 不只是全域變數，它是可被 Devtools 追蹤的狀態容器。

### 一句話總結

Pinia 是 Vue 3 管理共享狀態的主流選擇。

---

## Day 20：Vuex 下，更新為 Pinia 下

### 這篇文章主要在講什麼

進一步使用 Pinia action、getter 與非同步資料載入。

### 為什麼需要這個概念

真實 store 常需要呼叫 API、處理 loading/error，而不只是保存字串。

### 學完這篇你應該會做到什麼

你應該能在 store 裡載入天氣資料。

### 核心重點

- 非同步 action 可以呼叫 API。
- Store 可保存 loading/error。
- API response 建議先 mapping。
- 不要把 API key 放前端。


### 主線專案銜接

這一天要接回 weather-dashboard 主線，而不是只停在單一語法。完成「Async Store」後，讀者應該能在同一個專案裡看到明確進度。

| 主線部分 | 這一天要補上的東西 | 完成後的可見結果 |
| --- | --- | --- |
| Async Store | Store 加入 async action | loading/error 由 store 管 |

當天接到主線專案的流程：

1. store action 呼叫 API service。
2. request 前後設定 loading。
3. catch 寫入 errorMessage。

### 當天做完後檢查

- finally 會關 loading。
- 失敗時 UI 有訊息。
- 不重複在多個 component 打同 API。
### 範例範圍地圖

| State | 負責的事情 |
| --- | --- |
| `items` | 天氣結果 |
| `loading` | 載入中 |
| `errorMessage` | 錯誤訊息 |

### 完整實作流程、範例與注意事項

```ts
export const useWeatherStore = defineStore('weather', () => {
  const items = ref<{ city: string; temperature: number }[]>([])
  const loading = ref(false)
  const errorMessage = ref('')

  async function load(city: string) {
    loading.value = true
    errorMessage.value = ''

    try {
      const response = await fetch(`/api/weather?city=${encodeURIComponent(city)}`)
      if (!response.ok) throw new Error('API failed')
      items.value.unshift(await response.json())
    } catch {
      errorMessage.value = '天氣資料讀取失敗'
    } finally {
      loading.value = false
    }
  }

  return { items, loading, errorMessage, load }
})
```

### 如果結果和預期不同

- loading 一直 true：finally 沒執行或例外沒處理。
- API key 外洩：確認前端沒有私密 key。

### 負面例子 / 錯誤用法

錯誤做法：每個 component 都各自呼叫同一支 API。
問題：重複 request，資料不同步。
修正方向：共享資料由 store 載入。

### 小練習

在 store 加 `clear()` 清除天氣結果。

### Junior 常見誤解

Store action 也要處理失敗，不是 API 成功才算流程。

### 一句話總結

Pinia 的實務重點是把共享資料的 loading/error 一起管理。

---

## Day 21：Vue Router 上

### 這篇文章主要在講什麼

認識 Router，讓 URL 對應不同頁面 component。

### 為什麼需要這個概念

SPA 需要讓使用者能用 URL 進入不同頁面，也需要上一頁、分享連結、導覽列。

### 學完這篇你應該會做到什麼

你應該能建立 home 與 weather 兩個 route。

### 核心重點

- `createRouter()` 建立 router。
- `createWebHistory()` 使用 history mode。
- routes array 定義 path 與 component。
- `<RouterView />` 顯示目前頁面。


### 主線專案銜接

這一天要接回 weather-dashboard 主線，而不是只停在單一語法。完成「Router 起點」後，讀者應該能在同一個專案裡看到明確進度。

| 主線部分 | 這一天要補上的東西 | 完成後的可見結果 |
| --- | --- | --- |
| Router 起點 | 建立首頁與天氣頁 | URL 可切換 view |

當天接到主線專案的流程：

1. 建立 router/index.ts。
2. main 註冊 router。
3. App 放 RouterLink/RouterView。

### 當天做完後檢查

- / 與 /weather 可切換。
- 不使用普通 a tag 重載 SPA。
- 重新整理問題留到部署處理。
### 範例範圍地圖

| 檔案 | 負責的事情 |
| --- | --- |
| `router/index.ts` | 定義路由 |
| `App.vue` | 放 RouterLink 與 RouterView |

### 完整實作流程、範例與注意事項

```ts
import { createRouter, createWebHistory } from 'vue-router'
import HomeView from '../views/HomeView.vue'
import WeatherView from '../views/WeatherView.vue'

export const router = createRouter({
  history: createWebHistory(),
  routes: [
    { path: '/', name: 'home', component: HomeView },
    { path: '/weather', name: 'weather', component: WeatherView },
  ],
})
```

```vue
<template>
  <RouterLink to="/">首頁</RouterLink>
  <RouterLink to="/weather">天氣</RouterLink>
  <RouterView />
</template>
```

### 如果結果和預期不同

- 頁面不切換：確認 `app.use(router)`。
- 重新整理 404：部署端缺 SPA fallback。

### 負面例子 / 錯誤用法

錯誤做法：用普通 `<a href>` 在 SPA 內切頁。
問題：整頁重載，狀態消失。
修正方向：用 `RouterLink`。

### 小練習

新增 `/about` 頁。

### Junior 常見誤解

Router 不是後端路由，它是在前端決定要顯示哪個 view。

### 一句話總結

Vue Router 讓 SPA 有可分享、可返回的 URL。

---

## Day 22：Vue Router 下

### 這篇文章主要在講什麼

學習動態路由、巢狀路由與 navigation guard。

### 為什麼需要這個概念

真實 App 會有詳情頁、登入保護頁、子頁面。

### 學完這篇你應該會做到什麼

你應該能建立 `/resumes/:id` 詳情頁與登入守衛。

### 核心重點

- `:id` 是 route param。
- `useRoute()` 讀目前 route。
- guard 可保護需要登入的頁面。
- meta 可標記 route 需求。


### 主線專案銜接

這一天要接回 weather-dashboard 主線，而不是只停在單一語法。完成「Router 進階」後，讀者應該能在同一個專案裡看到明確進度。

| 主線部分 | 這一天要補上的東西 | 完成後的可見結果 |
| --- | --- | --- |
| Router 進階 | 加入動態路由或 guard | 可讀 route param 或保護頁面 |

當天接到主線專案的流程：

1. 建立動態 route。
2. 用 useRoute 讀 param。
3. 需要登入的頁面用 meta/guard。

### 當天做完後檢查

- 沒有無限 redirect。
- 前端 guard 不被當成真正安全。
- route param 型別有處理。
### 範例範圍地圖

| 部分 | 負責的事情 |
| --- | --- |
| Dynamic route | 顯示詳情 |
| Meta | 標記 requiresAuth |
| Guard | 未登入導向 login |

### 完整實作流程、範例與注意事項

```ts
const routes = [
  { path: '/login', name: 'login', component: LoginView },
  {
    path: '/resumes/:id',
    name: 'resume.show',
    component: ResumeShowView,
    meta: { requiresAuth: true },
  },
]

router.beforeEach((to) => {
  const isLoggedIn = Boolean(localStorage.getItem('token'))
  if (to.meta.requiresAuth && !isLoggedIn) {
    return { name: 'login' }
  }
})
```

### 如果結果和預期不同

- id 讀不到：在 component 用 `useRoute().params.id`。
- 無限 redirect：login route 不要也標 requiresAuth。

### 負面例子 / 錯誤用法

錯誤做法：只在 navbar 隱藏保護頁連結。
問題：使用者仍可直接輸入 URL。
修正方向：加 navigation guard；後端 API 也要檢查 token。

### 小練習

建立 `/users/:userId/resumes/:resumeId` route。

### Junior 常見誤解

前端 guard 只改善體驗，真正安全仍要後端授權。

### 一句話總結

Router guard 管前端導頁，但不能取代後端權限。

---

## Day 23：部署 Vue 專案

### 這篇文章主要在講什麼

原文使用 Netlify 部署 Vue 專案；2026 可用 Netlify、Vercel、GitHub Pages。

### 為什麼需要這個概念

專案能在本機跑不代表能給別人用，部署會遇到 build、base path、SPA fallback。

### 學完這篇你應該會做到什麼

你應該能 build Vue app 並部署到靜態平台。

### 核心重點

- build command：`npm run build`。
- output：`dist`。
- SPA history mode 要 fallback。
- 環境變數要分 dev/prod。


### 主線專案銜接

這一天要接回 weather-dashboard 主線，而不是只停在單一語法。完成「部署」後，讀者應該能在同一個專案裡看到明確進度。

| 主線部分 | 這一天要補上的東西 | 完成後的可見結果 |
| --- | --- | --- |
| 部署 | 建立 build 與 SPA fallback | 部署後子頁刷新不 404 |

當天接到主線專案的流程：

1. 本機跑 build。
2. 設定 dist 為發布目錄。
3. 平台加 fallback 到 index.html。

### 當天做完後檢查

- npm run build 通過。
- npm run preview 可看。
- /weather 重新整理正常。
### 範例範圍地圖

| 平台設定 | 值 |
| --- | --- |
| Build command | `npm run build` |
| Publish directory | `dist` |
| Node version | LTS |

### 完整實作流程、範例與注意事項

1. 本機跑 `npm run build`。
2. 確認 `dist` 產生。
3. 平台設定 build command 與 publish directory。
4. 若使用 history mode，設定 fallback。

```text
Netlify _redirects：
/* /index.html 200
```

### 如果結果和預期不同

- 重新整理子頁 404：缺 fallback。
- 環境變數 undefined：平台沒有設定 prod env。

### 負面例子 / 錯誤用法

錯誤做法：沒跑 build 就直接推上去部署。
問題：部署時才發現 TypeScript 或 import 錯。
修正方向：本機先 build。

### 小練習

把 weather app 部署到任一靜態平台。

### Junior 常見誤解

前端部署不是上傳原始碼，而是上傳 build 後的靜態檔。

### 一句話總結

Vue 部署基本三件事：build、dist、SPA fallback。

---

## Day 24：天氣 App 上

### 這篇文章主要在講什麼

開始天氣 app，先定義 API、輸入城市與基本資料流。

### 為什麼需要這個概念

API 專案的核心不是畫卡片，而是輸入、request、loading、error、response mapping。

### 學完這篇你應該會做到什麼

你應該能建立搜尋表單與 API service。

### 核心重點

- API key 不應放前端。
- 建議用後端 proxy 或 serverless function。
- 前端 service 負責呼叫自己的 API endpoint。
- 表單要處理空輸入。


### 主線專案銜接

這一天要接回 weather-dashboard 主線，而不是只停在單一語法。完成「API Service」後，讀者應該能在同一個專案裡看到明確進度。

| 主線部分 | 這一天要補上的東西 | 完成後的可見結果 |
| --- | --- | --- |
| API Service | 建立 weatherApi.ts | API 細節不在 component |

當天接到主線專案的流程：

1. 定義 WeatherResult。
2. fetch 自己的 /api/weather。
3. mapping 外部 response。

### 當天做完後檢查

- API key 不在前端。
- service 可替換資料來源。
- component 不吃原始 response 欄位。
### 範例範圍地圖

| 部分 | 位置 | 負責的事情 |
| --- | --- | --- |
| View | `WeatherView.vue` | 搜尋 UI |
| Service | `weatherApi.ts` | 呼叫 API |

### 完整實作流程、範例與注意事項

```ts
// src/services/weatherApi.ts
export type WeatherResult = {
  city: string
  temperature: number
}

export async function getWeather(city: string): Promise<WeatherResult> {
  // 範例用途：用城市名稱查天氣。
  // 主要輸入：city 來自搜尋框。
  // 回傳結果：整理後的 WeatherResult。
  const response = await fetch(`/api/weather?city=${encodeURIComponent(city)}`)
  if (!response.ok) throw new Error('Weather API failed')
  return await response.json()
}
```

### 如果結果和預期不同

- CORS error：需要後端 proxy。
- API key 外洩：不要把私密 key 放 Vite 前端環境變數。

### 負面例子 / 錯誤用法

錯誤做法：把私密 weather API key 寫在 `WeatherView.vue`。
問題：build 後任何人都看得到。
修正方向：放後端或 serverless。

### 小練習

建立搜尋框，空白時不呼叫 API。

### Junior 常見誤解

`VITE_` 環境變數會進前端包，不適合放秘密。

### 一句話總結

天氣 app 的第一步是安全地設計 API 邊界。

---

## Day 25：天氣 App 中

### 這篇文章主要在講什麼

處理 loading、error 與 API response。

### 為什麼需要這個概念

API 不會永遠成功，使用者需要知道正在載入、失敗或沒有資料。

### 學完這篇你應該會做到什麼

你應該能在搜尋時顯示 loading 與錯誤訊息。

### 核心重點

- request 前 `loading = true`。
- catch 顯示錯誤。
- finally 關閉 loading。
- 空資料要有提示。


### 主線專案銜接

這一天要接回 weather-dashboard 主線，而不是只停在單一語法。完成「API UI 狀態」後，讀者應該能在同一個專案裡看到明確進度。

| 主線部分 | 這一天要補上的東西 | 完成後的可見結果 |
| --- | --- | --- |
| API UI 狀態 | WeatherView 顯示 loading/error | 查詢失敗有友善訊息 |

當天接到主線專案的流程：

1. 搜尋開始清 error。
2. 顯示 loading。
3. catch 後顯示 error。

### 當天做完後檢查

- loading 不會卡住。
- 空結果有提示。
- 使用者知道發生什麼事。
### 範例範圍地圖

| State | 負責的事情 |
| --- | --- |
| `loading` | 顯示查詢中 |
| `errorMessage` | 顯示錯誤 |
| `result` | 顯示天氣 |

### 完整實作流程、範例與注意事項

```vue
<script setup lang="ts">
import { ref } from 'vue'
import { getWeather, type WeatherResult } from '../services/weatherApi'

const city = ref('Taipei')
const result = ref<WeatherResult | null>(null)
const loading = ref(false)
const errorMessage = ref('')

async function search() {
  loading.value = true
  errorMessage.value = ''

  try {
    result.value = await getWeather(city.value)
  } catch {
    errorMessage.value = '天氣資料讀取失敗'
  } finally {
    loading.value = false
  }
}
</script>
```

### 如果結果和預期不同

- loading 卡住：finally 沒設回 false。
- 錯誤訊息一直在：新查詢前沒清空。

### 負面例子 / 錯誤用法

錯誤做法：catch 空白，失敗時什麼都不顯示。
問題：使用者以為系統沒反應。
修正方向：顯示可理解錯誤訊息。

### 小練習

加入「重新查詢」按鈕。

### Junior 常見誤解

API 專案不是只處理成功資料，失敗狀態同樣是功能。

### 一句話總結

Loading、error、empty 是 API UI 的基本三狀態。

---

## Day 26：天氣 App 下

### 這篇文章主要在講什麼

完成天氣 app component 拆分與資料顯示。

### 為什麼需要這個概念

當畫面變多，service、view、card component 分工能讓程式更好維護。

### 學完這篇你應該會做到什麼

你應該能建立 `WeatherCard` component 顯示結果。

### 核心重點

- API 呼叫放 service。
- 頁面負責流程。
- Card 負責顯示。
- Props 定義資料型別。


### 主線專案銜接

這一天要接回 weather-dashboard 主線，而不是只停在單一語法。完成「畫面拆分」後，讀者應該能在同一個專案裡看到明確進度。

| 主線部分 | 這一天要補上的東西 | 完成後的可見結果 |
| --- | --- | --- |
| 畫面拆分 | 抽 SearchForm 與 WeatherCard | 流程和顯示分離 |

當天接到主線專案的流程：

1. SearchForm 管輸入與 emit。
2. WeatherCard 管顯示。
3. WeatherView 管流程狀態。

### 當天做完後檢查

- WeatherCard 不呼叫 API。
- SearchForm 不知道 store 細節。
- 父層流程清楚。
### 範例範圍地圖

| Component | 負責的事情 |
| --- | --- |
| `WeatherView` | 查詢流程 |
| `WeatherCard` | 顯示資料 |

### 完整實作流程、範例與注意事項

```vue
<!-- WeatherCard.vue -->
<script setup lang="ts">
defineProps<{
  city: string
  temperature: number
}>()
</script>

<template>
  <article>
    <h2>{{ city }}</h2>
    <p>{{ temperature }}°C</p>
  </article>
</template>
```

```vue
<WeatherCard
  v-if="result"
  :city="result.city"
  :temperature="result.temperature"
/>
```

### 如果結果和預期不同

- Prop 型別錯：確認 result mapping。
- Component 太依賴 API 原始格式：用 service mapping。

### 負面例子 / 錯誤用法

錯誤做法：WeatherCard 直接呼叫 API。
問題：顯示元件與資料取得耦合。
修正方向：父層查資料，子層顯示 props。

### 小練習

新增 `description` prop。

### Junior 常見誤解

Card component 應該專心顯示資料，不該知道資料從哪個 API 來。

### 一句話總結

完成 API 專案時，要把流程與顯示拆開。

---

## Day 27：Vue Best Practices

### 這篇文章主要在講什麼

整理寫 Vue 時應該知道的實務習慣。

### 為什麼需要這個概念

能跑的 Vue 和好維護的 Vue 不一樣；資料流、命名、component 邊界會影響長期維護。

### 學完這篇你應該會做到什麼

你應該能檢查自己的 Vue component 是否太大、資料流是否清楚。

### 核心重點

- Component 單一責任。
- Props down, events up。
- API 呼叫抽 service。
- 共用狀態才放 Pinia。
- `v-for` key 要穩定。


### 主線專案銜接

這一天要接回 weather-dashboard 主線，而不是只停在單一語法。完成「重構檢查」後，讀者應該能在同一個專案裡看到明確進度。

| 主線部分 | 這一天要補上的東西 | 完成後的可見結果 |
| --- | --- | --- |
| 重構檢查 | 依 best practices 調整邊界 | service/store/component 分工清楚 |

當天接到主線專案的流程：

1. 檢查 mega component。
2. 抽出 service/composable。
3. 確認 key、props、emit。

### 當天做完後檢查

- 沒有 index key。
- 沒有子層改 props。
- API mapping 集中。
### 範例範圍地圖

| 問題 | 建議位置 |
| --- | --- |
| API 呼叫 | `services/` |
| 共享狀態 | `stores/` |
| 重用邏輯 | `composables/` |
| UI 顯示 | `components/` |

### 完整實作流程、範例與注意事項

```text
檢查流程：
1. 這個 component 是否超過 200 行且混雜多種責任？
2. API response 是否直接散落在 template？
3. 是否用 index 當 key？
4. 子元件是否直接改 props？
5. 私密 key 是否進前端？
```

### 如果結果和預期不同

- 重構後壞掉：一次只移一個責任，搭配小測試或手動檢查。

### 負面例子 / 錯誤用法

錯誤做法：所有邏輯放在一個 mega component。
問題：難讀、難測、難修改。
修正方向：拆 service、component、store、composable。

### 小練習

把 WeatherView 的 API 呼叫抽到 service。

### Junior 常見誤解

Best practice 不是規矩崇拜，而是降低未來修改成本。

### 一句話總結

好 Vue 專案的關鍵是清楚資料流與清楚責任邊界。

---

## Day 28：COVID-19 追蹤 App 上

### 這篇文章主要在講什麼

原文以 COVID-19 app 練習 API；2026 可改成通用公開資料 dashboard，例如疫情、匯率、天氣或開放資料。

### 為什麼需要這個概念

Dashboard 類專案重點是資料模型、查詢條件與資料來源可靠性。

### 學完這篇你應該會做到什麼

你應該能定義 dashboard 的資料型別與 API service。

### 核心重點

- 先定義前端自己的資料型別。
- 外部 API response 要 mapping。
- 公開 API 可能停用，要抽換來源。
- 顯示資料要標註更新時間。


### 主線專案銜接

這一天要接回 weather-dashboard 主線，而不是只停在單一語法。完成「Dashboard 模型」後，讀者應該能在同一個專案裡看到明確進度。

| 主線部分 | 這一天要補上的東西 | 完成後的可見結果 |
| --- | --- | --- |
| Dashboard 模型 | 建立公開資料 dashboard 型別 | 外部 API 被轉成穩定資料模型 |

當天接到主線專案的流程：

1. 定義 PublicStat 或 dashboard item。
2. service mapping。
3. Card 顯示 mapping 後資料。

### 當天做完後檢查

- 標示更新時間。
- API 停用時可替換 service。
- template 不依賴外部欄位。
### 範例範圍地圖

| 部分 | 負責的事情 |
| --- | --- |
| `statsApi.ts` | 取得資料 |
| `DashboardView` | 顯示總覽 |
| `StatCard` | 顯示單筆數值 |

### 完整實作流程、範例與注意事項

```ts
export type PublicStat = {
  label: string
  value: number
  updatedAt: string
}

export async function getPublicStats(): Promise<PublicStat[]> {
  // 範例用途：取得公開統計資料。
  // 回傳結果：轉成前端穩定格式，不暴露外部 API 欄位。
  const response = await fetch('/api/public-stats')
  if (!response.ok) throw new Error('Stats API failed')
  return await response.json()
}
```

### 如果結果和預期不同

- API 停用：service 層抽換來源。
- 數字格式難讀：用 `Intl.NumberFormat`。

### 負面例子 / 錯誤用法

錯誤做法：template 直接依賴外部 API 原始欄位。
問題：API 欄位改名時整個畫面壞。
修正方向：service mapping。

### 小練習

把數字顯示加上千分位。

### Junior 常見誤解

公開資料 API 也會變動，前端要有隔離層。

### 一句話總結

Dashboard 的第一步是穩定自己的資料模型。

---

## Day 29：COVID-19 追蹤 App 中

### 這篇文章主要在講什麼

加入篩選、排序、圖表或卡片顯示。

### 為什麼需要這個概念

Dashboard 不只是顯示資料，還要讓使用者快速找到重點。

### 學完這篇你應該會做到什麼

你應該能用 computed 做資料篩選與排序。

### 核心重點

- 原始資料不要直接改壞。
- 篩選與排序用 computed。
- 空結果要顯示提示。
- 圖表資料最好也做 mapping。


### 主線專案銜接

這一天要接回 weather-dashboard 主線，而不是只停在單一語法。完成「篩選排序」後，讀者應該能在同一個專案裡看到明確進度。

| 主線部分 | 這一天要補上的東西 | 完成後的可見結果 |
| --- | --- | --- |
| 篩選排序 | 用 computed 產生顯示資料 | Dashboard 可搜尋與排序 |

當天接到主線專案的流程：

1. 保留原始 stats。
2. computed 做 filter/sort。
3. empty state 顯示無結果。

### 當天做完後檢查

- 沒有直接破壞原陣列排序。
- template 不塞長 filter。
- 篩選條件清楚。
### 範例範圍地圖

| State | 負責的事情 |
| --- | --- |
| `stats` | 原始資料 |
| `keyword` | 篩選字 |
| `filteredStats` | 顯示資料 |

### 完整實作流程、範例與注意事項

```vue
<script setup lang="ts">
import { computed, ref } from 'vue'

const keyword = ref('')
const stats = ref([
  { label: 'Taipei', value: 120 },
  { label: 'Tainan', value: 80 },
])

const filteredStats = computed(() => {
  return stats.value
    .filter((item) => item.label.toLowerCase().includes(keyword.value.toLowerCase()))
    .sort((a, b) => b.value - a.value)
})
</script>
```

### 如果結果和預期不同

- 原始排序被改掉：避免直接對原陣列 sort，先複製。
- 空結果沒提示：補 `v-if`。

### 負面例子 / 錯誤用法

錯誤做法：在 template 裡塞很長的 filter + sort。
問題：template 難讀。
修正方向：移到 computed。

### 小練習

加入最小數值篩選條件。

### Junior 常見誤解

computed 不只顯示數字，也很適合整理畫面要用的陣列。

### 一句話總結

Dashboard 的可讀性來自清楚的篩選與排序狀態。

---

## Day 30：COVID-19 追蹤 App 下

### 這篇文章主要在講什麼

完成專案收尾：錯誤處理、部署、README、下一步。

### 為什麼需要這個概念

專案收尾能讓你把練習變成作品，而不是只有本機能跑的 demo。

### 學完這篇你應該會做到什麼

你應該能完成 build、部署檢查與專案 README。

### 核心重點

- `npm run build` 必跑。
- README 要寫啟動方式。
- API 失敗要有 fallback UI。
- 部署後測試首頁與子頁重新整理。


### 主線專案銜接

這一天要接回 weather-dashboard 主線，而不是只停在單一語法。完成「收尾」後，讀者應該能在同一個專案裡看到明確進度。

| 主線部分 | 這一天要補上的東西 | 完成後的可見結果 |
| --- | --- | --- |
| 收尾 | 補 README、build、部署檢查 | 專案可被別人啟動 |

當天接到主線專案的流程：

1. 寫 Project Setup。
2. 跑 build/preview。
3. 部署後測首頁與子頁。

### 當天做完後檢查

- README 有 install/dev/build。
- 部署環境變數有說明。
- 主線功能走完一次。
### 範例範圍地圖

| 檢查 | 目的 |
| --- | --- |
| build | 確認可打包 |
| preview | 預覽正式結果 |
| README | 讓別人能啟動 |
| deploy | 對外展示 |

### 完整實作流程、範例與注意事項

```powershell
# 範例用途：專案收尾前檢查。
npm run build
npm run preview
```

```markdown
## Project Setup

```powershell
npm install
npm run dev
```

## Build

```powershell
npm run build
```
```

### 如果結果和預期不同

- build 失敗：先修 TypeScript/import。
- 部署子頁 404：設定 SPA fallback。
- API 失敗：確認 production API URL。

### 負面例子 / 錯誤用法

錯誤做法：沒有 README、沒有 build 檢查就當成完成。
問題：別人無法執行，你之後也很難接回來。
修正方向：補啟動、build、部署與環境變數說明。

### 小練習

把專案部署後，請朋友用手機開首頁與子頁測試。

### Junior 常見誤解

作品不是只看畫面，能不能被啟動、部署、排錯也很重要。

### 一句話總結

30 天 Vue 的成果，是你能用清楚資料流完成一個可部署的互動前端。
