# Vue3 手把手教學 2026 更新整理

來源系列：<https://ithelp.ithome.com.tw/m/users/20129072/ironman/3052>

整理日期：2026-05-04

這份筆記把原本 Vue.js 入門系列整理成 2026 年比較常見的 Vue 3 學習路線。原系列較接近 Vue 2 時代的寫法，這裡會用 Vue 3、Vite、Composition API、`<script setup>`、Pinia、Vue Router 4 重新包裝。

筆記目標不是讓你只「看得懂 code」，而是讓完全沒碰過 Vue 的人知道：

- 這個概念為什麼要學。
- 程式碼要放在哪個檔案。
- 要怎麼操作畫面或執行指令。
- 做完後應該看到什麼結果。
- 如果結果不同，要先查哪裡。
- AI 額外整理出的 2026 補充知識點是什麼。

## 這份筆記怎麼讀

建議你不要一次把 30 天都看完。每天照著「完整實作流程」動手做，確認畫面有變化，再讀「負面例子」和「Junior 常見誤解」。Vue 的重點是資料狀態、畫面、元件、路由、全域狀態彼此如何連起來；只背 API 名稱很容易卡住。

全系列會用一個延伸主線專案：`weather-dashboard`。這是 2026 補強設計，不代表原文一定使用同一個專案。你會一步一步做出一個天氣查詢 Dashboard，過程中學到 Vue 的基礎能力。

## Vue 2 到 Vue 3 對照

| 舊時代常見寫法 | 2026 建議寫法 |
| --- | --- |
| Vue CLI | Vite |
| Options API | Composition API + `<script setup>` |
| Vuex | Pinia |
| EventBus | props / emit / provide / inject / Pinia |
| `new Vue()` | `createApp()` |
| filters | computed 或 method |

## 主線專案

### 專案最終會長成什麼

最後會完成一個簡化版 Weather Dashboard：

- 可以輸入城市名稱。
- 可以顯示查詢結果、loading、error、empty 狀態。
- 可以把查詢紀錄存在 Pinia store。
- 可以用 Vue Router 在「查詢頁」與「歷史紀錄頁」切換。
- 可以整理成可 build、可部署的 Vue 3 專案。

### 需要的檔案地圖

| 類型 | 位置 | 負責的事情 |
| --- | --- | --- |
| App 入口 | `src/main.ts` | 建立 Vue app，掛載 Pinia 與 Router |
| 根元件 | `src/App.vue` | 放整體版面與 `RouterView` |
| 查詢頁 | `src/views/WeatherView.vue` | 輸入城市、呼叫 store、顯示狀態 |
| 歷史頁 | `src/views/HistoryView.vue` | 顯示查詢紀錄 |
| 表單元件 | `src/components/WeatherSearchForm.vue` | 接收城市輸入並 emit search |
| 顯示元件 | `src/components/WeatherCard.vue` | 顯示天氣結果 |
| 共用卡片 | `src/components/BaseCard.vue` | 練習 slot |
| 導覽列 | `src/components/AppNavbar.vue` | 切換頁面 |
| Store | `src/stores/weather.ts` | 管理查詢結果、loading、error、history |
| API Service | `src/services/weatherApi.ts` | 包裝 API 呼叫與 response mapping |
| Router | `src/router/index.ts` | 管理 `/weather`、`/history` |
| Deploy | `public/_redirects` | SPA 部署 fallback |

### 30 天交付物地圖

| Day | 主題 | 當天交付物 |
| --- | --- | --- |
| 1 | Vue 是什麼 | 建立 Vite Vue 3 專案並跑起來 |
| 2 | data & methods | 做一個 counter，理解 reactive state |
| 3 | directives 上 | 練習 `:href`、`@click`、文字顯示 |
| 4 | `v-if` / `v-for` | 顯示 todo list 與 empty state |
| 5 | `v-model` / directive | 做城市輸入框與自動 focus |
| 6 | class / style binding | 依狀態改變樣式 |
| 7 | Vue Devtools | 用工具觀察 component 與 state |
| 8 | computed | 做衍生資料與顯示格式 |
| 9 | watch | 觀察輸入變化與做提示 |
| 10 | lifecycle | 掛載 timer，並正確清理 |
| 11 | components | 拆出 `WeatherCard` |
| 12 | props | 父層把資料傳給子層 |
| 13 | emit | 子層把事件傳回父層 |
| 14 | slot | 做可重用 `BaseCard` |
| 15 | SFC | 整理 `.vue` 單檔元件 |
| 16 | Vite 專案結構 | 整理資料夾與 alias 概念 |
| 17 | EventBus 替代 | 理解為何不用 EventBus |
| 18 | store 入門 | 建立 Pinia store |
| 19 | Pinia 讀寫 | 用 Devtools 觀察 store |
| 20 | async action | 把 API 狀態放進 store |
| 21 | Router 上 | 建立頁面路由 |
| 22 | Router 下 | 路由參數、導覽與 fallback |
| 23 | 部署 | build、preview、SPA fallback |
| 24 | API service | 建立 `weatherApi.ts` |
| 25 | API UI 狀態 | 顯示 loading / error / result |
| 26 | component 組合 | 組合表單、卡片、頁面 |
| 27 | best practices | 檢查 component / store / service 分工 |
| 28 | Dashboard data | 建立統計資料與 mapping |
| 29 | filter / sort | 用 computed 做搜尋與排序 |
| 30 | 收尾 | README、build、部署檢查 |

### 主線端到端流程

1. 使用者進入 `/weather`。
2. `WeatherView` 顯示 `WeatherSearchForm`。
3. 使用者輸入城市並按搜尋。
4. `WeatherSearchForm` emit `search` 事件給父層。
5. `WeatherView` 呼叫 Pinia store 的 `load(city)`。
6. Store 設定 `loading = true`，呼叫 `weatherApi.getWeather(city)`。
7. API service 取得資料後轉成前端好用的 `WeatherResult`。
8. Store 更新 `result` 與 `history`。
9. `WeatherView` 根據 loading / error / empty / success 顯示不同畫面。
10. 使用者進入 `/history` 時，`HistoryView` 從 store 讀取紀錄。

### 工作流程式範例設計

這份筆記的範例不只用來展示語法，而是模擬實務任務。每一天都會盡量對應到一種工作流程：

1. 需求或任務從哪裡來：例如 PM 要新增查詢欄位、QA 回報 loading 沒消失、後端調整 API response。
2. 你要先判斷改哪一層：component、view、store、service、router、README 或部署設定。
3. 你要知道輸入是什麼：使用者輸入、URL param、API response、store state、環境變數。
4. 你要知道輸出是什麼：畫面狀態、路由變化、API request、store 更新、build 結果。
5. 你要知道怎麼交付：跑 dev、用 Devtools 看資料、測 empty / loading / error / success、跑 build。

所以看到範例時，不要只問「這段 code 做什麼」，還要問「這段 code 在專案流程裡負責哪一段」。

### 主線做完後檢查

- `npm run dev` 可以開啟開發伺服器。
- `/weather` 可以輸入城市並看到結果或模擬資料。
- `/history` 可以看到查詢紀錄。
- `npm run build` 可以成功產生 `dist`。
- 直接重新整理 `/weather` 不會因 SPA fallback 設定而 404。

---

## Day 1：Vue.js 是什麼

### 這篇文章主要在講什麼

Vue 是一個前端框架，用來把資料狀態和畫面連在一起。你不需要手動一直操作 DOM，而是改資料，讓 Vue 幫你更新畫面。

### 為什麼需要這個概念

如果只用原生 JavaScript 或 jQuery，畫面一複雜，就會到處出現 `querySelector`、手動改文字、手動新增節點。Vue 的價值是把「資料」變成畫面的來源，讓 UI 比較可預測。

### 學完這篇你應該會做到什麼

- 建立一個 Vue 3 + TypeScript + Vite 專案。
- 知道 `src/main.ts` 和 `src/App.vue` 的角色。
- 能啟動 dev server 並確認畫面有跑起來。

### 核心重點

- Vue app 從 `createApp(App).mount('#app')` 開始。
- `App.vue` 是根元件。
- Vite 是開發工具，負責啟動、熱更新與 build。
- Vue 的核心思路是資料驅動畫面。

### 真實工作流程例子

工作任務：你加入一個前端專案，第一件事不是馬上寫功能，而是確認本機環境能不能跑起來。

你要做的是：依 README 或專案指令安裝套件、啟動 dev server、確認首頁會顯示，並截圖或回報「本機已可開發」。如果這一步失敗，後面所有功能修改都沒有可靠基礎。

### 主線專案銜接

今天建立主線專案的地基。

1. 建立 `weather-dashboard` 專案。
2. 啟動 Vite dev server。
3. 修改 `src/App.vue`，確認畫面會跟著檔案變更更新。

具體會動到：

- `package.json`：放 npm scripts。
- `src/main.ts`：建立 Vue app。
- `src/App.vue`：目前先顯示一段歡迎文字。

做完可驗證：

- 終端機出現 Vite local URL。
- 瀏覽器能看到 Vue 頁面。
- 修改 `App.vue` 後畫面自動更新。

### 當天做完後檢查

- 已安裝 Node.js LTS。
- `npm install` 沒有錯誤。
- `npm run dev` 能成功執行。
- 瀏覽器開啟 local URL 能看到頁面。
- 修改 `App.vue` 後不用重開 server 也會更新。

### 範例範圍地圖

| 部分 | 位置 / 指令 | 負責的事情 |
| --- | --- | --- |
| 建立專案 | `npm create vite@latest weather-dashboard -- --template vue-ts` | 產生 Vue 3 專案 |
| 安裝套件 | `npm install` | 安裝相依套件 |
| 啟動開發 | `npm run dev` | 開啟 dev server |
| 根元件 | `src/App.vue` | 顯示第一個畫面 |

### 完整實作流程、範例與注意事項

完整流程：

1. 在筆記資料夾外或你習慣放專案的位置開啟 PowerShell。
2. 執行建立專案指令。
3. 進入專案資料夾。
4. 安裝套件。
5. 啟動 dev server。
6. 用瀏覽器打開終端機顯示的 local URL。
7. 修改 `src/App.vue`，確認畫面熱更新。

```powershell
# 範例用途：建立 Vue 3 + TypeScript 專案。
# 主要輸入：weather-dashboard 是資料夾名稱，可以改成你的專案名稱。
# 執行結果：產生專案、安裝套件、啟動本機開發伺服器。
npm create vite@latest weather-dashboard -- --template vue-ts
cd weather-dashboard
npm install
npm run dev
```

把 `src/App.vue` 先改成：

```vue
<template>
  <main>
    <h1>Weather Dashboard</h1>
    <p>今天先確認 Vue 專案已經跑起來。</p>
  </main>
</template>
```

注意事項：

- 指令要在專案資料夾內執行，否則 `npm run dev` 會找不到 `package.json`。
- 如果 port 被占用，Vite 可能會自動換 port，以終端機顯示為準。
- 不要先急著安裝 Pinia 或 Router，先確認最小專案會跑。

### 如果結果和預期不同

- `npm` 找不到：先安裝 Node.js LTS，並重新開啟 PowerShell。
- 瀏覽器打不開：確認 `npm run dev` 還在執行中。
- 畫面沒更新：確認你改的是目前專案的 `src/App.vue`。

### 負面例子 / 錯誤用法

錯誤做法：還沒確認專案能跑，就先貼一大堆 component、router、store。

問題：錯誤來源會混在一起，你不知道是環境錯、Vue 錯，還是套件設定錯。

修正方向：Day 1 只做一件事，確認最小 Vue app 能啟動。

### 小練習

把 `Weather Dashboard` 改成你的練習專案名稱，並新增一行今天日期。

### Junior 常見誤解

Vue 不是一個 HTML 模板工具而已。它的核心是「資料改變，畫面跟著改變」。

### 一句話總結

先讓 Vue 專案跑起來，後面的所有學習才有地方放。

---

## Day 2：data & methods

### 這篇文章主要在講什麼

早期 Vue 會講 `data` 和 `methods`。Vue 3 在 `<script setup>` 中通常使用 `ref()` 建立資料，用一般 function 當作方法。

### 為什麼需要這個概念

互動式畫面一定需要資料狀態，例如按鈕點了幾次、目前輸入什麼城市、API 是否正在載入。沒有 state，就只能手動改 DOM。

### 學完這篇你應該會做到什麼

- 使用 `ref()` 建立 reactive state。
- 在 script 中用 `.value` 修改 state。
- 用 `@click` 呼叫 function。

### 核心重點

- `ref(0)` 會建立可被 Vue 追蹤的資料。
- `<script setup>` 裡讀寫 ref 要用 `.value`。
- template 裡 Vue 會自動 unwrap，所以寫 `{{ count }}`。
- method 就是一般 function。

### 真實工作流程例子

工作任務：PM 說「搜尋按鈕點下去後，先在畫面上顯示使用者按了幾次，讓我們確認事件有接到」。

你要做的是：在 component 建立 state 保存次數，用 button click 更新 state，再用畫面文字驗證事件真的有觸發。這是很多功能開發的第一步：先確認互動事件和畫面更新正常，再接 API 或複雜流程。

### 主線專案銜接

今天先做一個 counter，理解 Weather Dashboard 未來的查詢次數、loading 狀態都會用同樣概念。

1. 修改 `src/App.vue`。
2. 新增 `count` state。
3. 新增 `increment()` 方法。
4. 點按鈕確認畫面數字會變。

具體會動到：

- `src/App.vue`：加入 `ref`、button、click handler。

做完可驗證：

- 按鈕每點一次數字加 1。
- 瀏覽器 Console 沒有錯。
- script 內使用 `count.value`，template 使用 `count`。

### 當天做完後檢查

- `import { ref } from 'vue'` 有寫。
- `count` 是 `ref(0)`。
- `increment()` 內有修改 `count.value`。
- button 有 `@click="increment"`。
- 點擊後畫面會更新。

### 範例範圍地圖

| 部分 | 位置 | 負責的事情 |
| --- | --- | --- |
| State | `src/App.vue` 的 `<script setup>` | 保存目前數字 |
| Method | `increment()` | 修改數字 |
| Template | `button` | 讓使用者觸發方法 |

### 完整實作流程、範例與注意事項

完整流程：

1. 開啟 `src/App.vue`。
2. 加入 `<script setup lang="ts">`。
3. 從 Vue import `ref`。
4. 建立 `count`。
5. 建立 `increment()`。
6. 在 template 放按鈕與目前數字。
7. 回到瀏覽器點按鈕測試。

```vue
<script setup lang="ts">
import { ref } from 'vue'

// 範例用途：保存按鈕被點擊的次數。
// 主要輸入：使用者每點一次 button，就會呼叫 increment。
// 執行結果：count.value 增加，template 重新顯示新數字。
const count = ref(0)

function increment() {
  count.value++
}
</script>

<template>
  <main>
    <h1>Weather Dashboard</h1>
    <p>你已經點了 {{ count }} 次。</p>
    <button type="button" @click="increment">增加次數</button>
  </main>
</template>
```

注意事項：

- 在 script 寫 `count++` 是錯的，因為 `count` 是 ref 物件。
- 在 template 不需要寫 `count.value`。
- function 名稱要描述行為，例如 `increment` 比 `doSomething` 清楚。

### 如果結果和預期不同

- 點了沒反應：檢查 button 是否有 `@click="increment"`。
- Console 顯示 `ref is not defined`：檢查 import。
- TypeScript 報錯：確認 `<script setup lang="ts">` 標籤完整。

### 負面例子 / 錯誤用法

錯誤做法：

```ts
let count = 0

function increment() {
  count++
}
```

問題：一般變數不會被 Vue 追蹤，畫面不一定會更新。

修正方向：需要顯示在畫面上的資料，用 `ref()` 或 `reactive()`。

### 小練習

新增一個 `decrement()`，讓按鈕可以把數字減 1。

### Junior 常見誤解

`.value` 不是多餘語法，它是 Vue 讓 primitive value 被追蹤的方式。

### 一句話總結

Vue 互動的起點是 reactive state 加上會改變 state 的 function。

---

## Day 3：Vue directives 上

### 這篇文章主要在講什麼

Directive 是 Vue template 裡以 `v-` 或簡寫符號出現的特殊語法，用來把資料、事件和 DOM 屬性連起來。

### 為什麼需要這個概念

畫面不只要顯示文字，也常需要綁定連結、class、事件。Directive 讓這些操作不用手動寫 DOM code。

### 學完這篇你應該會做到什麼

- 用 `:href` 綁定連結。
- 用 `@click` 綁定事件。
- 用 `v-text` 或 `{{ }}` 顯示文字。

### 核心重點

- `:href` 是 `v-bind:href` 的簡寫。
- `@click` 是 `v-on:click` 的簡寫。
- `{{ message }}` 適合顯示純文字。
- `v-html` 有 XSS 風險，新手先避免。

### 真實工作流程例子

工作任務：設計稿上有一個「查看官方文件」連結，使用者點擊後也要在頁面上留下操作提示。

你要做的是：把 URL 當成資料管理，用 `:href` 綁定，點擊時用 `@click` 更新提示 state。真實專案中，這類做法可延伸到「點擊追蹤」、「開啟外部說明」、「顯示已讀狀態」。

### 主線專案銜接

今天讓首頁多一個外部文件連結，並在點擊後顯示狀態文字。

1. 在 `src/App.vue` 建立 `docsUrl`。
2. 用 `:href` 綁到 `<a>`。
3. 用 `@click` 更新 `message`。

具體會動到：

- `src/App.vue`：練習資料綁定與事件綁定。

做完可驗證：

- 連結 href 是 Vue 文件網址。
- 點擊連結後文字會改變。
- 沒有使用 `document.querySelector()`。

### 當天做完後檢查

- `docsUrl` 有被 `:href` 使用。
- `message` 有顯示在畫面。
- `markClicked()` 會修改 `message.value`。
- 點擊連結時不會出現錯誤。
- 沒有使用 `v-html` 顯示不可信內容。

### 範例範圍地圖

| 部分 | 位置 | 負責的事情 |
| --- | --- | --- |
| URL state | `docsUrl` | 保存連結目標 |
| Message state | `message` | 保存點擊後提示 |
| Event | `markClicked()` | 點擊時修改提示 |

### 完整實作流程、範例與注意事項

完整流程：

1. 開啟 `src/App.vue`。
2. 建立 `docsUrl` 和 `message`。
3. 建立 `markClicked()`。
4. 在 template 放 `<a>`。
5. 用 `:href` 綁定連結。
6. 用 `@click` 綁定點擊事件。
7. 在瀏覽器點擊並觀察文字變化。

```vue
<script setup lang="ts">
import { ref } from 'vue'

// 範例用途：讓連結與提示文字都由 Vue state 控制。
// 主要輸入：使用者點擊 GitHub / Vue 文件連結。
// 執行結果：message 更新，畫面顯示「你點過連結」。
const docsUrl = ref('https://vuejs.org/')
const message = ref('還沒點擊文件連結')

function markClicked() {
  message.value = '你點過 Vue 官方文件連結'
}
</script>

<template>
  <main>
    <a :href="docsUrl" target="_blank" rel="noreferrer" @click="markClicked">
      Vue 官方文件
    </a>
    <p>{{ message }}</p>
  </main>
</template>
```

注意事項：

- `href="docsUrl"` 會變成文字 docsUrl，不會讀取變數。
- `:href="docsUrl"` 才是把變數值綁上去。
- 外部連結搭配 `target="_blank"` 時，建議加 `rel="noreferrer"`。

### 如果結果和預期不同

- 連結跳錯位置：用瀏覽器檢查元素看 href 是否正確。
- 文字沒改：檢查 `@click` 是否拼錯。
- 開新分頁被擋：先確認瀏覽器設定，這和 Vue 無關。

### 負面例子 / 錯誤用法

錯誤做法：用 `v-html` 顯示使用者輸入的 HTML。

問題：如果內容含有惡意 script，可能造成 XSS。

修正方向：預設用 `{{ }}` 顯示純文字，只有在內容來源可信且已消毒時才考慮 `v-html`。

### 小練習

新增一個 `button`，點擊後把 `docsUrl` 改成 Vue Router 官方文件。

### Junior 常見誤解

`:` 不是裝飾符號，而是告訴 Vue「右邊是 JavaScript 表達式」。

### 一句話總結

Directive 是 Vue template 和資料、事件、屬性溝通的橋。

---

## Day 4：v-if & v-for

### 這篇文章主要在講什麼

`v-if` 用來決定要不要顯示某段畫面，`v-for` 用來把陣列資料渲染成列表。

### 為什麼需要這個概念

真實畫面常會有「有資料才顯示列表，沒有資料顯示空狀態」。Weather Dashboard 未來查不到資料時，也需要這種 UI 狀態。

### 學完這篇你應該會做到什麼

- 用 `v-if` / `v-else` 顯示不同狀態。
- 用 `v-for` 顯示陣列。
- 知道為什麼要寫 `:key`。

### 核心重點

- `v-if` 會真的新增或移除 DOM。
- `v-show` 是用 CSS 顯示或隱藏。
- `v-for` 一定要給穩定的 `:key`。
- 不建議同一個元素同時放 `v-if` 和 `v-for`。

### 真實工作流程例子

工作任務：後端還沒接好 API，但 PM 要先看「查詢紀錄列表」和「沒有紀錄時的提示」。

你要做的是：先用假資料建立列表，確認有資料和沒資料兩種狀態都能顯示。這是前端常見工作流程：API 未完成時，先用 mock data 做 UI 狀態，之後再替換資料來源。

### 主線專案銜接

今天先用 todo list 模擬未來的查詢紀錄列表。

1. 建立 `todos` 陣列。
2. 有資料時用 `v-for` 顯示。
3. 沒資料時用 `v-if` 顯示 empty state。

具體會動到：

- `src/App.vue`：建立列表資料與條件顯示。

做完可驗證：

- 有資料時顯示多個項目。
- 把陣列清空時顯示「目前沒有資料」。
- 每個項目都有穩定 `:key`。

### 當天做完後檢查

- `todos` 是陣列。
- `v-if="todos.length === 0"` 有處理空狀態。
- `v-for="todo in todos"` 有搭配 `:key="todo.id"`。
- template 沒有直接修改陣列。
- 瀏覽器 Console 沒有 key 警告。

### 範例範圍地圖

| 部分 | 位置 | 負責的事情 |
| --- | --- | --- |
| List state | `todos` | 保存列表資料 |
| Empty UI | `v-if` | 沒資料時顯示提示 |
| List UI | `v-for` | 有資料時逐筆顯示 |

### 完整實作流程、範例與注意事項

完整流程：

1. 開啟 `src/App.vue`。
2. 建立 `todos`。
3. 在 template 先判斷 `todos.length`。
4. 有資料時渲染 `<ul>`。
5. 用 `:key` 綁定每筆資料的 id。
6. 暫時把 `todos` 改成空陣列，測試 empty state。

```vue
<script setup lang="ts">
import { ref } from 'vue'

// 範例用途：用陣列資料渲染清單。
// 主要輸入：todos 陣列，未來可替換成查詢歷史紀錄。
// 執行結果：有資料時顯示列表，沒資料時顯示空狀態。
const todos = ref([
  { id: 1, title: '建立 Vue 專案' },
  { id: 2, title: '練習 ref 和 click' },
])
</script>

<template>
  <p v-if="todos.length === 0">目前沒有資料。</p>

  <ul v-else>
    <li v-for="todo in todos" :key="todo.id">
      {{ todo.title }}
    </li>
  </ul>
</template>
```

注意事項：

- `:key` 不要優先用 index，因為列表重新排序或刪除時容易讓 Vue 誤判。
- empty state 不是小事；真實產品中使用者需要知道「不是壞掉，是沒有資料」。
- `v-if` 和 `v-for` 放同一個元素會讓邏輯難讀，先在外層分開。

### 如果結果和預期不同

- 顯示 `[object Object]`：你直接印了整個物件，應改成 `todo.title`。
- Console 有 key 警告：補上 `:key`。
- 空陣列時整頁空白：補上 `v-if` empty state。

### 負面例子 / 錯誤用法

錯誤做法：

```vue
<li v-for="(todo, index) in todos" :key="index">
  {{ todo.title }}
</li>
```

問題：資料順序改變時，index 不是穩定身份，可能造成畫面狀態錯亂。

修正方向：資料要有自己的 id，使用 `:key="todo.id"`。

### 小練習

新增一個「清空」按鈕，把 `todos.value = []`，觀察 empty state 是否出現。

### Junior 常見誤解

`key` 不是給人看的編號，而是 Vue 用來辨識每個節點身份的依據。

### 一句話總結

列表 UI 的基本功是資料陣列、條件顯示、穩定 key。

---

## Day 5：v-model & custom directives

### 這篇文章主要在講什麼

`v-model` 用來讓表單輸入和 state 雙向同步；custom directive 則可以封裝少量直接操作 DOM 的行為。

### 為什麼需要這個概念

Weather Dashboard 需要輸入城市名稱。你希望使用者在 input 打字時，Vue state 立刻知道目前輸入值。

### 學完這篇你應該會做到什麼

- 用 `v-model` 綁定 input。
- 使用 `.trim` 修飾符清理輸入。
- 寫一個簡單 `v-focus` directive。

### 核心重點

- `v-model` 等於 value + input event 的常用組合。
- `v-model.trim` 會自動移除前後空白。
- custom directive 適合處理 focus、tooltip 這類 DOM 行為。
- 商業邏輯不要放進 directive。

### 真實工作流程例子

工作任務：客服回報使用者常忘記先點城市輸入框，希望進入查詢頁時游標自動停在 input。

你要做的是：城市文字用 `v-model` 管理，focus 這種 DOM 行為用 directive 處理。這能分清楚「表單資料」和「操作體驗」兩件事。

### 主線專案銜接

今天做出城市輸入欄位，這會成為搜尋表單的前身。

1. 建立 `city` state。
2. input 使用 `v-model.trim`。
3. 建立 `vFocus` 讓頁面載入後自動 focus。
4. 在畫面顯示目前輸入值。

具體會動到：

- `src/App.vue`：新增城市輸入框。

做完可驗證：

- 打字時下方文字同步更新。
- 頁面載入後游標在 input。
- 前後空白會被 trim。

### 當天做完後檢查

- input 有 `v-model.trim="city"`。
- `city` 是 `ref('')`。
- `vFocus` 有 `mounted()`。
- input 有 `v-focus`。
- 輸入空白前後不會保留在 state 裡。

### 範例範圍地圖

| 部分 | 位置 | 負責的事情 |
| --- | --- | --- |
| Input state | `city` | 保存城市名稱 |
| 表單輸入 | `<input>` | 讓使用者輸入 |
| Custom directive | `vFocus` | 自動 focus input |

### 完整實作流程、範例與注意事項

完整流程：

1. 開啟 `src/App.vue`。
2. 建立 `city` state。
3. 建立 `vFocus` 物件。
4. 在 template 建立 input。
5. input 加上 `v-model.trim`。
6. input 加上 `v-focus`。
7. 重新整理頁面，確認游標自動進入 input。

```vue
<script setup lang="ts">
import { ref } from 'vue'

// 範例用途：保存使用者輸入的城市名稱。
// 主要輸入：input 文字。
// 執行結果：city 會和 input 內容同步，未來會拿去查詢天氣。
const city = ref('')

const vFocus = {
  mounted(el: HTMLInputElement) {
    el.focus()
  },
}
</script>

<template>
  <label for="city">城市名稱</label>
  <input id="city" v-model.trim="city" v-focus placeholder="例如 Taipei" />
  <p>目前輸入：{{ city || '尚未輸入' }}</p>
</template>
```

注意事項：

- custom directive 名稱在 script 中寫 `vFocus`，template 使用 `v-focus`。
- `v-model` 只負責同步資料，不負責驗證資料是否有效。
- 表單送出時仍要檢查空字串。

### 如果結果和預期不同

- 頁面沒有 focus：確認 directive 名稱是 `vFocus` 與 `v-focus`。
- 打字畫面沒同步：確認 `city` 是 ref 且有 import。
- 空白沒有移除：確認有使用 `.trim`。

### 負面例子 / 錯誤用法

錯誤做法：在 directive 裡面直接呼叫 API。

問題：directive 的責任會變得混亂，測試與維護都困難。

修正方向：directive 只處理 DOM 行為；資料流程交給 component、composable 或 store。

### 小練習

新增一個提示：當 `city` 是空字串時顯示「請輸入城市」。

### Junior 常見誤解

`v-model` 不是魔法，它只是幫你省掉手動監聽 input event 和更新 state 的樣板 code。

### 一句話總結

表單輸入用 `v-model` 管資料，DOM 小動作用 custom directive 封裝。

---

## Day 6：class 與 style binding

### 這篇文章主要在講什麼

Vue 可以依照 state 動態改變 class 或 style，讓畫面狀態更清楚。

### 為什麼需要這個概念

使用者輸入空城市時，你可能想讓提示變紅；查詢成功時顯示綠色狀態。這些都不應該靠手動操作 DOM。

### 學完這篇你應該會做到什麼

- 用 `:class` 根據條件切換樣式。
- 用 `:style` 綁定簡單樣式。
- 知道狀態樣式應該從資料推導。

### 核心重點

- `:class="{ invalid: hasError }"` 是物件語法。
- `:class="[baseClass, statusClass]"` 是陣列語法。
- 大量樣式仍應寫在 CSS class，而不是全塞進 inline style。
- UI 狀態要能從 state 看出來。

### 真實工作流程例子

工作任務：QA 回報使用者按搜尋後，空城市也能送出，畫面沒有明顯錯誤提示。

你要做的是：用 state 或 computed 判斷輸入是否合法，再把這個判斷接到 class 和提示文字。交付時要測「空字串」、「一個字」、「正常城市」三種狀態。

### 主線專案銜接

今天讓城市輸入欄位有驗證樣式。

1. 建立 `hasCityError`。
2. input 根據錯誤狀態套用 class。
3. 錯誤時顯示提示文字。

具體會動到：

- `src/App.vue`：加入 class binding 和 style。

做完可驗證：

- 沒輸入城市時有錯誤樣式。
- 輸入城市後錯誤樣式消失。
- class 由 state 控制，不是手動改 DOM。

### 當天做完後檢查

- 有 `computed` 或簡單條件判斷錯誤狀態。
- input 有 `:class`。
- CSS class 有定義。
- 錯誤文字只在需要時出現。
- 沒有使用 `element.style.xxx`。

### 範例範圍地圖

| 部分 | 位置 | 負責的事情 |
| --- | --- | --- |
| State | `city` | 使用者輸入 |
| Derived state | `hasCityError` | 判斷是否錯誤 |
| Style | `<style scoped>` | 定義錯誤樣式 |

### 完整實作流程、範例與注意事項

完整流程：

1. 開啟 `src/App.vue`。
2. import `computed`。
3. 建立 `hasCityError`。
4. input 加上 `:class`。
5. 新增錯誤文字。
6. 補上 `<style scoped>`。
7. 清空和輸入城市各測一次。

```vue
<script setup lang="ts">
import { computed, ref } from 'vue'

const city = ref('')

// 範例用途：從輸入值推導 UI 是否要顯示錯誤。
// 主要輸入：city.value。
// 執行結果：city 空白時，input 會套用 invalid class。
const hasCityError = computed(() => city.value.length === 0)
</script>

<template>
  <input
    v-model.trim="city"
    :class="{ invalid: hasCityError }"
    placeholder="例如 Taipei"
  />
  <p v-if="hasCityError" class="error">請輸入城市名稱。</p>
</template>

<style scoped>
.invalid {
  border: 1px solid #d93025;
}

.error {
  color: #d93025;
}
</style>
```

注意事項：

- `computed` 很適合表達「由其他資料推導出的狀態」。
- inline style 可以用，但長期維護通常 class 比較清楚。
- scoped style 只影響目前元件，適合入門練習。

### 如果結果和預期不同

- class 沒出現：用 Devtools 檢查 input class。
- 錯誤提示一直出現：確認 `city` 是否真的有更新。
- CSS 沒效果：確認 class 名稱拼字一致。

### 負面例子 / 錯誤用法

錯誤做法：用 `document.querySelector('input').style.border = ...`。

問題：你繞過 Vue 的資料流程，畫面狀態會分散在 DOM 操作裡。

修正方向：用 state 推導 class，讓畫面和資料保持一致。

### 小練習

當 `city.length >= 2` 時顯示綠色提示「可以搜尋」。

### Junior 常見誤解

動態樣式不是「讓 CSS 變複雜」，而是讓 UI 狀態能被資料控制。

### 一句話總結

畫面樣式應該跟著 state 變，而不是靠手動抓 DOM。

---

## Day 7：Vue Devtools

### 這篇文章主要在講什麼

Vue Devtools 是用來觀察 Vue component、props、state、Pinia store 和事件的瀏覽器工具。

### 為什麼需要這個概念

新手常遇到「畫面沒變，不知道資料有沒有變」。Devtools 可以讓你確認問題在資料、元件、事件還是畫面。

### 學完這篇你應該會做到什麼

- 安裝或開啟 Vue Devtools。
- 找到目前 component。
- 查看 `city`、`count` 這類 state。

### 核心重點

- Devtools 是除錯工具，不是 production 功能。
- 可以看 component tree。
- 可以看 props 和 state。
- 之後搭配 Pinia 可以看 store。

### 真實工作流程例子

工作任務：你改了表單，但 QA 說畫面顯示不對。你不能只猜，要先確認資料有沒有進 Vue state。

你要做的是：開 Vue Devtools，看 component state 是否隨 input 改變。如果 state 正確，問題可能在 template 或樣式；如果 state 不對，問題可能在 `v-model` 或事件。

### 主線專案銜接

今天不新增大型功能，而是學會觀察目前的 Vue app。

1. 開啟瀏覽器開發者工具。
2. 找到 Vue Devtools 面板。
3. 修改 input，觀察 state 是否變化。

具體會動到：

- 不一定修改檔案；重點是操作瀏覽器工具。

做完可驗證：

- Devtools 能看到 `App` component。
- 修改城市輸入時，state 跟著變。
- 如果畫面錯了，能先確認資料是否正確。

### 當天做完後檢查

- 已安裝 Vue Devtools extension 或使用支援的瀏覽器工具。
- `npm run dev` 正在執行。
- Vue 面板看得到 component tree。
- 點選 `App` 可以看到 state。
- 修改 input 時 state 有更新。

### 範例範圍地圖

| 部分 | 位置 / 介面 | 負責的事情 |
| --- | --- | --- |
| Dev server | `npm run dev` | 讓 app 在本機跑 |
| Browser Devtools | Vue 面板 | 查看 component |
| Component state | `App` | 觀察目前資料 |

### 完整實作流程、範例與注意事項

完整流程：

1. 執行 `npm run dev`。
2. 用瀏覽器開啟 Vite local URL。
3. 開啟瀏覽器開發者工具。
4. 切到 Vue 面板。
5. 點選 `App` component。
6. 在畫面 input 輸入 `Taipei`。
7. 回 Devtools 觀察 state 是否改變。

```text
操作重點：
1. 畫面操作：在 input 輸入城市。
2. Devtools 檢查：確認 App component 的 city 變成 Taipei。
3. 判斷方式：如果 state 有變但畫面沒變，問題多半在 template；如果 state 沒變，問題多半在 v-model 或事件。
```

注意事項：

- Devtools 面板看不到 Vue，先確認頁面是 dev server 開的，不是打開錯檔案。
- production build 可能不顯示完整除錯資訊。
- 不要用 Devtools 修改資料當作正式功能，那只是除錯。

### 如果結果和預期不同

- 沒看到 Vue 面板：重新整理頁面，或確認 extension 已啟用。
- 看不到 state：點對 component，可能你選到 DOM panel 不是 Vue panel。
- state 有變但畫面沒變：檢查 template 是否顯示正確變數。

### 負面例子 / 錯誤用法

錯誤做法：只看畫面猜問題，不看 state。

問題：你可能把 v-model、computed、template、CSS 問題混在一起。

修正方向：先用 Devtools 確認資料，再判斷是哪一層壞。

### 小練習

在 Devtools 觀察 Day 6 的 `hasCityError`，看看輸入城市前後它如何變化。

### Junior 常見誤解

Devtools 不是只有資深工程師才用；新手更需要它來看資料流。

### 一句話總結

不確定 Vue 發生什麼事時，先用 Devtools 看資料和元件。

---

## Day 8：computed

### 這篇文章主要在講什麼

`computed` 用來建立「由其他 state 推導出的資料」，例如格式化文字、篩選結果、是否可搜尋。

### 為什麼需要這個概念

如果 template 裡塞太多運算，畫面會很難讀；如果每次都手動更新衍生資料，又容易忘記同步。

### 學完這篇你應該會做到什麼

- 建立 computed。
- 用 computed 判斷 button 是否可用。
- 用 computed 顯示格式化文字。

### 核心重點

- computed 適合「有回傳值」的推導資料。
- computed 會根據依賴快取。
- 不要在 computed 裡做 API 呼叫或改 state。
- template 應該保持好讀。

### 真實工作流程例子

工作任務：PM 要搜尋按鈕在城市名稱太短時不能按，並顯示目前準備查詢的城市。

你要做的是：不要把判斷全部寫在 template，改用 computed 建立 `canSearch` 和顯示文字。交付時要測 button disabled 狀態和提示文字是否同步。

### 主線專案銜接

今天讓搜尋按鈕只有在城市名稱有效時才能按。

1. 建立 `canSearch`。
2. 建立 `cityLabel`。
3. button 用 `:disabled="!canSearch"`。

具體會動到：

- `src/App.vue`：加入 computed 作為 UI 判斷。

做完可驗證：

- 沒輸入城市時 button disabled。
- 輸入至少 2 個字後 button 可以按。
- 顯示文字會格式化。

### 當天做完後檢查

- 有 import `computed`。
- `canSearch` 只負責回傳 boolean。
- `cityLabel` 只負責顯示文字。
- computed 裡沒有修改其他 state。
- button disabled 狀態正確。

### 範例範圍地圖

| 部分 | 位置 | 負責的事情 |
| --- | --- | --- |
| 原始 state | `city` | 使用者輸入 |
| 衍生 state | `canSearch` | 判斷是否可搜尋 |
| 衍生文字 | `cityLabel` | 顯示目前城市 |
| UI | button / p | 使用 computed 結果 |

### 完整實作流程、範例與注意事項

完整流程：

1. 開啟 `src/App.vue`。
2. import `computed`。
3. 建立 `canSearch`。
4. 建立 `cityLabel`。
5. button 綁定 disabled。
6. 測試空字串、一個字、兩個字以上。

```vue
<script setup lang="ts">
import { computed, ref } from 'vue'

const city = ref('')

// 範例用途：判斷城市名稱是否足夠搜尋。
// 主要輸入：city.value。
// 執行結果：template 可以用 canSearch 決定 button 是否 disabled。
const canSearch = computed(() => city.value.length >= 2)

const cityLabel = computed(() => {
  return city.value ? `準備查詢：${city.value}` : '請先輸入城市'
})
</script>

<template>
  <input v-model.trim="city" placeholder="例如 Taipei" />
  <p>{{ cityLabel }}</p>
  <button type="button" :disabled="!canSearch">搜尋</button>
</template>
```

注意事項：

- computed 名稱建議像變數，不要像動作；例如 `canSearch`，不是 `checkCanSearch`。
- computed 不接參數；需要接參數的邏輯通常用 function。
- computed 裡不要寫副作用。

### 如果結果和預期不同

- button 一直不能按：檢查 `city.value.length >= 2` 條件。
- computed 沒更新：確認依賴的是 reactive state。
- template 太亂：把表達式移到 computed。

### 負面例子 / 錯誤用法

錯誤做法：

```ts
const canSearch = computed(() => {
  city.value = city.value.trim()
  return city.value.length >= 2
})
```

問題：computed 裡修改 state 會讓資料流變難預測。

修正方向：資料清理用 `v-model.trim` 或事件處理；computed 只做推導。

### 小練習

新增 `cityUppercase`，把輸入城市轉成大寫顯示。

### Junior 常見誤解

computed 不是 method 的替代品；它適合表示「由資料算出的資料」。

### 一句話總結

能由 state 推導出來的顯示資料，優先考慮 computed。

---

## Day 9：watch

### 這篇文章主要在講什麼

`watch` 用來觀察某個 state 的變化，並在變化時執行副作用，例如寫 log、呼叫 API、同步 localStorage。

### 為什麼需要這個概念

有些事情不是「算出一個值」，而是「資料變了，要做一件事」。例如城市輸入太短時顯示提示，或未來做 debounce 查詢。

### 學完這篇你應該會做到什麼

- 使用 `watch()` 觀察 `city`。
- 區分 computed 和 watch。
- 避免在 watch 中造成無限更新。

### 核心重點

- computed 用來回傳衍生值。
- watch 用來處理副作用。
- watch 可以取得新值與舊值。
- 不要濫用 watch 取代正常資料流。

### 真實工作流程例子

工作任務：產品想在使用者輸入城市時即時提示「至少 2 個字」，未來可能改成輸入後自動查詢。

你要做的是：用 watch 觀察輸入變化，先做提示文字；若未來要自動查詢，再加 debounce，避免每打一個字就打 API。

### 主線專案銜接

今天用 watch 觀察城市輸入，做出簡單提示文字。

1. 建立 `city`。
2. 建立 `hint`。
3. watch `city` 並更新 hint。

具體會動到：

- `src/App.vue`：新增 watch。

做完可驗證：

- 輸入一個字時提示「至少 2 個字」。
- 輸入兩個字以上時提示「可以搜尋」。
- 清空時提示回到初始狀態。

### 當天做完後檢查

- 有 import `watch`。
- watch 觀察的是 `city`。
- callback 有使用 `newCity`。
- 沒有在 watch 裡反覆修改 `city` 自己。
- hint 顯示正確。

### 範例範圍地圖

| 部分 | 位置 | 負責的事情 |
| --- | --- | --- |
| 被觀察資料 | `city` | 使用者輸入 |
| 副作用結果 | `hint` | 顯示提示 |
| Watcher | `watch(city, callback)` | 輸入變化時更新提示 |

### 完整實作流程、範例與注意事項

完整流程：

1. 開啟 `src/App.vue`。
2. import `watch`。
3. 建立 `hint`。
4. 使用 `watch(city, ...)`。
5. 根據輸入長度更新提示。
6. 打字測試不同狀態。

```vue
<script setup lang="ts">
import { ref, watch } from 'vue'

const city = ref('')
const hint = ref('請輸入城市名稱')

// 範例用途：觀察 city 改變後更新提示文字。
// 主要輸入：newCity 是使用者最新輸入。
// 執行結果：hint 會依輸入長度改變，template 重新顯示。
watch(city, (newCity) => {
  if (newCity.length === 0) {
    hint.value = '請輸入城市名稱'
  } else if (newCity.length < 2) {
    hint.value = '城市名稱至少需要 2 個字'
  } else {
    hint.value = '可以搜尋'
  }
})
</script>

<template>
  <input v-model.trim="city" placeholder="例如 Taipei" />
  <p>{{ hint }}</p>
</template>
```

注意事項：

- 這個例子也能用 computed 做，這裡是為了學 watch。
- 真實專案中 watch 更常用在 API、localStorage、第三方 library 同步。
- watch 裡修改被 watch 的同一個資料要小心。

### 如果結果和預期不同

- hint 沒變：確認 input 有綁 `v-model`。
- watch 沒觸發：確認觀察的是 ref 本身，不是 `city.value`。
- 文字一直閃：檢查 watch 是否反覆修改同一個來源。

### 負面例子 / 錯誤用法

錯誤做法：所有顯示文字都用 watch 手動同步。

問題：你會製造很多需要維護的同步狀態。

修正方向：能用 computed 推導的資料就用 computed；需要副作用才用 watch。

### 小練習

使用 watch 在 Console 印出城市從舊值變成新值，例如 `Taipei -> Tainan`。

### Junior 常見誤解

watch 不是「比較進階的 computed」，它是處理副作用的工具。

### 一句話總結

資料改變後要做事，用 watch；只要算值，用 computed。

---

## Day 10：Vue lifecycle

### 這篇文章主要在講什麼

Lifecycle 是元件從建立、掛載到移除的生命週期。Vue 3 常用 `onMounted` 和 `onUnmounted`。

### 為什麼需要這個概念

有些事情要等畫面掛載後才能做，例如 timer、第三方套件、DOM 測量；元件離開時也要清理資源，避免記憶體或事件殘留。

### 學完這篇你應該會做到什麼

- 使用 `onMounted()` 啟動 timer。
- 使用 `onUnmounted()` 清理 timer。
- 知道副作用要有清理策略。

### 核心重點

- `onMounted`：元件已經掛到畫面後執行。
- `onUnmounted`：元件被移除前後用來清理。
- timer、event listener、subscription 都要清理。
- 不要在 lifecycle 裡塞太多商業邏輯。

### 真實工作流程例子

工作任務：Dashboard 要顯示目前時間或資料最後更新時間，而且離開頁面後不能繼續跑背景 timer。

你要做的是：進頁面時建立 interval，離開頁面時清理。交付時不只看時間會不會動，也要確認切頁後沒有重複 timer。

### 主線專案銜接

今天做一個「最後更新時間」timer，未來 Dashboard 可以顯示資料多久前更新。

1. 建立 `now`。
2. mounted 時啟動 interval。
3. unmounted 時清除 interval。

具體會動到：

- `src/App.vue`：加入 lifecycle hooks。

做完可驗證：

- 畫面時間每秒更新。
- 離開元件時 interval 會被清除。
- Console 沒有累積錯誤。

### 當天做完後檢查

- 有 import `onMounted`、`onUnmounted`。
- interval id 有保存。
- `clearInterval()` 有被呼叫。
- timer 不會重複建立。
- template 顯示目前時間。

### 範例範圍地圖

| 部分 | 位置 | 負責的事情 |
| --- | --- | --- |
| State | `now` | 保存目前時間 |
| Mounted | `onMounted` | 啟動 timer |
| Unmounted | `onUnmounted` | 清理 timer |

### 完整實作流程、範例與注意事項

完整流程：

1. 開啟 `src/App.vue`。
2. import lifecycle hooks。
3. 建立 `now`。
4. 宣告 `timerId`。
5. 在 `onMounted` 建立 interval。
6. 在 `onUnmounted` 清掉 interval。
7. 觀察畫面時間是否每秒更新。

```vue
<script setup lang="ts">
import { onMounted, onUnmounted, ref } from 'vue'

const now = ref(new Date())
let timerId: number | undefined

// 範例用途：元件掛載後啟動計時器。
// 主要輸入：瀏覽器每 1000ms 觸發一次 interval。
// 執行結果：now 更新，畫面顯示最新時間。
onMounted(() => {
  timerId = window.setInterval(() => {
    now.value = new Date()
  }, 1000)
})

onUnmounted(() => {
  if (timerId !== undefined) {
    window.clearInterval(timerId)
  }
})
</script>

<template>
  <p>現在時間：{{ now.toLocaleTimeString() }}</p>
</template>
```

注意事項：

- `setInterval` 一定要搭配 `clearInterval`。
- lifecycle 適合接外部世界，不適合塞所有初始化邏輯。
- TypeScript 在瀏覽器中可用 `number | undefined` 表示 timer id。

### 如果結果和預期不同

- 時間不動：確認 `onMounted` 有執行。
- TypeScript timer 型別錯：使用 `window.setInterval`。
- 切頁後仍一直跑：確認 `onUnmounted` 有清理。

### 負面例子 / 錯誤用法

錯誤做法：建立 interval 但不清理。

問題：元件重複進出後，背景 timer 會越來越多。

修正方向：凡是 mounted 建立的外部資源，都要想 unmounted 怎麼清。

### 小練習

新增一個 `lastUpdatedLabel` computed，顯示 `最後更新：HH:mm:ss`。

### Junior 常見誤解

mounted 不是「所有 code 都放這裡」的地方，它只處理需要等元件掛載後才能做的事。

### 一句話總結

建立副作用時，也要同時設計清理方式。

---

## Day 11：Vue components

### 這篇文章主要在講什麼

Component 是 Vue 的元件，讓你把畫面拆成可理解、可重用的小區塊。

### 為什麼需要這個概念

如果所有畫面都寫在 `App.vue`，很快就會變成難讀的大檔案。Weather Dashboard 至少應該把顯示天氣結果的區塊拆成 `WeatherCard`。

### 學完這篇你應該會做到什麼

- 建立 `src/components/WeatherCard.vue`。
- 在父元件 import 子元件。
- 使用 component 顯示固定資料。

### 核心重點

- Component 應該有清楚責任。
- 檔名通常使用 PascalCase。
- 父元件透過 template 使用子元件。
- 先拆 UI，再慢慢加入 props。

### 真實工作流程例子

工作任務：設計稿中的天氣卡片之後會在查詢頁、歷史頁、收藏頁重複使用。

你要做的是：先拆出 `WeatherCard`，讓它成為獨立 UI 單位。實務上常先用固定資料確認視覺，再接 props 和真資料。

### 主線專案銜接

今天建立第一個主線元件 `WeatherCard`。

1. 建立 `src/components`。
2. 新增 `WeatherCard.vue`。
3. 在 `App.vue` 使用它。

具體會動到：

- `src/components/WeatherCard.vue`
- `src/App.vue`

做完可驗證：

- 畫面顯示 WeatherCard 內容。
- App.vue 變得比較乾淨。
- component 檔案可以被正常 import。

### 當天做完後檢查

- `WeatherCard.vue` 已建立。
- `App.vue` 有 import `WeatherCard`。
- template 有 `<WeatherCard />`。
- 檔名大小寫一致。
- 頁面沒有 import 錯誤。

### 範例範圍地圖

| 部分 | 位置 | 負責的事情 |
| --- | --- | --- |
| 子元件 | `src/components/WeatherCard.vue` | 顯示天氣卡片 |
| 父元件 | `src/App.vue` | 使用 WeatherCard |

### 完整實作流程、範例與注意事項

完整流程：

1. 在 `src` 底下建立 `components` 資料夾。
2. 新增 `WeatherCard.vue`。
3. 寫入固定卡片內容。
4. 回到 `App.vue` import。
5. 在 template 放 `<WeatherCard />`。
6. 瀏覽器確認卡片出現。

```vue
<!-- src/components/WeatherCard.vue -->
<template>
  <article class="weather-card">
    <h2>Taipei</h2>
    <p>目前溫度：26°C</p>
  </article>
</template>

<style scoped>
.weather-card {
  border: 1px solid #ddd;
  padding: 16px;
}
</style>
```

```vue
<!-- src/App.vue -->
<script setup lang="ts">
import WeatherCard from './components/WeatherCard.vue'
</script>

<template>
  <main>
    <h1>Weather Dashboard</h1>
    <WeatherCard />
  </main>
</template>
```

注意事項：

- component 一開始可以先顯示固定資料，確認拆分成功。
- 之後才用 props 改成動態資料。
- 不要為了拆而拆；能降低理解成本才拆。

### 如果結果和預期不同

- import 找不到：檢查路徑和檔名大小寫。
- 畫面空白：看 Console 是否有 component 錯誤。
- style 沒效果：確認 class 名稱一致。

### 負面例子 / 錯誤用法

錯誤做法：`WeatherCard` 裡直接寫 API 呼叫、表單輸入、路由切換。

問題：卡片責任太多，未來難重用也難測。

修正方向：卡片先專注顯示資料，資料取得交給頁面或 store。

### 小練習

再建立一個 `AppHeader.vue`，把標題搬進去。

### Junior 常見誤解

Component 不是資料夾分類而已，它代表一個有清楚責任的 UI 單位。

### 一句話總結

當畫面開始變長，就把有明確責任的區塊拆成 component。

---

## Day 12：props

### 這篇文章主要在講什麼

Props 是父元件傳資料給子元件的方式。

### 為什麼需要這個概念

`WeatherCard` 不應該永遠顯示 Taipei 和 26°C。父層應該把查詢結果傳進去，卡片只負責顯示。

### 學完這篇你應該會做到什麼

- 用 `defineProps` 宣告 props。
- 從父元件傳入 city 和 temperature。
- 知道子元件不應直接修改 props。

### 核心重點

- Props 是單向資料流：parent -> child。
- 子元件應把 props 當唯讀資料。
- TypeScript 可讓 props 型別更清楚。
- Props 命名要反映資料意義。

### 真實工作流程例子

工作任務：後端查詢結果已經存在父層頁面，現在要交給卡片元件顯示。

你要做的是：父層保存資料，子層用 props 接收資料。這樣以後 API response 改了，主要由父層或 service 處理，卡片仍維持顯示責任。

### 主線專案銜接

今天把 `WeatherCard` 改成可以顯示父層傳入的資料。

1. 在 `WeatherCard.vue` 宣告 props。
2. 在 `App.vue` 建立假資料。
3. 父層用 `:city` 和 `:temperature` 傳入。

具體會動到：

- `src/components/WeatherCard.vue`
- `src/App.vue`

做完可驗證：

- 修改父層資料，卡片跟著變。
- 子元件沒有自己的固定 city。
- props 型別清楚。

### 當天做完後檢查

- `WeatherCard` 有 `defineProps`。
- props 包含 `city` 和 `temperature`。
- `App.vue` 有用 `:` 傳動態值。
- 子元件沒有修改 props。
- 畫面顯示父層資料。

### 範例範圍地圖

| 部分 | 位置 | 負責的事情 |
| --- | --- | --- |
| 父層資料 | `src/App.vue` | 保存查詢結果 |
| Props 宣告 | `WeatherCard.vue` | 定義需要哪些資料 |
| 子層顯示 | `WeatherCard.vue` template | 呈現資料 |

### 完整實作流程、範例與注意事項

完整流程：

1. 開啟 `WeatherCard.vue`。
2. 加入 `defineProps`。
3. 用 props 取代固定文字。
4. 回到 `App.vue` 建立假資料。
5. 使用 `<WeatherCard :city="..." :temperature="..." />`。
6. 修改假資料測試畫面是否跟著變。

```vue
<!-- src/components/WeatherCard.vue -->
<script setup lang="ts">
// 範例用途：宣告父元件必須提供哪些天氣資料。
// 主要輸入：city 與 temperature 由父層傳入。
// 執行結果：WeatherCard 用 props 顯示資料，不自己決定資料來源。
defineProps<{
  city: string
  temperature: number
}>()
</script>

<template>
  <article>
    <h2>{{ city }}</h2>
    <p>目前溫度：{{ temperature }}°C</p>
  </article>
</template>
```

```vue
<!-- src/App.vue -->
<script setup lang="ts">
import { ref } from 'vue'
import WeatherCard from './components/WeatherCard.vue'

const result = ref({
  city: 'Taipei',
  temperature: 26,
})
</script>

<template>
  <WeatherCard :city="result.city" :temperature="result.temperature" />
</template>
```

注意事項：

- `city="result.city"` 會傳入字串 `result.city`。
- `:city="result.city"` 才會傳入變數值。
- 子元件不要改 props；要改資料應該通知父層。

### 如果結果和預期不同

- 顯示 `result.city`：你少了冒號。
- TypeScript 報缺少 prop：父層沒有傳必要資料。
- 子元件改 props 警告：改用 emit 通知父層。

### 負面例子 / 錯誤用法

錯誤做法：子元件直接修改 props。

問題：父層和子層會搶資料控制權，資料流變混亂。

修正方向：父層擁有資料，子層用 props 顯示，需要改變時 emit 事件。

### 小練習

新增 `description` prop，顯示例如 `晴時多雲`。

### Junior 常見誤解

Props 不是「複製一份資料給子元件改」，而是父層把資料交給子層顯示。

### 一句話總結

Props 讓父元件把資料清楚地傳給子元件。

---

## Day 13：emit

### 這篇文章主要在講什麼

Emit 是子元件通知父元件「某件事發生了」的方式。

### 為什麼需要這個概念

搜尋表單會放在子元件，但真正查詢資料的流程通常由父層或 store 控制。子元件不應自己決定整個 app 的資料流程。

### 學完這篇你應該會做到什麼

- 用 `defineEmits` 宣告事件。
- 子元件 emit `search`。
- 父元件接收事件並執行方法。

### 核心重點

- Props down, events up。
- 子元件發事件，不直接改父層資料。
- emit 名稱要描述發生的事。
- 事件 payload 要清楚。

### 真實工作流程例子

工作任務：UI 工程師拆出搜尋表單，但查詢流程仍要由頁面統一控管。

你要做的是：表單元件只 emit `search(city)`，頁面收到後決定要不要呼叫 store、顯示 loading、處理 error。這就是常見的父子元件協作流程。

### 主線專案銜接

今天建立 `WeatherSearchForm`，讓它輸入城市並把搜尋事件交給父層。

1. 建立 `src/components/WeatherSearchForm.vue`。
2. 子元件 emit `search`。
3. 父層 `App.vue` 接收事件。

具體會動到：

- `src/components/WeatherSearchForm.vue`
- `src/App.vue`

做完可驗證：

- 在 input 輸入城市後按搜尋。
- 父層收到城市名稱。
- 畫面顯示「你搜尋了 Taipei」。

### 當天做完後檢查

- 子元件有 `defineEmits`。
- emit payload 是 city string。
- form 有 `@submit.prevent`。
- 父層有 `@search="handleSearch"`。
- 父層方法有收到 city。

### 範例範圍地圖

| 部分 | 位置 | 負責的事情 |
| --- | --- | --- |
| 子元件 | `WeatherSearchForm.vue` | 管理輸入並 emit |
| 父元件 | `App.vue` | 接收搜尋事件 |
| Event payload | `city: string` | 傳遞城市名稱 |

### 完整實作流程、範例與注意事項

完整流程：

1. 建立 `WeatherSearchForm.vue`。
2. 在子元件建立 `city`。
3. 宣告 `search` emit。
4. form submit 時 emit 城市名稱。
5. 在 `App.vue` import 表單。
6. 父層用 `@search` 接事件。
7. 輸入城市並按搜尋測試。

```vue
<!-- src/components/WeatherSearchForm.vue -->
<script setup lang="ts">
import { ref } from 'vue'

const city = ref('')

// 範例用途：宣告子元件會往外送出 search 事件。
// 主要輸入：city 是使用者在表單輸入的城市名稱。
// 執行結果：父元件會收到 city，並決定後續查詢流程。
const emit = defineEmits<{
  search: [city: string]
}>()

function submit() {
  if (!city.value) return
  emit('search', city.value)
}
</script>

<template>
  <form @submit.prevent="submit">
    <input v-model.trim="city" placeholder="例如 Taipei" />
    <button type="submit">搜尋</button>
  </form>
</template>
```

```vue
<!-- src/App.vue -->
<script setup lang="ts">
import { ref } from 'vue'
import WeatherSearchForm from './components/WeatherSearchForm.vue'

const lastSearch = ref('')

function handleSearch(city: string) {
  lastSearch.value = city
}
</script>

<template>
  <WeatherSearchForm @search="handleSearch" />
  <p v-if="lastSearch">你搜尋了：{{ lastSearch }}</p>
</template>
```

注意事項：

- `@submit.prevent` 可以避免表單送出後整頁刷新。
- 子元件不要直接 import store 是一種可選設計；這裡先練父子溝通。
- emit payload 要小而清楚。

### 如果結果和預期不同

- 按搜尋頁面刷新：確認有 `.prevent`。
- 父層沒收到：確認事件名稱 `search` 一致。
- 空字串也送出：確認 submit 有檢查。

### 負面例子 / 錯誤用法

錯誤做法：子元件直接修改父層傳進來的 props。

問題：資料流反向，父層不知道資料什麼時候被改。

修正方向：子元件 emit 事件，父層決定如何更新資料。

### 小練習

讓 submit 後清空 input，但要先確認父層已收到城市名稱。

### Junior 常見誤解

Emit 不是「呼叫父元件 function」，而是子元件發出事件，由父層選擇要不要接。

### 一句話總結

子元件想通知父層，就 emit 事件，不直接改父層資料。

---

## Day 14：slot

### 這篇文章主要在講什麼

Slot 讓父元件可以把一段內容塞進子元件預留的位置。

### 為什麼需要這個概念

很多 UI 外框相同，但內容不同，例如卡片、彈窗、版面區塊。Slot 可以讓外框重用，內容保持彈性。

### 學完這篇你應該會做到什麼

- 建立 `BaseCard`。
- 用 `<slot />` 顯示父層傳入內容。
- 知道 props 和 slot 的差別。

### 核心重點

- props 傳資料，slot 傳模板內容。
- slot 適合做 UI 外框。
- base component 不應知道太多業務細節。
- named slot 可以處理多個區域。

### 真實工作流程例子

工作任務：設計系統要一個共用卡片外框，但每個頁面卡片內容都不同。

你要做的是：建立 `BaseCard` 管外框和間距，內容用 slot 交給使用端決定。這樣卡片風格一致，但不會把業務內容寫死在共用元件。

### 主線專案銜接

今天把 `WeatherCard` 外框改由 `BaseCard` 提供。

1. 建立 `BaseCard.vue`。
2. 裡面放 `<slot />`。
3. 在 `WeatherCard` 或 `App.vue` 使用。

具體會動到：

- `src/components/BaseCard.vue`
- `src/App.vue`

做完可驗證：

- BaseCard 負責外框。
- 父層內容可以正常顯示在 slot。
- 外框樣式可以重用。

### 當天做完後檢查

- `BaseCard` 有 `<slot />`。
- 父層有把內容包在 `<BaseCard>` 裡。
- slot 內容有出現在畫面。
- BaseCard 沒有寫死天氣資料。
- CSS 只處理外框。

### 範例範圍地圖

| 部分 | 位置 | 負責的事情 |
| --- | --- | --- |
| 共用外框 | `BaseCard.vue` | 提供卡片樣式與 slot |
| 使用端 | `App.vue` | 傳入要顯示的內容 |

### 完整實作流程、範例與注意事項

完整流程：

1. 建立 `src/components/BaseCard.vue`。
2. 在 template 放 `<section>` 和 `<slot />`。
3. 加上基本樣式。
4. 在 `App.vue` import。
5. 用 `<BaseCard>` 包住任意內容。
6. 瀏覽器確認內容出現在卡片內。

```vue
<!-- src/components/BaseCard.vue -->
<template>
  <section class="base-card">
    <slot />
  </section>
</template>

<style scoped>
.base-card {
  border: 1px solid #ddd;
  padding: 16px;
}
</style>
```

```vue
<!-- src/App.vue -->
<script setup lang="ts">
import BaseCard from './components/BaseCard.vue'
</script>

<template>
  <BaseCard>
    <h2>Taipei</h2>
    <p>目前溫度：26°C</p>
  </BaseCard>
</template>
```

注意事項：

- 如果 `BaseCard` 裡沒有 `<slot />`，父層內容不會顯示。
- Base component 應該偏通用，不要寫死天氣文字。
- slot 是傳模板，不是傳純資料。

### 如果結果和預期不同

- 卡片空白：檢查 `BaseCard.vue` 是否有 `<slot />`。
- 樣式沒套用：確認 class 名稱。
- TypeScript import 錯：檢查路徑。

### 負面例子 / 錯誤用法

錯誤做法：`BaseCard` 裡寫死 `Taipei`。

問題：它就不再是 base component，只能服務單一場景。

修正方向：共用元件只處理外框，具體內容交給 slot。

### 小練習

替 `BaseCard` 加一個 named slot：`<slot name="title" />`。

### Junior 常見誤解

Slot 不是 props 的進階版；props 傳值，slot 傳畫面結構。

### 一句話總結

外框固定、內容彈性時，slot 很好用。

---

## Day 15：Single File Components

### 這篇文章主要在講什麼

Single File Component，簡稱 SFC，就是 `.vue` 檔案。它把 script、template、style 放在同一個元件檔案中。

### 為什麼需要這個概念

Vue 專案通常會由很多 `.vue` component 組成。理解 SFC 結構，才知道程式邏輯、畫面和樣式該放哪。

### 學完這篇你應該會做到什麼

- 分辨 `<script setup>`、`<template>`、`<style scoped>`。
- 把元件整理成可讀的 SFC。
- 知道 scoped style 的作用。

### 核心重點

- `<script setup>` 放資料與邏輯。
- `<template>` 放畫面。
- `<style scoped>` 放只影響此元件的樣式。
- SFC 讓 component 更容易維護。

### 真實工作流程例子

工作任務：你接手別人的 Vue 檔，發現邏輯、畫面、樣式混在一起，很難改。

你要做的是：把資料和方法放 script、畫面放 template、元件樣式放 scoped style。這不是排版潔癖，而是讓 code review 和後續維護更容易。

### 主線專案銜接

今天整理目前的 `WeatherSearchForm` 和 `WeatherCard`，讓每個檔案都有清楚 SFC 結構。

1. 檢查每個 `.vue` 檔案。
2. 確認 script、template、style 分工。
3. 把元件樣式放回自己的檔案。

具體會動到：

- `src/components/WeatherSearchForm.vue`
- `src/components/WeatherCard.vue`
- `src/components/BaseCard.vue`

做完可驗證：

- 每個元件責任清楚。
- style 不會影響其他元件。
- App.vue 不再塞所有樣式。

### 當天做完後檢查

- 每個 component 都是 `.vue` 檔。
- 邏輯在 script。
- 畫面在 template。
- 元件私有樣式使用 scoped。
- 檔案長度仍可閱讀。

### 範例範圍地圖

| 部分 | 位置 | 負責的事情 |
| --- | --- | --- |
| Script | `.vue` 的 `<script setup>` | 資料與方法 |
| Template | `.vue` 的 `<template>` | 畫面結構 |
| Style | `.vue` 的 `<style scoped>` | 元件樣式 |

### 完整實作流程、範例與注意事項

完整流程：

1. 開啟 `WeatherCard.vue`。
2. 把 props 放在 script。
3. 把 HTML 放在 template。
4. 把卡片樣式放在 style scoped。
5. 回瀏覽器確認顯示不變。
6. 對其他 component 做同樣整理。

```vue
<!-- src/components/WeatherCard.vue -->
<script setup lang="ts">
defineProps<{
  city: string
  temperature: number
}>()
</script>

<template>
  <article class="weather-card">
    <h2>{{ city }}</h2>
    <p>{{ temperature }}°C</p>
  </article>
</template>

<style scoped>
.weather-card {
  border: 1px solid #ddd;
  padding: 16px;
}
</style>
```

注意事項：

- scoped 不代表完全不能被外部影響，仍會受到全域 CSS 基礎樣式影響。
- SFC 不是要把所有東西塞一檔，而是讓「同一個元件的東西」集中。
- 若檔案太長，可能代表元件責任太多。

### 如果結果和預期不同

- 樣式突然不見：確認 style 沒放錯檔案。
- 元件顯示不出來：檢查 template 是否只留一個有效根結構或語法正確。
- props 報錯：確認 script 沒刪到 defineProps。

### 負面例子 / 錯誤用法

錯誤做法：把所有 CSS 都丟在 `App.vue`，所有 component 共用一堆模糊 class。

問題：小改一個元件可能影響全站。

修正方向：元件私有樣式放 scoped；全域樣式只放真正全站共用的規則。

### 小練習

檢查目前哪個 `.vue` 檔最長，試著拆出一個更小的 component。

### Junior 常見誤解

SFC 不是「一個頁面一個檔案」，而是「一個元件一個檔案」。

### 一句話總結

`.vue` 檔的三段式結構，讓元件的邏輯、畫面和樣式放在合理位置。

---

## Day 16：Vite 專案結構

### 這篇文章主要在講什麼

原文可能使用 Vue CLI；2026 建議用 Vite。今天重點是理解專案結構和常見資料夾分工。

### 為什麼需要這個概念

專案一變大，檔案如果亂放，新手會不知道 component、page、store、API service 各自在哪裡。

### 學完這篇你應該會做到什麼

- 建立 `views`、`components`、`services`、`stores` 資料夾。
- 知道不同檔案應該放哪裡。
- 理解 `npm run dev` 和 `npm run build`。

### 核心重點

- `components` 放可重用 UI 元件。
- `views` 放 route 對應頁面。
- `services` 放 API 呼叫。
- `stores` 放全域狀態。
- Vite 負責開發與打包。

### 真實工作流程例子

工作任務：專案開始從 demo 變成正式功能，PM 之後還會要求歷史頁、設定頁和 API 串接。

你要做的是：先整理資料夾責任，讓頁面、元件、API、store 有固定位置。這會直接影響新人接手速度和日後功能擴充。

### 主線專案銜接

今天把專案整理成 Weather Dashboard 後續可擴充的結構。

1. 建立資料夾。
2. 把元件放進 `components`。
3. 預留 `views`、`services`、`stores`。

具體會動到：

- `src/components`
- `src/views`
- `src/services`
- `src/stores`

做完可驗證：

- 專案結構清楚。
- import 路徑仍正確。
- dev server 沒壞。

### 當天做完後檢查

- `components` 存在。
- `views` 存在。
- `services` 存在。
- `stores` 存在。
- 移動檔案後 import 路徑已更新。

### 範例範圍地圖

| 部分 | 位置 | 負責的事情 |
| --- | --- | --- |
| UI 元件 | `src/components` | 可重用區塊 |
| 頁面 | `src/views` | 路由頁面 |
| API | `src/services` | 外部資料 |
| State | `src/stores` | 全域狀態 |

### 完整實作流程、範例與注意事項

完整流程：

1. 在 `src` 底下建立 `views`、`services`、`stores`。
2. 確認既有元件都在 `components`。
3. 如果移動檔案，更新 import。
4. 執行 `npm run dev`。
5. 檢查畫面是否正常。
6. 執行 `npm run build` 確認沒有型別或路徑錯誤。

```text
src/
  components/
    BaseCard.vue
    WeatherCard.vue
    WeatherSearchForm.vue
  services/
  stores/
  views/
  App.vue
  main.ts
```

注意事項：

- 資料夾名稱不是法律，但團隊要一致。
- `views` 通常放頁面，`components` 放頁面內使用的小元件。
- 不要為了「看起來專業」建立一堆空資料夾；這裡是為後續 Day 鋪路。

### 如果結果和預期不同

- import 錯：檢查相對路徑。
- build 錯但 dev 可跑：仍要修，部署會失敗。
- 找不到 component：確認檔名大小寫。

### 負面例子 / 錯誤用法

錯誤做法：所有檔案都丟在 `src` 根目錄。

問題：專案成長後很難找，也不容易判斷責任。

修正方向：依責任分資料夾，不依「今天想到什麼」亂放。

### 小練習

建立 `src/views/WeatherView.vue`，先只顯示 `Weather Page`。

### Junior 常見誤解

資料夾結構不是儀式感，它是在幫你降低找檔案和理解責任的成本。

### 一句話總結

Vite 專案要先有清楚結構，後面加 Router、Pinia、API 才不會亂。

---

## Day 17：EventBus 與替代方案

### 這篇文章主要在講什麼

Vue 2 時代常用 EventBus 讓元件互相發事件；Vue 3 更建議用 props / emit、provide / inject、Pinia 或 composable。

### 為什麼需要這個概念

EventBus 看似方便，但資料流會變得很難追。你不知道誰發事件、誰接事件、什麼時候改資料。

### 學完這篇你應該會做到什麼

- 知道為什麼不建議濫用 EventBus。
- 會用情境選擇溝通方式。
- 為後續 Pinia 做準備。

### 核心重點

- 父子溝通：props / emit。
- 跨多層但局部：provide / inject。
- 多頁或全域狀態：Pinia。
- 純邏輯重用：composable。

### 真實工作流程例子

工作任務：工程師提議用 EventBus 讓表單通知任何地方「天氣查好了」，但你需要評估風險。

你要做的是：先畫資料流，判斷是父子、跨層還是跨頁共享。若是跨頁查詢紀錄，用 Pinia；若只是表單通知頁面，用 emit。不要為了方便讓資料流變隱形。

### 主線專案銜接

今天做的是判斷練習：搜尋表單和查詢結果應該怎麼溝通？

1. 表單通知父層：使用 emit。
2. 查詢結果跨頁保存：使用 Pinia。
3. 共用外框：使用 props / slot。

具體會動到：

- 可不改檔案，但要整理架構判斷。
- 可在 `README` 或筆記中記錄分工。

做完可驗證：

- 表單不直接控制卡片。
- 卡片不直接查 API。
- 跨頁資料準備放 store。

### 當天做完後檢查

- 父子溝通仍用 props / emit。
- 沒有新增 EventBus。
- 需要跨頁的資料已標記未來放 Pinia。
- 元件責任沒有互相穿透。
- 能說出每種溝通方式適用情境。

### 範例範圍地圖

| 情境 | 建議方式 | 原因 |
| --- | --- | --- |
| 父傳子資料 | props | 單向清楚 |
| 子通知父 | emit | 事件向上 |
| 跨頁查詢紀錄 | Pinia | 多處共享 |
| 深層表單設定 | provide / inject | 局部跨層 |

### 完整實作流程、範例與注意事項

完整流程：

1. 列出目前元件：`App`、`WeatherSearchForm`、`WeatherCard`。
2. 判斷資料擁有者是誰。
3. 表單只負責輸入與 emit。
4. 卡片只負責 props 顯示。
5. 查詢紀錄未來放 store。
6. 檢查是否有元件互相直接操控。

```text
WeatherSearchForm --emit search(city)--> WeatherView / App
WeatherView / App --props result--> WeatherCard
WeatherView / App --未來呼叫--> Pinia store
```

注意事項：

- EventBus 最大問題是「太自由」，自由到很難追。
- 小專案也可以不用 Pinia，但當資料跨頁或多元件共用時就值得使用。
- 不要為了避免 props 多傳一層，就立刻導入全域狀態。

### 如果結果和預期不同

- 你不知道資料在哪裡被改：可能溝通方式太分散。
- 子元件直接 import 父層資料：重新檢查責任。
- props 傳太深：評估 provide / inject 或 Pinia。

### 負面例子 / 錯誤用法

錯誤做法：建立全域 bus，任何元件都可以 `emit('weather-loaded')`。

問題：未來很難搜尋完整資料流，事件名稱也容易撞。

修正方向：局部溝通用 props / emit，全域共享用 Pinia。

### 小練習

畫出目前資料流，標出哪個元件「擁有資料」、哪個元件「只顯示資料」。

### Junior 常見誤解

不是不能發事件，而是不要讓全域事件變成看不見的資料流。

### 一句話總結

Vue 3 更重視可追蹤的資料流，而不是到處廣播事件。

---

## Day 18：store 入門

### 這篇文章主要在講什麼

Store 是用來保存多個元件或多個頁面都需要的狀態。Vue 3 常用 Pinia。

### 為什麼需要這個概念

查詢結果和歷史紀錄不只一個元件會用。把它放在單一元件會讓跨頁分享變困難。

### 學完這篇你應該會做到什麼

- 安裝 Pinia。
- 在 `main.ts` 註冊 Pinia。
- 建立第一個 weather store。

### 核心重點

- Pinia 是 Vue 官方推薦的狀態管理工具。
- Store 放共享狀態與相關 action。
- Component 可以使用 store，但不要把所有小狀態都丟 store。
- Local state 和 global state 要分清楚。

### 真實工作流程例子

工作任務：歷史紀錄頁需要讀取查詢頁產生的資料，所以資料不能只存在查詢頁 component。

你要做的是：把查詢結果和紀錄放到 Pinia store，讓 `/weather` 和 `/history` 都能讀同一份狀態。表單正在輸入的暫存文字則仍可留在表單元件。

### 主線專案銜接

今天建立 `src/stores/weather.ts`。

1. 安裝 Pinia。
2. 在 `main.ts` 掛上 Pinia。
3. 建立 `useWeatherStore`。

具體會動到：

- `package.json`
- `src/main.ts`
- `src/stores/weather.ts`

做完可驗證：

- app 能正常啟動。
- component 可以讀取 store state。
- Devtools 未來可看到 Pinia store。

### 當天做完後檢查

- `pinia` 已安裝。
- `main.ts` 有 `createPinia()`。
- app 有 `.use(pinia)`。
- `weather.ts` 有 `defineStore`。
- component 能 import `useWeatherStore`。

### 範例範圍地圖

| 部分 | 位置 / 指令 | 負責的事情 |
| --- | --- | --- |
| 安裝 | `npm install pinia` | 加入 Pinia |
| 註冊 | `src/main.ts` | 讓 Vue app 使用 Pinia |
| Store | `src/stores/weather.ts` | 保存 weather 狀態 |

### 完整實作流程、範例與注意事項

完整流程：

1. 停止或保留 dev server 都可以。
2. 在專案根目錄執行安裝。
3. 修改 `src/main.ts`。
4. 建立 `src/stores/weather.ts`。
5. 在 `App.vue` 暫時讀取 store。
6. 開啟瀏覽器確認顯示 store 資料。

```powershell
# 範例用途：安裝 Pinia 狀態管理工具。
# 主要輸入：pinia 套件名稱。
# 執行結果：package.json 增加 pinia，相依套件安裝到 node_modules。
npm install pinia
```

```ts
// src/main.ts
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import App from './App.vue'

const app = createApp(App)
const pinia = createPinia()

app.use(pinia)
app.mount('#app')
```

```ts
// src/stores/weather.ts
import { defineStore } from 'pinia'
import { ref } from 'vue'

export const useWeatherStore = defineStore('weather', () => {
  // 範例用途：保存目前查詢城市。
  // 主要輸入：未來由搜尋表單傳入 city。
  // 執行結果：多個元件可以讀取同一份 currentCity。
  const currentCity = ref('Taipei')

  return { currentCity }
})
```

注意事項：

- Pinia 要在 `mount` 前 `.use()`。
- store id `weather` 要穩定，不要隨便改。
- 不是所有資料都放 store；表單暫存輸入仍可留在 component。

### 如果結果和預期不同

- `Cannot find module 'pinia'`：確認有執行 `npm install pinia`。
- Store 無法使用：確認 `main.ts` 有 `.use(pinia)`。
- Devtools 看不到：重新整理頁面。

### 負面例子 / 錯誤用法

錯誤做法：把每個 input 的每個字都放全域 store。

問題：全域狀態太多，任何元件都可能改它，維護成本上升。

修正方向：只有跨元件、跨頁或需要集中管理的資料才放 store。

### 小練習

在 store 新增 `history = ref<string[]>([])`，先不用操作它。

### Junior 常見誤解

Store 不是比較高級的 ref，而是「共享狀態的集中位置」。

### 一句話總結

當資料需要被多個地方共用，才值得放進 Pinia store。

---

## Day 19：Pinia 讀寫

### 這篇文章主要在講什麼

今天練習從 component 讀取 Pinia store，並透過 action 修改 store。

### 為什麼需要這個概念

如果所有元件都直接亂改 store，長期會難追。把修改流程包成 action，可以讓狀態變化更有語意。

### 學完這篇你應該會做到什麼

- 在 component 使用 store。
- 在 store 建立 action。
- 用 Devtools 觀察 store 變化。

### 核心重點

- `useWeatherStore()` 在 setup 中呼叫。
- action 是 store 裡負責改狀態的方法。
- `storeToRefs` 可避免解構後失去響應性。
- Devtools 可以看 Pinia state。

### 真實工作流程例子

工作任務：QA 說按「搜尋 Tainan」後，歷史紀錄頁仍顯示 Taipei。

你要做的是：用 action 集中更新 `currentCity` 或 history，再用 Devtools 確認 store 是否真的更新。如果 store 更新但頁面沒變，才去查 component 顯示邏輯。

### 主線專案銜接

今天把搜尋城市寫進 store。

1. store 新增 `setCity(city)`。
2. component 呼叫 action。
3. Devtools 觀察 `currentCity`。

具體會動到：

- `src/stores/weather.ts`
- `src/App.vue`

做完可驗證：

- 搜尋後 store 的 currentCity 變化。
- 畫面顯示 store 資料。
- Devtools 看到 Pinia state。

### 當天做完後檢查

- store 有 `setCity`。
- component 有呼叫 `useWeatherStore()`。
- 使用 `storeToRefs` 讀 state。
- button 或表單能更新 currentCity。
- Devtools 可看到變化。

### 範例範圍地圖

| 部分 | 位置 | 負責的事情 |
| --- | --- | --- |
| State | `weather.ts` | 保存 currentCity |
| Action | `setCity` | 修改 currentCity |
| Component | `App.vue` | 呼叫 action |

### 完整實作流程、範例與注意事項

完整流程：

1. 打開 `src/stores/weather.ts`。
2. 新增 `setCity` action。
3. 回到 `App.vue`。
4. 使用 `useWeatherStore()`。
5. 用 `storeToRefs` 取出 state。
6. 點擊按鈕或 submit 表單修改 store。
7. 打開 Devtools 觀察 Pinia。

```ts
// src/stores/weather.ts
import { defineStore } from 'pinia'
import { ref } from 'vue'

export const useWeatherStore = defineStore('weather', () => {
  const currentCity = ref('Taipei')

  // 範例用途：集中修改目前城市。
  // 主要輸入：city 由表單或按鈕傳入。
  // 執行結果：currentCity 更新，使用 store 的畫面會跟著更新。
  function setCity(city: string) {
    currentCity.value = city
  }

  return { currentCity, setCity }
})
```

```vue
<!-- src/App.vue -->
<script setup lang="ts">
import { storeToRefs } from 'pinia'
import { useWeatherStore } from './stores/weather'

const weatherStore = useWeatherStore()
const { currentCity } = storeToRefs(weatherStore)
</script>

<template>
  <p>目前城市：{{ currentCity }}</p>
  <button type="button" @click="weatherStore.setCity('Tainan')">
    改成 Tainan
  </button>
</template>
```

注意事項：

- 直接 `const { currentCity } = weatherStore` 可能造成響應性問題；用 `storeToRefs`。
- action 名稱要描述狀態變化。
- component 可以呼叫 action，但不要把所有流程都塞在 template。

### 如果結果和預期不同

- 畫面沒更新：確認使用 `storeToRefs`。
- action 不是 function：確認 store return 有包含 `setCity`。
- Devtools 看不到 Pinia：確認 main.ts 已註冊 Pinia。

### 負面例子 / 錯誤用法

錯誤做法：多個 component 各自用不同方式修改同一份 store state。

問題：狀態變化難追，bug 很難定位。

修正方向：把有語意的修改包成 action。

### 小練習

新增 `addHistory(city)` action，把城市加入 history 陣列。

### Junior 常見誤解

Store state 可以直接改，但不代表任何地方都應該隨便改。

### 一句話總結

Pinia action 讓共享狀態的修改流程更清楚。

---

## Day 20：Store async action

### 這篇文章主要在講什麼

今天把非同步查詢流程放進 Pinia action，並管理 loading、error、result。

### 為什麼需要這個概念

API 查詢不會瞬間完成，而且可能失敗。UI 需要知道現在是 loading、error 還是 success。

### 學完這篇你應該會做到什麼

- 在 store 寫 async action。
- 管理 loading / error / result。
- 用假 API 模擬查詢流程。

### 核心重點

- async action 可以集中 API 流程。
- request 前設 `loading = true`。
- catch 設定 error。
- finally 關掉 loading。

### 真實工作流程例子

工作任務：使用者按搜尋後，畫面要顯示查詢中；API 失敗時要顯示錯誤，而且不能永遠 loading。

你要做的是：把整個 request lifecycle 放進 async action：開始、成功、失敗、結束。交付時要刻意測成功和失敗，不只測 happy path。

### 主線專案銜接

今天先用假資料完成 `load(city)`。

1. 定義 `WeatherResult`。
2. store 新增 `result`、`loading`、`errorMessage`。
3. 建立 async `load(city)`。

具體會動到：

- `src/stores/weather.ts`
- `src/App.vue`

做完可驗證：

- 點搜尋會短暫 loading。
- 成功後 result 更新。
- 空城市會出現 error。

### 當天做完後檢查

- `loading` 預設 false。
- `errorMessage` 預設空字串。
- `result` 可為 null。
- `load` 有 try / catch / finally。
- 畫面有根據狀態顯示。

### 範例範圍地圖

| 部分 | 位置 | 負責的事情 |
| --- | --- | --- |
| Type | `WeatherResult` | 定義結果形狀 |
| Store state | `weather.ts` | 保存 loading / error / result |
| Async action | `load(city)` | 模擬查詢 |
| UI | `App.vue` | 顯示狀態 |

### 完整實作流程、範例與注意事項

完整流程：

1. 開啟 `src/stores/weather.ts`。
2. 新增 `WeatherResult` type。
3. 新增三個 state。
4. 寫 `load(city)`。
5. 在 component 呼叫 `weatherStore.load('Taipei')`。
6. 觀察 loading 和 result。

```ts
// src/stores/weather.ts
import { defineStore } from 'pinia'
import { ref } from 'vue'

type WeatherResult = {
  city: string
  temperature: number
}

export const useWeatherStore = defineStore('weather', () => {
  const result = ref<WeatherResult | null>(null)
  const loading = ref(false)
  const errorMessage = ref('')

  async function load(city: string) {
    loading.value = true
    errorMessage.value = ''

    try {
      if (!city) throw new Error('City is required')

      // 範例用途：先用假延遲模擬 API。
      // 主要輸入：city 是使用者要查詢的城市。
      // 執行結果：result 被更新，畫面可以顯示查詢結果。
      await new Promise((resolve) => window.setTimeout(resolve, 500))
      result.value = { city, temperature: 26 }
    } catch {
      result.value = null
      errorMessage.value = '查詢失敗，請確認城市名稱'
    } finally {
      loading.value = false
    }
  }

  return { result, loading, errorMessage, load }
})
```

注意事項：

- loading 要放在 finally 關掉，避免失敗後永遠轉圈。
- error 發生時要清掉舊 result，避免使用者看到舊資料誤以為成功。
- 真實 API 會在 Day 24 接上。

### 如果結果和預期不同

- loading 永遠 true：檢查 finally。
- error 後還顯示舊資料：catch 裡清掉 result。
- 按鈕連點造成混亂：之後可在 loading 時 disabled。

### 負面例子 / 錯誤用法

錯誤做法：component 自己到處處理 loading、error、API，store 只放 result。

問題：流程分散，其他頁面要重用查詢狀態會很麻煩。

修正方向：把同一個查詢流程集中在 store action。

### 小練習

在 loading 時讓搜尋按鈕 disabled。

### Junior 常見誤解

非同步流程不是只要 `await`，還要設計等待中、失敗、成功的畫面狀態。

### 一句話總結

API 狀態要和資料一起被管理，UI 才能可靠顯示。

---

## Day 21：Vue Router 上

### 這篇文章主要在講什麼

Vue Router 讓 Vue SPA 可以依照 URL 顯示不同頁面。

### 為什麼需要這個概念

Weather Dashboard 需要查詢頁和歷史紀錄頁。沒有 Router 就只能靠 state 切畫面，URL 不能代表目前頁面。

### 學完這篇你應該會做到什麼

- 安裝 Vue Router。
- 建立 `router/index.ts`。
- 使用 `RouterView` 顯示頁面。

### 核心重點

- Route 是 URL 和 component 的對應。
- `RouterView` 是頁面顯示出口。
- `RouterLink` 用來切換路由。
- SPA 切頁不會整頁重載。

### 真實工作流程例子

工作任務：PM 要查詢頁和歷史頁可以各自被分享 URL，例如直接給同事 `/history`。

你要做的是：用 Vue Router 建立頁面路由，而不是用一個變數切換畫面。交付時要測直接進 URL、點導覽、重新整理。

### 主線專案銜接

今天建立 `/weather` 和 `/history`。

1. 安裝 router。
2. 建立兩個 view。
3. 在 `main.ts` 註冊 router。
4. 在 `App.vue` 放 `RouterView`。

具體會動到：

- `src/router/index.ts`
- `src/views/WeatherView.vue`
- `src/views/HistoryView.vue`
- `src/main.ts`
- `src/App.vue`

做完可驗證：

- `/weather` 顯示查詢頁。
- `/history` 顯示歷史頁。
- 點連結不會整頁刷新。

### 當天做完後檢查

- `vue-router` 已安裝。
- `routes` 有 `/weather` 和 `/history`。
- `main.ts` 有 `.use(router)`。
- `App.vue` 有 `RouterView`。
- `RouterLink` 可以切頁。

### 範例範圍地圖

| 部分 | 位置 / 指令 | 負責的事情 |
| --- | --- | --- |
| 安裝 | `npm install vue-router` | 加入 Router |
| Router | `src/router/index.ts` | 定義 URL |
| Views | `src/views` | 對應頁面 |
| Root | `src/App.vue` | 顯示連結與頁面出口 |

### 完整實作流程、範例與注意事項

完整流程：

1. 安裝 `vue-router`。
2. 建立 `src/router/index.ts`。
3. 建立 `WeatherView.vue` 和 `HistoryView.vue`。
4. 修改 `main.ts` 註冊 router。
5. 修改 `App.vue` 放 `RouterLink` 和 `RouterView`。
6. 測試兩個 URL。

```powershell
npm install vue-router
```

```ts
// src/router/index.ts
import { createRouter, createWebHistory } from 'vue-router'
import WeatherView from '../views/WeatherView.vue'
import HistoryView from '../views/HistoryView.vue'

export const router = createRouter({
  history: createWebHistory(),
  routes: [
    { path: '/', redirect: '/weather' },
    { path: '/weather', component: WeatherView },
    { path: '/history', component: HistoryView },
  ],
})
```

```vue
<!-- src/App.vue -->
<script setup lang="ts">
import { RouterLink, RouterView } from 'vue-router'
</script>

<template>
  <nav>
    <RouterLink to="/weather">查詢</RouterLink>
    <RouterLink to="/history">歷史</RouterLink>
  </nav>

  <RouterView />
</template>
```

注意事項：

- `RouterView` 少了，路由 component 不會顯示。
- `createWebHistory()` 部署時需要 SPA fallback。
- 頁面 component 建議放 `views`。

### 如果結果和預期不同

- 點連結沒畫面：確認 `RouterView`。
- URL 直接打開 404：本機 dev 通常沒問題，部署要 Day 23 fallback。
- import 找不到 view：檢查檔名與路徑。

### 負面例子 / 錯誤用法

錯誤做法：用一般 `<a href="/history">` 在 SPA 內切頁。

問題：可能造成整頁刷新，失去 SPA 導航體驗。

修正方向：Vue Router 頁面切換使用 `RouterLink`。

### 小練習

新增 `/about` 頁面，顯示專案說明。

### Junior 常見誤解

Router 不是後端路由；它是在前端決定 URL 對應哪個 component。

### 一句話總結

Vue Router 讓 SPA 的畫面狀態可以用 URL 表達。

---

## Day 22：Vue Router 下

### 這篇文章主要在講什麼

今天補路由參數、導覽、not found fallback，以及如何在 component 裡讀 route。

### 為什麼需要這個概念

真實專案常有 `/weather/Taipei` 或 `/detail/123` 這種路由。你需要知道參數從哪裡來，以及找不到頁面時怎麼處理。

### 學完這篇你應該會做到什麼

- 使用 dynamic route。
- 使用 `useRoute()` 讀參數。
- 建立 404 頁面。

### 核心重點

- `:city` 是 route param。
- `useRoute()` 讀目前路由資訊。
- `useRouter()` 做程式化導覽。
- `/:pathMatch(.*)*` 可處理 not found。

### 真實工作流程例子

工作任務：客服回報使用者貼錯網址時看到空白頁，希望顯示「找不到頁面」並能回首頁。

你要做的是：建立 fallback route 和 NotFoundView。若路由有城市參數，也要處理沒有參數或參數無效的情境。

### 主線專案銜接

今天新增 `/weather/:city`，讓 URL 可以帶城市。

1. 建立 dynamic route。
2. 在 `WeatherView` 讀 city param。
3. 建立 not found fallback。

具體會動到：

- `src/router/index.ts`
- `src/views/WeatherView.vue`
- `src/views/NotFoundView.vue`

做完可驗證：

- `/weather/Taipei` 能讀到 `Taipei`。
- 亂打 URL 會看到 Not Found。
- 點按鈕可導到指定城市。

### 當天做完後檢查

- routes 有 `/weather/:city?` 或 `/weather/:city`。
- `WeatherView` 有 `useRoute()`。
- 有 NotFound route。
- 導覽不會整頁刷新。
- param 不存在時有預設狀態。

### 範例範圍地圖

| 部分 | 位置 | 負責的事情 |
| --- | --- | --- |
| Dynamic route | `router/index.ts` | 定義城市參數 |
| Route reader | `WeatherView.vue` | 讀取 param |
| Fallback | `NotFoundView.vue` | 顯示 404 |

### 完整實作流程、範例與注意事項

完整流程：

1. 建立 `NotFoundView.vue`。
2. 修改 route path。
3. 在 `WeatherView` import `useRoute`。
4. 建立 computed 讀取 `route.params.city`。
5. 瀏覽器打開 `/weather/Taipei`。
6. 打開不存在的路徑測試 fallback。

```ts
// src/router/index.ts
import { createRouter, createWebHistory } from 'vue-router'
import WeatherView from '../views/WeatherView.vue'
import HistoryView from '../views/HistoryView.vue'
import NotFoundView from '../views/NotFoundView.vue'

export const router = createRouter({
  history: createWebHistory(),
  routes: [
    { path: '/', redirect: '/weather' },
    { path: '/weather/:city?', component: WeatherView },
    { path: '/history', component: HistoryView },
    { path: '/:pathMatch(.*)*', component: NotFoundView },
  ],
})
```

```vue
<!-- src/views/WeatherView.vue -->
<script setup lang="ts">
import { computed } from 'vue'
import { useRoute } from 'vue-router'

const route = useRoute()

// 範例用途：從 URL 讀取城市名稱。
// 主要輸入：/weather/Taipei 中的 Taipei。
// 執行結果：畫面顯示目前 route 指定城市。
const routeCity = computed(() => String(route.params.city ?? ''))
</script>

<template>
  <p v-if="routeCity">URL 城市：{{ routeCity }}</p>
  <p v-else>請輸入城市。</p>
</template>
```

注意事項：

- route param 可能是 `string | string[]`，新手可先用 `String()` 簡化。
- optional param 用 `?`，例如 `:city?`。
- Not Found route 要放在最後。

### 如果結果和預期不同

- Not Found 吃掉所有路由：確認 fallback 放最後。
- city 是 undefined：確認 URL 是否真的有帶 param。
- TypeScript 型別不舒服：先把 param 轉成 String。

### 負面例子 / 錯誤用法

錯誤做法：直接假設 `route.params.city` 永遠存在。

問題：使用者可能進 `/weather`，沒有城市就會出錯或顯示怪內容。

修正方向：param 要有 fallback 或空狀態。

### 小練習

新增一個按鈕，點擊後用 `useRouter().push('/weather/Tainan')` 導到 Tainan。

### Junior 常見誤解

URL 參數也是外部輸入，不能假設一定正確。

### 一句話總結

Router 不只切頁，也能讓 URL 帶入頁面需要的參數。

---

## Day 23：Vue 專案部署

### 這篇文章主要在講什麼

今天學 Vue 專案 build、preview，以及 SPA 部署常見的 fallback 設定。

### 為什麼需要這個概念

開發環境能跑不代表部署後能跑。Vue Router 使用 history mode 時，重新整理 `/weather` 可能讓伺服器找不到檔案而 404。

### 學完這篇你應該會做到什麼

- 執行 `npm run build`。
- 用 `npm run preview` 檢查 build 結果。
- 理解 SPA fallback。

### 核心重點

- `npm run build` 產生 `dist`。
- `npm run preview` 預覽 build 結果。
- History mode 需要把未知路徑 fallback 到 `index.html`。
- 部署平台要設定 output directory。

### 真實工作流程例子

工作任務：功能在本機正常，但部署到 Netlify 後，直接打開 `/history` 變 404。

你要做的是：先跑 build 和 preview，再設定 SPA fallback。這是前端部署常見問題，不是 Router 寫錯，而是伺服器不知道所有路徑都要回 `index.html`。

### 主線專案銜接

今天確認 Weather Dashboard 可以被打包。

1. 執行 build。
2. 預覽 dist。
3. 加上 Netlify `_redirects` 範例。

具體會動到：

- `public/_redirects`
- `dist` 由 build 產生，不手動改。

做完可驗證：

- build 成功。
- preview 可以切 `/weather`、`/history`。
- 部署平台知道 publish directory 是 `dist`。

### 當天做完後檢查

- `npm run build` 成功。
- `dist` 有產生。
- `npm run preview` 可以開啟。
- `/weather` 重新整理不會壞。
- 部署設定有 SPA fallback。

### 範例範圍地圖

| 部分 | 位置 / 指令 | 負責的事情 |
| --- | --- | --- |
| Build | `npm run build` | 產生正式檔案 |
| Preview | `npm run preview` | 預覽正式檔案 |
| Fallback | `public/_redirects` | Netlify SPA 路由設定 |
| Output | `dist` | 部署目錄 |

### 完整實作流程、範例與注意事項

完整流程：

1. 在專案根目錄執行 `npm run build`。
2. 確認出現 `dist` 資料夾。
3. 執行 `npm run preview`。
4. 開啟 preview URL。
5. 測試 `/weather`、`/history`。
6. 若部署 Netlify，建立 `public/_redirects`。

```powershell
# 範例用途：產生 production build 並在本機預覽。
# 主要輸入：目前專案原始碼。
# 執行結果：dist 資料夾被產生，preview server 用 dist 內容啟動。
npm run build
npm run preview
```

```text
# public/_redirects
/* /index.html 200
```

注意事項：

- `dist` 是產物，通常不手動修改。
- 每個部署平台 fallback 寫法不同。
- build 失敗要先修，不要硬部署。

### 如果結果和預期不同

- build 失敗：看第一個 TypeScript 或 import 錯誤。
- preview 打開空白：檢查 Console 與 base path。
- 部署後重新整理 404：檢查 SPA fallback。

### 負面例子 / 錯誤用法

錯誤做法：只測 `npm run dev`，沒有測 build 就部署。

問題：dev server 會幫你處理很多事，正式環境可能不同。

修正方向：每次部署前至少跑 `npm run build`。

### 小練習

查你的部署平台要如何設定 SPA fallback，記在 README。

### Junior 常見誤解

部署不是把原始碼丟上去而已，多數前端專案要先 build。

### 一句話總結

Vue SPA 部署前要 build，使用 history route 時要設定 fallback。

---

## Day 24：Weather API Service

### 這篇文章主要在講什麼

今天把 API 呼叫包成 service，讓 component 和 store 不直接知道 fetch 細節。

### 為什麼需要這個概念

API response 通常不會剛好等於畫面要的格式。Service 可以負責呼叫、檢查錯誤、mapping。

### 學完這篇你應該會做到什麼

- 建立 `src/services/weatherApi.ts`。
- 定義 `WeatherResult`。
- 寫 `getWeather(city)`。

### 核心重點

- API key 不要硬寫在前端公開程式碼。
- Service 負責外部 API 細節。
- Component 不應直接處理 response 結構。
- Mapping 讓 UI 使用穩定格式。

### 真實工作流程例子

工作任務：後端或第三方天氣 API 回傳欄位很多，而且命名不是前端想要的格式。

你要做的是：在 service 層把 raw response 轉成 `WeatherResult`。這樣 UI 元件只面對穩定欄位，API 變動時也比較知道要修哪裡。

### 主線專案銜接

今天替 Day 20 的假資料準備真正 API 介面。

1. 建立 service 檔。
2. 定義回傳 type。
3. 用 fetch 呼叫 `/api/weather`。

具體會動到：

- `src/services/weatherApi.ts`
- `src/stores/weather.ts` 之後會 import 使用。

做完可驗證：

- TypeScript 能 import `getWeather`。
- API 失敗會丟出錯誤。
- Store 不需要知道 fetch URL 細節。

### 當天做完後檢查

- service 檔案存在。
- `WeatherResult` type 有 export。
- `getWeather` 有 encode city。
- response 不 ok 時會 throw。
- 沒有在 component 裡直接 fetch。

### 範例範圍地圖

| 部分 | 位置 | 負責的事情 |
| --- | --- | --- |
| Type | `weatherApi.ts` | 定義前端需要的資料 |
| Function | `getWeather(city)` | 呼叫 API |
| Caller | `weather.ts` | store 呼叫 service |

### 完整實作流程、範例與注意事項

完整流程：

1. 建立 `src/services/weatherApi.ts`。
2. 定義 `WeatherResult`。
3. 建立 `getWeather(city)`。
4. 對 city 做 `encodeURIComponent`。
5. 檢查 `response.ok`。
6. 回傳 JSON。
7. 暫時不在 component 直接呼叫。

```ts
// src/services/weatherApi.ts
export type WeatherResult = {
  city: string
  temperature: number
  description: string
}

export async function getWeather(city: string): Promise<WeatherResult> {
  // 範例用途：集中處理天氣 API request。
  // 主要輸入：city 是使用者輸入的城市名稱，會被放進 query string。
  // 執行結果：成功時回傳 WeatherResult；失敗時丟出 Error 給 store 處理。
  const response = await fetch(`/api/weather?city=${encodeURIComponent(city)}`)

  if (!response.ok) {
    throw new Error('Weather API failed')
  }

  return await response.json()
}
```

注意事項：

- 真實 API key 不應直接放前端，因為 build 後使用者看得到。
- 若需要保護 key，使用後端 API 或 serverless function。
- `encodeURIComponent` 可以避免城市名稱中的空白或特殊字元弄壞 URL。

### 如果結果和預期不同

- CORS error：通常需要後端 proxy 或 serverless function。
- 404：檢查 `/api/weather` 是否真的存在。
- JSON 格式不符：在 service 做 mapping，不要讓 component 承擔。

### 負面例子 / 錯誤用法

錯誤做法：每個 component 都自己 fetch 同一個 API。

問題：錯誤處理、mapping、URL 規則會到處重複。

修正方向：集中在 service，再由 store 或頁面呼叫。

### 小練習

把 `WeatherResult` 加上 `humidity: number`，並思考 UI 要不要立刻顯示。

### Junior 常見誤解

`.env` 裡的 `VITE_` 變數不是秘密；前端 build 後仍可能被看見。

### 一句話總結

API 細節集中在 service，畫面和 store 才能保持乾淨。

---

## Day 25：Weather API UI 狀態

### 這篇文章主要在講什麼

今天把 API 查詢的 loading、error、empty、success 狀態顯示到畫面。

### 為什麼需要這個概念

使用者需要知道系統正在查、查不到、失敗，還是成功。只有成功畫面的 app 不是完整 app。

### 學完這篇你應該會做到什麼

- 在 UI 顯示 loading。
- 在 UI 顯示 error。
- 有 result 時顯示 WeatherCard。

### 核心重點

- loading 時避免重複提交。
- error 要給人能理解的訊息。
- result 為 null 時要有 empty state。
- UI 狀態應該由 store state 決定。

### 真實工作流程例子

工作任務：QA 要求查詢流程至少測四種狀態：尚未查詢、查詢中、查詢失敗、查詢成功。

你要做的是：讓 store 提供這些狀態，頁面只根據 state 顯示 UI。交付時用假 API、錯誤城市或 mock error 分別測四種狀態。

### 主線專案銜接

今天把 `WeatherView` 接上 store 狀態。

1. 在 `WeatherView` 使用 `WeatherSearchForm`。
2. submit 時呼叫 store `load(city)`。
3. 根據 store 顯示不同狀態。

具體會動到：

- `src/views/WeatherView.vue`
- `src/stores/weather.ts`
- `src/components/WeatherCard.vue`

做完可驗證：

- 搜尋時顯示 loading。
- 失敗時顯示錯誤。
- 成功時顯示卡片。

### 當天做完後檢查

- `WeatherView` 有使用 store。
- loading 文字有出現。
- error 文字有出現。
- result 存在時才顯示 WeatherCard。
- 沒 result 且沒 loading / error 時有 empty state。

### 範例範圍地圖

| 部分 | 位置 | 負責的事情 |
| --- | --- | --- |
| View | `WeatherView.vue` | 組合狀態 UI |
| Store | `weather.ts` | 提供 loading / error / result |
| Card | `WeatherCard.vue` | 顯示成功結果 |

### 完整實作流程、範例與注意事項

完整流程：

1. 開啟 `WeatherView.vue`。
2. import store 和 `storeToRefs`。
3. 取出 `result`、`loading`、`errorMessage`。
4. 建立 `handleSearch(city)`。
5. template 放表單。
6. 用 `v-if` / `v-else-if` / `v-else` 顯示狀態。
7. 測試成功與失敗。

```vue
<!-- src/views/WeatherView.vue -->
<script setup lang="ts">
import { storeToRefs } from 'pinia'
import WeatherCard from '../components/WeatherCard.vue'
import WeatherSearchForm from '../components/WeatherSearchForm.vue'
import { useWeatherStore } from '../stores/weather'

const weatherStore = useWeatherStore()
const { result, loading, errorMessage } = storeToRefs(weatherStore)

function handleSearch(city: string) {
  weatherStore.load(city)
}
</script>

<template>
  <WeatherSearchForm @search="handleSearch" />

  <p v-if="loading">查詢中...</p>
  <p v-else-if="errorMessage">{{ errorMessage }}</p>
  <WeatherCard
    v-else-if="result"
    :city="result.city"
    :temperature="result.temperature"
  />
  <p v-else>尚未查詢天氣。</p>
</template>
```

注意事項：

- 狀態順序很重要，通常 loading 優先。
- error 和 result 要避免同時顯示造成混淆。
- 表單 submit 時不要讓頁面刷新。

### 如果結果和預期不同

- 一直顯示 loading：檢查 store finally。
- 成功後還顯示 error：成功時要清掉 error。
- WeatherCard props 報錯：確認 result type 和 props 對得上。

### 負面例子 / 錯誤用法

錯誤做法：只在 Console log error，不在畫面顯示。

問題：使用者不知道發生什麼事，只覺得 app 壞了。

修正方向：每個 API 流程至少有 loading、error、empty、success。

### 小練習

在 loading 時讓 `WeatherSearchForm` 的搜尋按鈕 disabled。

### Junior 常見誤解

API UI 不是只顯示資料；等待中和失敗狀態同樣是功能的一部分。

### 一句話總結

完整 API 畫面一定要處理 loading、error、empty、success。

---

## Day 26：Weather app component 組合

### 這篇文章主要在講什麼

今天把表單、卡片、頁面、store 組合成較完整的 Weather Dashboard。

### 為什麼需要這個概念

真實 Vue app 不是一段 code，而是多個 component 和 store 合作。新手要知道每個檔案負責什麼，以及資料怎麼流。

### 學完這篇你應該會做到什麼

- 組合 `WeatherSearchForm` 和 `WeatherCard`。
- 讓頁面負責流程，元件負責呈現。
- 說出端到端資料流。

### 核心重點

- Form emit city。
- View 呼叫 store。
- Store 呼叫 service。
- Card 用 props 顯示 result。

### 真實工作流程例子

工作任務：你要向同事說明查詢功能的資料流，讓他知道 bug 該從哪一層查。

你要做的是：用「Form -> View -> Store -> Service -> Store -> Card」說明端到端流程。這種能力在 code review、交接、debug 時比單純會寫語法更重要。

### 主線專案銜接

今天完成查詢頁的主要組合。

1. `WeatherSearchForm` 負責輸入。
2. `WeatherView` 負責接事件與顯示狀態。
3. `WeatherCard` 負責顯示結果。

具體會動到：

- `src/components/WeatherSearchForm.vue`
- `src/views/WeatherView.vue`
- `src/components/WeatherCard.vue`

做完可驗證：

- 從輸入到顯示結果的流程完整。
- 每個元件責任單純。
- 新手能追出資料流。

### 當天做完後檢查

- SearchForm 沒有顯示天氣結果。
- WeatherCard 沒有處理搜尋。
- WeatherView 有組合兩者。
- Store 是唯一查詢狀態來源。
- 可以描述完整流程。

### 範例範圍地圖

| 部分 | 位置 | 負責的事情 |
| --- | --- | --- |
| SearchForm | `components/WeatherSearchForm.vue` | 輸入與送出 |
| WeatherView | `views/WeatherView.vue` | 查詢流程與狀態切換 |
| WeatherCard | `components/WeatherCard.vue` | 顯示結果 |
| Store | `stores/weather.ts` | 管理資料與 API 狀態 |

### 完整實作流程、範例與注意事項

完整流程：

1. 使用者在 `WeatherSearchForm` 輸入 `Taipei`。
2. 表單 submit 後 emit `search`。
3. `WeatherView` 的 `handleSearch` 收到 city。
4. `WeatherView` 呼叫 `weatherStore.load(city)`。
5. Store 更新 loading 並取得 result。
6. `WeatherView` 根據 result 顯示 `WeatherCard`。
7. `WeatherCard` 用 props 顯示城市與溫度。

```text
使用者輸入
-> WeatherSearchForm emit search(city)
-> WeatherView handleSearch(city)
-> Pinia weatherStore.load(city)
-> weatherApi.getWeather(city)
-> Store result 更新
-> WeatherCard props 顯示
```

注意事項：

- 這一天重點是「怎麼連」，不是新增很多 code。
- 如果你追不出資料流，代表元件責任可能分錯。
- 可以用 Devtools 一路看事件與 state。

### 如果結果和預期不同

- 表單送出但沒查詢：先看 emit 是否發出。
- Store 有 result 但卡片沒顯示：檢查 WeatherView 條件。
- 卡片顯示 undefined：檢查 props 名稱和 result 欄位。

### 負面例子 / 錯誤用法

錯誤做法：WeatherCard 自己呼叫 API，又接 props，又管理 loading。

問題：元件責任混亂，之後很難重用。

修正方向：頁面負責流程，store 負責狀態，card 負責顯示。

### 小練習

把 `WeatherCard` 加上 `description` 顯示，並從 result 傳入。

### Junior 常見誤解

會寫單一 component 不代表會組 app；真正重要的是元件之間如何合作。

### 一句話總結

Vue app 的可維護性來自清楚的 component 分工和資料流。

---

## Day 27：Vue best practices

### 這篇文章主要在講什麼

今天整理 Vue 專案常見好習慣：元件分工、資料流、API service、store、命名與列表 key。

### 為什麼需要這個概念

功能做得出來只是第一步。能不能維護、擴充、除錯，取決於平常的設計習慣。

### 學完這篇你應該會做到什麼

- 檢查元件是否責任過多。
- 檢查 API 是否集中。
- 檢查 store 是否濫用。
- 檢查列表 key 是否穩定。

### 核心重點

- Component 負責 UI，Service 負責 API，Store 負責共享狀態。
- Props down, events up。
- 不要在 template 寫複雜邏輯。
- `v-for` 使用穩定 key。

### 真實工作流程例子

工作任務：功能做完後進 code review，同事要求你確認 API 呼叫是否分散、component 是否太胖、列表 key 是否安全。

你要做的是：用搜尋工具和檔案責任檢查專案。這是實務中很常見的交付前自我 review，而不是等 reviewer 幫你抓。

### 主線專案銜接

今天做一次 Weather Dashboard 架構檢查。

1. 檢查 `WeatherView` 是否太胖。
2. 檢查 API 是否只在 service。
3. 檢查 store 是否只放共享狀態。

具體會動到：

- 可能只調整命名和搬移少量邏輯。
- 檢查 `components`、`views`、`services`、`stores`。

做完可驗證：

- 每個檔案能用一句話說明責任。
- 沒有 component 直接重複 fetch。
- 沒有 index key。

### 當天做完後檢查

- `WeatherSearchForm` 只處理輸入。
- `WeatherCard` 只顯示資料。
- `WeatherView` 組合流程。
- `weatherApi.ts` 處理 API。
- `weather.ts` 處理共享狀態。

### 範例範圍地圖

| 檢查項目 | 位置 | 合格標準 |
| --- | --- | --- |
| Component | `src/components` | 單一清楚責任 |
| View | `src/views` | 組合流程 |
| Service | `src/services` | API 集中 |
| Store | `src/stores` | 共享狀態集中 |

### 完整實作流程、範例與注意事項

完整流程：

1. 列出所有 `.vue` 檔。
2. 對每個檔案寫一句責任說明。
3. 搜尋 `fetch(`，確認只在 service 或刻意設計的位置。
4. 搜尋 `:key="index"`，確認沒有使用。
5. 檢查 template 是否有複雜 filter / sort。
6. 把複雜推導移到 computed。

```powershell
# 範例用途：快速檢查專案中是否有分散的 fetch 或 index key。
# 主要輸入：目前 src 資料夾。
# 執行結果：列出符合條件的位置，讓你逐一檢查。
rg "fetch\\(" src
rg ':key="index"' src
```

注意事項：

- Best practice 不是死規則，要看專案規模。
- 小專案可以簡單，但不要混亂。
- 重構前先確認功能有測過或至少手動檢查過。

### 如果結果和預期不同

- 找到多個 fetch：判斷是否應搬進 service。
- 找到 index key：看資料是否有 id。
- View 太長：拆 component 或 composable。

### 負面例子 / 錯誤用法

錯誤做法：為了「架構漂亮」把一個小功能拆成十幾個檔案。

問題：抽象太早，反而增加理解成本。

修正方向：當重複、責任混亂或檔案太長時再拆。

### 小練習

替每個 component 寫一句責任描述，寫不出來的檔案通常需要整理。

### Junior 常見誤解

Best practice 不是背規則，而是讓未來的你比較容易理解和修改。

### 一句話總結

好的 Vue 專案，是每個檔案的責任都能被快速說清楚。

---

## Day 28：Dashboard data mapping

### 這篇文章主要在講什麼

今天把 API 或外部資料轉成 Dashboard 需要的顯示格式，也就是 data mapping。

### 為什麼需要這個概念

外部 API 欄位常常很深、命名不一致、格式不適合 UI。直接在 template 讀原始 response 會讓畫面脆弱。

### 學完這篇你應該會做到什麼

- 定義 UI 需要的資料格式。
- 把 raw response 轉成 `WeatherResult`。
- 知道 mapping 應放在 service。

### 核心重點

- UI 不應依賴外部 API 的完整原始結構。
- Service 可以做 response mapping。
- TypeScript type 讓資料形狀更清楚。
- 顯示數字前可先格式化。

### 真實工作流程例子

工作任務：後端把 API response 從 `{ temp: 26 }` 改成 `{ main: { temp: 26.4 } }`，但 UI 不應該大改。

你要做的是：只修改 service mapping，讓 component 繼續吃 `WeatherResult.temperature`。這就是把外部資料格式和 UI 解耦的價值。

### 主線專案銜接

今天補強 `weatherApi.ts` 的 mapping。

1. 定義 raw response type。
2. 定義 UI result type。
3. 寫 `mapWeatherResponse()`。

具體會動到：

- `src/services/weatherApi.ts`
- `src/components/WeatherCard.vue`

做完可驗證：

- UI 使用簡單欄位。
- API response 改動時主要改 service。
- WeatherCard 不知道 raw response 長相。

### 當天做完後檢查

- 有 raw type。
- 有 UI type。
- 有 mapping function。
- component 沒直接讀 raw response。
- mapping 處理必要 fallback。

### 範例範圍地圖

| 部分 | 位置 | 負責的事情 |
| --- | --- | --- |
| Raw type | `weatherApi.ts` | 描述 API 原始格式 |
| UI type | `WeatherResult` | 描述畫面需要格式 |
| Mapping | `mapWeatherResponse` | 轉換資料 |
| Card | `WeatherCard.vue` | 顯示 UI type |

### 完整實作流程、範例與注意事項

完整流程：

1. 開啟 `weatherApi.ts`。
2. 新增 `WeatherApiResponse`。
3. 新增 `WeatherResult`。
4. 建立 `mapWeatherResponse(raw)`。
5. `getWeather()` 回傳 mapping 後結果。
6. 確認 WeatherCard 只吃 `WeatherResult` 欄位。

```ts
// src/services/weatherApi.ts
type WeatherApiResponse = {
  name: string
  main: { temp: number }
  weather: Array<{ description: string }>
}

export type WeatherResult = {
  city: string
  temperature: number
  description: string
}

function mapWeatherResponse(raw: WeatherApiResponse): WeatherResult {
  // 範例用途：把外部 API 格式轉成前端 UI 穩定使用的格式。
  // 主要輸入：raw 是 API 原始 response。
  // 執行結果：回傳 WeatherResult，component 不需要知道 raw.main.temp 這種結構。
  return {
    city: raw.name,
    temperature: Math.round(raw.main.temp),
    description: raw.weather[0]?.description ?? '無描述',
  }
}
```

注意事項：

- raw response 欄位可能不存在，要處理 fallback。
- mapping 放 service 比放 template 更好測、更好維護。
- UI type 不必包含 API 所有欄位。

### 如果結果和預期不同

- description 是 undefined：檢查 fallback。
- temperature 小數太長：用 `Math.round` 或格式化。
- API 欄位改名：主要修 mapping。

### 負面例子 / 錯誤用法

錯誤做法：在 template 寫 `result.main.temp`、`result.weather[0].description`。

問題：UI 直接依賴外部 API 結構，API 小變動就會壞。

修正方向：在 service 轉成 UI 專用格式。

### 小練習

新增 `temperatureLabel`，讓 UI 顯示 `26°C` 而不是單純數字。

### Junior 常見誤解

TypeScript type 不是只是為了不報錯，它也在描述資料責任邊界。

### 一句話總結

外部資料進入 app 前，先 mapping 成 UI 好用的格式。

---

## Day 29：filter / sort 與 computed

### 這篇文章主要在講什麼

今天用 computed 對 Dashboard 或歷史紀錄資料做篩選與排序。

### 為什麼需要這個概念

列表資料常需要搜尋、排序、過濾。把這些邏輯寫在 template 會讓畫面難讀，也可能重複計算。

### 學完這篇你應該會做到什麼

- 用 computed 篩選 history。
- 用 computed 排序資料。
- 建立 empty state。

### 核心重點

- filter / sort 適合放 computed。
- `sort()` 會改原陣列，必要時先複製。
- 搜尋字串要處理大小寫。
- 篩選後沒資料也要顯示狀態。

### 真實工作流程例子

工作任務：使用者查詢很多城市後，希望在歷史紀錄頁搜尋 Taipei，並依最新時間排序。

你要做的是：把搜尋字留在 HistoryView，原始紀錄留在 store，篩選排序用 computed。交付時要測有符合資料、沒有符合資料、清空關鍵字三種情境。

### 主線專案銜接

今天讓歷史紀錄頁可以搜尋和排序。

1. 在 store 保存 history。
2. `HistoryView` 建立 `keyword`。
3. computed 產生 `filteredHistory`。

具體會動到：

- `src/stores/weather.ts`
- `src/views/HistoryView.vue`

做完可驗證：

- 輸入關鍵字後列表變少。
- 排序不會破壞原資料。
- 沒符合結果時顯示 empty state。

### 當天做完後檢查

- `keyword` 是 local state。
- `filteredHistory` 是 computed。
- sort 前有複製陣列。
- empty state 有顯示。
- template 沒直接塞複雜 filter。

### 範例範圍地圖

| 部分 | 位置 | 負責的事情 |
| --- | --- | --- |
| History state | `weather.ts` | 保存查詢紀錄 |
| Keyword | `HistoryView.vue` | 使用者搜尋字 |
| Computed | `filteredHistory` | 篩選與排序 |
| UI | `HistoryView.vue` | 顯示結果或空狀態 |

### 完整實作流程、範例與注意事項

完整流程：

1. 確認 store 有 `history`。
2. 開啟 `HistoryView.vue`。
3. 建立 `keyword`。
4. 用 `storeToRefs` 取得 history。
5. 建立 `filteredHistory`。
6. template 顯示 input 和列表。
7. 測試有結果與沒結果。

```vue
<!-- src/views/HistoryView.vue -->
<script setup lang="ts">
import { computed, ref } from 'vue'
import { storeToRefs } from 'pinia'
import { useWeatherStore } from '../stores/weather'

const keyword = ref('')
const weatherStore = useWeatherStore()
const { history } = storeToRefs(weatherStore)

// 範例用途：根據關鍵字篩選並排序查詢紀錄。
// 主要輸入：keyword 與 store 裡的 history。
// 執行結果：template 使用 filteredHistory 顯示處理後列表。
const filteredHistory = computed(() => {
  const text = keyword.value.toLowerCase()

  return [...history.value]
    .filter((item) => item.city.toLowerCase().includes(text))
    .sort((a, b) => b.searchedAt.localeCompare(a.searchedAt))
})
</script>

<template>
  <input v-model.trim="keyword" placeholder="搜尋歷史城市" />

  <p v-if="filteredHistory.length === 0">沒有符合的紀錄。</p>
  <ul v-else>
    <li v-for="item in filteredHistory" :key="item.searchedAt">
      {{ item.city }} - {{ item.searchedAt }}
    </li>
  </ul>
</template>
```

注意事項：

- `[...history.value]` 是為了避免 `sort()` 改到原陣列。
- `keyword` 只在歷史頁使用，所以放 local state 即可。
- 大量資料時可能需要後端搜尋或分頁。

### 如果結果和預期不同

- 原始順序被改：確認 sort 前有複製。
- 搜尋大小寫不一致：雙方都轉小寫。
- 空結果整頁空白：補 empty state。

### 負面例子 / 錯誤用法

錯誤做法：在 template 直接寫很長的 `history.filter(...).sort(...)`。

問題：template 變難讀，除錯也不方便。

修正方向：把列表推導放 computed，template 只負責顯示。

### 小練習

新增一個排序切換：最新優先 / 最舊優先。

### Junior 常見誤解

computed 不只適合算一個文字，也很適合整理畫面要顯示的列表資料。

### 一句話總結

列表的篩選與排序應該先整理成 computed，再交給 template 顯示。

---

## Day 30：收尾、README、Build 檢查

### 這篇文章主要在講什麼

最後一天把專案整理成別人可以下載、安裝、執行、build、部署的狀態。

### 為什麼需要這個概念

程式跑得起來不代表專案完整。README、build 檢查、部署提醒，會決定別人能不能接手你的專案。

### 學完這篇你應該會做到什麼

- 撰寫基本 README。
- 執行 build 和 preview。
- 檢查環境變數與部署設定。

### 核心重點

- README 至少包含 install、dev、build、preview。
- build 要在交付前跑。
- API 設定要說清楚。
- 部署平台要設定 SPA fallback。

### 真實工作流程例子

工作任務：你要把 Weather Dashboard 交給另一位工程師或部署到平台。

你要做的是：確保 README 能讓對方從零跑起專案，build 能過，preview 能正常切頁，API key 或 proxy 限制有寫清楚。這才是可交接的工作成果。

### 主線專案銜接

今天完成 Weather Dashboard 的交付檢查。

1. 補 README。
2. 執行 build。
3. 執行 preview。
4. 確認路由與 API 狀態。

具體會動到：

- `README.md`
- `public/_redirects`
- 不手動修改 `dist`

做完可驗證：

- 新人照 README 能跑專案。
- build 成功。
- preview 可以切頁。
- 部署提醒清楚。

### 當天做完後檢查

- README 有安裝步驟。
- README 有開發步驟。
- README 有 build 步驟。
- README 有 API / env 說明。
- `npm run build` 成功。

### 範例範圍地圖

| 部分 | 位置 / 指令 | 負責的事情 |
| --- | --- | --- |
| README | `README.md` | 專案使用說明 |
| Build | `npm run build` | 檢查正式打包 |
| Preview | `npm run preview` | 預覽正式結果 |
| Deploy | `public/_redirects` | SPA fallback |

### 完整實作流程、範例與注意事項

完整流程：

1. 開啟或建立 `README.md`。
2. 寫專案用途。
3. 寫 install / dev / build / preview 指令。
4. 寫 API 設定提醒。
5. 執行 `npm run build`。
6. 執行 `npm run preview`。
7. 測試 `/weather` 和 `/history`。

````markdown
# Weather Dashboard

Vue 3 + Vite + Pinia + Vue Router 練習專案。

## Project Setup

```powershell
npm install
```

## Development

```powershell
npm run dev
```

## Build

```powershell
npm run build
```

## Preview

```powershell
npm run preview
```

## API Notes

如果接真實天氣 API，請透過後端或 serverless function 保護 API key。
````

注意事項：

- README 是給未來的自己和其他人看的，不是裝飾。
- build 失敗代表交付風險，不要忽略。
- 如果接真 API，要說明環境變數和 proxy。

### 如果結果和預期不同

- 新人照 README 跑不起來：補 Node 版本與前置需求。
- build 失敗：優先修 TypeScript 和 import。
- preview 路由壞掉：檢查 Router history 與 fallback。

### 負面例子 / 錯誤用法

錯誤做法：只留一句「npm install 後自己看」。

問題：接手者不知道如何跑、如何 build、API 怎麼設定。

修正方向：README 要能讓完全沒碰過專案的人照步驟跑起來。

### 小練習

在 README 加上「功能列表」與「學到的 Vue 概念」。

### Junior 常見誤解

專案收尾不是最後才想到的雜事，而是交付品質的一部分。

### 一句話總結

能 build、能 preview、README 說得清楚，專案才算真正交得出去。
