# **data type**

#### Array（陣列）



為連續型記憶體儲存空間，

透過 index 存取元素，

空間複雜度為 O(n)。

✔ 核心特性

1\. 記憶體連續配置（Contiguous Memory）

2\. 可隨機存取（Random Access）

3\. index 計算位置（base + offset）

✔ 時間複雜度

讀取（Access）        → O(1)

搜尋（Search）        → O(n)

插入（Insert）        → O(n)

刪除（Delete）        → O(n)



👉（插入/刪除慢的原因：需要搬移元素）



✔ 優點

1\. 存取速度快（O(1)）

2\. 記憶體連續 → CPU cache 命中率高

3\. 結構簡單，容易實作

✔ 缺點

1\. 大小固定（靜態配置）

2\. 插入/刪除成本高（需搬移資料）

3\. 可能造成記憶體浪費（預留空間）

✔ 實務補充（很重要）



👉 在 .NET / C#：



List<T> 本質 = 動態陣列

擴容時：

會建立新陣列 + copy（成本 O(n)）





#### Stack（堆疊）



採用 LIFO（Last In First Out）原則，

只能在一端（Top）進行操作。

✔ 基本操作

push（加入） → O(1)

pop（移除）  → O(1)

peek（查看） → O(1)

✔ 核心特性

1\. 後進先出（LIFO）

2\. 僅允許單端操作

3\. 可用 array 或 linked list 實作



🔸 Stack（Subroutine Call / Recursive）記憶體運作

🔹 Stack 記憶體結構

高位址

│

│  ← Stack Top（最新）

│  ───────────────

│  Stack Frame（function B）

│  ───────────────

│  Stack Frame（function A）

│  ───────────────

│  main()

│

低位址(先)



👉 每一層 function = 一個 Stack Frame



🔸 ✔ Subroutine Call（函式呼叫）實際流程

Step 1：呼叫 function

→ 建立新的 Stack Frame（push）



Step 2：Stack Frame 內容包含

→ return address（回到哪一行）

→ parameters（參數）

→ local variables（區域變數）

→ 暫存暫存器（暫存運算狀態）



Step 3：執行 function



Step 4：function 結束

→ 取出 return address

→ pop stack frame

→ 回到呼叫位置繼續執行

🔸 ✔ Recursive Call（遞迴）記憶體運作



👉 例：



int f(int n) {

&#x20;   if (n == 0) return 1;

&#x20;   return n \* f(n - 1);

}

🔹 呼叫過程（Push）

f(3)

→ push f(3)

→ push f(2)

→ push f(1)

→ push f(0)



👉 記憶體：



Top → f(0)

&#x20;      f(1)

&#x20;      f(2)

&#x20;      f(3)

🔹 回傳過程（Pop）

f(0) 回傳 1 → pop

f(1) = 1 \* 1 → pop

f(2) = 2 \* 1 → pop

f(3) = 3 \* 2 → pop

🔹 核心觀念

1\. 每次遞迴 = 新 stack frame

2\. 記憶體向上堆疊（push）

3\. 遇到 base case 才開始釋放（pop）

⚠️ 風險

Stack Overflow：

遞迴過深 → stack 空間不足 → crash

👉 風險：

Stack Overflow（遞迴太深）



✔ Compiler Parsing（你提的）

利用 stack：

1\. 檢查括號是否匹配

2\. 處理運算子優先順序

3\. 建立語法結構（AST 前處理）

✔ Zero Operand（堆疊式運算）

例如：後序表達式（Postfix）



3 4 + 5 \*



流程：

push 3 → push 4 → + → push 7 → push 5 → \* → 35queue  fifo, job scheduling, io需求





#### Queue（佇列）



採用 FIFO（First In First Out），

資料從一端加入（rear），另一端移除（front）。

✔ 基本操作

enqueue（加入） → O(1)

dequeue（移除） → O(1)

peek（查看）    → O(1)

✔ 核心特性

1\. 先進先出（FIFO）

2\. 兩端操作（front / rear）

3\. 常用 circular queue 避免空間浪費

✔ 時間複雜度

所有基本操作皆為 O(1)



🔸 ✔ 記憶體真實運作（重點）



👉 有兩種實作：



(1) 線性 Queue（會浪費空間）

enqueue / dequeue 只會往前走



缺點：

前面空間用完但不能重用

(2) Circular Queue（實務常用）

index = (index + 1) % size



👉 形成環狀：



\[ A ]\[ B ]\[ C ]\[ D ]\[ E ]

&#x20;  ↑                ↑

&#x20;front            rear



👉 空間可以重複利用



🔸 ✔ Queue 在記憶體的核心觀念

1\. 資料連續儲存（array-based）

2\. 透過 front / rear 控制存取

3\. 不搬移資料（只移動指標）





✔ 應用（考試重點）

1\. Job Scheduling（工作排程）

&#x20;  → CPU 排程（先到先服務）



2\. I/O 請求

&#x20;  → 磁碟、網路請求排隊



3\. BFS（廣度優先搜尋）

&#x20;  → Tree / Graph traversal



4\. Producer-Consumer（生產者消費者）

✔ 實務補充（很重要）



👉 在 .NET：



Queue<T> → FIFO

ConcurrentQueue<T> → 多執行緒安全



👉 Web / 系統：



Message Queue（如 RabbitMQ）

Request 排隊處理



🔸 前序（Preorder）

應用：複製樹、序列化（Serialize）



👉 實務：



JSON / XML 結構輸出

API 回傳樹狀資料



👉 本質：

✔ 先處理自己 → 再處理子節點



🔸 中序（Inorder）

應用：排序（Binary Search Tree）



👉 實務：



DB index（B-Tree / BST 概念）

有序資料輸出



👉 本質：

✔ 會得到「排序後結果」



🔸 後序（Postorder）

應用：刪除、釋放資源



👉 實務（超重要）：



Tree delete（先刪子節點）

GC（垃圾回收）

檔案/資料夾刪除（先刪內容再刪資料夾）



👉 本質：

✔ 先處理子節點 → 再處理自己



🔹 🔥 你問的「IDE / 語法底層邏輯」🔥



這段我幫你講白（這是工程師理解層）



✔ Compiler / IDE 其實在用：

1️⃣ Stack



👉 用在：



function call

recursive

expression parsing



👉 例如你寫：



a + b \* c



👉 編譯器會：



用 stack 處理優先順序

2️⃣ Tree（AST 抽象語法樹）



👉 編譯器會把程式變成：



Syntax Tree（語法樹）



👉 然後：



Preorder → 產生程式碼

Postorder → 計算 expression

3️⃣ Queue



👉 用在：



工作排程（IDE 背景分析）

async task 排隊

# **sort**





# **複雜度**

### **時間**

用來描述程式執行時間隨輸入大小 I 增加時的變化。



理論上 T(P) 為實際執行時間，

但因受硬體與環境影響，不適合直接測量。



因此改以「基本操作的執行次數」來表示，

建立時間函數 T(P) = f(I)，

並以 Big-O 表示其成長趨勢。



時間複雜度可依輸入情況分為三種：



(1) 最好情況（Best Case）

Ω（Omega）

在所有輸入中執行時間最短的情況，

代表最理想狀況。



(2) 平均情況（Average Case）

Θ（Theta）

所有輸入情況下的平均執行時間，

代表一般實務情況。



(3) 最壞情況（Worst Case）

O（Big-O）

在所有輸入中執行時間最長的情況，

代表執行時間的上界，也是最常用分析方式。



Best Case   → Ω(...)

Average Case → Θ(...)

Worst Case  → O(...)



一般時間複雜度分析預設為 Worst Case（O）。



**Master Method**

**用來分析以下型態的遞迴時間複雜度：**



**T(n) = aT(n/b) + f(n)**



**其中：**

**a ＝ 子問題個數**

**n/b ＝ 每個子問題的規模**

**f(n) ＝ 分解與合併所需時間**



**比較 f(n) 與 n^(log\_b a)**



**(1) Case 1：子問題主導**

**若 f(n) < n^(log\_b a)**



**👉 結論：**



**T(n) = Θ(n^(log\_b a))**



**✔ 意思：**



**子問題成本比較大**

**主導整體時間**





**(2) Case 2：平衡情況**

**若 f(n) = n^(log\_b a)**



**👉 結論：**



**T(n) = Θ(n^(log\_b a) log n)**



**✔ 意思：**



**子問題 + 合併成本一樣重要**

**會多一層 log n**



**(3) Case 3：外部工作主導**

**若 f(n) > n^(log\_b a)**



**👉 結論：**



**T(n) = Θ(f(n))**



**✔ 意思：**



**合併/處理成本最大**

**主導整體時間**

### **空間**

### **(1) 固定空間（Fixed Space）**



👉 與輸入大小 I 無關，不會隨資料量改變



包含：



a. 指令空間（Instruction Space）

→ 程式本身的程式碼大小（編譯後就固定）



b. 單一變數（Simple Variables）

→ 如 int i, float x 等基本型別變數



c. 固定變數（Fixed-size Variables）

→ 固定大小的陣列或結構（例如 int arr\[100]）



d. 常數（Constants）

→ const, final 等固定值



✔ 以上全部都是：O(1)(2) 變數

一個程式跑完所需的的記憶體，如果是被包含在那一個function內 也要計算



### **(2) 變動空間（Variable Space）**



👉 與輸入大小 I 有關，會隨資料量改變



包含：



動態配置記憶體（例如 new, malloc）

陣列大小依輸入決定（例如 new int\[n]）

遞迴呼叫所使用的 stack 空間

function 呼叫時的參數與區域變數（若與輸入相關）



👉 你原本那句可以修正成：



一個程式執行時所需的額外記憶體，若該記憶體配置發生在某個 function 內，仍需納入計算（只要與輸入 I 有關）



**S(P) = 固定空間 + 變動空間**



### Call by Value：

&#x20;   傳遞變數的值，會產生資料的複本，

&#x20;   若資料量大，將增加額外空間使用，影響空間複雜度。



### Call by Address（Reference）：

&#x20;   傳遞變數的記憶體位址，不會複製資料，

&#x20;   僅需固定大小空間，空間複雜度為 O(1)。



因此：

&#x20;   Call by Value 可能因資料複製導致空間複雜度增加，

&#x20;   Call by Reference 則因不複製資料而節省記憶體空間。

