# 寫給朋友的 PHP 從 0 到 100 實戰教程：2026 更新整理

## 來源

- 原系列頁：https://ithelp.ithome.com.tw/users/20107394/ironman/1332?page=1
- RSS 來源：https://ithelp.ithome.com.tw/rss/series/1332
- 原作者：falconwei
- 原系列時間：2017-12-18 到 2018-01-16
- 2026 更新參考：
  - PHP Supported Versions：https://www.php.net/supported-versions.php
  - PHP 8.4 Release Announcement：https://www.php.net/releases/8.4/en.php
  - PHP 8.5 Release Announcement：https://www.php.net/releases/8.5/en.php

## 這份筆記怎麼讀

這個系列原本的精神很清楚：不要先被框架嚇到，先用 PHP 寫出一個真的會動的網站，從變數、判斷、迴圈、物件、資料庫、會員註冊登入、寄信、API、OAuth、金流，一路走到簡單電商。

2026 年再讀這系列，重點不是照抄 2018 年的寫法，而是保留「從需求推回程式」的學習順序，並把實作更新成現代 PHP 的安全習慣：

- PHP 版本建議以 PHP 8.4 或 PHP 8.5 為目標；PHP 官方在 2026-05-02 顯示 8.2 到 8.5 仍在支援週期內，其中 8.5 已於 2025-11-20 發布。
- 資料庫一律優先使用 PDO、prepared statements、明確錯誤模式。
- 密碼一律使用 `password_hash()` 與 `password_verify()`，不要自己加密密碼。
- 表單輸入要驗證，輸出 HTML 要 escape。
- 登入、金流、OAuth callback 這類流程不能只看「有沒有參數」，還要驗證來源、狀態、簽章、CSRF token。
- Composer、環境變數、autoload、log、分層架構已經是基本功，不再是進階選配。

## 整體地圖

| 範圍 | 原系列主題 | 讀者要學會的能力 | 2026 更新重點 |
| --- | --- | --- | --- |
| Day 1-4 | 語法、變數、if else、迴圈 | 看懂程式如何一行一行處理資料 | 補上型別、嚴格比較、可測試的小函式 |
| Day 5-10 | 物件、MySQL、環境、Composer、路由 | 把零散頁面整理成小型網站 | 使用 Composer autoload、環境變數、Clean URL |
| Day 11-16 | 資料庫連線、輸入、驗證、日期、寄信、PDO | 建立可維護的資料存取與表單流程 | PDO prepared statements、例外處理、SMTP 設定外移 |
| Day 17-22 | 註冊、登入、開通、重設密碼、flash session、log | 完成會員系統的核心流程 | session 安全、token 有效期限、Monolog |
| Day 23-26 | 簡訊 API、Facebook 登入、ECPay | 串接外部服務 | SDK、callback 驗證、秘密資訊不可寫死 |
| Day 27-30 | 電商準備、購物車、金流回站、原始碼 | 串起商務流程 | 訂單狀態機、webhook 驗證、交易不可只存在 session |

## 來源清單

| Day | 標題 | 連結 |
| --- | --- | --- |
| 1 | PHP教學: 寫程式真的不難，從零開始也可以 | https://ithelp.ithome.com.tw/articles/10191950 |
| 2 | PHP教學: 把資料記到變數盒子 | https://ithelp.ithome.com.tw/articles/10192186 |
| 3 | PHP教學: 一分鐘搞懂如果...就...的 if else 語法 | https://ithelp.ithome.com.tw/articles/10192563 |
| 4 | PHP教學: 用迴圈解決重複無聊的工作 | https://ithelp.ithome.com.tw/articles/10192807 |
| 5 | PHP教學: 開始架設自己的遊戲攻略網站 class 物件篇 | https://ithelp.ithome.com.tw/articles/10192984 |
| 6 | PHP教學: 用物件的方式連接 MYSQL 資料庫 | https://ithelp.ithome.com.tw/articles/10193372 |
| 7 | PHP教學: 架設 PHP 網站開發環境 | https://ithelp.ithome.com.tw/articles/10193603 |
| 8 | PHP教學: 必學-超強套件管理器 composer | https://ithelp.ithome.com.tw/articles/10193632 |
| 9 | PHP教學: 從單一網頁到基礎框架概念 | https://ithelp.ithome.com.tw/articles/10194090 |
| 10 | PHP教學: Clean URL 優化連結 | https://ithelp.ithome.com.tw/articles/10194207 |
| 11 | PHP教學: 使用單例模式來連接資料庫 | https://ithelp.ithome.com.tw/articles/10194443 |
| 12 | PHP教學: 取得使用者輸入的資料存到資料庫 | https://ithelp.ithome.com.tw/articles/10194762 |
| 13 | PHP教學: 檢查使用者輸入的資料 | https://ithelp.ithome.com.tw/articles/10194900 |
| 14 | PHP教學: 學習日期與時間控制 | https://ithelp.ithome.com.tw/articles/10195149 |
| 15 | PHP教學: 用 phpmailer + gmail smtp 實作寄信功能 | https://ithelp.ithome.com.tw/articles/10195228 |
| 16 | PHP教程: 將 MySQLi 升級為 PDO | https://ithelp.ithome.com.tw/articles/10195426 |
| 17 | PHP教學: 實作註冊流程 | https://ithelp.ithome.com.tw/articles/10195675 |
| 18 | PHP教程: 實作登入與登出機制 | https://ithelp.ithome.com.tw/articles/10195818 |
| 19 | PHP教程: 實作收信開通帳號機制 | https://ithelp.ithome.com.tw/articles/10195972 |
| 20 | PHP教學: 實作重置密碼流程 | https://ithelp.ithome.com.tw/articles/10196178 |
| 21 | PHP教程: 優化架構與實作 flash session 附原始碼 | https://ithelp.ithome.com.tw/articles/10196311 |
| 22 | PHP教學: 使用 monolog 來追蹤問題 | https://ithelp.ithome.com.tw/articles/10196317 |
| 23 | PHP教學: 串接簡訊王 API | https://ithelp.ithome.com.tw/articles/10196595 |
| 24 | PHP教學: 實作 FACEBOOK 臉書登入 | https://ithelp.ithome.com.tw/articles/10196699 |
| 25 | PHP教學: 實作臉書帳號登入後的註冊 | https://ithelp.ithome.com.tw/articles/10196843 |
| 26 | PHP教學: 串接綠界 ECpay 線上支付 | https://ithelp.ithome.com.tw/articles/10196962 |
| 27 | PHP教程: 實作簡單電商網站 準備篇 | https://ithelp.ithome.com.tw/articles/10197014 |
| 28 | PHP教學: 實作商務邏輯層 | https://ithelp.ithome.com.tw/articles/10197131 |
| 29 | PHP教學: 刷完信用卡導回網站的處理 | https://ithelp.ithome.com.tw/articles/10197305 |
| 30 | 竟然要結束了那就大放送附專案原始碼 | https://ithelp.ithome.com.tw/articles/10197413 |

## Day 1-4：從語法到流程控制

### 這幾篇主要在講什麼

原文用很生活化的例子說明程式其實是「一行一行描述事情」。Day 1 先讓讀者知道 PHP 可以印出文字、註解與分號；Day 2 用變數避免重複修改；Day 3 用 `if else` 表達條件；Day 4 用迴圈處理大量訂單比對。

### 為什麼需要這些概念

沒有變數，資料只能被硬寫在每一行；沒有條件判斷，程式不能根據使用者狀態做不同處理；沒有迴圈，遇到 6000 筆訂單就只能人工比對。這些不是語法考題，而是所有網站功能的底層骨架。

### 定義名稱對照表

| 角色 | 文章中的名稱 | 功能 / 責任 |
| --- | --- | --- |
| Output | `echo` | 把字串輸出到畫面 |
| Variable | `$pokemon`, `$atk` | 暫存資料，讓資料可以重複使用 |
| Condition | `if`, `else` | 根據條件決定要跑哪段程式 |
| Loop | `foreach` | 對一批資料逐筆處理 |
| Array | `$order_total`, `$order_receive` | 用 key/value 存放多筆資料 |

### 實務使用情境

- 根據會員是否登入，顯示「登入」或「登出」按鈕。
- 根據訂單金額計算折扣。
- 逐筆檢查物流商回傳的收款資料。
- 把產品清單轉成 HTML 卡片。

### 不適合使用的情境

- 條件越寫越多層時，不該繼續塞 `if else`，應該抽成函式或策略物件。
- 大量資料不該一次全塞進陣列，應該分頁或用資料庫查詢處理。
- 金額計算不該用浮點數亂算，應使用整數分或 decimal 類型策略。

### 完整實作流程、Coding 例子與注意事項

完整流程：

1. 準備系統訂單清單與物流回款清單。
2. 用訂單編號當 key，金額當 value。
3. 逐筆檢查物流資料是否存在。
4. 比對金額是否一致。
5. 輸出缺漏訂單與金額不一致的訂單。

```php
<?php
declare(strict_types=1);

// 範例用途：模擬 Day 4 的對帳需求，用 foreach 找出物流回款缺漏或金額錯誤。
// 主要輸入：
// - $orders：系統內訂單，key 是訂單編號，value 是應收金額。
// - $received：物流商回款，key 是訂單編號，value 是實收金額。
// 回傳結果：
// - 回傳 missing 與 amountMismatch 兩組資料，讓後續可以寄信、匯出 CSV 或顯示在後台。
function reconcileCashOnDelivery(array $orders, array $received): array
{
    $missing = [];
    $amountMismatch = [];

    foreach ($orders as $orderId => $expectedAmount) {
        if (!array_key_exists($orderId, $received)) {
            $missing[] = $orderId;
            continue;
        }

        if ($received[$orderId] !== $expectedAmount) {
            $amountMismatch[$orderId] = [
                'expected' => $expectedAmount,
                'actual' => $received[$orderId],
            ];
        }
    }

    return [
        'missing' => $missing,
        'amountMismatch' => $amountMismatch,
    ];
}

$orders = [
    '1221000' => 100,
    '1221001' => 200,
    '1221002' => 300,
];

$received = [
    '1221000' => 100,
    '1221001' => 0,
];

print_r(reconcileCashOnDelivery($orders, $received));
```

注意事項：

- 原文 Day 3 作業裡的 `$weather = "raining"` 是賦值，不是比較；比較應該用 `==` 或更建議用 `===`。
- 2026 年寫 PHP 建議加上 `declare(strict_types=1);`，讓型別錯誤早點暴露。
- 陣列 key 如果是訂單編號，要避免前導 0 被當數字吃掉，通常用字串最穩。

### 負面例子 / 錯誤用法

錯誤做法：

```php
if ($weather = 'raining') {
    echo 'bring umbrella';
}
```

問題是 `=` 會把 `'raining'` 指派給 `$weather`，條件幾乎永遠成立。修正方向是使用嚴格比較：

```php
if ($weather === 'raining') {
    echo 'bring umbrella';
}
```

### Junior 常見誤解

- `=`、`==`、`===` 不一樣；新專案預設優先用 `===`。
- `foreach` 不是只能印資料，也可以累計、分類、檢查錯誤。
- 變數不是越短越好；`$expectedAmount` 比 `$a` 更容易維護。

### 一句話總結

Day 1-4 的核心是：程式就是用變數保存狀態、用條件做決策、用迴圈處理大量資料。

## Day 5-10：從單頁程式到小型網站架構

### 這幾篇主要在講什麼

這段從 class 物件、MySQL、開發環境、Composer、單一入口、Clean URL 開始，把「散落的 PHP 檔案」整理成「有結構的網站」。

### 為什麼需要這些概念

當網站只有一頁時，直接寫 `index.php` 還能忍；但只要開始有首頁、清單頁、詳情頁、登入頁、付款頁，如果每個檔案都自己 include、自己連線資料庫、自己判斷 URL，維護成本會快速爆炸。

### 定義名稱對照表

| 角色 | 文章中的名稱 | 功能 / 責任 |
| --- | --- | --- |
| Entity / Model | 英雄物件、資料物件 | 封裝一筆資料與相關行為 |
| Database Connection | MySQL 連線 | 讓 PHP 可以讀寫資料庫 |
| Dependency Manager | Composer | 安裝套件與提供 autoload |
| Front Controller | `index.php` | 統一接住請求後分派到不同頁面 |
| Clean URL | `/game/list` | 讓網址可讀、可分享、可被路由處理 |

### 實務使用情境

- 把 `/product?id=10` 改成 `/products/10`。
- 使用 Composer 安裝 `vlucas/phpdotenv`、`monolog/monolog`。
- 用一個入口檔處理所有 route，避免每頁重複設定。

### 不適合使用的情境

- 小練習可以簡單 include，但正式專案不該讓 controller、SQL、HTML 全混在同一檔。
- 不需要為了學路由立刻手刻大型框架；可以先理解 front controller，再學 Laravel 或 Symfony。

### 完整實作流程、Coding 例子與注意事項

完整流程：

1. 建立 `public/index.php` 作為唯一入口。
2. 設定 Web Server rewrite，把請求導向 `index.php`。
3. 用 `parse_url()` 取得 path。
4. 用 `match` 對應 controller。
5. 找不到 route 時回傳 404。

```php
<?php
declare(strict_types=1);

// 範例用途：模擬 Day 9-10 的單一入口與 Clean URL。
// 主要輸入：
// - $_SERVER['REQUEST_URI']：瀏覽器打進來的網址，例如 /products 或 /login。
// 回傳結果 / 副作用：
// - 根據 path 輸出不同頁面；找不到時設定 HTTP 404。
$path = parse_url($_SERVER['REQUEST_URI'] ?? '/', PHP_URL_PATH) ?: '/';

$handler = match ($path) {
    '/' => static fn() => '首頁',
    '/products' => static fn() => '商品列表',
    '/login' => static fn() => '登入頁',
    default => null,
};

if ($handler === null) {
    http_response_code(404);
    echo '404 Not Found';
    exit;
}

echo $handler();
```

注意事項：

- `REQUEST_URI` 來自使用者，不要直接拿去 include 檔案，否則可能變成任意檔案讀取風險。
- 真正專案應使用成熟 router 或框架，不要讓 route 表無限制膨脹。
- Composer 的 `vendor/` 不一定要 commit，通常 commit `composer.json` 與 `composer.lock`。

### 負面例子 / 錯誤用法

錯誤做法：

```php
include 'controller/' . $_GET['page'] . '.php';
```

問題是使用者可能傳入 `../../secret` 類似路徑。修正方向是使用白名單 route，不讓外部輸入直接決定檔案路徑。

### Junior 常見誤解

- Clean URL 不是只有漂亮，它讓 route 成為系統設計的一部分。
- Composer 不只是下載套件，也解決 class autoload 問題。
- 物件不是「把 function 包進 class」而已，重點是把資料與行為放在合理邊界內。

### 一句話總結

Day 5-10 的核心是：網站變大後，要用物件、Composer、單一入口與路由把混亂收斂起來。

## Day 11-16：資料庫、輸入驗證、時間、寄信與 PDO

### 這幾篇主要在講什麼

這段開始進入網站後端的基本功：連接資料庫、接收表單、驗證輸入、處理日期時間、寄信，最後把 MySQLi 升級成 PDO。

### 為什麼需要這些概念

網站真正有價值的地方通常是資料：會員、訂單、付款紀錄、驗證信、操作紀錄。只要使用者能輸入資料，就會有錯誤資料、惡意資料與非預期資料；只要資料要進資料庫，就必須避免 SQL injection。

### 定義名稱對照表

| 角色 | 文章中的名稱 | 功能 / 責任 |
| --- | --- | --- |
| Database Gateway | Database 類別 | 集中管理資料庫連線與查詢 |
| User Input | `$_POST` | 使用者從表單送出的資料 |
| Validator | 檢查使用者輸入 | 驗證必填、格式、重複資料 |
| DateTime | 日期與時間控制 | 處理時間格式、期限、到期日 |
| Mailer | PHPMailer + Gmail SMTP | 寄出驗證信或通知信 |
| PDO | PHP Data Object | 跨資料庫、支援 prepared statement 的資料存取 API |

### 實務使用情境

- 註冊時檢查 email 格式與是否重複。
- 建立資料時自動寫入 `created_at`。
- 寄出帳號開通信或重設密碼信。
- 使用 prepared statement 寫入資料庫。

### 不適合使用的情境

- 不該把 SMTP 帳密寫在 PHP 類別裡。
- 不該用字串拼接 SQL。
- 不該把所有欄位都只檢查 `!empty()`，不同欄位需要不同驗證規則。

### 完整實作流程、Coding 例子與注意事項

完整流程：

1. 從 `.env` 或安全設定讀取資料庫 DSN、帳號、密碼。
2. 建立 PDO，設定例外模式與 fetch 模式。
3. 從 `$_POST` 取輸入，但先做 trim 與格式驗證。
4. 用 prepared statement 寫入資料。
5. 捕捉例外，寫 log，不把敏感錯誤直接顯示給使用者。

```php
<?php
declare(strict_types=1);

// 範例用途：模擬 Day 12-16 的註冊資料寫入，但改成 2026 建議的 PDO prepared statement。
// 主要輸入：
// - $email：使用者在註冊表單輸入的 email。
// - $plainPassword：使用者輸入的原始密碼，只能短暫存在記憶體，不可寫入資料庫。
// 回傳結果 / 副作用：
// - 成功時新增 members 資料列；密碼只存 password_hash 後的結果。
function registerMember(PDO $pdo, string $email, string $plainPassword): int
{
    $email = trim($email);

    if (!filter_var($email, FILTER_VALIDATE_EMAIL)) {
        throw new InvalidArgumentException('Email 格式不正確');
    }

    if (mb_strlen($plainPassword) < 12) {
        throw new InvalidArgumentException('密碼至少需要 12 個字元');
    }

    $passwordHash = password_hash($plainPassword, PASSWORD_DEFAULT);

    $statement = $pdo->prepare(
        'INSERT INTO members (email, password_hash, created_at) VALUES (:email, :password_hash, :created_at)'
    );

    $statement->execute([
        'email' => $email,
        'password_hash' => $passwordHash,
        'created_at' => (new DateTimeImmutable('now', new DateTimeZone('Asia/Taipei')))->format('Y-m-d H:i:s'),
    ]);

    return (int) $pdo->lastInsertId();
}
```

注意事項：

- PDO 不是自動安全，安全來自 prepared statement 與正確綁定參數。
- `PASSWORD_DEFAULT` 會隨 PHP 版本演進；資料庫欄位長度建議至少 `varchar(255)`。
- 時區要明確設定，否則本機、測試機、正式機可能時間不同。

### 負面例子 / 錯誤用法

錯誤做法：

```php
$sql = "INSERT INTO members (email) VALUES ('" . $_POST['email'] . "')";
$pdo->query($sql);
```

問題是使用者輸入會直接進 SQL，可能造成 SQL injection。修正方向是使用 `prepare()` 與 `execute()` 傳參數。

### Junior 常見誤解

- 驗證輸入不是為了讓使用者麻煩，而是保護資料品質與系統安全。
- prepared statement 不是語法比較漂亮，而是把 SQL 結構和資料分開。
- 寄信失敗不一定代表程式錯，也可能是 SMTP、DNS、憑證、帳號安全設定問題。

### 一句話總結

Day 11-16 的核心是：凡是外部輸入進資料庫或外部服務，都要驗證、參數化、記錄錯誤。

## Day 17-22：會員系統、token 流程、flash session 與 log

### 這幾篇主要在講什麼

這段把前面的資料庫與寄信串成完整會員系統：註冊、登入、登出、email 開通、重設密碼、flash message、Monolog 追蹤問題。

### 為什麼需要這些概念

會員系統是很多網站的門禁。門禁如果設計不好，輕則使用者收不到信、無法登入，重則帳號被盜、重設密碼連結被濫用、log 洩漏個資。

### 定義名稱對照表

| 角色 | 文章中的名稱 | 功能 / 責任 |
| --- | --- | --- |
| Registration Flow | 註冊流程 | 建立會員與寄出開通信 |
| Authentication | 登入與登出 | 驗證身分、建立或移除 session |
| Activation Token | 收信開通帳號 | 確認 email 擁有權 |
| Reset Token | 重置密碼流程 | 讓忘記密碼者安全更新密碼 |
| Flash Session | flash session | 暫存一次性提示訊息 |
| Logger | Monolog | 記錄錯誤、警告與重要事件 |

### 實務使用情境

- 註冊後寄出 30 分鐘內有效的驗證信。
- 登入成功後重新產生 session id。
- 忘記密碼時寄出一次性 token。
- API 或背景工作失敗時寫入 log。

### 不適合使用的情境

- 不該把開通狀態只存在 session；開通結果要寫回資料庫。
- 不該讓重設密碼 token 永久有效。
- 不該在 log 裡寫入明文密碼、完整 access token、信用卡資料。

### 完整實作流程、Coding 例子與注意事項

完整流程：

1. 使用者送出 email 與密碼。
2. 系統建立會員，密碼 hash 後保存。
3. 系統建立隨機 token，保存 token hash 與過期時間。
4. 寄出包含 token 的驗證連結。
5. 使用者點擊連結後，比對 token hash 與期限。
6. 成功後啟用帳號，並刪除或標記 token 已使用。

```php
<?php
declare(strict_types=1);

// 範例用途：建立 email 開通 token。這是 Day 19 的現代化安全版本。
// 主要輸入：
// - $memberId：剛註冊成功的會員 id。
// 回傳結果 / 副作用：
// - 回傳明文 token 只用來寄信；資料庫只保存 hash，避免資料庫外洩時 token 可直接被使用。
function createActivationToken(PDO $pdo, int $memberId): string
{
    $token = bin2hex(random_bytes(32));
    $tokenHash = hash('sha256', $token);
    $expiresAt = (new DateTimeImmutable('+30 minutes'))->format('Y-m-d H:i:s');

    $statement = $pdo->prepare(
        'INSERT INTO member_activation_tokens (member_id, token_hash, expires_at) VALUES (:member_id, :token_hash, :expires_at)'
    );

    $statement->execute([
        'member_id' => $memberId,
        'token_hash' => $tokenHash,
        'expires_at' => $expiresAt,
    ]);

    return $token;
}
```

注意事項：

- token 要用 `random_bytes()`，不要用 `date()`、`rand()`、email 拼湊。
- 登入成功後應使用 `session_regenerate_id(true)` 降低 session fixation 風險。
- flash message 用完要清掉，否則同一訊息會重複出現。

### 負面例子 / 錯誤用法

錯誤做法：

```php
$resetToken = md5($email . time());
```

問題是 token 可預測，攻擊者可能猜測或重放。修正方向是使用 `random_bytes()`，資料庫保存 hash，並加上過期時間與使用狀態。

### Junior 常見誤解

- 登入不是只查到帳號密碼就好，還要處理 session、錯誤訊息、嘗試次數與登出。
- log 不是只在出錯才需要，重要流程例如寄信、付款 callback、OAuth 失敗都值得記錄。
- token 是臨時鑰匙，不是永久密碼。

### 一句話總結

Day 17-22 的核心是：會員流程要把密碼、session、token、log 都當成安全邊界來設計。

## Day 23-26：簡訊、Facebook 登入與 ECPay 金流

### 這幾篇主要在講什麼

這段把網站接到外部世界：簡訊 API、Facebook OAuth 登入、綠界 ECPay 線上支付。原文展示了如何呼叫外部 API、處理 callback、接收付款結果。

### 為什麼需要這些概念

現代網站很少完全單機運作。登入可能交給 OAuth provider，通知可能用簡訊或 email，付款一定要交給金流。這些地方的難點不在「發 request」而已，而在驗證回來的人是不是可信。

### 定義名稱對照表

| 角色 | 文章中的名稱 | 功能 / 責任 |
| --- | --- | --- |
| SMS Provider | 簡訊王 API | 發送簡訊通知或驗證碼 |
| OAuth Provider | Facebook | 使用第三方身分登入 |
| Callback Handler | `fb-callback`, `ecpay-callback` | 接收外部服務導回資料 |
| Payment Gateway | ECPay 綠界 | 建立付款頁與回傳交易結果 |
| Signature / CheckMacValue | 綠界檢查碼 | 驗證回傳資料未被竄改 |

### 實務使用情境

- 使用 Facebook、Google 或 LINE 登入。
- 付款完成後更新訂單狀態。
- 簡訊通知使用者訂單已出貨。
- 外部 API 失敗時重試或寫入待補償任務。

### 不適合使用的情境

- 不該把 API key、HashKey、HashIV 寫死在 repo。
- 不該只看 `RtnCode == 1` 就認定付款成功，還要驗證檢查碼與訂單金額。
- 不該讓 callback 直接信任 session；付款結果應以資料庫訂單為準。

### 完整實作流程、Coding 例子與注意事項

完整流程：

1. 使用者建立訂單，系統先寫入 `pending` 狀態。
2. 系統把訂單編號、金額、回呼網址送給金流。
3. 使用者在金流頁付款。
4. 金流導回網站或呼叫 webhook。
5. 系統驗證簽章、商店代號、金額、訂單編號。
6. 驗證成功後把訂單更新為 `paid`，並記錄交易編號。

```php
<?php
declare(strict_types=1);

// 範例用途：模擬 Day 29 的付款 callback，但補上「先驗證，再更新訂單」的安全順序。
// 主要輸入：
// - $payload：金流 POST 回來的資料。
// 回傳結果 / 副作用：
// - 驗證成功才更新訂單狀態；驗證失敗會丟出例外並記錄 log。
function handlePaymentCallback(PDO $pdo, array $payload, callable $verifySignature): void
{
    if (!$verifySignature($payload)) {
        throw new RuntimeException('付款回傳簽章驗證失敗');
    }

    if (($payload['RtnCode'] ?? null) !== '1') {
        throw new RuntimeException('付款未成功：' . ($payload['RtnMsg'] ?? 'unknown'));
    }

    $merchantTradeNo = (string) $payload['MerchantTradeNo'];
    $paidAmount = (int) $payload['TradeAmt'];
    $gatewayTradeNo = (string) $payload['TradeNo'];

    $pdo->beginTransaction();

    $order = $pdo->prepare('SELECT id, total_amount, status FROM orders WHERE merchant_trade_no = :merchant_trade_no FOR UPDATE');
    $order->execute(['merchant_trade_no' => $merchantTradeNo]);
    $row = $order->fetch(PDO::FETCH_ASSOC);

    if (!$row) {
        $pdo->rollBack();
        throw new RuntimeException('找不到訂單');
    }

    if ((int) $row['total_amount'] !== $paidAmount) {
        $pdo->rollBack();
        throw new RuntimeException('付款金額與訂單金額不一致');
    }

    if ($row['status'] !== 'paid') {
        $update = $pdo->prepare('UPDATE orders SET status = :status, gateway_trade_no = :gateway_trade_no WHERE id = :id');
        $update->execute([
            'status' => 'paid',
            'gateway_trade_no' => $gatewayTradeNo,
            'id' => $row['id'],
        ]);
    }

    $pdo->commit();
}
```

注意事項：

- callback 可能重複送達，所以更新訂單要能處理重複通知。
- 金額必須用資料庫訂單重新確認，不能只相信 POST 內容。
- OAuth callback 要驗證 `state`，避免 CSRF 或登入流程被串改。

### 負面例子 / 錯誤用法

錯誤做法：

```php
if ($_POST['RtnCode'] == 1) {
    $_SESSION['order']->setOrderComplete();
}
```

問題是付款狀態只存在 session，不可靠也不適合稽核；而且沒有驗證簽章、金額與訂單。修正方向是以資料庫訂單為準，先驗證 callback，再用交易更新訂單狀態。

### Junior 常見誤解

- API callback 是外部輸入，不是系統內部可信事件。
- OAuth 登入不是「拿到 email 就登入」，還要確認 provider id、token、state。
- 付款成功頁不等於付款成功；真正可信的是經驗證的金流通知與資料庫狀態。

### 一句話總結

Day 23-26 的核心是：串接外部服務時，最重要的是驗證、記錄、可重試，而不是只把 API 打通。

## Day 27-30：簡單電商、購物車、訂單完成與專案收尾

### 這幾篇主要在講什麼

最後四篇把會員登入、購物車、商品、加購、付款、感謝頁與專案原始碼串起來。這是整個系列從「學語法」走到「做產品」的收束。

### 為什麼需要這些概念

電商不是只有購物車陣列。真正的商務流程要處理商品價格、庫存、優惠、訂單狀態、付款結果、下載權限、清除購物車、錯誤補償。任何一步只存在 session，都可能在重新整理、跨裝置、付款導回、背景通知時失效。

### 定義名稱對照表

| 角色 | 文章中的名稱 | 功能 / 責任 |
| --- | --- | --- |
| Cart | `Cart` | 暫存使用者想購買的商品 |
| Product | 商品、免費圖庫、加購商品 | 可被加入購物車與結帳的項目 |
| Order | `Order` | 付款前後都需要保存的交易紀錄 |
| Upsell | 加購頁 | 在主商品之外提供額外購買選項 |
| Thank You Page | `thankyou.php` | 付款完成後顯示結果與下載入口 |

### 實務使用情境

- 使用者登入後把免費商品加入購物車。
- 使用者加購付費商品後建立訂單。
- 付款成功後開放下載權限。
- 使用者離開感謝頁後清除購物車，但保留訂單紀錄。

### 不適合使用的情境

- 不該把整個 `Order` serialize 後存在 session 當唯一來源。
- 不該讓商品價格只從前端或 session 來，結帳時要重新查資料庫價格。
- 不該在沒有交易鎖的情況下更新付款狀態與下載權限。

### 完整實作流程、Coding 例子與注意事項

完整流程：

1. 使用者把商品加入購物車，購物車可存在 session 或資料庫。
2. 結帳時從資料庫重新查商品價格，建立 `pending` 訂單。
3. 將訂單送往金流。
4. callback 驗證成功後把訂單改成 `paid`。
5. 根據訂單明細開放下載權限。
6. 清除購物車，但保留訂單與付款紀錄。

```php
<?php
declare(strict_types=1);

final class CartItem
{
    // 範例用途：表示購物車中的一個商品項目。
    // 主要輸入：
    // - productId：商品資料庫 id。
    // - quantity：購買數量。
    // 回傳結果 / 副作用：
    // - 這是資料物件，本身不寫資料庫。
    public function __construct(
        public readonly int $productId,
        public readonly int $quantity,
    ) {
        if ($quantity < 1) {
            throw new InvalidArgumentException('商品數量至少為 1');
        }
    }
}

final class Cart
{
    /** @var array<int, CartItem> */
    private array $items = [];

    public function add(CartItem $item): void
    {
        $this->items[$item->productId] = $item;
    }

    /** @return CartItem[] */
    public function items(): array
    {
        return array_values($this->items);
    }
}
```

注意事項：

- 這個範例只示範購物車資料結構，不包含付款與庫存，不能直接當正式電商核心。
- 真實結帳必須從資料庫重新取價格，避免使用者竄改 session 或前端價格。
- 訂單狀態建議至少有 `pending`、`paid`、`failed`、`cancelled`，不要只用 0/1 讓未來維護者猜。

### 負面例子 / 錯誤用法

錯誤做法：

```php
$_SESSION['order'] = base64_encode(serialize($order));
```

問題是 session 不適合當訂單唯一真相，也不方便稽核、補單、客服查詢。修正方向是訂單、訂單明細、付款紀錄都要寫入資料庫；session 最多保存購物車或流程提示。

### Junior 常見誤解

- 購物車和訂單不是同一件事；購物車是意圖，訂單是交易紀錄。
- 感謝頁只是 UI，不能代表交易完成。
- 清除購物車不代表刪除訂單；訂單是商務紀錄，要保留。

### 一句話總結

Day 27-30 的核心是：電商流程要把購物車、訂單、付款、權限分清楚，資料庫才是交易真相。

## 2026 年更新版學習路線

1. 先用 PHP 8.4 或 PHP 8.5 建立 CLI 練習環境，跑通變數、if、foreach、function。
2. 學會 Composer autoload，所有 class 走 `src/`，入口放 `public/index.php`。
3. 使用 PDO 與 prepared statement 寫一個最小會員註冊。
4. 補上 `password_hash()`、email token、session regenerate。
5. 使用 Monolog 記錄登入、寄信、付款 callback 的結果。
6. 串接外部 API 前先設計失敗處理、重試策略、簽章驗證。
7. 最後再學 Laravel 或 Symfony，會更容易理解框架幫你解決了什麼。

## 這系列今天仍然值得學的地方

- 它很強調「Why -> How -> What」，適合新手建立直覺。
- 它不是只教語法，而是一路帶到真的網站流程。
- 它示範了工程師最重要的能力：遇到真實需求時，把問題拆成可以逐步驗證的小功能。

## 需要用 2026 實務修正的地方

- 不要把 config secret 寫在 repo，改用環境變數或秘密管理。
- 不要把付款成功只存在 session，要以資料庫與金流簽章為準。
- 不要用可預測 token，改用 `random_bytes()`。
- 不要用字串拼 SQL，使用 PDO prepared statement。
- 不要把 controller、SQL、HTML 全混在一起；至少拆成 router、controller、service、repository、view。

## 最終總結

這個系列最珍貴的不是 2018 年的 PHP 寫法，而是「從一個想完成的網站功能倒推需要哪些程式能力」。2026 年重讀時，可以把它當成 PHP 入門地圖：照著原本的順序建立直覺，再用 PHP 8.4/8.5、PDO、Composer、token 安全、log、callback 驗證，把它升級成更接近真實工作的後端開發筆記。
