# 寫給朋友的 PHP 從 0 到 100 實戰教程：2026 Junior 友善版整理

## 來源

- 原系列頁：https://ithelp.ithome.com.tw/users/20107394/ironman/1332?page=1
- RSS 來源：https://ithelp.ithome.com.tw/rss/series/1332
- 原作者：falconwei
- 原系列時間：2017-12-18 到 2018-01-16
- 2026 更新參考：
  - PHP Supported Versions：https://www.php.net/supported-versions.php
  - PHP 8.4 Release Announcement：https://www.php.net/releases/8.4/en.php
  - PHP 8.5 Release Announcement：https://www.php.net/releases/8.5/en.php

## 先給完全沒學過的 junior 一句話

PHP 可以先想成「會在伺服器上幫你組出網頁、處理表單、讀寫資料庫的程式語言」。你不用一開始就懂框架、架構、金流、OAuth。先把它想成一個很勤快的店員：

1. 客人打開網頁。
2. PHP 收到請求。
3. PHP 根據資料、條件和流程做事。
4. PHP 回傳 HTML、JSON、轉址，或把資料寫進資料庫。

這份筆記的目標不是把 30 篇文章壓縮成摘要，而是幫完全沒基礎的 junior 建立一條可走的路。每一篇 Day 都會用「先懂人話版 -> 正式概念 -> 你可以怎麼練 -> PHP 小範例 -> 常見坑」來整理。

## 2026 年讀這系列要注意什麼

原文是 2018 年的入門教學，精神很好，但部分實作到 2026 年應該升級：

- PHP 版本：建議用 PHP 8.4 或 PHP 8.5。PHP 官方在 2026-05-02 顯示 8.2、8.3、8.4、8.5 還在支援週期內，但新學習者可以直接從 8.4/8.5 開始。
- 資料庫：優先使用 PDO 與 prepared statement，不要用字串拼 SQL。
- 密碼：使用 `password_hash()` 與 `password_verify()`，不要自己寫加密規則。
- 設定：資料庫密碼、SMTP 密碼、金流 HashKey 不要 commit 到 GitHub。
- 外部服務：OAuth、簡訊、金流 callback 都是外部輸入，要驗證簽章、token、state、金額與訂單。
- 專案結構：練習時可以簡單，正式專案要拆出 controller、service、repository、view。

## 整體學習地圖

| 階段 | Day | 你正在學什麼 | 學完應該能做什麼 |
| --- | --- | --- | --- |
| 入門語法 | 1-4 | 輸出、變數、條件、迴圈 | 寫出會根據資料做判斷的小程式 |
| 網站雛形 | 5-10 | 物件、資料庫、Composer、路由 | 做出比較不混亂的小型 PHP 網站 |
| 表單與資料 | 11-16 | PDO、表單輸入、驗證、時間、寄信 | 安全地收資料、存資料、寄通知 |
| 會員系統 | 17-22 | 註冊、登入、開通、重設密碼、log | 做出基本會員流程 |
| 外部服務 | 23-26 | 簡訊、Facebook 登入、ECPay | 知道串 API 時要驗證與記錄 |
| 電商收尾 | 27-30 | 購物車、訂單、付款結果、專案整理 | 看懂簡單電商流程的核心邏輯 |

## 建議練習方式

完全沒學過的人不要一次讀完整份。比較好的節奏是：

1. 一天只讀 1 到 2 個 Day。
2. 每個 PHP 範例都自己手打一遍。
3. 每次只改一個地方，例如改變數、改條件、改陣列資料。
4. 先看錯誤訊息，不急著 Google；試著猜它在抱怨哪一行。
5. 每讀完一個階段，就做一個小作品，例如訂單對帳、留言表單、簡單會員註冊。

## 第一次學 PHP 時，怎麼讀這份筆記的註解

這份筆記的程式碼註解會刻意寫得比較像老師在旁邊講話，不會只寫「建立變數」這種沒有幫助的句子。你可以用這個順序讀每段 code：

1. 先看「範例用途」：知道這段程式在模擬什麼情境。
2. 再看「輸入」：知道資料從哪裡來，例如變數、表單、網址、資料庫。
3. 再看「輸出 / 副作用」：知道執行後會印出什麼、寫入什麼、更新什麼。
4. 最後才逐行看語法：先理解目的，再理解符號。

常見符號可以先這樣記：

| 符號 / 寫法 | 新手可以先這樣理解 |
| --- | --- |
| `$name` | 變數，像貼了名字的盒子 |
| `=` | 把右邊的資料放進左邊變數 |
| `===` | 比較兩邊是否真的一樣 |
| `.` | 把字串接起來 |
| `->` | 呼叫物件裡的功能或資料 |
| `=>` | 陣列 key 對應 value |
| `{ }` | 一段程式區塊 |
| `;` | 一句 PHP 指令結束 |

## 來源清單

| Day | 標題 | 連結 |
| --- | --- | --- |
| 1 | PHP教學: 寫程式真的不難，從零開始也可以 | https://ithelp.ithome.com.tw/articles/10191950 |
| 2 | PHP教學: 把資料記到變數盒子 | https://ithelp.ithome.com.tw/articles/10192186 |
| 3 | PHP教學: 一分鐘搞懂如果...就...的 if else 語法 | https://ithelp.ithome.com.tw/articles/10192563 |
| 4 | PHP教學: 用迴圈解決重複無聊的工作 | https://ithelp.ithome.com.tw/articles/10192807 |
| 5 | PHP教學: 開始架設自己的遊戲攻略網站 class 物件篇 | https://ithelp.ithome.com.tw/articles/10192984 |
| 6 | PHP教學: 用物件的方式連接 MYSQL 資料庫 | https://ithelp.ithome.com.tw/articles/10193372 |
| 7 | PHP教學: 架設 PHP 網站開發環境 | https://ithelp.ithome.com.tw/articles/10193603 |
| 8 | PHP教學: 必學-超強套件管理器 composer | https://ithelp.ithome.com.tw/articles/10193632 |
| 9 | PHP教學: 從單一網頁到基礎框架概念 | https://ithelp.ithome.com.tw/articles/10194090 |
| 10 | PHP教學: Clean URL 優化連結 | https://ithelp.ithome.com.tw/articles/10194207 |
| 11 | PHP教學: 使用單例模式來連接資料庫 | https://ithelp.ithome.com.tw/articles/10194443 |
| 12 | PHP教學: 取得使用者輸入的資料存到資料庫 | https://ithelp.ithome.com.tw/articles/10194762 |
| 13 | PHP教學: 檢查使用者輸入的資料 | https://ithelp.ithome.com.tw/articles/10194900 |
| 14 | PHP教學: 學習日期與時間控制 | https://ithelp.ithome.com.tw/articles/10195149 |
| 15 | PHP教學: 用 phpmailer + gmail smtp 實作寄信功能 | https://ithelp.ithome.com.tw/articles/10195228 |
| 16 | PHP教程: 將 MySQLi 升級為 PDO | https://ithelp.ithome.com.tw/articles/10195426 |
| 17 | PHP教學: 實作註冊流程 | https://ithelp.ithome.com.tw/articles/10195675 |
| 18 | PHP教程: 實作登入與登出機制 | https://ithelp.ithome.com.tw/articles/10195818 |
| 19 | PHP教程: 實作收信開通帳號機制 | https://ithelp.ithome.com.tw/articles/10195972 |
| 20 | PHP教學: 實作重置密碼流程 | https://ithelp.ithome.com.tw/articles/10196178 |
| 21 | PHP教程: 優化架構與實作 flash session 附原始碼 | https://ithelp.ithome.com.tw/articles/10196311 |
| 22 | PHP教學: 使用 monolog 來追蹤問題 | https://ithelp.ithome.com.tw/articles/10196317 |
| 23 | PHP教學: 串接簡訊王 API | https://ithelp.ithome.com.tw/articles/10196595 |
| 24 | PHP教學: 實作 FACEBOOK 臉書登入 | https://ithelp.ithome.com.tw/articles/10196699 |
| 25 | PHP教學: 實作臉書帳號登入後的註冊 | https://ithelp.ithome.com.tw/articles/10196843 |
| 26 | PHP教學: 串接綠界 ECpay 線上支付 | https://ithelp.ithome.com.tw/articles/10196962 |
| 27 | PHP教程: 實作簡單電商網站 準備篇 | https://ithelp.ithome.com.tw/articles/10197014 |
| 28 | PHP教學: 實作商務邏輯層 | https://ithelp.ithome.com.tw/articles/10197131 |
| 29 | PHP教學: 刷完信用卡導回網站的處理 | https://ithelp.ithome.com.tw/articles/10197305 |
| 30 | 竟然要結束了那就大放送附專案原始碼 | https://ithelp.ithome.com.tw/articles/10197413 |

---

## Day 1：寫程式真的不難，從零開始也可以

### 先懂人話版

程式不是魔法，而是把事情拆成一行一行的指令。你可以先把 PHP 想成「照著命令做事的助手」。你寫 `echo "Hello";`，它就把 `Hello` 印出來。

### 正式概念

| 名稱 | 意思 |
| --- | --- |
| `echo` | 輸出文字 |
| `;` | 一句指令的結尾 |
| `//` | 註解，給人看的說明，不會被執行 |
| bug | 程式錯誤，可能是語法錯，也可能是邏輯錯 |


### 學完這篇你應該會做到什麼

- 能用自己的話說明 Day 1：寫程式真的不難，從零開始也可以 在 PHP 網站中的用途。
- 能照著範例寫出可執行的 PHP、SQL、設定或流程。
- 能用瀏覽器、CLI、資料庫結果或錯誤訊息確認自己做對了。

### 實作任務情境

把這篇當成一個小任務：你要替一個逐步成形的 PHP 小網站加入 Day 1：寫程式真的不難，從零開始也可以 相關能力，並確定它真的能執行、能查錯、能被下一篇繼續擴充。

### 操作前檢查

- 確認 PHP、Composer、Web server、資料庫或第三方服務是否已準備好；不需要的項目不要硬裝。
- 若會寫入資料庫，先準備測試資料庫，不要直接操作正式資料。
- 若會使用 API key、SMTP 密碼或付款金鑰，一律放在環境變數或設定檔，不要寫死在範例中。
### 範例形式與實作

1. 建立 `day1.php`。
2. 寫三行 `echo`。
3. 故意刪掉一個分號，觀察錯誤訊息。
4. 把分號補回來，再執行一次。

```php
<?php
declare(strict_types=1);

// 範例用途：
// 這段是 PHP 的第一個練習：請 PHP 幫我們把文字印出來。
//
// 新手先知道：
// - echo 代表「輸出」。
// - 每一行最後的 ; 代表「這句指令結束」。
// - \n 代表換行，讓下一段文字不要黏在同一行。
//
// 輸入：沒有輸入，文字都直接寫在程式裡。
// 輸出：在畫面或命令列印出三行文字。
echo "battle start\n";
echo "Pikachu uses Thunderbolt\n";
echo "battle end\n";
```

### 範例對應概念說明

- `<?php` 告訴檔案後面開始是 PHP 程式。
- `declare(strict_types=1);` 讓 PHP 對型別更嚴格，初學時可以養成好習慣。
- `\n` 是換行，讓輸出不要擠在同一行。


### 如果結果和預期不同

- 頁面空白：先打開錯誤顯示或查 Web server / PHP error log。
- 資料沒有寫入：確認連線設定、SQL、欄位名稱、交易是否 commit。
- 第三方服務失敗：先查 API 回傳碼、credential、callback URL 與環境變數。

### 做完後檢查

- 用瀏覽器或 CLI 實際跑一次本篇範例。
- 若有資料庫，確認資料表內容真的符合預期。
- 若有登入、寄信、付款或外部 API，確認成功情境與失敗情境都有可觀察結果。

### 小練習

1. 把範例改成另一個欄位或另一種輸入資料。
2. 故意輸入錯誤資料，觀察錯誤訊息或驗證結果。
3. 寫一句話記錄這篇在整個 PHP 網站流程中扮演的角色。

### 負面例子 / 錯誤用法

```php
echo "battle end
```

錯在字串少了結尾引號，也少了分號。後果是 PHP 會直接語法錯誤，程式不會執行。修正方向是讓每一句都有完整的引號與分號。

### Junior 常見誤解

- 看到錯誤訊息不代表你不適合寫程式；錯誤訊息是線索。
- 註解不是越多越好，要寫「為什麼這樣做」，不要只翻譯程式。

### 一句話總結

PHP 入門第一步是看懂一行指令如何被執行，並學會從錯誤訊息找線索。

## Day 2：把資料記到變數盒子

### 先懂人話版

變數就像貼了標籤的盒子。你把「妙蛙種子」放進 `$pokemon` 這個盒子，後面要用這個名字時，就不用到處重寫。

### 正式概念

| 名稱 | 意思 |
| --- | --- |
| 變數 | 暫時保存資料的名字 |
| 字串 | 文字資料，例如 `"Pikachu"` |
| 數字 | 可以計算的資料，例如 `100` |
| `.` | PHP 的字串串接符號 |


### 學完這篇你應該會做到什麼

- 能用自己的話說明 Day 2：把資料記到變數盒子 在 PHP 網站中的用途。
- 能照著範例寫出可執行的 PHP、SQL、設定或流程。
- 能用瀏覽器、CLI、資料庫結果或錯誤訊息確認自己做對了。

### 實作任務情境

把這篇當成一個小任務：你要替一個逐步成形的 PHP 小網站加入 Day 2：把資料記到變數盒子 相關能力，並確定它真的能執行、能查錯、能被下一篇繼續擴充。

### 操作前檢查

- 確認 PHP、Composer、Web server、資料庫或第三方服務是否已準備好；不需要的項目不要硬裝。
- 若會寫入資料庫，先準備測試資料庫，不要直接操作正式資料。
- 若會使用 API key、SMTP 密碼或付款金鑰，一律放在環境變數或設定檔，不要寫死在範例中。
### 範例形式與實作

1. 建立一個 `$name` 變數。
2. 把它組合進一句話。
3. 改一次變數值，確認所有輸出一起改變。

```php
<?php
declare(strict_types=1);

// 範例用途：
// 練習「把資料放進變數」，再把變數組合成一句話。
//
// 新手先知道：
// - $pokemon 是變數，像一個貼著 pokemon 標籤的盒子。
// - = 代表把右邊的 "Pikachu" 放進左邊的 $pokemon。
// - . 代表把多段字串接起來。
//
// 輸入：$pokemon 與 $skill 是程式內先設定的資料。
// 輸出：組合後的文字，例如 Pikachu uses Thunderbolt。
$pokemon = "Pikachu";
$skill = "Thunderbolt";

echo $pokemon . " uses " . $skill . "\n";
```

### 範例對應概念說明

- `$pokemon` 保存角色名稱。
- `$skill` 保存招式名稱。
- `.` 把多段文字接成一句完整句子。


### 如果結果和預期不同

- 頁面空白：先打開錯誤顯示或查 Web server / PHP error log。
- 資料沒有寫入：確認連線設定、SQL、欄位名稱、交易是否 commit。
- 第三方服務失敗：先查 API 回傳碼、credential、callback URL 與環境變數。

### 做完後檢查

- 用瀏覽器或 CLI 實際跑一次本篇範例。
- 若有資料庫，確認資料表內容真的符合預期。
- 若有登入、寄信、付款或外部 API，確認成功情境與失敗情境都有可觀察結果。

### 小練習

1. 把範例改成另一個欄位或另一種輸入資料。
2. 故意輸入錯誤資料，觀察錯誤訊息或驗證結果。
3. 寫一句話記錄這篇在整個 PHP 網站流程中扮演的角色。

### 負面例子 / 錯誤用法

```php
echo "Pikachu uses Thunderbolt\n";
echo "Pikachu wins\n";
echo "Pikachu runs away\n";
```

如果之後要把 `Pikachu` 改成 `Charmander`，你要改很多行。修正方向是把會重複出現、未來可能改變的資料抽成變數。

### Junior 常見誤解

- 變數不是為了讓程式變短，而是讓資料有名字。
- 變數名稱要讓人看得懂，`$pokemonName` 通常比 `$x` 好。

### 一句話總結

變數讓資料有名字，之後改資料時就不用到處找。

## Day 3：一分鐘搞懂 if else

### 先懂人話版

`if else` 就是「如果...否則...」。如果會員已登入，就顯示會員中心；否則顯示登入按鈕。網站幾乎每天都在做這件事。

### 正式概念

| 名稱 | 意思 |
| --- | --- |
| `if` | 條件成立時執行 |
| `else` | 條件不成立時執行 |
| `===` | 嚴格比較，值和型別都要相同 |
| `=` | 指派，把右邊資料放到左邊變數 |


### 學完這篇你應該會做到什麼

- 能用自己的話說明 Day 3：一分鐘搞懂 if else 在 PHP 網站中的用途。
- 能照著範例寫出可執行的 PHP、SQL、設定或流程。
- 能用瀏覽器、CLI、資料庫結果或錯誤訊息確認自己做對了。

### 實作任務情境

把這篇當成一個小任務：你要替一個逐步成形的 PHP 小網站加入 Day 3：一分鐘搞懂 if else 相關能力，並確定它真的能執行、能查錯、能被下一篇繼續擴充。

### 操作前檢查

- 確認 PHP、Composer、Web server、資料庫或第三方服務是否已準備好；不需要的項目不要硬裝。
- 若會寫入資料庫，先準備測試資料庫，不要直接操作正式資料。
- 若會使用 API key、SMTP 密碼或付款金鑰，一律放在環境變數或設定檔，不要寫死在範例中。
### 範例形式與實作

1. 設定 `$isLoggedIn = true`。
2. 用 `if else` 顯示不同文字。
3. 把它改成 `false`，觀察輸出。

```php
<?php
declare(strict_types=1);

// 範例用途：
// 練習「如果使用者已登入，就顯示會員中心；否則請他登入」。
//
// 新手先知道：
// - true 代表「是」或「成立」。
// - if (...) 代表「如果括號裡的條件成立」。
// - else 代表「否則」。
// - === true 代表確認這個值真的等於 true。
//
// 輸入：$isLoggedIn 代表使用者是否登入。
// 輸出：根據登入狀態顯示不同訊息。
$isLoggedIn = true;

if ($isLoggedIn === true) {
    echo "歡迎回來，這裡是會員中心\n";
} else {
    echo "請先登入\n";
}
```


### 如果結果和預期不同

- 頁面空白：先打開錯誤顯示或查 Web server / PHP error log。
- 資料沒有寫入：確認連線設定、SQL、欄位名稱、交易是否 commit。
- 第三方服務失敗：先查 API 回傳碼、credential、callback URL 與環境變數。

### 做完後檢查

- 用瀏覽器或 CLI 實際跑一次本篇範例。
- 若有資料庫，確認資料表內容真的符合預期。
- 若有登入、寄信、付款或外部 API，確認成功情境與失敗情境都有可觀察結果。

### 小練習

1. 把範例改成另一個欄位或另一種輸入資料。
2. 故意輸入錯誤資料，觀察錯誤訊息或驗證結果。
3. 寫一句話記錄這篇在整個 PHP 網站流程中扮演的角色。

### 負面例子 / 錯誤用法

```php
$weather = "sunny";

if ($weather = "raining") {
    echo "帶傘\n";
}
```

錯在 `$weather = "raining"` 是指派，不是比較。後果是 `$weather` 被改成 `"raining"`，條件也會被當成成立。修正方向：

```php
if ($weather === "raining") {
    echo "帶傘\n";
}
```

### Junior 常見誤解

- `=` 和 `===` 是完全不同的事。
- 條件不是只能判斷文字，也可以判斷數字、布林值、陣列是否有資料。

### 一句話總結

`if else` 讓程式可以根據不同情況走不同路。

## Day 4：用迴圈解決重複無聊的工作

### 先懂人話版

迴圈就是「同一件事幫我做很多次」。如果你有 6000 筆訂單，不可能用眼睛一筆一筆對帳，程式可以用 `foreach` 逐筆檢查。

### 正式概念

| 名稱 | 意思 |
| --- | --- |
| 陣列 array | 一次保存多筆資料 |
| key | 資料的索引或名字 |
| value | key 對應的值 |
| `foreach` | 逐筆取出陣列資料 |


### 學完這篇你應該會做到什麼

- 能用自己的話說明 Day 4：用迴圈解決重複無聊的工作 在 PHP 網站中的用途。
- 能照著範例寫出可執行的 PHP、SQL、設定或流程。
- 能用瀏覽器、CLI、資料庫結果或錯誤訊息確認自己做對了。

### 實作任務情境

把這篇當成一個小任務：你要替一個逐步成形的 PHP 小網站加入 Day 4：用迴圈解決重複無聊的工作 相關能力，並確定它真的能執行、能查錯、能被下一篇繼續擴充。

### 操作前檢查

- 確認 PHP、Composer、Web server、資料庫或第三方服務是否已準備好；不需要的項目不要硬裝。
- 若會寫入資料庫，先準備測試資料庫，不要直接操作正式資料。
- 若會使用 API key、SMTP 密碼或付款金鑰，一律放在環境變數或設定檔，不要寫死在範例中。
### 範例形式與實作

1. 建立系統訂單陣列。
2. 建立物流收款陣列。
3. 用訂單編號比對是否收到款項。
4. 把問題訂單印出來。

```php
<?php
declare(strict_types=1);

// 範例用途：
// 練習 foreach 與陣列 key/value，模擬物流對帳。
//
// 新手先知道：
// - $orders 是「系統認為應該收到的錢」。
// - $received 是「物流商實際回報收到的錢」。
// - "A001" => 100 的意思是：訂單 A001 對應金額 100。
// - foreach 會一筆一筆把訂單拿出來檢查。
//
// 輸入：$orders 是系統應收金額，$received 是物流實收金額。
// 輸出：印出沒有收到款或金額不一致的訂單。
$orders = [
    "A001" => 100,
    "A002" => 200,
    "A003" => 300,
];

$received = [
    "A001" => 100,
    "A002" => 0,
];

foreach ($orders as $orderId => $expectedAmount) {
    // array_key_exists 用來確認物流清單裡有沒有這張訂單。
    // 如果沒有，代表物流資料缺漏，先記錄問題，再 continue 去檢查下一筆。
    if (!array_key_exists($orderId, $received)) {
        echo $orderId . " 沒有出現在物流清單\n";
        continue;
    }

    // !== 是嚴格比較，這裡用來確認實收金額是否和應收金額完全一致。
    if ($received[$orderId] !== $expectedAmount) {
        echo $orderId . " 金額不一致\n";
    }
}
```


### 如果結果和預期不同

- 頁面空白：先打開錯誤顯示或查 Web server / PHP error log。
- 資料沒有寫入：確認連線設定、SQL、欄位名稱、交易是否 commit。
- 第三方服務失敗：先查 API 回傳碼、credential、callback URL 與環境變數。

### 做完後檢查

- 用瀏覽器或 CLI 實際跑一次本篇範例。
- 若有資料庫，確認資料表內容真的符合預期。
- 若有登入、寄信、付款或外部 API，確認成功情境與失敗情境都有可觀察結果。

### 小練習

1. 把範例改成另一個欄位或另一種輸入資料。
2. 故意輸入錯誤資料，觀察錯誤訊息或驗證結果。
3. 寫一句話記錄這篇在整個 PHP 網站流程中扮演的角色。

### 負面例子 / 錯誤用法

錯誤做法是用三個不同變數保存三筆訂單：`$order1`、`$order2`、`$order3`。當資料變成 6000 筆時會完全無法維護。修正方向是使用陣列，讓資料可以被迴圈處理。

### Junior 常見誤解

- 陣列不只是清單，也可以像字典一樣用訂單編號當 key。
- `continue` 的意思是跳過本次迴圈，處理下一筆。

### 一句話總結

迴圈讓程式能穩定處理大量重複資料。

## Day 5：開始架設自己的網站，class 物件篇

### 先懂人話版

class 可以先想成「設計圖」。你想建立很多英雄，每個英雄都有名字、血量、攻擊力，如果全部用散落變數會很亂；class 讓同一類資料有固定結構。

### 正式概念

| 名稱 | 意思 |
| --- | --- |
| class | 類別，物件的設計圖 |
| object | 物件，照 class 建出來的實體 |
| property | 屬性，物件保存的資料 |
| method | 方法，物件可以做的事 |


### 學完這篇你應該會做到什麼

- 能用自己的話說明 Day 5：開始架設自己的網站，class 物件篇 在 PHP 網站中的用途。
- 能照著範例寫出可執行的 PHP、SQL、設定或流程。
- 能用瀏覽器、CLI、資料庫結果或錯誤訊息確認自己做對了。

### 實作任務情境

把這篇當成一個小任務：你要替一個逐步成形的 PHP 小網站加入 Day 5：開始架設自己的網站，class 物件篇 相關能力，並確定它真的能執行、能查錯、能被下一篇繼續擴充。

### 操作前檢查

- 確認 PHP、Composer、Web server、資料庫或第三方服務是否已準備好；不需要的項目不要硬裝。
- 若會寫入資料庫，先準備測試資料庫，不要直接操作正式資料。
- 若會使用 API key、SMTP 密碼或付款金鑰，一律放在環境變數或設定檔，不要寫死在範例中。
### 範例形式與實作

1. 建立 `Hero` 類別。
2. 放入名字與血量。
3. 寫一個 `attack()` 方法。

```php
<?php
declare(strict_types=1);

// 範例用途：
// 練習 class，把「英雄的資料」和「英雄可以做的事」放在一起。
//
// 新手先知道：
// - class Hero 像是在畫一張英雄設計圖。
// - new Hero(...) 才是真的照設計圖做出一個英雄。
// - private 代表這個資料只給 Hero 自己內部使用。
// - $this 代表「目前這個物件自己」。
//
// 輸入：建立 Hero 時傳入 name 與 hp。
// 輸出：attack() 回傳攻擊描述文字。
final class Hero
{
    public function __construct(
        private string $name,
        private int $hp,
    ) {
    }

    public function attack(string $target): string
    {
        return $this->name . " attacks " . $target;
    }
}

$hero = new Hero("Knight", 100);
echo $hero->attack("Dragon") . "\n";
```


### 如果結果和預期不同

- 頁面空白：先打開錯誤顯示或查 Web server / PHP error log。
- 資料沒有寫入：確認連線設定、SQL、欄位名稱、交易是否 commit。
- 第三方服務失敗：先查 API 回傳碼、credential、callback URL 與環境變數。

### 做完後檢查

- 用瀏覽器或 CLI 實際跑一次本篇範例。
- 若有資料庫，確認資料表內容真的符合預期。
- 若有登入、寄信、付款或外部 API，確認成功情境與失敗情境都有可觀察結果。

### 小練習

1. 把範例改成另一個欄位或另一種輸入資料。
2. 故意輸入錯誤資料，觀察錯誤訊息或驗證結果。
3. 寫一句話記錄這篇在整個 PHP 網站流程中扮演的角色。

### 負面例子 / 錯誤用法

如果你把英雄名稱、血量、攻擊力拆成很多平行陣列，例如 `$names`、`$hps`、`$attacks`，很容易第 3 個名字對到第 4 個血量。修正方向是用 class 把同一個英雄的資料包在同一個物件。

### Junior 常見誤解

- class 不是比較高級的函式，而是把「同一種東西的資料與行為」整理在一起。
- 不要一開始就追求複雜設計，先讓物件的責任清楚。

### 一句話總結

class 幫你把同一種資料整理成可理解、可重複建立的物件。

## Day 6：用物件方式連接 MySQL 資料庫

### 先懂人話版

資料庫像倉庫，PHP 是店員。店員要拿資料前，必須先有倉庫地址、帳號、密碼，並建立連線。

### 正式概念

| 名稱 | 意思 |
| --- | --- |
| Database | 保存資料的地方 |
| Connection | PHP 與資料庫之間的連線 |
| Query | 對資料庫下指令 |
| Result | 資料庫回傳的結果 |


### 學完這篇你應該會做到什麼

- 能用自己的話說明 Day 6：用物件方式連接 MySQL 資料庫 在 PHP 網站中的用途。
- 能照著範例寫出可執行的 PHP、SQL、設定或流程。
- 能用瀏覽器、CLI、資料庫結果或錯誤訊息確認自己做對了。

### 實作任務情境

把這篇當成一個小任務：你要替一個逐步成形的 PHP 小網站加入 Day 6：用物件方式連接 MySQL 資料庫 相關能力，並確定它真的能執行、能查錯、能被下一篇繼續擴充。

### 操作前檢查

- 確認 PHP、Composer、Web server、資料庫或第三方服務是否已準備好；不需要的項目不要硬裝。
- 若會寫入資料庫，先準備測試資料庫，不要直接操作正式資料。
- 若會使用 API key、SMTP 密碼或付款金鑰，一律放在環境變數或設定檔，不要寫死在範例中。
### 範例形式與實作

1. 先用資料庫工具建立 `products` 資料表。
2. 寫 PHP 建立 PDO 連線。
3. 查詢產品資料。

```php
<?php
declare(strict_types=1);

// 範例用途：
// 建立 PDO 連線並查詢商品。
//
// 新手先知道：
// - PDO 是 PHP 用來和資料庫溝通的工具。
// - host 是資料庫位置，dbname 是資料庫名稱，charset 是文字編碼。
// - root / password 是範例帳密，真實專案不要寫死在程式裡。
// - query 是對資料庫下查詢指令。
//
// 輸入：DSN、帳號、密碼，真實專案應放在 .env，不要寫死。
// 輸出：逐筆印出商品名稱。
$pdo = new PDO(
    "mysql:host=127.0.0.1;dbname=demo;charset=utf8mb4",
    "root",
    "password",
    [PDO::ATTR_ERRMODE => PDO::ERRMODE_EXCEPTION]
);

$statement = $pdo->query("SELECT name FROM products ORDER BY id DESC");

foreach ($statement->fetchAll(PDO::FETCH_ASSOC) as $row) {
    echo $row["name"] . "\n";
}
```


### 如果結果和預期不同

- 頁面空白：先打開錯誤顯示或查 Web server / PHP error log。
- 資料沒有寫入：確認連線設定、SQL、欄位名稱、交易是否 commit。
- 第三方服務失敗：先查 API 回傳碼、credential、callback URL 與環境變數。

### 做完後檢查

- 用瀏覽器或 CLI 實際跑一次本篇範例。
- 若有資料庫，確認資料表內容真的符合預期。
- 若有登入、寄信、付款或外部 API，確認成功情境與失敗情境都有可觀察結果。

### 小練習

1. 把範例改成另一個欄位或另一種輸入資料。
2. 故意輸入錯誤資料，觀察錯誤訊息或驗證結果。
3. 寫一句話記錄這篇在整個 PHP 網站流程中扮演的角色。

### 負面例子 / 錯誤用法

不要把正式資料庫密碼直接寫進 GitHub。後果是任何看到 repo 的人都可能連進資料庫。修正方向是使用 `.env` 或主機提供的秘密設定。

### Junior 常見誤解

- 成功連線不代表 SQL 一定安全。
- 查詢資料和修改資料都要考慮錯誤處理。

### 一句話總結

資料庫連線是 PHP 讀寫長期資料的入口，但秘密資訊要保護好。

## Day 7：架設 PHP 網站開發環境

### 先懂人話版

環境就是讓程式能跑起來的地方。PHP 程式需要 PHP 執行環境，網站通常還需要 web server 和資料庫。

### 正式概念

| 名稱 | 意思 |
| --- | --- |
| PHP CLI | 在命令列執行 PHP |
| Web Server | 接收瀏覽器請求，例如 Apache、Nginx |
| Document Root | 網站公開入口資料夾 |
| Localhost | 你自己的電腦伺服器 |


### 學完這篇你應該會做到什麼

- 能用自己的話說明 Day 7：架設 PHP 網站開發環境 在 PHP 網站中的用途。
- 能照著範例寫出可執行的 PHP、SQL、設定或流程。
- 能用瀏覽器、CLI、資料庫結果或錯誤訊息確認自己做對了。

### 實作任務情境

把這篇當成一個小任務：你要替一個逐步成形的 PHP 小網站加入 Day 7：架設 PHP 網站開發環境 相關能力，並確定它真的能執行、能查錯、能被下一篇繼續擴充。

### 操作前檢查

- 確認 PHP、Composer、Web server、資料庫或第三方服務是否已準備好；不需要的項目不要硬裝。
- 若會寫入資料庫，先準備測試資料庫，不要直接操作正式資料。
- 若會使用 API key、SMTP 密碼或付款金鑰，一律放在環境變數或設定檔，不要寫死在範例中。
### 範例形式與實作

1. 安裝 PHP。
2. 在專案根目錄建立 `public/index.php`。
3. 用 PHP 內建 server 跑起來。

```powershell
php -S 127.0.0.1:8000 -t public
```

```php
<?php
declare(strict_types=1);

// public/index.php
// 範例用途：
// 確認本機 PHP 網站可以被瀏覽器打開。
//
// 新手先知道：
// - 這個檔案放在 public/index.php。
// - 先用 php -S 127.0.0.1:8000 -t public 啟動。
// - 再用瀏覽器打開 http://127.0.0.1:8000。
echo "PHP server is working";
```


### 如果結果和預期不同

- 頁面空白：先打開錯誤顯示或查 Web server / PHP error log。
- 資料沒有寫入：確認連線設定、SQL、欄位名稱、交易是否 commit。
- 第三方服務失敗：先查 API 回傳碼、credential、callback URL 與環境變數。

### 做完後檢查

- 用瀏覽器或 CLI 實際跑一次本篇範例。
- 若有資料庫，確認資料表內容真的符合預期。
- 若有登入、寄信、付款或外部 API，確認成功情境與失敗情境都有可觀察結果。

### 小練習

1. 把範例改成另一個欄位或另一種輸入資料。
2. 故意輸入錯誤資料，觀察錯誤訊息或驗證結果。
3. 寫一句話記錄這篇在整個 PHP 網站流程中扮演的角色。

### 負面例子 / 錯誤用法

不要把整個專案根目錄都當公開目錄，否則 `.env`、設定檔、log 可能被瀏覽器讀到。修正方向是只公開 `public/`。

### Junior 常見誤解

- PHP 可以用命令列跑，也可以透過 web server 回應瀏覽器。
- 本機跑得動不代表正式機設定一樣，要記錄環境需求。

### 一句話總結

開發環境的目標是讓你能穩定執行、測試、觀察 PHP 程式。

## Day 8：Composer 套件管理器

### 先懂人話版

Composer 像 PHP 的工具箱管理員。你要寄信、寫 log、讀 `.env`，不用全部自己寫，可以安裝成熟套件。

### 正式概念

| 名稱 | 意思 |
| --- | --- |
| Composer | PHP 套件管理工具 |
| package | 套件，別人寫好的可重用程式 |
| autoload | 自動載入 class |
| `composer.json` | 專案套件設定 |


### 學完這篇你應該會做到什麼

- 能用自己的話說明 Day 8：Composer 套件管理器 在 PHP 網站中的用途。
- 能照著範例寫出可執行的 PHP、SQL、設定或流程。
- 能用瀏覽器、CLI、資料庫結果或錯誤訊息確認自己做對了。

### 實作任務情境

把這篇當成一個小任務：你要替一個逐步成形的 PHP 小網站加入 Day 8：Composer 套件管理器 相關能力，並確定它真的能執行、能查錯、能被下一篇繼續擴充。

### 操作前檢查

- 確認 PHP、Composer、Web server、資料庫或第三方服務是否已準備好；不需要的項目不要硬裝。
- 若會寫入資料庫，先準備測試資料庫，不要直接操作正式資料。
- 若會使用 API key、SMTP 密碼或付款金鑰，一律放在環境變數或設定檔，不要寫死在範例中。
### 範例形式與實作

1. 執行 `composer init`。
2. 安裝 `monolog/monolog`。
3. 在 PHP 載入 `vendor/autoload.php`。

```php
<?php
declare(strict_types=1);

require __DIR__ . "/vendor/autoload.php";

use Monolog\Handler\StreamHandler;
use Monolog\Logger;

// 範例用途：
// 練習使用 Composer 安裝的 Monolog 寫 log。
//
// 新手先知道：
// - require 載入 Composer 產生的 autoload 檔案。
// - use 讓我們可以用比較短的類別名稱。
// - Logger 是寫日記的人，StreamHandler 決定日記要寫到哪個檔案。
//
// 輸入：log 訊息。
// 輸出 / 副作用：寫入 app.log。
$logger = new Logger("app");
$logger->pushHandler(new StreamHandler(__DIR__ . "/app.log"));
$logger->info("Composer autoload works");
```


### 如果結果和預期不同

- 頁面空白：先打開錯誤顯示或查 Web server / PHP error log。
- 資料沒有寫入：確認連線設定、SQL、欄位名稱、交易是否 commit。
- 第三方服務失敗：先查 API 回傳碼、credential、callback URL 與環境變數。

### 做完後檢查

- 用瀏覽器或 CLI 實際跑一次本篇範例。
- 若有資料庫，確認資料表內容真的符合預期。
- 若有登入、寄信、付款或外部 API，確認成功情境與失敗情境都有可觀察結果。

### 小練習

1. 把範例改成另一個欄位或另一種輸入資料。
2. 故意輸入錯誤資料，觀察錯誤訊息或驗證結果。
3. 寫一句話記錄這篇在整個 PHP 網站流程中扮演的角色。

### 負面例子 / 錯誤用法

不要手動下載套件後到處 include。後果是版本難管理，也很難知道專案依賴什麼。修正方向是用 Composer 管理依賴。

### Junior 常見誤解

- `vendor/` 是安裝結果，`composer.json` 和 `composer.lock` 才是依賴紀錄。
- autoload 不是魔法，它是 Composer 幫你建立 class 載入規則。

### 一句話總結

Composer 讓 PHP 專案可以可靠使用第三方套件與自動載入 class。

## Day 9：從單一網頁到基礎框架概念

### 先懂人話版

一開始每頁一個 PHP 檔很直覺，但頁面多了會變亂。框架概念的第一步，是讓所有請求先進同一個入口，再分派到不同功能。

### 正式概念

| 名稱 | 意思 |
| --- | --- |
| Front Controller | 統一入口，例如 `public/index.php` |
| Route | 網址和功能的對應 |
| Controller | 處理某個請求的程式 |
| View | 負責畫面輸出的模板 |


### 學完這篇你應該會做到什麼

- 能用自己的話說明 Day 9：從單一網頁到基礎框架概念 在 PHP 網站中的用途。
- 能照著範例寫出可執行的 PHP、SQL、設定或流程。
- 能用瀏覽器、CLI、資料庫結果或錯誤訊息確認自己做對了。

### 實作任務情境

把這篇當成一個小任務：你要替一個逐步成形的 PHP 小網站加入 Day 9：從單一網頁到基礎框架概念 相關能力，並確定它真的能執行、能查錯、能被下一篇繼續擴充。

### 操作前檢查

- 確認 PHP、Composer、Web server、資料庫或第三方服務是否已準備好；不需要的項目不要硬裝。
- 若會寫入資料庫，先準備測試資料庫，不要直接操作正式資料。
- 若會使用 API key、SMTP 密碼或付款金鑰，一律放在環境變數或設定檔，不要寫死在範例中。
### 範例形式與實作

1. 讓 `/` 顯示首頁。
2. 讓 `/products` 顯示商品頁。
3. 找不到頁面時回 404。

```php
<?php
declare(strict_types=1);

// 範例用途：
// 建立最小 route 分派，讓不同網址顯示不同內容。
//
// 新手先知道：
// - $_SERVER["REQUEST_URI"] 是瀏覽器打進來的網址路徑。
// - parse_url(..., PHP_URL_PATH) 只取路徑，不取 query string。
// - http_response_code(404) 代表告訴瀏覽器「這頁不存在」。
//
// 輸入：瀏覽器請求路徑。
// 輸出：不同路徑回不同內容。
$path = parse_url($_SERVER["REQUEST_URI"] ?? "/", PHP_URL_PATH) ?: "/";

if ($path === "/") {
    echo "首頁";
} elseif ($path === "/products") {
    echo "商品列表";
} else {
    http_response_code(404);
    echo "找不到頁面";
}
```


### 如果結果和預期不同

- 頁面空白：先打開錯誤顯示或查 Web server / PHP error log。
- 資料沒有寫入：確認連線設定、SQL、欄位名稱、交易是否 commit。
- 第三方服務失敗：先查 API 回傳碼、credential、callback URL 與環境變數。

### 做完後檢查

- 用瀏覽器或 CLI 實際跑一次本篇範例。
- 若有資料庫，確認資料表內容真的符合預期。
- 若有登入、寄信、付款或外部 API，確認成功情境與失敗情境都有可觀察結果。

### 小練習

1. 把範例改成另一個欄位或另一種輸入資料。
2. 故意輸入錯誤資料，觀察錯誤訊息或驗證結果。
3. 寫一句話記錄這篇在整個 PHP 網站流程中扮演的角色。

### 負面例子 / 錯誤用法

不要讓使用者用 `?page=xxx` 直接決定 include 哪個檔案。後果可能是任意檔案讀取。修正方向是用固定白名單 route。

### Junior 常見誤解

- 框架不是一堆神秘規則，本質是幫你整理請求、資料、畫面。
- route 是網站功能地圖，不只是漂亮網址。

### 一句話總結

基礎框架概念是把請求集中管理，讓網站變大後仍然可維護。

## Day 10：Clean URL 優化連結

### 先懂人話版

Clean URL 是讓網址像人話，例如 `/products/10` 比 `/index.php?page=product&id=10` 更清楚，也更適合分享和維護。

### 正式概念

| 名稱 | 意思 |
| --- | --- |
| Clean URL | 乾淨、可讀的網址 |
| Rewrite | web server 把請求轉給入口檔 |
| Path Parameter | 路徑中的參數，例如商品 id |


### 學完這篇你應該會做到什麼

- 能用自己的話說明 Day 10：Clean URL 優化連結 在 PHP 網站中的用途。
- 能照著範例寫出可執行的 PHP、SQL、設定或流程。
- 能用瀏覽器、CLI、資料庫結果或錯誤訊息確認自己做對了。

### 實作任務情境

把這篇當成一個小任務：你要替一個逐步成形的 PHP 小網站加入 Day 10：Clean URL 優化連結 相關能力，並確定它真的能執行、能查錯、能被下一篇繼續擴充。

### 操作前檢查

- 確認 PHP、Composer、Web server、資料庫或第三方服務是否已準備好；不需要的項目不要硬裝。
- 若會寫入資料庫，先準備測試資料庫，不要直接操作正式資料。
- 若會使用 API key、SMTP 密碼或付款金鑰，一律放在環境變數或設定檔，不要寫死在範例中。
### 範例形式與實作

1. 請求 `/products/10`。
2. 拆出 `10`。
3. 用它查商品。

```php
<?php
declare(strict_types=1);

// 範例用途：
// 從 Clean URL 取出商品 id。
//
// 新手先知道：
// - /products/10 這種網址可以拆成 products 和 10。
// - trim(..., "/") 是把路徑前後的 / 拿掉。
// - explode("/", $path) 是用 / 把字串切成陣列。
// - (int) 會把資料轉成整數，但正式專案仍要先驗證格式。
//
// 輸入：/products/10。
// 輸出：商品 id。
$path = trim(parse_url($_SERVER["REQUEST_URI"] ?? "/", PHP_URL_PATH) ?: "/", "/");
$parts = explode("/", $path);

if (($parts[0] ?? "") === "products" && isset($parts[1])) {
    $productId = (int) $parts[1];
    echo "你正在看商品 #" . $productId;
} else {
    echo "首頁";
}
```


### 如果結果和預期不同

- 頁面空白：先打開錯誤顯示或查 Web server / PHP error log。
- 資料沒有寫入：確認連線設定、SQL、欄位名稱、交易是否 commit。
- 第三方服務失敗：先查 API 回傳碼、credential、callback URL 與環境變數。

### 做完後檢查

- 用瀏覽器或 CLI 實際跑一次本篇範例。
- 若有資料庫，確認資料表內容真的符合預期。
- 若有登入、寄信、付款或外部 API，確認成功情境與失敗情境都有可觀察結果。

### 小練習

1. 把範例改成另一個欄位或另一種輸入資料。
2. 故意輸入錯誤資料，觀察錯誤訊息或驗證結果。
3. 寫一句話記錄這篇在整個 PHP 網站流程中扮演的角色。

### 負面例子 / 錯誤用法

不要只因為 `(int)` 轉型就覺得一切安全。`/products/abc` 會變成 `0`，可能查到錯誤資料。修正方向是先用 `ctype_digit()` 驗證格式。

### Junior 常見誤解

- Clean URL 需要 web server rewrite 配合。
- 路徑參數仍然是使用者輸入，要驗證。

### 一句話總結

Clean URL 讓網址更像網站功能地圖，但路徑資料仍然要驗證。

## Day 11：使用單例模式來連接資料庫

### 先懂人話版

原文用單例模式避免到處重複建立資料庫連線。對 junior 來說，先理解「資料庫連線應該集中管理」比記住 Singleton 名詞更重要。

### 正式概念

| 名稱 | 意思 |
| --- | --- |
| Singleton | 讓某個類別只建立一個實例 |
| Database Connection | 資料庫連線 |
| Shared Resource | 多處共用的資源 |


### 學完這篇你應該會做到什麼

- 能用自己的話說明 Day 11：使用單例模式來連接資料庫 在 PHP 網站中的用途。
- 能照著範例寫出可執行的 PHP、SQL、設定或流程。
- 能用瀏覽器、CLI、資料庫結果或錯誤訊息確認自己做對了。

### 實作任務情境

把這篇當成一個小任務：你要替一個逐步成形的 PHP 小網站加入 Day 11：使用單例模式來連接資料庫 相關能力，並確定它真的能執行、能查錯、能被下一篇繼續擴充。

### 操作前檢查

- 確認 PHP、Composer、Web server、資料庫或第三方服務是否已準備好；不需要的項目不要硬裝。
- 若會寫入資料庫，先準備測試資料庫，不要直接操作正式資料。
- 若會使用 API key、SMTP 密碼或付款金鑰，一律放在環境變數或設定檔，不要寫死在範例中。

### 範例形式與實作

```php
<?php
declare(strict_types=1);

// 範例用途：
// 集中建立 PDO。這是學習用簡化版，用來理解「不要到處重複連資料庫」。
//
// 新手先知道：
// - static 屬性屬於 class 本身，不屬於某一個物件。
// - ?PDO 代表這個值可以是 PDO，也可以是 null。
// - self::$connection 代表存取這個 class 自己的 static 屬性。
//
// 輸入：無，實際設定寫在類別內；正式專案應改用環境變數注入。
// 輸出：回傳同一個 PDO 連線。
final class Database
{
    private static ?PDO $connection = null;

    public static function connection(): PDO
    {
        if (self::$connection === null) {
            self::$connection = new PDO(
                "mysql:host=127.0.0.1;dbname=demo;charset=utf8mb4",
                "root",
                "password",
                [PDO::ATTR_ERRMODE => PDO::ERRMODE_EXCEPTION]
            );
        }

        return self::$connection;
    }
}
```


### 如果結果和預期不同

- 頁面空白：先打開錯誤顯示或查 Web server / PHP error log。
- 資料沒有寫入：確認連線設定、SQL、欄位名稱、交易是否 commit。
- 第三方服務失敗：先查 API 回傳碼、credential、callback URL 與環境變數。

### 做完後檢查

- 用瀏覽器或 CLI 實際跑一次本篇範例。
- 若有資料庫，確認資料表內容真的符合預期。
- 若有登入、寄信、付款或外部 API，確認成功情境與失敗情境都有可觀察結果。

### 小練習

1. 把範例改成另一個欄位或另一種輸入資料。
2. 故意輸入錯誤資料，觀察錯誤訊息或驗證結果。
3. 寫一句話記錄這篇在整個 PHP 網站流程中扮演的角色。

### 負面例子 / 錯誤用法

Singleton 用太多會讓測試變難，因為程式到處直接呼叫全域狀態。修正方向是進階後學 dependency injection，把 PDO 從外部傳進需要的 class。

### Junior 常見誤解

- Singleton 不是「比較專業」的寫法，它只是管理共用物件的一種方式。
- 初學可以用它理解集中管理，但正式大型專案通常會用 DI container。

### 一句話總結

Day 11 的重點是集中管理資料庫連線，而不是盲目迷信 Singleton。

## Day 12：取得使用者輸入的資料存到資料庫

### 先懂人話版

表單就是使用者把資料交給網站。PHP 會從 `$_POST` 取到資料，但不能拿到就直接存，因為使用者可能沒填、亂填，甚至惡意填。

### 正式概念

| 名稱 | 意思 |
| --- | --- |
| `$_POST` | 表單用 POST 方法送來的資料 |
| Input Validation | 輸入驗證 |
| Insert | 新增資料到資料庫 |
| Prepared Statement | 把 SQL 和資料分開，降低 SQL injection 風險 |


### 學完這篇你應該會做到什麼

- 能用自己的話說明 Day 12：取得使用者輸入的資料存到資料庫 在 PHP 網站中的用途。
- 能照著範例寫出可執行的 PHP、SQL、設定或流程。
- 能用瀏覽器、CLI、資料庫結果或錯誤訊息確認自己做對了。

### 實作任務情境

把這篇當成一個小任務：你要替一個逐步成形的 PHP 小網站加入 Day 12：取得使用者輸入的資料存到資料庫 相關能力，並確定它真的能執行、能查錯、能被下一篇繼續擴充。

### 操作前檢查

- 確認 PHP、Composer、Web server、資料庫或第三方服務是否已準備好；不需要的項目不要硬裝。
- 若會寫入資料庫，先準備測試資料庫，不要直接操作正式資料。
- 若會使用 API key、SMTP 密碼或付款金鑰，一律放在環境變數或設定檔，不要寫死在範例中。

### 完整範圍補強：留言表單從畫面到資料庫

Day 12 不應只看一段 `INSERT`。新手需要知道資料表、資料庫連線、表單頁、處理頁、驗證與查詢確認如何接在一起。下面用最小留言表單示範完整路徑。

#### 範例範圍地圖

| 部分 | 檔案 / 位置 | 責任 |
| --- | --- | --- |
| 資料表 | MySQL `comments` table | 保存留言內容與建立時間 |
| 連線設定 | `config/database.php` | 集中建立 PDO 連線 |
| 表單畫面 | `public/new-comment.php` | 讓使用者輸入留言 |
| 寫入處理 | `public/create-comment.php` | 驗證輸入並用 prepared statement 寫入 |
| 驗證結果 | SQL 查詢或管理工具 | 確認資料真的進資料庫 |

建立資料表：

```sql
CREATE TABLE comments (
    id INT AUTO_INCREMENT PRIMARY KEY,
    message VARCHAR(500) NOT NULL,
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
);
```

`config/database.php`：

```php
<?php
declare(strict_types=1);

function db(): PDO
{
    return new PDO(
        "mysql:host=127.0.0.1;dbname=practice;charset=utf8mb4",
        "practice_user",
        "practice_password",
        [
            PDO::ATTR_ERRMODE => PDO::ERRMODE_EXCEPTION,
            PDO::ATTR_DEFAULT_FETCH_MODE => PDO::FETCH_ASSOC,
        ]
    );
}
```

`public/new-comment.php`：

```php
<form method="post" action="/create-comment.php">
    <label for="message">留言</label>
    <textarea id="message" name="message" required maxlength="500"></textarea>
    <button type="submit">送出</button>
</form>
```

`public/create-comment.php`：

```php
<?php
declare(strict_types=1);

require __DIR__ . "/../config/database.php";

$message = trim($_POST["message"] ?? "");

if ($message === "") {
    http_response_code(422);
    exit("留言不能空白");
}

if (mb_strlen($message) > 500) {
    http_response_code(422);
    exit("留言最多 500 字");
}

$statement = db()->prepare(
    "INSERT INTO comments (message) VALUES (:message)"
);
$statement->execute(["message" => $message]);

header("Location: /new-comment.php?created=1");
exit;
```

#### 端到端流程

1. 使用者在 `new-comment.php` 填表單。
2. 瀏覽器用 POST 把 `message` 送到 `create-comment.php`。
3. PHP 從 `$_POST` 取值，先做空白與長度檢查。
4. `db()` 建立 PDO 連線。
5. prepared statement 把留言安全寫入 `comments`。
6. 成功後 redirect 回表單頁，資料庫可查到新資料。

#### 做完後檢查

```sql
SELECT id, message, created_at
FROM comments
ORDER BY id DESC
LIMIT 5;
```

- 空白留言會被擋下來。
- 超過 500 字會被擋下來。
- 正常留言送出後，資料表會新增一筆資料。

#### 後續 Day 怎麼沿用這個範例

- Day 13 的輸入檢查可以直接擴充 `create-comment.php` 的驗證規則。
- Day 16 的 PDO 可以回頭看 `config/database.php`，把連線與查詢方式統一。
- Day 17 到 Day 18 的註冊登入，可以沿用「表單頁、處理頁、驗證、prepared statement、redirect」這條流程。
- Day 21 的 flash session 可以取代 `?created=1`，讓成功訊息更像正式網站。

### 範例形式與實作

```php
<?php
declare(strict_types=1);

// 範例用途：
// 接收留言表單並寫入資料庫。
//
// 新手先知道：
// - $_POST["message"] 是使用者從表單送來的留言。
// - ?? "" 的意思是：如果 message 不存在，就先當成空字串。
// - trim() 會移除前後空白，避免使用者只輸入空格。
// - prepare + execute 是安全傳入資料庫參數的常見寫法。
//
// 輸入：$_POST["message"]。
// 副作用：新增一筆留言。
$message = trim($_POST["message"] ?? "");

if ($message === "") {
    exit("留言不能空白");
}

$pdo = Database::connection();
$statement = $pdo->prepare("INSERT INTO comments (message) VALUES (:message)");
$statement->execute(["message" => $message]);

echo "留言成功";
```


### 如果結果和預期不同

- 頁面空白：先打開錯誤顯示或查 Web server / PHP error log。
- 資料沒有寫入：確認連線設定、SQL、欄位名稱、交易是否 commit。
- 第三方服務失敗：先查 API 回傳碼、credential、callback URL 與環境變數。

### 做完後檢查

- 用瀏覽器或 CLI 實際跑一次本篇範例。
- 若有資料庫，確認資料表內容真的符合預期。
- 若有登入、寄信、付款或外部 API，確認成功情境與失敗情境都有可觀察結果。

### 小練習

1. 把範例改成另一個欄位或另一種輸入資料。
2. 故意輸入錯誤資料，觀察錯誤訊息或驗證結果。
3. 寫一句話記錄這篇在整個 PHP 網站流程中扮演的角色。

### 負面例子 / 錯誤用法

```php
$pdo->query("INSERT INTO comments (message) VALUES ('" . $_POST["message"] . "')");
```

錯在直接把使用者輸入拼到 SQL。後果是 SQL injection。修正方向是 prepared statement。

### Junior 常見誤解

- 前端有 required 不代表後端不用驗證。
- `trim()` 只能清掉前後空白，不等於資料安全。

### 一句話總結

任何使用者輸入都要先驗證，再用 prepared statement 存進資料庫。

## Day 13：檢查使用者輸入的資料

### 先懂人話版

驗證資料像櫃台檢查申請表：姓名不能空白、email 要像 email、密碼長度要夠。沒有驗證，資料庫會變成垃圾桶。

### 正式概念

| 名稱 | 意思 |
| --- | --- |
| Required | 必填 |
| Format | 格式，例如 email |
| Length | 長度限制 |
| Business Rule | 業務規則，例如 email 不能重複 |


### 學完這篇你應該會做到什麼

- 能用自己的話說明 Day 13：檢查使用者輸入的資料 在 PHP 網站中的用途。
- 能照著範例寫出可執行的 PHP、SQL、設定或流程。
- 能用瀏覽器、CLI、資料庫結果或錯誤訊息確認自己做對了。

### 實作任務情境

把這篇當成一個小任務：你要替一個逐步成形的 PHP 小網站加入 Day 13：檢查使用者輸入的資料 相關能力，並確定它真的能執行、能查錯、能被下一篇繼續擴充。

### 操作前檢查

- 確認 PHP、Composer、Web server、資料庫或第三方服務是否已準備好；不需要的項目不要硬裝。
- 若會寫入資料庫，先準備測試資料庫，不要直接操作正式資料。
- 若會使用 API key、SMTP 密碼或付款金鑰，一律放在環境變數或設定檔，不要寫死在範例中。
### 範例形式與實作

```php
<?php
declare(strict_types=1);

// 範例用途：
// 把驗證邏輯集中成函式，讓註冊流程比較乾淨。
//
// 新手先知道：
// - function 是把一段可重複使用的程式包起來。
// - array 是陣列，這裡用來收集錯誤訊息。
// - filter_var(..., FILTER_VALIDATE_EMAIL) 會檢查 email 格式。
// - return 代表把結果交回呼叫這個函式的地方。
//
// 輸入：email 與 password。
// 輸出：錯誤訊息陣列；空陣列代表通過。
function validateRegistration(string $email, string $password): array
{
    $errors = [];

    if (!filter_var($email, FILTER_VALIDATE_EMAIL)) {
        $errors[] = "Email 格式不正確";
    }

    if (mb_strlen($password) < 12) {
        $errors[] = "密碼至少 12 個字元";
    }

    return $errors;
}
```


### 如果結果和預期不同

- 頁面空白：先打開錯誤顯示或查 Web server / PHP error log。
- 資料沒有寫入：確認連線設定、SQL、欄位名稱、交易是否 commit。
- 第三方服務失敗：先查 API 回傳碼、credential、callback URL 與環境變數。

### 做完後檢查

- 用瀏覽器或 CLI 實際跑一次本篇範例。
- 若有資料庫，確認資料表內容真的符合預期。
- 若有登入、寄信、付款或外部 API，確認成功情境與失敗情境都有可觀察結果。

### 小練習

1. 把範例改成另一個欄位或另一種輸入資料。
2. 故意輸入錯誤資料，觀察錯誤訊息或驗證結果。
3. 寫一句話記錄這篇在整個 PHP 網站流程中扮演的角色。

### 負面例子 / 錯誤用法

只寫 `if (!empty($email))` 不夠，因為 `hello` 不是空白，但也不是 email。修正方向是根據欄位意義選擇驗證規則。

### Junior 常見誤解

- 驗證不是為了刁難使用者，而是保護系統資料品質。
- 錯誤訊息要讓使用者知道怎麼修正。

### 一句話總結

驗證資料是讓系統可以信任自己資料的第一道門。

## Day 14：學習日期與時間控制

### 先懂人話版

時間在網站裡很常見：註冊時間、訂單時間、token 過期時間、活動截止日。時間如果沒處理好，跨時區或過期判斷會出錯。

### 正式概念

| 名稱 | 意思 |
| --- | --- |
| DateTimeImmutable | PHP 的日期時間物件 |
| Timezone | 時區 |
| Format | 輸出格式 |
| Expiration | 過期時間 |


### 學完這篇你應該會做到什麼

- 能用自己的話說明 Day 14：學習日期與時間控制 在 PHP 網站中的用途。
- 能照著範例寫出可執行的 PHP、SQL、設定或流程。
- 能用瀏覽器、CLI、資料庫結果或錯誤訊息確認自己做對了。

### 實作任務情境

把這篇當成一個小任務：你要替一個逐步成形的 PHP 小網站加入 Day 14：學習日期與時間控制 相關能力，並確定它真的能執行、能查錯、能被下一篇繼續擴充。

### 操作前檢查

- 確認 PHP、Composer、Web server、資料庫或第三方服務是否已準備好；不需要的項目不要硬裝。
- 若會寫入資料庫，先準備測試資料庫，不要直接操作正式資料。
- 若會使用 API key、SMTP 密碼或付款金鑰，一律放在環境變數或設定檔，不要寫死在範例中。
### 範例形式與實作

```php
<?php
declare(strict_types=1);

// 範例用途：
// 建立 token 過期時間，例如註冊驗證信 30 分鐘後失效。
//
// 新手先知道：
// - DateTimeImmutable 是日期時間物件。
// - Asia/Taipei 明確指定台北時區，避免不同機器時間判斷不一致。
// - modify("+30 minutes") 會得到 30 分鐘後的新時間。
// - format("Y-m-d H:i:s") 會轉成資料庫常見格式。
//
// 輸入：目前時間。
// 輸出：30 分鐘後的資料庫時間字串。
$now = new DateTimeImmutable("now", new DateTimeZone("Asia/Taipei"));
$expiresAt = $now->modify("+30 minutes");

echo $expiresAt->format("Y-m-d H:i:s");
```


### 如果結果和預期不同

- 頁面空白：先打開錯誤顯示或查 Web server / PHP error log。
- 資料沒有寫入：確認連線設定、SQL、欄位名稱、交易是否 commit。
- 第三方服務失敗：先查 API 回傳碼、credential、callback URL 與環境變數。

### 做完後檢查

- 用瀏覽器或 CLI 實際跑一次本篇範例。
- 若有資料庫，確認資料表內容真的符合預期。
- 若有登入、寄信、付款或外部 API，確認成功情境與失敗情境都有可觀察結果。

### 小練習

1. 把範例改成另一個欄位或另一種輸入資料。
2. 故意輸入錯誤資料，觀察錯誤訊息或驗證結果。
3. 寫一句話記錄這篇在整個 PHP 網站流程中扮演的角色。

### 負面例子 / 錯誤用法

不要在不同地方混用不同時區。後果是明明 token 應該有效，卻被判定過期。修正方向是專案統一使用明確時區，資料庫也要有一致策略。

### Junior 常見誤解

- `time()` 是 Unix timestamp，不是人類可讀時間。
- `DateTimeImmutable` 改時間會回傳新物件，不會改原物件。

### 一句話總結

日期時間不是只會顯示，它會影響系統規則與安全期限。

## Day 15：用 PHPMailer + Gmail SMTP 實作寄信

### 先懂人話版

寄信不是 PHP 自己把信送到對方信箱，而是 PHP 去拜託 SMTP 服務幫忙送。PHPMailer 是幫你和 SMTP 溝通的工具。

### 正式概念

| 名稱 | 意思 |
| --- | --- |
| SMTP | 寄信協定 |
| PHPMailer | PHP 常用寄信套件 |
| Host | SMTP 伺服器位置 |
| Credential | 帳號密碼或 app password |


### 學完這篇你應該會做到什麼

- 能用自己的話說明 Day 15：用 PHPMailer + Gmail SMTP 實作寄信 在 PHP 網站中的用途。
- 能照著範例寫出可執行的 PHP、SQL、設定或流程。
- 能用瀏覽器、CLI、資料庫結果或錯誤訊息確認自己做對了。

### 實作任務情境

把這篇當成一個小任務：你要替一個逐步成形的 PHP 小網站加入 Day 15：用 PHPMailer + Gmail SMTP 實作寄信 相關能力，並確定它真的能執行、能查錯、能被下一篇繼續擴充。

### 操作前檢查

- 確認 PHP、Composer、Web server、資料庫或第三方服務是否已準備好；不需要的項目不要硬裝。
- 若會寫入資料庫，先準備測試資料庫，不要直接操作正式資料。
- 若會使用 API key、SMTP 密碼或付款金鑰，一律放在環境變數或設定檔，不要寫死在範例中。
### 範例形式與實作

```php
<?php
declare(strict_types=1);

use PHPMailer\PHPMailer\PHPMailer;

require __DIR__ . "/vendor/autoload.php";

// 範例用途：
// 寄出註冊驗證信的概念範例。
//
// 新手先知道：
// - PHPMailer 是別人寫好的寄信工具，我們用 Composer 安裝。
// - $_ENV["SMTP_HOST"] 代表從環境變數讀設定，不把密碼寫死。
// - setFrom 是寄件人，addAddress 是收件人。
// - send() 真的送出信，可能因帳密、網路或 SMTP 設定失敗。
//
// 輸入：收件者 email 與驗證連結。
// 副作用：透過 SMTP 寄出 email。
$mail = new PHPMailer(true);
$mail->isSMTP();
$mail->Host = $_ENV["SMTP_HOST"];
$mail->Username = $_ENV["SMTP_USER"];
$mail->Password = $_ENV["SMTP_PASSWORD"];
$mail->setFrom("noreply@example.com", "Demo App");
$mail->addAddress("user@example.com");
$mail->Subject = "請驗證你的帳號";
$mail->Body = "請點擊連結完成驗證：https://example.com/activate?token=...";
$mail->send();
```


### 如果結果和預期不同

- 頁面空白：先打開錯誤顯示或查 Web server / PHP error log。
- 資料沒有寫入：確認連線設定、SQL、欄位名稱、交易是否 commit。
- 第三方服務失敗：先查 API 回傳碼、credential、callback URL 與環境變數。

### 做完後檢查

- 用瀏覽器或 CLI 實際跑一次本篇範例。
- 若有資料庫，確認資料表內容真的符合預期。
- 若有登入、寄信、付款或外部 API，確認成功情境與失敗情境都有可觀察結果。

### 小練習

1. 把範例改成另一個欄位或另一種輸入資料。
2. 故意輸入錯誤資料，觀察錯誤訊息或驗證結果。
3. 寫一句話記錄這篇在整個 PHP 網站流程中扮演的角色。

### 負面例子 / 錯誤用法

不要把 Gmail 密碼寫在程式裡。後果是 repo 外洩就等於信箱外洩。修正方向是使用環境變數、app password 或雲端秘密管理。

### Junior 常見誤解

- 寄信失敗不一定是程式錯，可能是帳號安全設定、DNS、SMTP 限制。
- 驗證信的連結應該有有效期限。

### 一句話總結

寄信功能要同時處理套件、SMTP 設定、秘密保護與錯誤紀錄。

## Day 16：將 MySQLi 升級為 PDO

### 先懂人話版

PDO 是 PHP 連資料庫的通用工具。你可以把它想成比較標準化的資料庫遙控器，重點是支援 prepared statement。

### 正式概念

| 名稱 | 意思 |
| --- | --- |
| PDO | PHP Data Objects |
| DSN | 資料庫連線字串 |
| Prepared Statement | 預備查詢 |
| Fetch Mode | 資料取回格式 |


### 學完這篇你應該會做到什麼

- 能用自己的話說明 Day 16：將 MySQLi 升級為 PDO 在 PHP 網站中的用途。
- 能照著範例寫出可執行的 PHP、SQL、設定或流程。
- 能用瀏覽器、CLI、資料庫結果或錯誤訊息確認自己做對了。

### 實作任務情境

把這篇當成一個小任務：你要替一個逐步成形的 PHP 小網站加入 Day 16：將 MySQLi 升級為 PDO 相關能力，並確定它真的能執行、能查錯、能被下一篇繼續擴充。

### 操作前檢查

- 確認 PHP、Composer、Web server、資料庫或第三方服務是否已準備好；不需要的項目不要硬裝。
- 若會寫入資料庫，先準備測試資料庫，不要直接操作正式資料。
- 若會使用 API key、SMTP 密碼或付款金鑰，一律放在環境變數或設定檔，不要寫死在範例中。
### 範例形式與實作

```php
<?php
declare(strict_types=1);

// 範例用途：
// 用 PDO 查詢單一會員。
//
// 新手先知道：
// - :id 是命名參數，先放在 SQL 裡當佔位符。
// - execute(["id" => 5]) 才把真正的 id 值帶進去。
// - fetch(PDO::FETCH_ASSOC) 會回傳關聯式陣列，例如 ["id" => 5]。
//
// 輸入：會員 id。
// 輸出：會員資料或 false。
$pdo = Database::connection();
$statement = $pdo->prepare("SELECT id, email FROM members WHERE id = :id");
$statement->execute(["id" => 5]);

$member = $statement->fetch(PDO::FETCH_ASSOC);
print_r($member);
```


### 如果結果和預期不同

- 頁面空白：先打開錯誤顯示或查 Web server / PHP error log。
- 資料沒有寫入：確認連線設定、SQL、欄位名稱、交易是否 commit。
- 第三方服務失敗：先查 API 回傳碼、credential、callback URL 與環境變數。

### 做完後檢查

- 用瀏覽器或 CLI 實際跑一次本篇範例。
- 若有資料庫，確認資料表內容真的符合預期。
- 若有登入、寄信、付款或外部 API，確認成功情境與失敗情境都有可觀察結果。

### 小練習

1. 把範例改成另一個欄位或另一種輸入資料。
2. 故意輸入錯誤資料，觀察錯誤訊息或驗證結果。
3. 寫一句話記錄這篇在整個 PHP 網站流程中扮演的角色。

### 負面例子 / 錯誤用法

使用 PDO 但仍然字串拼 SQL，一樣不安全。PDO 不是護身符，prepared statement 才是重點。

### Junior 常見誤解

- `prepare()` 是準備 SQL 結構，`execute()` 才把資料帶進去執行。
- `fetch()` 取一筆，`fetchAll()` 取多筆。

### 一句話總結

PDO 的價值在於統一資料庫操作並安全地傳入參數。

## Day 17：實作註冊流程

### 先懂人話版

註冊流程不是「把 email 和密碼存起來」而已。正確流程要驗證輸入、檢查 email 是否重複、hash 密碼、建立會員、寄驗證信。

### 正式概念

| 名稱 | 意思 |
| --- | --- |
| Registration | 註冊 |
| Password Hash | 密碼雜湊 |
| Duplicate Check | 重複資料檢查 |
| Activation | 帳號開通 |


### 學完這篇你應該會做到什麼

- 能用自己的話說明 Day 17：實作註冊流程 在 PHP 網站中的用途。
- 能照著範例寫出可執行的 PHP、SQL、設定或流程。
- 能用瀏覽器、CLI、資料庫結果或錯誤訊息確認自己做對了。

### 實作任務情境

把這篇當成一個小任務：你要替一個逐步成形的 PHP 小網站加入 Day 17：實作註冊流程 相關能力，並確定它真的能執行、能查錯、能被下一篇繼續擴充。

### 操作前檢查

- 確認 PHP、Composer、Web server、資料庫或第三方服務是否已準備好；不需要的項目不要硬裝。
- 若會寫入資料庫，先準備測試資料庫，不要直接操作正式資料。
- 若會使用 API key、SMTP 密碼或付款金鑰，一律放在環境變數或設定檔，不要寫死在範例中。
### 範例形式與實作

```php
<?php
declare(strict_types=1);

// 範例用途：
// 註冊會員時安全保存密碼。
//
// 新手先知道：
// - $password 是使用者剛輸入的明文密碼，只能短暫存在記憶體。
// - password_hash() 會把密碼轉成不可直接還原的 hash。
// - active = 0 代表帳號還沒完成 email 開通。
//
// 輸入：email 與明文密碼。
// 副作用：寫入 members 資料表。
function register(PDO $pdo, string $email, string $password): void
{
    $hash = password_hash($password, PASSWORD_DEFAULT);

    $statement = $pdo->prepare(
        "INSERT INTO members (email, password_hash, active) VALUES (:email, :hash, 0)"
    );

    $statement->execute([
        "email" => $email,
        "hash" => $hash,
    ]);
}
```


### 如果結果和預期不同

- 頁面空白：先打開錯誤顯示或查 Web server / PHP error log。
- 資料沒有寫入：確認連線設定、SQL、欄位名稱、交易是否 commit。
- 第三方服務失敗：先查 API 回傳碼、credential、callback URL 與環境變數。

### 做完後檢查

- 用瀏覽器或 CLI 實際跑一次本篇範例。
- 若有資料庫，確認資料表內容真的符合預期。
- 若有登入、寄信、付款或外部 API，確認成功情境與失敗情境都有可觀察結果。

### 小練習

1. 把範例改成另一個欄位或另一種輸入資料。
2. 故意輸入錯誤資料，觀察錯誤訊息或驗證結果。
3. 寫一句話記錄這篇在整個 PHP 網站流程中扮演的角色。

### 負面例子 / 錯誤用法

不要把明文密碼存進資料庫。後果是資料庫外洩時，所有使用者密碼直接外洩。修正方向是使用 `password_hash()`。

### Junior 常見誤解

- hash 不是加密，通常不能解回原文。
- 註冊成功不代表帳號已開通，可以先設 `active = 0`。

### 一句話總結

註冊流程的底線是驗證資料並永遠不要保存明文密碼。

## Day 18：實作登入與登出機制

### 先懂人話版

登入就是確認「你真的是這個帳號的人」，確認後用 session 記住你已登入。登出就是把這個登入狀態清掉。

### 正式概念

| 名稱 | 意思 |
| --- | --- |
| Authentication | 身分驗證 |
| Session | 伺服器端保存使用者狀態 |
| `password_verify()` | 驗證明文密碼是否符合 hash |
| Logout | 清除登入狀態 |


### 學完這篇你應該會做到什麼

- 能用自己的話說明 Day 18：實作登入與登出機制 在 PHP 網站中的用途。
- 能照著範例寫出可執行的 PHP、SQL、設定或流程。
- 能用瀏覽器、CLI、資料庫結果或錯誤訊息確認自己做對了。

### 實作任務情境

把這篇當成一個小任務：你要替一個逐步成形的 PHP 小網站加入 Day 18：實作登入與登出機制 相關能力，並確定它真的能執行、能查錯、能被下一篇繼續擴充。

### 操作前檢查

- 確認 PHP、Composer、Web server、資料庫或第三方服務是否已準備好；不需要的項目不要硬裝。
- 若會寫入資料庫，先準備測試資料庫，不要直接操作正式資料。
- 若會使用 API key、SMTP 密碼或付款金鑰，一律放在環境變數或設定檔，不要寫死在範例中。
### 範例形式與實作

```php
<?php
declare(strict_types=1);

// 範例用途：
// 登入時驗證密碼並建立 session。
//
// 新手先知道：
// - 先用 email 找會員資料，不直接用明文密碼查資料庫。
// - password_verify() 會檢查使用者輸入的密碼是否符合資料庫裡的 hash。
// - session_regenerate_id(true) 會換一個新的 session id，降低被固定 session 的風險。
// - $_SESSION["member_id"] 是伺服器記住「目前登入者是誰」。
//
// 輸入：email 與密碼。
// 副作用：登入成功後寫入 $_SESSION["member_id"]。
function login(PDO $pdo, string $email, string $password): bool
{
    $statement = $pdo->prepare("SELECT id, password_hash FROM members WHERE email = :email AND active = 1");
    $statement->execute(["email" => $email]);
    $member = $statement->fetch(PDO::FETCH_ASSOC);

    if (!$member || !password_verify($password, $member["password_hash"])) {
        return false;
    }

    session_regenerate_id(true);
    $_SESSION["member_id"] = (int) $member["id"];
    return true;
}
```


### 如果結果和預期不同

- 頁面空白：先打開錯誤顯示或查 Web server / PHP error log。
- 資料沒有寫入：確認連線設定、SQL、欄位名稱、交易是否 commit。
- 第三方服務失敗：先查 API 回傳碼、credential、callback URL 與環境變數。

### 做完後檢查

- 用瀏覽器或 CLI 實際跑一次本篇範例。
- 若有資料庫，確認資料表內容真的符合預期。
- 若有登入、寄信、付款或外部 API，確認成功情境與失敗情境都有可觀察結果。

### 小練習

1. 把範例改成另一個欄位或另一種輸入資料。
2. 故意輸入錯誤資料，觀察錯誤訊息或驗證結果。
3. 寫一句話記錄這篇在整個 PHP 網站流程中扮演的角色。

### 負面例子 / 錯誤用法

不要用 `SELECT * FROM members WHERE email = '$email' AND password = '$password'`。錯在明文密碼與 SQL injection 風險。修正方向是查出 hash 後用 `password_verify()`。

### Junior 常見誤解

- session 存的是登入狀態，不應存敏感密碼。
- 登入成功後重新產生 session id 可以降低 session fixation 風險。

### 一句話總結

登入流程要安全驗證密碼，並用 session 保存已登入狀態。

## Day 19：實作收信開通帳號機制

### 先懂人話版

開通信是用來確認「這個 email 真的是你的」。系統寄一把臨時鑰匙給你，你點連結後，系統才把帳號打開。

### 正式概念

| 名稱 | 意思 |
| --- | --- |
| Activation Token | 開通 token |
| Token Hash | token 的雜湊值 |
| Expiration | 過期時間 |
| One-time Use | 只能使用一次 |


### 學完這篇你應該會做到什麼

- 能用自己的話說明 Day 19：實作收信開通帳號機制 在 PHP 網站中的用途。
- 能照著範例寫出可執行的 PHP、SQL、設定或流程。
- 能用瀏覽器、CLI、資料庫結果或錯誤訊息確認自己做對了。

### 實作任務情境

把這篇當成一個小任務：你要替一個逐步成形的 PHP 小網站加入 Day 19：實作收信開通帳號機制 相關能力，並確定它真的能執行、能查錯、能被下一篇繼續擴充。

### 操作前檢查

- 確認 PHP、Composer、Web server、資料庫或第三方服務是否已準備好；不需要的項目不要硬裝。
- 若會寫入資料庫，先準備測試資料庫，不要直接操作正式資料。
- 若會使用 API key、SMTP 密碼或付款金鑰，一律放在環境變數或設定檔，不要寫死在範例中。
### 範例形式與實作

```php
<?php
declare(strict_types=1);

// 範例用途：
// 建立開通 token，資料庫只存 hash。
//
// 新手先知道：
// - random_bytes(32) 會產生很難猜的隨機資料。
// - bin2hex() 會把隨機 bytes 轉成網址比較好攜帶的字串。
// - hash("sha256", $token) 是把 token 轉成 hash 後存資料庫。
// - 明文 token 只拿去寄信，不要長期保存。
//
// 輸入：會員 id。
// 輸出：明文 token，僅用於寄信。
function createActivationToken(PDO $pdo, int $memberId): string
{
    $token = bin2hex(random_bytes(32));
    $tokenHash = hash("sha256", $token);
    $expiresAt = (new DateTimeImmutable("+30 minutes"))->format("Y-m-d H:i:s");

    $statement = $pdo->prepare(
        "INSERT INTO activation_tokens (member_id, token_hash, expires_at) VALUES (:member_id, :token_hash, :expires_at)"
    );

    $statement->execute([
        "member_id" => $memberId,
        "token_hash" => $tokenHash,
        "expires_at" => $expiresAt,
    ]);

    return $token;
}
```


### 如果結果和預期不同

- 頁面空白：先打開錯誤顯示或查 Web server / PHP error log。
- 資料沒有寫入：確認連線設定、SQL、欄位名稱、交易是否 commit。
- 第三方服務失敗：先查 API 回傳碼、credential、callback URL 與環境變數。

### 做完後檢查

- 用瀏覽器或 CLI 實際跑一次本篇範例。
- 若有資料庫，確認資料表內容真的符合預期。
- 若有登入、寄信、付款或外部 API，確認成功情境與失敗情境都有可觀察結果。

### 小練習

1. 把範例改成另一個欄位或另一種輸入資料。
2. 故意輸入錯誤資料，觀察錯誤訊息或驗證結果。
3. 寫一句話記錄這篇在整個 PHP 網站流程中扮演的角色。

### 負面例子 / 錯誤用法

不要用 `md5($email . time())` 產生 token。它太容易被猜。修正方向是 `random_bytes()`。

### Junior 常見誤解

- token 是臨時鑰匙，不是永久密碼。
- 資料庫最好存 token hash，不要存明文 token。

### 一句話總結

帳號開通流程要使用不可預測、會過期、最好只能用一次的 token。

## Day 20：實作重置密碼流程

### 先懂人話版

忘記密碼不能把舊密碼寄回去，因為系統根本不應該知道明文密碼。正確做法是寄一個短時間有效的重設連結，讓使用者設定新密碼。

### 正式概念

| 名稱 | 意思 |
| --- | --- |
| Password Reset | 重設密碼 |
| Reset Token | 重設密碼 token |
| Token Expiry | token 過期 |
| Password Rehash | 重新 hash 新密碼 |


### 學完這篇你應該會做到什麼

- 能用自己的話說明 Day 20：實作重置密碼流程 在 PHP 網站中的用途。
- 能照著範例寫出可執行的 PHP、SQL、設定或流程。
- 能用瀏覽器、CLI、資料庫結果或錯誤訊息確認自己做對了。

### 實作任務情境

把這篇當成一個小任務：你要替一個逐步成形的 PHP 小網站加入 Day 20：實作重置密碼流程 相關能力，並確定它真的能執行、能查錯、能被下一篇繼續擴充。

### 操作前檢查

- 確認 PHP、Composer、Web server、資料庫或第三方服務是否已準備好；不需要的項目不要硬裝。
- 若會寫入資料庫，先準備測試資料庫，不要直接操作正式資料。
- 若會使用 API key、SMTP 密碼或付款金鑰，一律放在環境變數或設定檔，不要寫死在範例中。
### 範例形式與實作

```php
<?php
declare(strict_types=1);

// 範例用途：
// 更新新密碼。重設密碼時，系統不會查回舊密碼，而是保存新的 hash。
//
// 新手先知道：
// - mb_strlen() 用來計算字串長度，對中文比較友善。
// - throw new InvalidArgumentException(...) 代表資料不合規則，直接丟出錯誤。
// - UPDATE 是 SQL 的更新資料語法。
//
// 輸入：會員 id 與新密碼。
// 副作用：更新資料庫中的 password_hash。
function updatePassword(PDO $pdo, int $memberId, string $newPassword): void
{
    if (mb_strlen($newPassword) < 12) {
        throw new InvalidArgumentException("密碼至少 12 個字元");
    }

    $hash = password_hash($newPassword, PASSWORD_DEFAULT);
    $statement = $pdo->prepare("UPDATE members SET password_hash = :hash WHERE id = :id");
    $statement->execute([
        "hash" => $hash,
        "id" => $memberId,
    ]);
}
```


### 如果結果和預期不同

- 頁面空白：先打開錯誤顯示或查 Web server / PHP error log。
- 資料沒有寫入：確認連線設定、SQL、欄位名稱、交易是否 commit。
- 第三方服務失敗：先查 API 回傳碼、credential、callback URL 與環境變數。

### 做完後檢查

- 用瀏覽器或 CLI 實際跑一次本篇範例。
- 若有資料庫，確認資料表內容真的符合預期。
- 若有登入、寄信、付款或外部 API，確認成功情境與失敗情境都有可觀察結果。

### 小練習

1. 把範例改成另一個欄位或另一種輸入資料。
2. 故意輸入錯誤資料，觀察錯誤訊息或驗證結果。
3. 寫一句話記錄這篇在整個 PHP 網站流程中扮演的角色。

### 負面例子 / 錯誤用法

不要寄出使用者舊密碼。後果是代表你保存了明文密碼，本身就是嚴重風險。

### Junior 常見誤解

- 忘記密碼是「重設」，不是「查詢原密碼」。
- 重設成功後，舊 token 應該失效。

### 一句話總結

重設密碼流程的安全核心是短效 token 與重新 hash 新密碼。

## Day 21：優化架構與 flash session

### 先懂人話版

flash session 是「只顯示一次的提示訊息」。例如註冊成功後轉到登入頁，登入頁要顯示「請收信開通帳號」，顯示完就清掉。

### 正式概念

| 名稱 | 意思 |
| --- | --- |
| Flash Message | 一次性訊息 |
| Redirect | 轉址 |
| Session | 暫存跨頁狀態 |
| Architecture | 專案結構整理 |


### 學完這篇你應該會做到什麼

- 能用自己的話說明 Day 21：優化架構與 flash session 在 PHP 網站中的用途。
- 能照著範例寫出可執行的 PHP、SQL、設定或流程。
- 能用瀏覽器、CLI、資料庫結果或錯誤訊息確認自己做對了。

### 實作任務情境

把這篇當成一個小任務：你要替一個逐步成形的 PHP 小網站加入 Day 21：優化架構與 flash session 相關能力，並確定它真的能執行、能查錯、能被下一篇繼續擴充。

### 操作前檢查

- 確認 PHP、Composer、Web server、資料庫或第三方服務是否已準備好；不需要的項目不要硬裝。
- 若會寫入資料庫，先準備測試資料庫，不要直接操作正式資料。
- 若會使用 API key、SMTP 密碼或付款金鑰，一律放在環境變數或設定檔，不要寫死在範例中。
### 範例形式與實作

```php
<?php
declare(strict_types=1);

// 範例用途：
// 設定與讀取 flash message，例如「註冊成功，請收信」。
//
// 新手先知道：
// - flash() 負責把訊息放進 session。
// - getFlash() 負責取出訊息，並立刻 unset 清掉。
// - ?string 代表可能回傳字串，也可能回傳 null。
//
// 輸入：提示訊息。
// 輸出：讀取後清除，避免重複顯示。
function flash(string $message): void
{
    $_SESSION["flash"] = $message;
}

function getFlash(): ?string
{
    $message = $_SESSION["flash"] ?? null;
    unset($_SESSION["flash"]);
    return $message;
}
```


### 如果結果和預期不同

- 頁面空白：先打開錯誤顯示或查 Web server / PHP error log。
- 資料沒有寫入：確認連線設定、SQL、欄位名稱、交易是否 commit。
- 第三方服務失敗：先查 API 回傳碼、credential、callback URL 與環境變數。

### 做完後檢查

- 用瀏覽器或 CLI 實際跑一次本篇範例。
- 若有資料庫，確認資料表內容真的符合預期。
- 若有登入、寄信、付款或外部 API，確認成功情境與失敗情境都有可觀察結果。

### 小練習

1. 把範例改成另一個欄位或另一種輸入資料。
2. 故意輸入錯誤資料，觀察錯誤訊息或驗證結果。
3. 寫一句話記錄這篇在整個 PHP 網站流程中扮演的角色。

### 負面例子 / 錯誤用法

如果讀取 flash 後不清掉，使用者每次刷新頁面都會看到舊訊息。修正方向是讀取後 `unset()`。

### Junior 常見誤解

- flash message 適合提示，不適合保存重要資料。
- 架構優化不是把檔案切很多，而是讓責任清楚。

### 一句話總結

flash session 適合保存一次性提示，架構整理則是讓專案責任更清楚。

## Day 22：使用 Monolog 追蹤問題

### 先懂人話版

log 像系統的日記。當寄信失敗、登入失敗、金流 callback 錯誤時，你不能只讓畫面顯示「失敗」，還要留下可以追查的紀錄。

### 正式概念

| 名稱 | 意思 |
| --- | --- |
| Logger | 寫 log 的工具 |
| Level | log 等級，例如 info、warning、error |
| Context | 補充資訊 |
| Handler | log 要寫去哪裡 |


### 學完這篇你應該會做到什麼

- 能用自己的話說明 Day 22：使用 Monolog 追蹤問題 在 PHP 網站中的用途。
- 能照著範例寫出可執行的 PHP、SQL、設定或流程。
- 能用瀏覽器、CLI、資料庫結果或錯誤訊息確認自己做對了。

### 實作任務情境

把這篇當成一個小任務：你要替一個逐步成形的 PHP 小網站加入 Day 22：使用 Monolog 追蹤問題 相關能力，並確定它真的能執行、能查錯、能被下一篇繼續擴充。

### 操作前檢查

- 確認 PHP、Composer、Web server、資料庫或第三方服務是否已準備好；不需要的項目不要硬裝。
- 若會寫入資料庫，先準備測試資料庫，不要直接操作正式資料。
- 若會使用 API key、SMTP 密碼或付款金鑰，一律放在環境變數或設定檔，不要寫死在範例中。
### 範例形式與實作

```php
<?php
declare(strict_types=1);

use Monolog\Handler\StreamHandler;
use Monolog\Logger;

require __DIR__ . "/vendor/autoload.php";

// 範例用途：
// 記錄登入失敗事件，方便之後查問題。
//
// 新手先知道：
// - warning 代表警告等級，不一定是系統壞掉，但值得注意。
// - 第二個參數 ["email" => "..."] 是 context，提供額外線索。
// - 不要把明文密碼放進 context。
//
// 輸入：email。
// 副作用：寫入 storage/app.log。
$logger = new Logger("auth");
$logger->pushHandler(new StreamHandler(__DIR__ . "/storage/app.log"));
$logger->warning("Login failed", ["email" => "user@example.com"]);
```


### 如果結果和預期不同

- 頁面空白：先打開錯誤顯示或查 Web server / PHP error log。
- 資料沒有寫入：確認連線設定、SQL、欄位名稱、交易是否 commit。
- 第三方服務失敗：先查 API 回傳碼、credential、callback URL 與環境變數。

### 做完後檢查

- 用瀏覽器或 CLI 實際跑一次本篇範例。
- 若有資料庫，確認資料表內容真的符合預期。
- 若有登入、寄信、付款或外部 API，確認成功情境與失敗情境都有可觀察結果。

### 小練習

1. 把範例改成另一個欄位或另一種輸入資料。
2. 故意輸入錯誤資料，觀察錯誤訊息或驗證結果。
3. 寫一句話記錄這篇在整個 PHP 網站流程中扮演的角色。

### 負面例子 / 錯誤用法

不要把明文密碼、完整 token、信用卡資料寫進 log。後果是 log 外洩時等同資料外洩。修正方向是只記錄必要識別資訊。

### Junior 常見誤解

- log 不是只給錯誤用，也可以記錄重要流程。
- log 要能幫你追查，但不能洩漏敏感資料。

### 一句話總結

Monolog 幫你留下系統運作線索，但敏感資料不能寫進 log。

## Day 23：串接簡訊王 API

### 先懂人話版

API 串接就是你的系統去請另一個系統幫忙做事。簡訊 API 的例子是：PHP 把電話和訊息送給簡訊服務，簡訊服務幫你送出去。

### 正式概念

| 名稱 | 意思 |
| --- | --- |
| API | 系統之間溝通的介面 |
| HTTP Request | 對外送出的請求 |
| API Key | 服務辨識你的憑證 |
| Response | 對方服務回傳結果 |


### 學完這篇你應該會做到什麼

- 能用自己的話說明 Day 23：串接簡訊王 API 在 PHP 網站中的用途。
- 能照著範例寫出可執行的 PHP、SQL、設定或流程。
- 能用瀏覽器、CLI、資料庫結果或錯誤訊息確認自己做對了。

### 實作任務情境

把這篇當成一個小任務：你要替一個逐步成形的 PHP 小網站加入 Day 23：串接簡訊王 API 相關能力，並確定它真的能執行、能查錯、能被下一篇繼續擴充。

### 操作前檢查

- 確認 PHP、Composer、Web server、資料庫或第三方服務是否已準備好；不需要的項目不要硬裝。
- 若會寫入資料庫，先準備測試資料庫，不要直接操作正式資料。
- 若會使用 API key、SMTP 密碼或付款金鑰，一律放在環境變數或設定檔，不要寫死在範例中。
### 範例形式與實作

```php
<?php
declare(strict_types=1);

// 範例用途：
// 示範呼叫簡訊 API 的形狀，URL 是範例，不是真實服務。
//
// 新手先知道：
// - function sendSms(...) 把「送簡訊」包成可重複使用的功能。
// - http_build_query() 會把陣列轉成 URL query string。
// - $_ENV["SMS_API_KEY"] 從環境變數讀 API key，不要寫死。
// - file_get_contents() 這裡用來發出簡化版 HTTP GET；正式專案建議用成熟 HTTP client。
//
// 輸入：電話與訊息。
// 輸出：API 回傳結果字串。
function sendSms(string $phone, string $message): string
{
    $payload = http_build_query([
        "api_key" => $_ENV["SMS_API_KEY"],
        "phone" => $phone,
        "message" => $message,
    ]);

    return file_get_contents("https://sms.example.com/send?" . $payload) ?: "";
}
```


### 如果結果和預期不同

- 頁面空白：先打開錯誤顯示或查 Web server / PHP error log。
- 資料沒有寫入：確認連線設定、SQL、欄位名稱、交易是否 commit。
- 第三方服務失敗：先查 API 回傳碼、credential、callback URL 與環境變數。

### 做完後檢查

- 用瀏覽器或 CLI 實際跑一次本篇範例。
- 若有資料庫，確認資料表內容真的符合預期。
- 若有登入、寄信、付款或外部 API，確認成功情境與失敗情境都有可觀察結果。

### 小練習

1. 把範例改成另一個欄位或另一種輸入資料。
2. 故意輸入錯誤資料，觀察錯誤訊息或驗證結果。
3. 寫一句話記錄這篇在整個 PHP 網站流程中扮演的角色。

### 負面例子 / 錯誤用法

不要忽略 API 回傳結果。簡訊可能失敗、額度不足、電話格式錯誤。修正方向是檢查 response，失敗時寫 log 或排入重試。

### Junior 常見誤解

- API 打出去不代表成功，要看對方回傳。
- API key 是秘密，不該出現在前端或 GitHub。

### 一句話總結

串接 API 的重點是送出正確請求、檢查回應、保護憑證。

## Day 24：實作 Facebook 登入

### 先懂人話版

Facebook 登入是讓 Facebook 幫你確認使用者身分。你的網站不要直接相信一個 email，而是要完成 OAuth 流程，拿到可信的使用者資訊。

### 正式概念

| 名稱 | 意思 |
| --- | --- |
| OAuth | 第三方授權登入流程 |
| Redirect URI | 登入後導回你網站的網址 |
| Access Token | 存取使用者資料的臨時憑證 |
| State | 防止 CSRF 的隨機值 |


### 學完這篇你應該會做到什麼

- 能用自己的話說明 Day 24：實作 Facebook 登入 在 PHP 網站中的用途。
- 能照著範例寫出可執行的 PHP、SQL、設定或流程。
- 能用瀏覽器、CLI、資料庫結果或錯誤訊息確認自己做對了。

### 實作任務情境

把這篇當成一個小任務：你要替一個逐步成形的 PHP 小網站加入 Day 24：實作 Facebook 登入 相關能力，並確定它真的能執行、能查錯、能被下一篇繼續擴充。

### 操作前檢查

- 確認 PHP、Composer、Web server、資料庫或第三方服務是否已準備好；不需要的項目不要硬裝。
- 若會寫入資料庫，先準備測試資料庫，不要直接操作正式資料。
- 若會使用 API key、SMTP 密碼或付款金鑰，一律放在環境變數或設定檔，不要寫死在範例中。
### 範例形式與實作

```php
<?php
declare(strict_types=1);

// 範例用途：
// 建立 OAuth state 並存入 session。
//
// 新手先知道：
// - state 是一段隨機字串，用來確認 callback 真的是同一段登入流程回來的。
// - $_SESSION["oauth_state"] 暫時記住這個 state。
// - 等 Facebook 導回 callback 時，要比對回來的 state 是否相同。
//
// 輸入：沒有。
// 輸出：state，之後要放進登入 URL。
$state = bin2hex(random_bytes(16));
$_SESSION["oauth_state"] = $state;

echo "redirect to provider with state=" . $state;
```


### 如果結果和預期不同

- 頁面空白：先打開錯誤顯示或查 Web server / PHP error log。
- 資料沒有寫入：確認連線設定、SQL、欄位名稱、交易是否 commit。
- 第三方服務失敗：先查 API 回傳碼、credential、callback URL 與環境變數。

### 做完後檢查

- 用瀏覽器或 CLI 實際跑一次本篇範例。
- 若有資料庫，確認資料表內容真的符合預期。
- 若有登入、寄信、付款或外部 API，確認成功情境與失敗情境都有可觀察結果。

### 小練習

1. 把範例改成另一個欄位或另一種輸入資料。
2. 故意輸入錯誤資料，觀察錯誤訊息或驗證結果。
3. 寫一句話記錄這篇在整個 PHP 網站流程中扮演的角色。

### 負面例子 / 錯誤用法

不要在 callback 只看到 `email` 就直接登入。後果是流程可能被偽造或帳號對錯人。修正方向是驗證 access token、app id、state，並保存 provider user id。

### Junior 常見誤解

- OAuth 不是把密碼交給 Facebook，而是讓 Facebook 回證明。
- `state` 很重要，不是可有可無的參數。

### 一句話總結

第三方登入的核心是完成可信驗證，而不是只拿到 email。

## Day 25：實作 Facebook 帳號登入後的註冊

### 先懂人話版

使用者第一次用 Facebook 登入時，你的網站可能還沒有他的會員資料。這時要把 Facebook user id 和本地會員綁定，而不是每次都建立新帳號。

### 正式概念

| 名稱 | 意思 |
| --- | --- |
| Provider User ID | 第三方平台的使用者 id |
| Account Linking | 第三方帳號與本地會員綁定 |
| Auto Registration | 第一次登入時自動建立會員 |


### 學完這篇你應該會做到什麼

- 能用自己的話說明 Day 25：實作 Facebook 帳號登入後的註冊 在 PHP 網站中的用途。
- 能照著範例寫出可執行的 PHP、SQL、設定或流程。
- 能用瀏覽器、CLI、資料庫結果或錯誤訊息確認自己做對了。

### 實作任務情境

把這篇當成一個小任務：你要替一個逐步成形的 PHP 小網站加入 Day 25：實作 Facebook 帳號登入後的註冊 相關能力，並確定它真的能執行、能查錯、能被下一篇繼續擴充。

### 操作前檢查

- 確認 PHP、Composer、Web server、資料庫或第三方服務是否已準備好；不需要的項目不要硬裝。
- 若會寫入資料庫，先準備測試資料庫，不要直接操作正式資料。
- 若會使用 API key、SMTP 密碼或付款金鑰，一律放在環境變數或設定檔，不要寫死在範例中。
### 範例形式與實作

```php
<?php
declare(strict_types=1);

// 範例用途：
// 用 Facebook id 找會員，找不到就建立。
//
// 新手先知道：
// - $facebookId 是 Facebook 給這個使用者的穩定 id。
// - 先查資料庫有沒有綁過這個 facebook_id。
// - 如果查得到，就回傳既有會員 id。
// - 如果查不到，才新增一筆本地會員資料。
//
// 輸入：Facebook user id 與 email。
// 輸出：本地會員 id。
function findOrCreateFacebookMember(PDO $pdo, string $facebookId, string $email): int
{
    $find = $pdo->prepare("SELECT id FROM members WHERE facebook_id = :facebook_id");
    $find->execute(["facebook_id" => $facebookId]);
    $id = $find->fetchColumn();

    if ($id !== false) {
        return (int) $id;
    }

    $create = $pdo->prepare("INSERT INTO members (email, facebook_id, active) VALUES (:email, :facebook_id, 1)");
    $create->execute([
        "email" => $email,
        "facebook_id" => $facebookId,
    ]);

    return (int) $pdo->lastInsertId();
}
```


### 如果結果和預期不同

- 頁面空白：先打開錯誤顯示或查 Web server / PHP error log。
- 資料沒有寫入：確認連線設定、SQL、欄位名稱、交易是否 commit。
- 第三方服務失敗：先查 API 回傳碼、credential、callback URL 與環境變數。

### 做完後檢查

- 用瀏覽器或 CLI 實際跑一次本篇範例。
- 若有資料庫，確認資料表內容真的符合預期。
- 若有登入、寄信、付款或外部 API，確認成功情境與失敗情境都有可觀察結果。

### 小練習

1. 把範例改成另一個欄位或另一種輸入資料。
2. 故意輸入錯誤資料，觀察錯誤訊息或驗證結果。
3. 寫一句話記錄這篇在整個 PHP 網站流程中扮演的角色。

### 負面例子 / 錯誤用法

不要只用 email 綁定第三方帳號，因為 email 可能缺失、變更或未驗證。修正方向是保存 provider 與 provider user id。

### Junior 常見誤解

- 第三方登入也需要本地會員資料，因為你的系統仍要保存訂單、權限、偏好。
- 自動註冊要避免重複建立帳號。

### 一句話總結

第三方登入後要把外部身分穩定地對應到本地會員。

## Day 26：串接綠界 ECPay 線上支付

### 先懂人話版

金流是你把訂單交給付款服務，付款服務收錢後再通知你結果。你不能只相信使用者回到成功頁，因為那只是瀏覽器畫面。

### 正式概念

| 名稱 | 意思 |
| --- | --- |
| Payment Gateway | 金流服務 |
| Merchant Trade No | 商店訂單編號 |
| CheckMacValue | 綠界簽章檢查值 |
| Callback | 金流通知付款結果 |


### 學完這篇你應該會做到什麼

- 能用自己的話說明 Day 26：串接綠界 ECPay 線上支付 在 PHP 網站中的用途。
- 能照著範例寫出可執行的 PHP、SQL、設定或流程。
- 能用瀏覽器、CLI、資料庫結果或錯誤訊息確認自己做對了。

### 實作任務情境

把這篇當成一個小任務：你要替一個逐步成形的 PHP 小網站加入 Day 26：串接綠界 ECPay 線上支付 相關能力，並確定它真的能執行、能查錯、能被下一篇繼續擴充。

### 操作前檢查

- 確認 PHP、Composer、Web server、資料庫或第三方服務是否已準備好；不需要的項目不要硬裝。
- 若會寫入資料庫，先準備測試資料庫，不要直接操作正式資料。
- 若會使用 API key、SMTP 密碼或付款金鑰，一律放在環境變數或設定檔，不要寫死在範例中。
### 範例形式與實作

```php
<?php
declare(strict_types=1);

// 範例用途：
// 建立送往金流前的訂單資料。
//
// 新手先知道：
// - $orderNo 是你自己系統產生的訂單編號。
// - $amount 是訂單金額，正式專案要從資料庫重新計算，不要相信前端。
// - ReturnURL 是金流付款後通知你網站的 callback URL。
// - 真正送出前通常還要依金流文件產生簽章。
//
// 輸入：訂單編號與金額。
// 輸出：金流表單需要的基本欄位。
function buildPaymentPayload(string $orderNo, int $amount): array
{
    return [
        "MerchantTradeNo" => $orderNo,
        "TotalAmount" => $amount,
        "TradeDesc" => "Demo order",
        "ReturnURL" => "https://example.com/payment/callback",
    ];
}
```


### 如果結果和預期不同

- 頁面空白：先打開錯誤顯示或查 Web server / PHP error log。
- 資料沒有寫入：確認連線設定、SQL、欄位名稱、交易是否 commit。
- 第三方服務失敗：先查 API 回傳碼、credential、callback URL 與環境變數。

### 做完後檢查

- 用瀏覽器或 CLI 實際跑一次本篇範例。
- 若有資料庫，確認資料表內容真的符合預期。
- 若有登入、寄信、付款或外部 API，確認成功情境與失敗情境都有可觀察結果。

### 小練習

1. 把範例改成另一個欄位或另一種輸入資料。
2. 故意輸入錯誤資料，觀察錯誤訊息或驗證結果。
3. 寫一句話記錄這篇在整個 PHP 網站流程中扮演的角色。

### 負面例子 / 錯誤用法

不要把測試 HashKey、HashIV 或正式金流秘密寫進公開 repo。修正方向是使用環境變數，並區分測試環境與正式環境。

### Junior 常見誤解

- 建立付款頁不代表已付款。
- 金流回傳資料要驗證簽章。

### 一句話總結

金流串接的核心是建立訂單、送出付款、驗證回傳。

## Day 27：實作簡單電商網站準備篇

### 先懂人話版

電商不是先從付款開始，而是先想清楚商品、使用者、購物車、訂單、付款狀態這幾個角色。

### 正式概念

| 名稱 | 意思 |
| --- | --- |
| Product | 商品 |
| Cart | 購物車 |
| Order | 訂單 |
| Payment Status | 付款狀態 |


### 學完這篇你應該會做到什麼

- 能用自己的話說明 Day 27：實作簡單電商網站準備篇 在 PHP 網站中的用途。
- 能照著範例寫出可執行的 PHP、SQL、設定或流程。
- 能用瀏覽器、CLI、資料庫結果或錯誤訊息確認自己做對了。

### 實作任務情境

把這篇當成一個小任務：你要替一個逐步成形的 PHP 小網站加入 Day 27：實作簡單電商網站準備篇 相關能力，並確定它真的能執行、能查錯、能被下一篇繼續擴充。

### 操作前檢查

- 確認 PHP、Composer、Web server、資料庫或第三方服務是否已準備好；不需要的項目不要硬裝。
- 若會寫入資料庫，先準備測試資料庫，不要直接操作正式資料。
- 若會使用 API key、SMTP 密碼或付款金鑰，一律放在環境變數或設定檔，不要寫死在範例中。
### 範例形式與實作

```php
<?php
declare(strict_types=1);

// 範例用途：
// 用陣列先模擬商品資料，之後正式專案會改成查資料庫。
//
// 新手先知道：
// - 外層的 1、2 是商品 id。
// - 每個商品又是一個陣列，保存 name 和 price。
// - ?? null 代表找不到商品時回傳 null，避免 undefined index 錯誤。
//
// 輸入：商品 id。
// 輸出：商品資料。
$products = [
    1 => ["name" => "免費圖庫", "price" => 0],
    2 => ["name" => "進階素材包", "price" => 100],
];

$productId = 2;
print_r($products[$productId] ?? null);
```


### 如果結果和預期不同

- 頁面空白：先打開錯誤顯示或查 Web server / PHP error log。
- 資料沒有寫入：確認連線設定、SQL、欄位名稱、交易是否 commit。
- 第三方服務失敗：先查 API 回傳碼、credential、callback URL 與環境變數。

### 做完後檢查

- 用瀏覽器或 CLI 實際跑一次本篇範例。
- 若有資料庫，確認資料表內容真的符合預期。
- 若有登入、寄信、付款或外部 API，確認成功情境與失敗情境都有可觀察結果。

### 小練習

1. 把範例改成另一個欄位或另一種輸入資料。
2. 故意輸入錯誤資料，觀察錯誤訊息或驗證結果。
3. 寫一句話記錄這篇在整個 PHP 網站流程中扮演的角色。

### 負面例子 / 錯誤用法

不要讓價格只從前端表單傳回來。後果是使用者可以把 100 元改成 1 元。修正方向是結帳時從資料庫重新查價格。

### Junior 常見誤解

- 商品、購物車、訂單是不同概念。
- 購物車是使用者意圖，訂單是商務紀錄。

### 一句話總結

做電商前要先分清楚商品、購物車與訂單。

## Day 28：實作商務邏輯層

### 先懂人話版

商務邏輯層是放「這個網站真正規則」的地方。例如登入後先送免費商品、加購商品不能重複加入、訂單總金額怎麼算。

### 正式概念

| 名稱 | 意思 |
| --- | --- |
| Business Logic | 商務規則 |
| Service | 負責處理一段業務流程 |
| Cart Item | 購物車項目 |
| Total Amount | 訂單總金額 |


### 學完這篇你應該會做到什麼

- 能用自己的話說明 Day 28：實作商務邏輯層 在 PHP 網站中的用途。
- 能照著範例寫出可執行的 PHP、SQL、設定或流程。
- 能用瀏覽器、CLI、資料庫結果或錯誤訊息確認自己做對了。

### 實作任務情境

把這篇當成一個小任務：你要替一個逐步成形的 PHP 小網站加入 Day 28：實作商務邏輯層 相關能力，並確定它真的能執行、能查錯、能被下一篇繼續擴充。

### 操作前檢查

- 確認 PHP、Composer、Web server、資料庫或第三方服務是否已準備好；不需要的項目不要硬裝。
- 若會寫入資料庫，先準備測試資料庫，不要直接操作正式資料。
- 若會使用 API key、SMTP 密碼或付款金鑰，一律放在環境變數或設定檔，不要寫死在範例中。
### 範例形式與實作

```php
<?php
declare(strict_types=1);

// 範例用途：
// 建立簡單購物車，避免同一商品重複加入。
//
// 新手先知道：
// - private array $items = [] 代表購物車內部有一個商品清單。
// - addIfNotExists() 是「如果不存在才加入」。
// - isset($this->items[$id]) 用商品 id 檢查購物車是否已有商品。
// - items() 回傳目前購物車內容，讓外部可以顯示。
//
// 輸入：商品 id、名稱、價格。
// 輸出：購物車內容。
final class Cart
{
    private array $items = [];

    public function addIfNotExists(int $id, string $name, int $price): void
    {
        if (!isset($this->items[$id])) {
            $this->items[$id] = ["name" => $name, "price" => $price, "qty" => 1];
        }
    }

    public function items(): array
    {
        return $this->items;
    }
}
```


### 如果結果和預期不同

- 頁面空白：先打開錯誤顯示或查 Web server / PHP error log。
- 資料沒有寫入：確認連線設定、SQL、欄位名稱、交易是否 commit。
- 第三方服務失敗：先查 API 回傳碼、credential、callback URL 與環境變數。

### 做完後檢查

- 用瀏覽器或 CLI 實際跑一次本篇範例。
- 若有資料庫，確認資料表內容真的符合預期。
- 若有登入、寄信、付款或外部 API，確認成功情境與失敗情境都有可觀察結果。

### 小練習

1. 把範例改成另一個欄位或另一種輸入資料。
2. 故意輸入錯誤資料，觀察錯誤訊息或驗證結果。
3. 寫一句話記錄這篇在整個 PHP 網站流程中扮演的角色。

### 負面例子 / 錯誤用法

不要把所有商務規則都塞在 view 裡。後果是畫面和規則混在一起，未來改 UI 會弄壞訂單邏輯。修正方向是把規則放到 service 或 class。

### Junior 常見誤解

- 商務邏輯不是 SQL，也不是 HTML，它是「系統規則」。
- class 的價值在於把規則收斂，不是單純換語法。

### 一句話總結

商務邏輯層讓系統規則有固定位置，不會散落在畫面裡。

## Day 29：刷完信用卡導回網站的處理

### 先懂人話版

付款導回網站時，不要急著顯示成功。你要先確認：這真的是金流送來的嗎？訂單存在嗎？金額對嗎？之前是不是已經處理過？

### 正式概念

| 名稱 | 意思 |
| --- | --- |
| Callback Handler | 接收金流回傳的程式 |
| Signature Verification | 簽章驗證 |
| Idempotent | 重複執行也不會造成錯誤 |
| Transaction | 資料庫交易 |


### 學完這篇你應該會做到什麼

- 能用自己的話說明 Day 29：刷完信用卡導回網站的處理 在 PHP 網站中的用途。
- 能照著範例寫出可執行的 PHP、SQL、設定或流程。
- 能用瀏覽器、CLI、資料庫結果或錯誤訊息確認自己做對了。

### 實作任務情境

把這篇當成一個小任務：你要替一個逐步成形的 PHP 小網站加入 Day 29：刷完信用卡導回網站的處理 相關能力，並確定它真的能執行、能查錯、能被下一篇繼續擴充。

### 操作前檢查

- 確認 PHP、Composer、Web server、資料庫或第三方服務是否已準備好；不需要的項目不要硬裝。
- 若會寫入資料庫，先準備測試資料庫，不要直接操作正式資料。
- 若會使用 API key、SMTP 密碼或付款金鑰，一律放在環境變數或設定檔，不要寫死在範例中。
### 範例形式與實作

```php
<?php
declare(strict_types=1);

// 範例用途：
// 示範付款成功後更新訂單狀態的安全順序。
//
// 新手先知道：
// - beginTransaction() 開始資料庫交易，確保中途失敗時可以 rollBack。
// - FOR UPDATE 會鎖住這筆訂單，避免同時被兩個 callback 改壞。
// - 先確認訂單存在，再確認金額一致，最後才更新 paid。
// - 如果任何一步不合理，就 rollBack 並丟錯誤。
//
// 輸入：金流回傳的訂單編號與付款金額。
// 副作用：驗證後才更新訂單。
function markOrderPaid(PDO $pdo, string $orderNo, int $paidAmount): void
{
    $pdo->beginTransaction();

    $statement = $pdo->prepare("SELECT id, total_amount, status FROM orders WHERE order_no = :order_no FOR UPDATE");
    $statement->execute(["order_no" => $orderNo]);
    $order = $statement->fetch(PDO::FETCH_ASSOC);

    if (!$order || (int) $order["total_amount"] !== $paidAmount) {
        $pdo->rollBack();
        throw new RuntimeException("訂單不存在或金額不一致");
    }

    if ($order["status"] !== "paid") {
        $update = $pdo->prepare("UPDATE orders SET status = 'paid' WHERE id = :id");
        $update->execute(["id" => $order["id"]]);
    }

    $pdo->commit();
}
```


### 如果結果和預期不同

- 頁面空白：先打開錯誤顯示或查 Web server / PHP error log。
- 資料沒有寫入：確認連線設定、SQL、欄位名稱、交易是否 commit。
- 第三方服務失敗：先查 API 回傳碼、credential、callback URL 與環境變數。

### 做完後檢查

- 用瀏覽器或 CLI 實際跑一次本篇範例。
- 若有資料庫，確認資料表內容真的符合預期。
- 若有登入、寄信、付款或外部 API，確認成功情境與失敗情境都有可觀察結果。

### 小練習

1. 把範例改成另一個欄位或另一種輸入資料。
2. 故意輸入錯誤資料，觀察錯誤訊息或驗證結果。
3. 寫一句話記錄這篇在整個 PHP 網站流程中扮演的角色。

### 負面例子 / 錯誤用法

不要只用 `$_SESSION["order"]` 判斷付款成功。付款流程可能離開網站、換瀏覽器、callback 重送。修正方向是以資料庫訂單與金流簽章為準。

### Junior 常見誤解

- 付款成功頁不等於付款成功。
- callback 可能來很多次，要能安全重複處理。

### 一句話總結

付款回站處理要先驗證，再更新資料庫訂單狀態。

## Day 30：專案原始碼與學習收尾

### 先懂人話版

最後一天不是結束，而是開始能自己拆需求。你已經看過一個網站從語法、資料庫、會員、API、金流慢慢長出來，接下來要練的是自己做小作品。

### 正式概念

| 名稱 | 意思 |
| --- | --- |
| README | 專案使用說明 |
| Setup | 安裝與設定流程 |
| Source Code | 原始碼 |
| Refactor | 在不改功能的前提下整理程式 |


### 學完這篇你應該會做到什麼

- 能用自己的話說明 Day 30：專案原始碼與學習收尾 在 PHP 網站中的用途。
- 能照著範例寫出可執行的 PHP、SQL、設定或流程。
- 能用瀏覽器、CLI、資料庫結果或錯誤訊息確認自己做對了。

### 實作任務情境

把這篇當成一個小任務：你要替一個逐步成形的 PHP 小網站加入 Day 30：專案原始碼與學習收尾 相關能力，並確定它真的能執行、能查錯、能被下一篇繼續擴充。

### 操作前檢查

- 確認 PHP、Composer、Web server、資料庫或第三方服務是否已準備好；不需要的項目不要硬裝。
- 若會寫入資料庫，先準備測試資料庫，不要直接操作正式資料。
- 若會使用 API key、SMTP 密碼或付款金鑰，一律放在環境變數或設定檔，不要寫死在範例中。
### 範例形式與實作

```markdown
# Demo PHP Shop

## 安裝

1. composer install
2. 複製 .env.example 成 .env
3. 設定資料庫連線
4. 匯入 database.sql
5. php -S 127.0.0.1:8000 -t public
```


### 如果結果和預期不同

- 頁面空白：先打開錯誤顯示或查 Web server / PHP error log。
- 資料沒有寫入：確認連線設定、SQL、欄位名稱、交易是否 commit。
- 第三方服務失敗：先查 API 回傳碼、credential、callback URL 與環境變數。

### 做完後檢查

- 用瀏覽器或 CLI 實際跑一次本篇範例。
- 若有資料庫，確認資料表內容真的符合預期。
- 若有登入、寄信、付款或外部 API，確認成功情境與失敗情境都有可觀察結果。

### 小練習

1. 把範例改成另一個欄位或另一種輸入資料。
2. 故意輸入錯誤資料，觀察錯誤訊息或驗證結果。
3. 寫一句話記錄這篇在整個 PHP 網站流程中扮演的角色。

### 負面例子 / 錯誤用法

不要只丟原始碼，沒有 README。後果是三個月後連自己都不知道怎麼跑。修正方向是每個專案都寫安裝、設定、執行、測試方式。

### Junior 常見誤解

- 能跑只是第一步，能讓別人理解和接手才是工程能力。
- 重構不是亂改，是讓下一次修改更容易。

### 一句話總結

學完系列後，真正的下一步是做小作品、寫 README、持續重構。

---

## 給 junior 的 2026 實作順序

1. 先用 PHP CLI 練 Day 1-4，不碰資料庫。
2. 建一個 `public/index.php`，理解 request 進來後怎麼分派。
3. 用 PDO 做留言板，練 `INSERT`、`SELECT`、驗證輸入。
4. 做會員註冊與登入，練 `password_hash()`、`password_verify()`、session。
5. 加上 email token，但先把寄信內容印出來，確認流程懂了再接 SMTP。
6. 加上 Monolog，把重要流程寫進 log。
7. 最後再做簡單購物車與訂單，不要一開始就碰金流。

## 一個最小作品題目

如果你完全沒基礎，讀完這份筆記後可以做這個作品：

### 會員留言板

功能：

- 註冊會員。
- 登入會員。
- 登入後可以新增留言。
- 首頁顯示所有留言。
- 登出後不能新增留言。

你會練到：

- Day 1-4：語法、變數、條件、迴圈。
- Day 6、11、16：PDO 資料庫。
- Day 12-13：表單輸入與驗證。
- Day 17-18：註冊登入。
- Day 21-22：提示訊息與 log。

## 最終總結

這個系列最值得保留的精神是：不要先背一堆術語，而是從「我想做一個網站功能」開始，把問題拆成變數、條件、迴圈、物件、資料庫、會員流程、外部 API。2026 年再學 PHP，可以保留這個學習順序，但實作上要升級成更安全、更清楚、更容易維護的寫法。
