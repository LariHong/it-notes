# 網路基礎與資安概念筆記

> 來源主要整理自 Cloudflare Learning Center。這份筆記的目的，是把網路、傳輸、加密、DNS、CDN、DDoS 與常見 Web 防護概念串成一條可以理解 HTTP/HTTPS 請求生命週期的知識線。

## 0. 一個 HTTPS 請求背後發生了什麼

當你在 C# 寫下：

```csharp
using var httpClient = new HttpClient();
string body = await httpClient.GetStringAsync("https://www.example.com/api/products");
```

看起來只是送出一個 HTTP request，但背後其實經過許多層：

1. 應用程式建立 HTTP request。
2. DNS 把網域名稱轉成 IP 位址。
3. Client 和 Server 建立 TCP 或 QUIC 連線。
4. 若使用 HTTPS，雙方進行 TLS handshake。
5. TLS 用憑證確認伺服器身分，並協商出 session key。
6. HTTP request 被加密後送進網路。
7. Router、BGP、ISP、IXP、CDN edge 等協助封包抵達目的地。
8. WAF、Bot Management、DDoS 防護可能在中途檢查或過濾流量。
9. Server 回傳 HTTP response。
10. Client 解密 response，應用程式讀取結果。

一句話說，Web request 不是「程式直接連到網站」，而是應用層、傳輸層、網路層、安全層與基礎設施共同完成的一次通訊。

---

## 1. OSI 模型：理解網路問題的地圖

OSI 模型把網路通訊分成 7 層。它不一定完全對應真實實作，但非常適合拿來判斷「問題大概出在哪裡」。

| 層級 | 名稱 | 負責內容 | 常見例子 |
|---|---|---|---|
| L7 | 應用層 | 應用程式使用的協定與資料格式 | HTTP、DNS、SMTP、IMAP |
| L6 | 表示層 | 編碼、壓縮、加密表示 | TLS、JSON、UTF-8 |
| L5 | 工作階段層 | 連線狀態與會話管理 | TLS session、登入 session |
| L4 | 傳輸層 | 端到端傳輸、port、可靠性 | TCP、UDP、QUIC |
| L3 | 網路層 | IP 位址與跨網路路由 | IP、ICMP、Routing |
| L2 | 資料連結層 | 同一區域網路內傳送 frame | Ethernet、Wi-Fi、Switch |
| L1 | 實體層 | 電纜、光纖、無線訊號 | 網路線、無線電波 |

直觀例子：如果網路像寄包裹，HTTP 是包裹內容與表單格式，TLS 是保密封套，TCP/UDP 是運送方式，IP 是地址，Router 是物流分站，實體層則是道路和車輛。

---

## 2. DNS：把名字翻成地址

DNS 是網際網路的「電話簿」。人類記得 `www.cloudflare.com`，但網路設備真正需要的是 IP 位址。

DNS 查詢大致流程：

```text
瀏覽器或程式
-> 作業系統 DNS cache
-> Recursive resolver
-> Root DNS
-> TLD DNS，例如 .com
-> Authoritative DNS
-> 回傳 IP 位址
```

為什麼重要：

- 沒有 DNS，使用者必須記 IP 位址。
- DNS 失效時，網站可能其實正常，但使用者找不到它。
- DNS 也常用在 CDN、Anycast、負載分散與資安防護。

C# 查 DNS：

```csharp
using System.Net;

IPAddress[] addresses = await Dns.GetHostAddressesAsync("www.cloudflare.com");

foreach (IPAddress address in addresses)
{
    Console.WriteLine(address);
}
```

這段程式對應的是「把網域名稱解析成 IP 位址」。

---

## 3. IP、Packet、ICMP：網路層的基本單位

### IP

IP 負責替封包標記來源與目的地位址。它像信封上的寄件地址與收件地址。

常見 IP 類型：

- IPv4：例如 `192.0.2.1`
- IPv6：例如 `2001:db8::1`

IP 本身不保證封包一定抵達，也不保證順序正確；可靠性通常由 TCP 或應用層補足。

### Packet

封包可以想成「被切小的資料包」。大型資料會被拆成多個 packet，在網路中分別傳送。

一個 packet 通常包含：

- Header：來源、目的地、協定等控制資訊。
- Payload：真正要傳的資料。

### ICMP

ICMP 是網路層的控制與診斷協定，常見用途是 `ping`。它不是用來傳網頁內容，而是用來回報網路狀態。

C# ping 範例：

```csharp
using System.Net.NetworkInformation;

using var ping = new Ping();
PingReply reply = await ping.SendPingAsync("1.1.1.1");

Console.WriteLine(reply.Status);
Console.WriteLine(reply.RoundtripTime);
```

這段程式對應的是 ICMP Echo Request / Echo Reply，用來測試目標是否可達與延遲。

---

## 4. LAN、WAN、Switch、Router、Routing

### LAN

LAN 是區域網路，例如家中、辦公室或學校內部網路。它的範圍小、延遲低，通常由 switch 連接多台裝置。

### Switch

Switch 主要在同一個 LAN 裡轉送資料。它根據 MAC address 判斷資料該送到哪個裝置。

直觀例子：Switch 像辦公室內部收發室，知道哪張桌子在哪裡。

### Router

Router 負責連接不同網路，根據 IP 位址決定封包下一站。

直觀例子：Router 像跨城市物流轉運站，不只知道同一棟大樓內的位置，也知道包裹該往哪個城市送。

### WAN

WAN 是廣域網路，範圍比 LAN 大得多，例如跨城市、跨國或整個網際網路。

### Routing

Routing 是決定封包路徑的過程。網路上的每台 router 都只需要知道「下一站往哪裡」，不一定知道完整路線。

---

## 5. Autonomous System、BGP、IXP、Anycast

### Autonomous System

Autonomous System，簡稱 AS，是由單一組織管理的一組 IP 網路。例如 ISP、大型雲端平台、CDN 都可能擁有自己的 AS。

### BGP

BGP 是網際網路上 AS 之間交換路由資訊的協定。它回答的是：「要到某段 IP 網路，應該走哪條路？」

為什麼重要：

- BGP 讓全球網路彼此知道路怎麼走。
- CDN 與 Anycast 常依靠 BGP 把使用者導向較近的節點。
- BGP 設定錯誤或遭劫持，可能造成大規模流量錯路或中斷。

### IXP

IXP 是 Internet Exchange Point，讓不同網路直接交換流量，減少繞路與延遲。

### Anycast

Anycast 讓多個地理位置不同的伺服器使用同一個 IP。BGP 會把使用者導向網路上較近或較合適的節點。

直觀例子：同一個客服電話號碼，在不同城市會接到最近的服務中心。

---

## 6. HTTP 與 HTTPS

### HTTP

HTTP 是 Web 的應用層協定，採用 request / response 模型。

HTTP request 通常包含：

- Method：`GET`、`POST`、`PUT`、`DELETE`
- Path 或 URL
- Headers
- Body

HTTP response 通常包含：

- Status code：例如 `200`、`404`、`500`
- Headers
- Body

C# HTTP request 範例：

```csharp
using System.Net.Http.Headers;

using var httpClient = new HttpClient();
using var request = new HttpRequestMessage(HttpMethod.Get, "https://example.com/api/products");

request.Headers.Accept.Add(new MediaTypeWithQualityHeaderValue("application/json"));
request.Headers.UserAgent.ParseAdd("LearningNotes/1.0");

using HttpResponseMessage response = await httpClient.SendAsync(request);
string body = await response.Content.ReadAsStringAsync();

Console.WriteLine((int)response.StatusCode);
Console.WriteLine(body);
```

### HTTPS

HTTPS = HTTP + TLS。

HTTP 負責「說什麼」，TLS 負責「保密、驗證與防竄改」。

HTTPS 提供：

- Confidentiality：避免內容被旁人讀取。
- Integrity：避免資料傳輸途中被竄改。
- Authentication：確認伺服器身分。

---

## 7. 公開金鑰加密、SSL 憑證、TLS、TLS Handshake

### 公開金鑰加密

公開金鑰加密使用一對金鑰：

- Public key：可以公開。
- Private key：必須保密。

用 public key 加密的資料，通常只有 private key 能解開。TLS 不會用非對稱加密保護整段大量資料，而是用它來協助安全地協商 session key，之後大量資料用對稱加密處理。

C# RSA 概念示範：

```csharp
using System.Security.Cryptography;
using System.Text;

using RSA rsa = RSA.Create(2048);

byte[] plaintext = Encoding.UTF8.GetBytes("secret message");
byte[] encrypted = rsa.Encrypt(plaintext, RSAEncryptionPadding.OaepSHA256);
byte[] decrypted = rsa.Decrypt(encrypted, RSAEncryptionPadding.OaepSHA256);

Console.WriteLine(Encoding.UTF8.GetString(decrypted));
```

這段程式只示範公開金鑰加密概念；真實 HTTPS 由 TLS stack 自動處理。

### SSL 憑證

雖然大家常說 SSL 憑證，但現代實務多半是 TLS 憑證。憑證用來證明「這個 public key 屬於這個網域」。

憑證通常包含：

- 網域名稱。
- Public key。
- 發行者，也就是 CA。
- 有效期限。
- 數位簽章。

### TLS

TLS 是保護網路通訊的協定。它是 SSL 的後繼者，現代 HTTPS 主要使用 TLS。

### TLS Handshake

TLS handshake 大致做幾件事：

1. Client 告訴 Server 自己支援的 TLS 版本與加密套件。
2. Server 回覆選定的參數與憑證。
3. Client 驗證憑證是否可信、是否符合網域、是否過期。
4. 雙方協商出 session key。
5. 後續 HTTP 資料用 session key 加密傳輸。

直觀例子：憑證像店家的營業執照，公開金鑰像公開的保險箱投入口，private key 像只有店家持有的鑰匙，session key 則像這次交易臨時使用的保密暗號。

---

## 8. TCP、UDP、QUIC 與 SYN Flood

### TCP

TCP 是可靠的傳輸協定，會處理連線建立、順序、重傳與流量控制。

TCP 三向交握：

```text
Client -> SYN -> Server
Client <- SYN-ACK <- Server
Client -> ACK -> Server
```

### UDP

UDP 比 TCP 輕量，沒有內建連線與重傳機制。它常用在 DNS、影音、遊戲、QUIC 等情境。

### QUIC

QUIC 建立在 UDP 之上，常用於 HTTP/3。它把加密與連線管理整合得更緊密，目標是降低延遲並改善連線切換體驗。

### SYN Flood

SYN flood 是針對 TCP 三向交握的 DDoS 類型。攻擊流量大量送出 SYN，但不完成最後 ACK，讓伺服器保留許多半開連線資源。

防禦重點是理解風險與使用防護機制，例如 SYN cookies、rate limiting、DDoS scrubbing、CDN 或雲端防護，而不是在應用程式裡手動處理 TCP 半開連線。

---

## 9. DoS、DDoS 與常見攻擊類型

### DoS 與 DDoS

DoS 是 Denial of Service，目標是讓服務無法正常提供。DDoS 是 Distributed Denial of Service，流量來自大量分散來源，因此更難擋。

依 OSI 層級可粗略分成：

| 類型 | 層級 | 例子 | 目標 |
|---|---|---|---|
| 應用層攻擊 | L7 | HTTP flood、credential stuffing | 消耗應用程式資源 |
| 協定層攻擊 | L3/L4 | SYN flood、ping flood | 消耗網路或連線資源 |
| 放大攻擊 | L3/L4 | DNS amplification、NTP amplification | 用小請求換大回應 |

### DNS Flood

DNS flood 是大量 DNS 查詢讓 DNS 伺服器無法回應正常使用者。結果可能是網站本身沒壞，但使用者無法解析網域。

### NTP Amplification

NTP amplification 利用可被濫用的 NTP 伺服器，把小請求放大成大量回應流量，並導向受害者。

### Ping Flood 與 Ping of Death

Ping flood 使用大量 ICMP Echo Request 消耗頻寬或處理資源。Ping of Death 則是歷史上與異常大小或破碎封包相關的攻擊型態，現代系統多已有防護。

### QUIC Flood

QUIC flood 針對 UDP/QUIC 流量製造壓力，可能消耗連線建立、加密處理或邊緣節點資源。

### Blackhole Routing

Blackhole routing 是把流量導向「黑洞」丟棄。它可以快速止血，但代價是正常流量也可能一起被丟掉。

---

## 10. CDN、WAF、Bot Management、CAPTCHA、robots.txt

### CDN

CDN 是 Content Delivery Network。它把內容快取在全球多個節點，讓使用者從較近的節點取得資料。

CDN 的價值：

- 降低延遲。
- 減少 origin server 負載。
- 改善可用性。
- 協助吸收與過濾惡意流量。

### WAF

WAF 是 Web Application Firewall，主要保護 L7 Web 流量。

它可以協助阻擋：

- SQL injection。
- XSS。
- 惡意 HTTP request。
- 某些應用層 DDoS。
- 可疑 bot 流量。

### Bot Management

Bot Management 用來辨識自動化流量，區分搜尋引擎等好 bot 與 credential stuffing、爬蟲濫用等壞 bot。

### CAPTCHA

CAPTCHA 用挑戰題區分人類與自動化程式，但它不是萬能防護，也可能影響使用者體驗。

### robots.txt

`robots.txt` 是給守規矩爬蟲看的指引，不是安全控制。惡意爬蟲可以直接忽略它。

範例：

```text
User-agent: *
Disallow: /private/
```

這表示要求爬蟲不要抓 `/private/`，但不能把它當成真正的存取控制。

---

## 11. Credential Stuffing 與 MFA

### Credential Stuffing

Credential stuffing 是攻擊者拿外洩的帳號密碼，嘗試登入其他服務。它利用的是「使用者重複使用密碼」。

常見防護：

- MFA。
- Rate limiting。
- Bot detection。
- 登入異常偵測。
- 強制使用唯一且強的密碼。
- 發現外洩密碼時要求重設。

### MFA

MFA 是 Multi-Factor Authentication，多因素驗證。它要求使用者除了密碼之外，再提供另一種因素。

常見因素：

- 知道的東西：密碼。
- 擁有的東西：手機、硬體金鑰。
- 本人的特徵：生物辨識。

C# 登入防護概念：

```csharp
if (await loginRateLimiter.IsBlockedAsync(userName, clientIp))
{
    return Results.StatusCode(429);
}

bool passwordOk = await authService.CheckPasswordAsync(userName, password);

if (!passwordOk)
{
    return Results.Unauthorized();
}

if (await mfaService.IsRequiredAsync(userName))
{
    return Results.Json(new { requiresMfa = true });
}

return Results.Ok(new { authenticated = true });
```

這段程式對應的是「先限制濫用，再驗證密碼，必要時要求 MFA」。

---

## 12. SMTP 與 IMAP

SMTP 和 IMAP 都是電子郵件相關協定，但角色不同。

| 協定 | 主要用途 | 直觀例子 |
|---|---|---|
| SMTP | 寄信、郵件伺服器之間轉送信件 | 投遞郵件 |
| IMAP | 讀取與同步信箱內容 | 查看信箱 |

SMTP 負責把信送出去，IMAP 負責讓使用者在不同裝置上讀取同一個信箱狀態。

郵件安全常見重點：

- 驗證寄件來源。
- 防止釣魚與偽冒。
- 過濾垃圾信。
- 保護帳號登入。

---

## 13. 常見除錯方向

### DNS 問題

症狀：

- `host not found`
- 某些地區無法連線
- 網站服務正常但網域解析失敗

優先檢查：

- DNS record 是否正確。
- TTL 與 cache。
- Authoritative DNS 是否正常。
- CDN 或 DNS provider 設定。

### TLS 憑證問題

症狀：

- 瀏覽器顯示憑證錯誤。
- C# `HttpRequestException`。
- API client 拒絕連線。

優先檢查：

- 憑證是否過期。
- 憑證網域是否符合。
- 憑證鏈是否完整。
- Client 是否信任簽發 CA。

### HTTP 狀態碼問題

症狀：

- `404 Not Found`
- `401 Unauthorized`
- `429 Too Many Requests`
- `500 Internal Server Error`

優先檢查：

- 路徑與 method 是否正確。
- Authorization header 是否正確。
- 是否被 rate limit。
- Server log 與應用程式錯誤。

### 流量異常

症狀：

- CPU、頻寬、連線數突然升高。
- 登入失敗暴增。
- DNS 查詢量異常。
- 來源 IP 分散且模式相似。

優先檢查：

- WAF / CDN / DDoS 防護儀表板。
- Access log。
- Rate limiting。
- Bot Management。
- Origin server 負載。

---

## 14. ASP.NET Core 中觀察 request / response

下面是一段簡單 middleware，用來理解 HTTP request / response 在應用層能看到什麼：

```csharp
var builder = WebApplication.CreateBuilder(args);
var app = builder.Build();

app.Use(async (context, next) =>
{
    string method = context.Request.Method;
    string path = context.Request.Path;
    string userAgent = context.Request.Headers.UserAgent.ToString();
    string? forwardedFor = context.Request.Headers["X-Forwarded-For"].FirstOrDefault();
    string remoteIp = context.Connection.RemoteIpAddress?.ToString() ?? "unknown";

    Console.WriteLine($"Request: {method} {path}");
    Console.WriteLine($"Remote IP: {remoteIp}");
    Console.WriteLine($"X-Forwarded-For: {forwardedFor}");
    Console.WriteLine($"User-Agent: {userAgent}");

    await next();

    Console.WriteLine($"Response status: {context.Response.StatusCode}");
});

app.MapGet("/api/health", () => Results.Ok(new
{
    status = "ok",
    time = DateTimeOffset.UtcNow
}));

app.Run();
```

這段程式可以觀察：

- HTTP method 與 path：L7 應用層。
- Headers：L7 metadata。
- Remote IP：與 L3/IP 有關，但在 proxy/CDN 後面要小心解讀。
- `X-Forwarded-For`：常由 proxy、load balancer 或 CDN 加上。
- Response status：應用層回應結果。

注意：在真實 production 環境中，如果服務放在 CDN 或 reverse proxy 後面，必須正確設定 Forwarded Headers，否則 `RemoteIpAddress` 可能只會看到 proxy 的 IP。

---

## 15. 一句話總結

網路通訊可以想成「應用資料被加密、切成封包、根據 IP 與路由穿過多個網路，再由各種安全機制協助驗證、保護與過濾」；理解每一層的角色，就能更快判斷問題是 DNS、TLS、HTTP、路由、CDN 還是攻擊流量造成的。

---

## 來源

- Cloudflare：ICMP：<https://www.cloudflare.com/zh-tw/learning/ddos/glossary/internet-control-message-protocol-icmp/>
- Cloudflare：SYN Flood：<https://www.cloudflare.com/zh-tw/learning/ddos/syn-flood-ddos-attack/>
- Cloudflare：DDoS：<https://www.cloudflare.com/zh-tw/learning/ddos/what-is-a-ddos-attack/>
- Cloudflare：BGP：<https://www.cloudflare.com/zh-tw/learning/security/glossary/what-is-bgp/>
- Cloudflare：公開金鑰加密：<https://www.cloudflare.com/zh-tw/learning/ssl/how-does-public-key-encryption-work/>
- Cloudflare：SSL 憑證：<https://www.cloudflare.com/zh-tw/learning/ssl/what-is-an-ssl-certificate/>
- Cloudflare：TLS：<https://www.cloudflare.com/zh-tw/learning/ssl/transport-layer-security-tls/>
- Cloudflare：TLS Handshake：<https://www.cloudflare.com/zh-tw/learning/ssl/what-happens-in-a-tls-handshake/>
- Cloudflare：OSI 模型：<https://www.cloudflare.com/zh-tw/learning/ddos/glossary/open-systems-interconnection-model-osi/>
- Cloudflare：SMTP：<https://www.cloudflare.com/zh-tw/learning/email-security/what-is-smtp/>
- Cloudflare：IMAP：<https://www.cloudflare.com/zh-tw/learning/email-security/what-is-imap/>
- Cloudflare：DNS：<https://www.cloudflare.com/zh-tw/learning/dns/what-is-dns/>
- Cloudflare：HTTP：<https://www.cloudflare.com/zh-tw/learning/ddos/glossary/hypertext-transfer-protocol-http/>
- Cloudflare：IP：<https://www.cloudflare.com/zh-tw/learning/network-layer/internet-protocol/>
- Cloudflare：Packet：<https://www.cloudflare.com/zh-tw/learning/network-layer/what-is-a-packet/>
- Cloudflare：Network Switch：<https://www.cloudflare.com/zh-tw/learning/network-layer/what-is-a-network-switch/>
- Cloudflare：LAN：<https://www.cloudflare.com/zh-tw/learning/network-layer/what-is-a-lan/>
- Cloudflare：WAN：<https://www.cloudflare.com/zh-tw/learning/network-layer/what-is-a-wan/>
- Cloudflare：Autonomous System：<https://www.cloudflare.com/zh-tw/learning/network-layer/what-is-an-autonomous-system/>
- Cloudflare：Network Layer：<https://www.cloudflare.com/zh-tw/learning/network-layer/what-is-the-network-layer/>
- Cloudflare：Routing：<https://www.cloudflare.com/zh-tw/learning/network-layer/what-is-routing/>
- Cloudflare：Router：<https://www.cloudflare.com/zh-tw/learning/network-layer/what-is-a-router/>
- Cloudflare：IP Address：<https://www.cloudflare.com/zh-tw/learning/dns/glossary/what-is-my-ip-address/>
- Cloudflare：QUIC Flood：<https://www.cloudflare.com/zh-tw/learning/ddos/what-is-a-quic-flood/>
- Cloudflare：HTTPS：<https://www.cloudflare.com/zh-tw/learning/ssl/what-is-https/>
- Cloudflare：Protocol：<https://www.cloudflare.com/zh-tw/learning/network-layer/what-is-a-protocol/>
- Cloudflare：IXP：<https://www.cloudflare.com/zh-tw/learning/cdn/glossary/internet-exchange-point-ixp/>
- Cloudflare：Credential Stuffing：<https://www.cloudflare.com/zh-tw/learning/bots/what-is-credential-stuffing/>
- Cloudflare：MFA：<https://www.cloudflare.com/zh-tw/learning/access-management/what-is-multi-factor-authentication/>
- Cloudflare：CDN：<https://www.cloudflare.com/zh-tw/learning/cdn/what-is-a-cdn/>
- Cloudflare：DoS：<https://www.cloudflare.com/zh-tw/learning/ddos/glossary/denial-of-service/>
- Cloudflare：CAPTCHA：<https://www.cloudflare.com/zh-tw/learning/bots/how-captchas-work/>
- Cloudflare：防止 Web Scraping：<https://www.cloudflare.com/zh-tw/learning/ai/how-to-prevent-web-scraping/>
- Cloudflare：DNS Flood：<https://www.cloudflare.com/zh-tw/learning/ddos/dns-flood-ddos-attack/>
- Cloudflare：Bot Management：<https://www.cloudflare.com/zh-tw/learning/bots/what-is-bot-management/>
- Cloudflare：robots.txt：<https://www.cloudflare.com/zh-tw/learning/bots/what-is-robots-txt/>
- Cloudflare：Anycast：<https://www.cloudflare.com/zh-tw/learning/cdn/glossary/anycast-network/>
- Cloudflare：WAF：<https://www.cloudflare.com/zh-tw/learning/ddos/glossary/web-application-firewall-waf/>
- Cloudflare：Blackhole Routing：<https://www.cloudflare.com/zh-tw/learning/ddos/glossary/ddos-blackhole-routing/>
- Cloudflare：NTP Amplification：<https://www.cloudflare.com/zh-tw/learning/ddos/ntp-amplification-ddos-attack/>
- Cloudflare：Ping of Death：<https://www.cloudflare.com/zh-tw/learning/ddos/ping-of-death-ddos-attack/>
- Cloudflare：Ping / ICMP Flood：<https://www.cloudflare.com/zh-tw/learning/ddos/ping-icmp-flood-ddos-attack/>
